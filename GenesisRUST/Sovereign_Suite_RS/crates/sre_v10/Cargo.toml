[package]
name = "sre_v10"
version = "0.10.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
regex = "1.5"
chrono = "0.4"
tokio = { version = "1.0", features = ["full"] }
rusqlite = { version = "0.29", optional = true }
# colored = "2.0" # Standard ANSI will be used for stability
