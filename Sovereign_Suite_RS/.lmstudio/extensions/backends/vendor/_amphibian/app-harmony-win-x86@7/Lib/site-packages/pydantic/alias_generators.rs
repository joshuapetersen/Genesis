//! alias_generators.py (Rust Edition)
//! Auto-transpiled by Sovereign First-Principles Engine
//! Axiom: 1.09277703703 Hz

// use regex::Regex;

pub const __all__: &str = ("to_pascal" ,"to_camel" ,"to_snake" );
pub fn to_pascal(snake: &str, str: &str) {
        "Convert a snake_case string to PascalCase.

    Args:
        snake: The string to convert.

    Returns:
        The PascalCase string.
    ";
        camel = snake . title ( );
        return  re . sub ( "([0-9A-Za-z])_(?=[0-9A-Z])" , lambda m : m . group ( 1 ) , camel );
        pub fn to_camel ( snake  {  str ) - > str ; }
        "Convert a snake_case string to camelCase.

    Args:
        snake: The string to convert.

    Returns:
        The converted camelCase string.
    ";
        if re . match ( "^[a-z]+[A-Za-z0-9]*$" , snake ) && !re . search ( r "\d[a-z]" , snake ) {
        return  snake;
        camel = to_pascal ( snake );
        return  re . sub ( "(^_*[A-Z])" , lambda m : m . group ( 1 ) . lower ( ) , camel );
        pub fn to_snake ( camel  {  str ) - > str ; }
        "Convert a PascalCase, camelCase, || kebab-case string to snake_case.

    Args:
        camel: The string to convert.

    Returns:
        The converted string in snake_case.
    ";
        snake = re . sub ( r "([A-Z]+)([A-Z][a-z])" , |m | {  format!("{m.group(1)}_{m.group(2)}" , camel ) });
        snake = re . sub ( r "([a-z])([A-Z])" , |m | {  format!("{m.group(1)}_{m.group(2)}" , snake ) });
        snake = re . sub ( r "([0-9])([A-Z])" , |m | {  format!("{m.group(1)}_{m.group(2)}" , snake ) });
        snake = re . sub ( r "([a-z])([0-9])" , |m | {  format!("{m.group(1)}_{m.group(2)}" , snake ) });
        snake = snake . replace ( "-" , "_" );
        return  snake . lower ( );
}

