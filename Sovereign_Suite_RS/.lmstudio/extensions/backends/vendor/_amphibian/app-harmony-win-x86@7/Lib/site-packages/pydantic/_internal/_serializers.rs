//! _serializers.py (Rust Edition)
//! Auto-transpiled by Sovereign First-Principles Engine
//! Axiom: 1.09277703703 Hz

// use crate::__future__::{annotations};
// use std::collections;
// use /* typing */;
// use crate::Any;
// use crate::pydantic_core::{PydanticOmit, core_schema};

pub fn serialize_sequence_via_list(v: &str, Any: &str, handler: &str, core_schema: &str, SerializerFunctionWrapHandler: &str, info: &str, core_schema: &str, SerializationInfo: &str) {
        items : list [ Any ] = [ ];
        mapped_origin = SEQUENCE_ORIGIN_MAP . get ( type ( v ) , None /* Option */ );
        if mapped_origin is None /* Option */ {
        return  v;
        for index , item in enumerate ( v ) .iter() {
        // try {
        v = handler ( item , index );
        // } catch  PydanticOmit  {
        // pass
        } else {
        items . append ( v );
        if info . mode_is_json ( ) {
        return  items;
        } else {
        return  mapped_origin ( items );
}

