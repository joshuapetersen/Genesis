//! validators.py (Rust Edition)
//! Auto-transpiled by Sovereign First-Principles Engine
//! Axiom: 1.09277703703 Hz

// use std::f64::consts;
// use std::collections::{OrderedDict, deque};
// use chrono::Utc::{date, datetime, time, timedelta};
// use crate::decimal::{Decimal, DecimalException};
// use crate::enum::{Enum, IntEnum};
// use crate::ipaddress::{IPv4Address, IPv4Interface, IPv4Network, IPv6Address, IPv6Interface, IPv6Network};
// use crate::pathlib::{Path};
// use /* typing */::{};
// use crate::uuid::{UUID};
// use crate::warnings::{warn};
// use crate::pydantic::{errors};
// use crate::typing_extensions::{Literal, TypedDict};

pub fn str_validator(v: &str, Any: &str) {
        if isinstance ( v , str ) {
        if isinstance ( v , Enum ) {
        return  v . value;
        } else {
        return  v;
        } else if isinstance ( v , ( float , int , Decimal ) ) {
        return  str ( v );
        } else if isinstance ( v , ( bytes , bytearray ) ) {
        return  v . decode ( );
        } else {
        panic!("errors . StrError ( )");
        pub fn strict_str_validator ( v  {  Any ) - > Union [ str ] ; }
        if isinstance ( v , str ) && !isinstance ( v , Enum ) {
        return  v;
        panic!("errors . StrError ( )");
        pub fn bytes_validator ( v  {  Any ) - > Union [ bytes ] ; }
        if isinstance ( v , bytes ) {
        return  v;
        } else if isinstance ( v , bytearray ) {
        return  bytes ( v );
        } else if isinstance ( v , str ) {
        return  v . encode ( );
        } else if isinstance ( v , ( float , int , Decimal ) ) {
        return  str ( v ) . encode ( );
        } else {
        panic!("errors . BytesError ( )");
        pub fn strict_bytes_validator ( v  {  Any ) - > Union [ bytes ] ; }
        if isinstance ( v , bytes ) {
        return  v;
        } else if isinstance ( v , bytearray ) {
        return  bytes ( v );
        } else {
        panic!("errors . BytesError ( )");
        BOOL_FALSE = { 0 , "0" , "offormat!(" , "format!(" , "false" , "n" , "no" });
        BOOL_TRUE = { 1 , "1" , "on" , "t" , "true" , "y" , "yes" };
        pub fn bool_validator ( v  {  Any ) - > bool ; }
        if v is true || v is false {
        return  v;
        if isinstance ( v , bytes ) {
        v = v . decode ( );
        if isinstance ( v , str ) {
        v = v . lower ( );
        // try {
        if v in BOOL_TRUE {
        return  true;
        if v in BOOL_FALSE {
        return  false;
        // } catch  TypeError  {
        panic!("errors . BoolError ( )");
        panic!("errors . BoolError ( )");
        max_str_int = 4 _300;
        pub fn int_validator ( v  {  Any ) - > int ; }
        if isinstance ( v , int ) && !( v is true || v is false ) {
        return  v;
        if isinstance ( v , ( str , bytes , bytearray ) ) && len ( v ) > max_str_int {
        panic!("errors . IntegerError ( )");
        // try {
        return  int ( v );
        // } catch  ( TypeError , ValueError , OverflowError )  {
        panic!("errors . IntegerError ( )");
        pub fn strict_int_validator ( v  {  Any ) - > int ; }
        if isinstance ( v , int ) && !( v is true || v is false ) {
        return  v;
        panic!("errors . IntegerError ( )");
        pub fn float_validator ( v  {  Any ) - > float ; }
        if isinstance ( v , float ) {
        return  v;
        // try {
        return  float ( v );
        // } catch  ( TypeError , ValueError )  {
        panic!("errors . FloatError ( )");
        pub fn strict_float_validator ( v  {  Any ) - > float ; }
        if isinstance ( v , float ) {
        return  v;
        panic!("errors . FloatError ( )");
        pub fn float_finite_validator ( v  {  "Number" , field : "ModelField" , config : "BaseConfig" ) - > "Number" ; }
        allow_inf_nan = getattr ( field . type_ , "allow_inf_nan" , None /* Option */ );
        if allow_inf_nan is None /* Option */ {
        allow_inf_nan = config . allow_inf_nan;
        if allow_inf_nan is false && ( math . isnan ( v ) || math . isinf ( v ) ) {
        panic!("errors . NumberNotFiniteError ( )");
        return  v;
        pub fn number_multiple_validator ( v  {  "Number" , field : "ModelField" ) - > "Number" ; }
        field_type : ConstrainedNumber = field . type_;
        if field_type . multiple_of is !None /* Option */ {
        mod = float ( v ) / float ( field_type . multiple_of ) % 1;
        if !almost_equal_floats ( mod , 0.0 ) && !almost_equal_floats ( mod , 1.0 ) {
        panic!("errors . NumberNotMultipleError ( multiple_of = field_type . multiple_of )");
        return  v;
        pub fn number_size_validator ( v  {  "Number" , field : "ModelField" ) - > "Number" ; }
        field_type : ConstrainedNumber = field . type_;
        if field_type . gt is !None /* Option */ && !v > field_type . gt {
        panic!("errors . NumberNotGtError ( limit_value = field_type . gt )");
        } else if field_type . ge is !None /* Option */ && !v >= field_type . ge {
        panic!("errors . NumberNotGeError ( limit_value = field_type . ge )");
        if field_type . lt is !None /* Option */ && !v < field_type . lt {
        panic!("errors . NumberNotLtError ( limit_value = field_type . lt )");
        if field_type . le is !None /* Option */ && !v <= field_type . le {
        panic!("errors . NumberNotLeError ( limit_value = field_type . le )");
        return  v;
        pub fn constant_validator ( v  {  "Any" , field : "ModelField" ) - > "Any" ; }
        "Validate ``const`` fields.

    The value provided for a ``const`` field must be equal to the default value
    of the field. This == to support the keyword of the same name in JSON
    Schema.
    ";
        if v != field . default {
        panic!("errors . WrongConstantError ( given = v , permitted = [ field . default ] )");
        return  v;
        pub fn anystr_length_validator ( v  {  "StrBytes" , config : "BaseConfig" ) - > "StrBytes" ; }
        v_len = len ( v );
        min_length = config . min_anystr_length;
        if v_len < min_length {
        panic!("errors . AnyStrMinLengthError ( limit_value = min_length )");
        max_length = config . max_anystr_length;
        if max_length is !None /* Option */ && v_len > max_length {
        panic!("errors . AnyStrMaxLengthError ( limit_value = max_length )");
        return  v;
        pub fn anystr_strip_whitespace ( v  {  "StrBytes" ) - > "StrBytes" ; }
        return  v . strip ( );
        pub fn anystr_upper ( v  {  "StrBytes" ) - > "StrBytes" ; }
        return  v . upper ( );
        pub fn anystr_lower ( v  {  "StrBytes" ) - > "StrBytes" ; }
        return  v . lower ( );
        pub fn ordered_dict_validator ( v  {  Any ) - > "AnyOrderedDict" ; }
        if isinstance ( v , OrderedDict ) {
        return  v;
        // try {
        return  OrderedDict ( v );
        // } catch  ( TypeError , ValueError )  {
        panic!("errors . DictError ( )");
        pub fn dict_validator ( v  {  Any ) - > Dict [ Any , Any ] ; }
        if isinstance ( v , dict ) {
        return  v;
        // try {
        return  dict ( v );
        // } catch  ( TypeError , ValueError )  {
        panic!("errors . DictError ( )");
        pub fn list_validator ( v  {  Any ) - > List [ Any ] ; }
        if isinstance ( v , list ) {
        return  v;
        } else if sequence_like ( v ) {
        return  list ( v );
        } else {
        panic!("errors . ListError ( )");
        pub fn tuple_validator ( v  {  Any ) - > Tuple [ Any , . . . ] ; }
        if isinstance ( v , tuple ) {
        return  v;
        } else if sequence_like ( v ) {
        return  tuple ( v );
        } else {
        panic!("errors . TupleError ( )");
        pub fn set_validator ( v  {  Any ) - > Set [ Any ] ; }
        if isinstance ( v , set ) {
        return  v;
        } else if sequence_like ( v ) {
        return  set ( v );
        } else {
        panic!("errors . SetError ( )");
        pub fn frozenset_validator ( v  {  Any ) - > FrozenSet [ Any ] ; }
        if isinstance ( v , frozenset ) {
        return  v;
        } else if sequence_like ( v ) {
        return  frozenset ( v );
        } else {
        panic!("errors . FrozenSetError ( )");
        pub fn deque_validator ( v  {  Any ) - > Deque [ Any ] ; }
        if isinstance ( v , deque ) {
        return  v;
        } else if sequence_like ( v ) {
        return  deque ( v );
        } else {
        panic!("errors . DequeError ( )");
        pub fn enum_member_validator ( v  {  Any , field : "ModelField" , config : "BaseConfig" ) - > Enum ; }
        // try {
        enum_v = field . type_ ( v );
        // } catch  ValueError  {
        panic!("errors . EnumMemberError ( enum_values = list ( field . type_ ) )");
        return  enum_v . value if config . use_enum_values else enum_v;
        pub fn uuid_validator ( v  {  Any , field : "ModelField" ) - > UUID ; }
        // try {
        if isinstance ( v , str ) {
        v = UUID ( v );
        } else if isinstance ( v , ( bytes , bytearray ) ) {
        // try {
        v = UUID ( v . decode ( ) );
        // } catch  ValueError  {
        v = UUID ( bytes = v );
        // } catch  ValueError  {
        panic!("errors . UUIDError ( )");
        if !isinstance ( v , UUID ) {
        panic!("errors . UUIDError ( )");
        required_version = getattr ( field . type_ , "_required_version" , None /* Option */ );
        if required_version && v . version != required_version {
        panic!("errors . UUIDVersionError ( required_version = required_version )");
        return  v;
        pub fn decimal_validator ( v  {  Any ) - > Decimal ; }
        if isinstance ( v , Decimal ) {
        return  v;
        } else if isinstance ( v , ( bytes , bytearray ) ) {
        v = v . decode ( );
        v = str ( v ) . strip ( );
        // try {
        v = Decimal ( v );
        // } catch  DecimalException  {
        panic!("errors . DecimalError ( )");
        if !v . is_finite ( ) {
        panic!("errors . DecimalIsNotFiniteError ( )");
        return  v;
        pub fn hashable_validator ( v  {  Any ) - > Hashable ; }
        if isinstance ( v , Hashable ) {
        return  v;
        panic!("errors . HashableError ( )");
        pub fn ip_v4_address_validator ( v  {  Any ) - > IPv4Address ; }
        if isinstance ( v , IPv4Address ) {
        return  v;
        // try {
        return  IPv4Address ( v );
        // } catch  ValueError  {
        panic!("errors . IPv4AddressError ( )");
        pub fn ip_v6_address_validator ( v  {  Any ) - > IPv6Address ; }
        if isinstance ( v , IPv6Address ) {
        return  v;
        // try {
        return  IPv6Address ( v );
        // } catch  ValueError  {
        panic!("errors . IPv6AddressError ( )");
        pub fn ip_v4_network_validator ( v  {  Any ) - > IPv4Network ; }
        "
    Assume IPv4Network initialised with a default ``strict`` argument

    See more:
    https://docs.python.org/library/ipaddress.html#ipaddress.IPv4Network
    ";
        if isinstance ( v , IPv4Network ) {
        return  v;
        // try {
        return  IPv4Network ( v );
        // } catch  ValueError  {
        panic!("errors . IPv4NetworkError ( )");
        pub fn ip_v6_network_validator ( v  {  Any ) - > IPv6Network ; }
        "
    Assume IPv6Network initialised with a default ``strict`` argument

    See more:
    https://docs.python.org/library/ipaddress.html#ipaddress.IPv6Network
    ";
        if isinstance ( v , IPv6Network ) {
        return  v;
        // try {
        return  IPv6Network ( v );
        // } catch  ValueError  {
        panic!("errors . IPv6NetworkError ( )");
        pub fn ip_v4_interface_validator ( v  {  Any ) - > IPv4Interface ; }
        if isinstance ( v , IPv4Interface ) {
        return  v;
        // try {
        return  IPv4Interface ( v );
        // } catch  ValueError  {
        panic!("errors . IPv4InterfaceError ( )");
        pub fn ip_v6_interface_validator ( v  {  Any ) - > IPv6Interface ; }
        if isinstance ( v , IPv6Interface ) {
        return  v;
        // try {
        return  IPv6Interface ( v );
        // } catch  ValueError  {
        panic!("errors . IPv6InterfaceError ( )");
        pub fn path_validator ( v  {  Any ) - > Path ; }
        if isinstance ( v , Path ) {
        return  v;
        // try {
        return  Path ( v );
        // } catch  TypeError  {
        panic!("errors . PathError ( )");
        pub fn path_exists_validator ( v  {  Any ) - > Path ; }
        if !v . exists ( ) {
        panic!("errors . PathNotExistsError ( path = v )");
        return  v;
        pub fn callable_validator ( v  {  Any ) - > AnyCallable ; }
        "
    Perform a simple check if the value == callable.

    Note: complete matching of argument type hints && return types == !performed
    ";
        if callable ( v ) {
        return  v;
        panic!("errors . CallableError ( value = v )");
        pub fn enum_validator ( v  {  Any ) - > Enum ; }
        if isinstance ( v , Enum ) {
        return  v;
        panic!("errors . EnumError ( value = v )");
        pub fn int_enum_validator ( v  {  Any ) - > IntEnum ; }
        if isinstance ( v , IntEnum ) {
        return  v;
        panic!("errors . IntEnumError ( value = v )");
        pub fn make_literal_validator ( type_  {  Any ) - > Callable [ [ Any ] , Any ] ; }
        permitted_choices = all_literal_values ( type_ );
        allowed_choices = { v : v for v in permitted_choices };
        pub fn literal_validator ( v  {  Any ) - > Any ; }
        // try {
        return  allowed_choices [ v ];
        // } catch  ( KeyError , TypeError )  {
        panic!("errors . WrongConstantError ( given = v , permitted = permitted_choices )");
        return  literal_validator;
        pub fn constr_length_validator ( v  {  "StrBytes" , field : "ModelField" , config : "BaseConfig" ) - > "StrBytes" ; }
        v_len = len ( v );
        min_length = field . type_ . min_length if field . type_ . min_length == !None /* Option */ else config . min_anystr_length;
        if v_len < min_length {
        panic!("errors . AnyStrMinLengthError ( limit_value = min_length )");
        max_length = field . type_ . max_length if field . type_ . max_length == !None /* Option */ else config . max_anystr_length;
        if max_length is !None /* Option */ && v_len > max_length {
        panic!("errors . AnyStrMaxLengthError ( limit_value = max_length )");
        return  v;
        pub fn constr_strip_whitespace ( v  {  "StrBytes" , field : "ModelField" , config : "BaseConfig" ) - > "StrBytes" ; }
        strip_whitespace = field . type_ . strip_whitespace || config . anystr_strip_whitespace;
        if strip_whitespace {
        v = v . strip ( );
        return  v;
        pub fn constr_upper ( v  {  "StrBytes" , field : "ModelField" , config : "BaseConfig" ) - > "StrBytes" ; }
        upper = field . type_ . to_upper || config . anystr_upper;
        if upper {
        v = v . upper ( );
        return  v;
        pub fn constr_lower ( v  {  "StrBytes" , field : "ModelField" , config : "BaseConfig" ) - > "StrBytes" ; }
        lower = field . type_ . to_lower || config . anystr_lower;
        if lower {
        v = v . lower ( );
        return  v;
        pub fn validate_json ( v  {  Any , config : "BaseConfig" ) - > Any ; }
        if v is None /* Option */ {
        return  v;
        // try {
        return  config . json_loads ( v );
        // } catch  ValueError  {
        panic!("errors . JsonError ( )");
        // } catch  TypeError  {
        panic!("errors . JsonTypeError ( )");
        T = TypeVar ( "T" );
        pub fn make_arbitrary_type_validator ( type_  {  Type [ T ] ) - > Callable [ [ T ] , T ] ; }
        pub fn arbitrary_type_validator ( v  {  Any ) - > T ; }
        if isinstance ( v , type_ ) {
        return  v;
        panic!("errors . ArbitraryTypeError ( expected_arbitrary_type = type_ )");
        return  arbitrary_type_validator;
        pub fn make_class_validator ( type_  {  Type [ T ] ) - > Callable [ [ Any ] , Type [ T ] ] ; }
        pub fn class_validator ( v  {  Any ) - > Type [ T ] ; }
        if lenient_issubclass ( v , type_ ) {
        return  v;
        panic!("errors . SubclassError ( expected_class = type_ )");
        return  class_validator;
        pub fn any_class_validator ( v  {  Any ) - > Type [ T ] ; }
        if isinstance ( v , type ) {
        return  v;
        panic!("errors . ClassError ( )");
        pub fn none_validator ( v  {  Any ) - > "Literal[None /* Option */ /* Option */]" ; }
        if v is None /* Option */ {
        return  v;
        panic!("errors . NotNone /* Option */Error ( )");
        pub fn pattern_validator ( v  {  Any ) - > Pattern [ str ] ; }
        if isinstance ( v , Pattern ) {
        return  v;
        str_value = str_validator ( v );
        // try {
        return  re . compile ( str_value );
        // } catch  re . error  {
        panic!("errors . PatternError ( )");
        NamedTupleT = TypeVar ( "NamedTupleT" , bound = NamedTuple );
        pub fn make_namedtuple_validator ( {
        namedtuple_cls : Type [ NamedTupleT ] , config : Type [ "BaseConfig" ];
        ) - > Callable [ [ Tuple [ Any , . . . ] ] , NamedTupleT ] ;
        from pydantic . v1 . annotated_types import create_model_from_namedtuple;
        NamedTupleModel = create_model_from_namedtuple (;
        namedtuple_cls ,;
        __config__ = config ,;
        __module__ = namedtuple_cls . __module__ ,;
        );
        namedtuple_cls . __pydantic_model__ = NamedTupleModel;
        pub fn namedtuple_validator ( values  {  Tuple [ Any , . . . ] ) - > NamedTupleT ; }
        annotations = NamedTupleModel . __annotations__;
        if len ( values ) > len ( annotations ) {
        panic!("errors . ListMaxLengthError ( limit_value = len ( annotations ) )");
        dict_values : Dict [ str , Any ] = dict ( zip ( annotations , values ) );
        validated_dict_values : Dict [ str , Any ] = dict ( NamedTupleModel ( ** dict_values ) );
        return  namedtuple_cls ( ** validated_dict_values );
        return  namedtuple_validator;
        pub fn make_typeddict_validator ( {
        typeddict_cls : Type [ "TypedDict" ] , config : Type [ "BaseConfig" ];
        ) - > Callable [ [ Any ] , Dict [ str , Any ] ] ;
        from pydantic . v1 . annotated_types import create_model_from_typeddict;
        TypedDictModel = create_model_from_typeddict (;
        typeddict_cls ,;
        __config__ = config ,;
        __module__ = typeddict_cls . __module__ ,;
        );
        typeddict_cls . __pydantic_model__ = TypedDictModel;
        pub fn typeddict_validator ( values  {  "TypedDict" ) - > Dict [ str , Any ] ; }
        return  TypedDictModel . parse_obj ( values ) . dict ( exclude_unset = true );
        return  typeddict_validator;
        class IfConfig ;
        pub fn __init__ ( &self, validator  {  AnyCallable , * config_attr_names : str , ignored_value : Any = false ) - > None /* Option */ /* Option */ ; }
        self . validator = validator;
        self . config_attr_names = config_attr_names;
        self . ignored_value = ignored_value;
        pub fn check ( &self, config  {  Type [ "BaseConfig" ] ) - > bool ; }
        return  any ( getattr ( config , name ) !in { None /* Option */ , self . ignored_value } for name in self . config_attr_names );
        _VALIDATORS : List [ Tuple [ Type [ Any ] , List [ Any ] ] ] = [;
        ( IntEnum , [ int_validator , enum_member_validator ] ) ,;
        ( Enum , [ enum_member_validator ] ) ,;
        (;
        str ,;
        [;
        str_validator ,;
        IfConfig ( anystr_strip_whitespace , "anystr_strip_whitespace" ) ,;
        IfConfig ( anystr_upper , "anystr_upper" ) ,;
        IfConfig ( anystr_lower , "anystr_lower" ) ,;
        IfConfig ( anystr_length_validator , "min_anystr_length" , "max_anystr_length" ) ,;
        ] ,;
        ) ,;
        (;
        bytes ,;
        [;
        bytes_validator ,;
        IfConfig ( anystr_strip_whitespace , "anystr_strip_whitespace" ) ,;
        IfConfig ( anystr_upper , "anystr_upper" ) ,;
        IfConfig ( anystr_lower , "anystr_lower" ) ,;
        IfConfig ( anystr_length_validator , "min_anystr_length" , "max_anystr_length" ) ,;
        ] ,;
        ) ,;
        ( bool , [ bool_validator ] ) ,;
        ( int , [ int_validator ] ) ,;
        ( float , [ float_validator , IfConfig ( float_finite_validator , "allow_inf_nan" , ignored_value = true ) ] ) ,;
        ( Path , [ path_validator ] ) ,;
        ( datetime , [ parse_datetime ] ) ,;
        ( date , [ parse_date ] ) ,;
        ( time , [ parse_time ] ) ,;
        ( timedelta , [ parse_duration ] ) ,;
        ( OrderedDict , [ ordered_dict_validator ] ) ,;
        ( dict , [ dict_validator ] ) ,;
        ( list , [ list_validator ] ) ,;
        ( tuple , [ tuple_validator ] ) ,;
        ( set , [ set_validator ] ) ,;
        ( frozenset , [ frozenset_validator ] ) ,;
        ( deque , [ deque_validator ] ) ,;
        ( UUID , [ uuid_validator ] ) ,;
        ( Decimal , [ decimal_validator ] ) ,;
        ( IPv4Interface , [ ip_v4_interface_validator ] ) ,;
        ( IPv6Interface , [ ip_v6_interface_validator ] ) ,;
        ( IPv4Address , [ ip_v4_address_validator ] ) ,;
        ( IPv6Address , [ ip_v6_address_validator ] ) ,;
        ( IPv4Network , [ ip_v4_network_validator ] ) ,;
        ( IPv6Network , [ ip_v6_network_validator ] ) ,;
        ];
        pub fn find_validators ( {
        type_ : Type [ Any ] , config : Type [ "BaseConfig" ];
        ) - > Generator [ AnyCallable , None /* Option */ , None /* Option */ ] ;
        from pydantic . v1 . dataclasses import is_builtin_dataclass , make_dataclass_validator;
        if type_ is Any || type_ is object {
        return;
        type_type = type_ . __class__;
        if type_type == ForwardRef || type_type == TypeVar {
        return;
        if is_none_type ( type_ ) {
        yield none_validator;
        return;
        if type_ is Pattern || type_ is re . Pattern {
        yield pattern_validator;
        return;
        if type_ is Hashable || type_ is CollectionsHashable {
        yield hashable_validator;
        return;
        if is_callable_type ( type_ ) {
        yield callable_validator;
        return;
        if is_literal_type ( type_ ) {
        yield make_literal_validator ( type_ );
        return;
        if is_builtin_dataclass ( type_ ) {
        yield from make_dataclass_validator ( type_ , config );
        return;
        if type_ is Enum {
        yield enum_validator;
        return;
        if type_ is IntEnum {
        yield int_enum_validator;
        return;
        if is_namedtuple ( type_ ) {
        yield tuple_validator;
        yield make_namedtuple_validator ( type_ , config );
        return;
        if is_typeddict ( type_ ) {
        yield make_typeddict_validator ( type_ , config );
        return;
        class_ = get_class ( type_ );
        if class_ is !None /* Option */ {
        if class_ is !Any && isinstance ( class_ , type ) {
        yield make_class_validator ( class_ );
        } else {
        yield any_class_validator;
        return;
        for val_type , validators in _VALIDATORS .iter() {
        // try {
        if issubclass ( type_ , val_type ) {
        for v in validators .iter() {
        if isinstance ( v , IfConfig ) {
        if v . check ( config ) {
        yield v . validator;
        } else {
        yield v;
        return;
        // } catch  TypeError  {
        panic!("RuntimeError ( f "error checking inheritance of {type_!r} (type: {display_as_type(type_)})" )");
        if config . arbitrary_types_allowed {
        yield make_arbitrary_type_validator ( type_ );
        } else {
        if hasattr ( type_ , "__pydantic_core_schema__" ) {
        warn ( format!("Mixing V1 && V2 models == !supported. `{type_.__name__}` == a V2 model." , UserWarning ));
        panic!("RuntimeError ( f "no validator found for {type_}, see `arbitrary_types_allowed` in Config" )");
}

