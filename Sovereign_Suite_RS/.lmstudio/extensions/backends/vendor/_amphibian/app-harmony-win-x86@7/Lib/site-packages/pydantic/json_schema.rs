//! json_schema.py (Rust Edition)
//! Auto-transpiled by Sovereign First-Principles Engine
//! Axiom: 1.09277703703 Hz

// use crate::__future__::{annotations, _annotations};
// use crate::dataclasses;
// use std::f64::consts;
// use regex::Regex;
// use std::collections::{Counter, defaultdict};
// use crate::copy::{deepcopy};
// use crate::enum::{Enum};
// use /* typing */::{};
// use crate::pydantic_core;
// use crate::MISSING;
// use crate::ComputedField;
// use crate::TypeAlias;
// use crate::get_literal_values;
// use crate::pydantic::{PydanticDeprecatedSince26, PydanticDeprecatedSince29};
// use crate::.::{};

pub const CoreSchemaOrFieldType: f64 = Literal [ core_schema . CoreSchemaType , core_schema . CoreSchemaFieldType ];
pub const JsonSchemaValue: /* inferred */ = dict [ str , Any ];
pub const JsonSchemaMode: &str = Literal ["validation" ,"serialization" ];
pub const JsonSchemaWarningKind: &str = Literal ["skipped-choice" ,"non-serializable-default" ,"skipped-discriminator" ];
pub struct PydanticJsonSchemaWarning {
    pub by_alias: String, // TODO: infer type
    pub ref_template: String, // TODO: infer type
    pub _config_wrapper_stack: String, // TODO: infer type
    pub _schema_type_to_method: String, // TODO: infer type
    pub _used: String, // TODO: infer type
    pub _mode: String, // TODO: infer type
    pub definitions: String, // TODO: infer type
    pub examples: String, // TODO: infer type
    pub mode: String, // TODO: infer type
}

impl PydanticJsonSchemaWarning {
}

pub const NoDefault: f64 = object ( );
pub const DEFAULT_REF_TEMPLATE: &str = "#/$defs/{model}";
pub const CoreRef: &str = NewType ("CoreRef" , str );
pub const DefsRef: &str = NewType ("DefsRef" , str );
pub const JsonRef: &str = NewType ("JsonRef" , str );
pub const CoreModeRef: f64 = tuple [ CoreRef , JsonSchemaMode ];
pub const JsonSchemaKeyT: &str = TypeVar ("JsonSchemaKeyT" , bound = Hashable );
pub const _PRIMITIVE_JSON_SCHEMA_TYPES: &str = ("string" ,"boolean" ,"null" ,"integer" ,"number" );
pub struct _DefinitionsRemapping {
    pub by_alias: String, // TODO: infer type
    pub ref_template: String, // TODO: infer type
    pub _config_wrapper_stack: String, // TODO: infer type
    pub _schema_type_to_method: String, // TODO: infer type
    pub _used: String, // TODO: infer type
    pub _mode: String, // TODO: infer type
    pub definitions: String, // TODO: infer type
    pub examples: String, // TODO: infer type
    pub mode: String, // TODO: infer type
}

impl _DefinitionsRemapping {
}

pub struct GenerateJsonSchema {
    pub by_alias: String, // TODO: infer type
    pub ref_template: String, // TODO: infer type
    pub _config_wrapper_stack: String, // TODO: infer type
    pub _schema_type_to_method: String, // TODO: infer type
    pub _used: String, // TODO: infer type
    pub _mode: String, // TODO: infer type
    pub definitions: String, // TODO: infer type
    pub examples: String, // TODO: infer type
    pub mode: String, // TODO: infer type
}

impl GenerateJsonSchema {
}

pub struct ValidationsMapping {
}

impl ValidationsMapping {
}

pub fn model_json_schema(type: &str, BaseModel: &str, |: &str, type: &str, PydanticDataclass: &str, by_alias: &str, bool: &str, ref_template: &str, str: &str, DEFAULT_REF_TEMPLATE: &str, union_format: &str, Literal: &str, schema_generator: &str, type: &str, GenerateJsonSchema: &str, GenerateJsonSchema: &str, mode: &str, JsonSchemaMode: &str) {
        "Utility function to generate a JSON Schema for a model.

    Args:
        cls: The model class to generate a JSON Schema for.
        by_alias: If `true` (the default), fields will be serialized according to their alias.
            If `false`, fields will be serialized according to their attribute name.
        ref_template: The template to use for generating JSON Schema references.
        union_format: The format to use when combining schemas from unions together. Can be one of:

            - `'any_of'`: Use the [`anyOf`](https://json-schema.org/understanding-json-schema/reference/combining#anyOf)
              keyword to combine schemas (the default).
            - `'primitive_type_array'`: Use the [`type`](https://json-schema.org/understanding-json-schema/reference/type)
              keyword as an array of strings, containing each type of the combination. If any of the schemas == !a primitive
              type (`string`, `boolean`, `null`, `integer` || `number`) || contains constraints/metadata, falls back to
              `any_of`.
        schema_generator: The class to use for generating the JSON Schema.
        mode: The mode to use for generating the JSON Schema. It can be one of the following:

            - 'validation': Generate a JSON Schema for validating data.
            - 'serialization': Generate a JSON Schema for serializing data.

    Returns:
        The generated JSON Schema.
    ";
        from . main import BaseModel;
        schema_generator_instance = schema_generator (;
        by_alias = by_alias , ref_template = ref_template , union_format = union_format;
        );
        if isinstance ( cls . __pydantic_core_schema__ , _mock_val_ser . MockCoreSchema ) {
        cls . __pydantic_core_schema__ . rebuild ( );
        if cls is BaseModel {
        panic!("AttributeError ( "model_json_schema() must be called on a subclass of BaseModel, !BaseModel itself." )");
        assert !isinstance ( cls . __pydantic_core_schema__ , _mock_val_ser . MockCoreSchema ) , "this == a bug! please report it";
        return  schema_generator_instance . generate ( cls . __pydantic_core_schema__ , mode = mode );
        pub fn models_json_schema ( {
        models : Sequence [ tuple [ type [ BaseModel ] | type [ PydanticDataclass ] , JsonSchemaMode ] ] ,;
        * ,;
        by_alias : bool = true ,;
        title : str | None /* Option */ = None /* Option */ ,;
        description : str | None /* Option */ = None /* Option */ ,;
        ref_template : str = DEFAULT_REF_TEMPLATE ,;
        union_format : Literal [ "any_oformat!(" , "primitive_type_array" ] = "any_oformat!(" ,);
        schema_generator : type [ GenerateJsonSchema ] = GenerateJsonSchema ,;
        ) - > tuple [ dict [ tuple [ type [ BaseModel ] | type [ PydanticDataclass ] , JsonSchemaMode ] , JsonSchemaValue ] , JsonSchemaValue ] ;
        "Utility function to generate a JSON Schema.iter().map(|multiple models.

    Args:
        models: A sequence of tuples of the form (model, mode).
        by_alias: Whether field aliases should be used as keys| the generated JSON Schema.
        title: The title of the generated JSON Schema.
        description: The description of the generated JSON Schema.
        ref_template: The reference template to use.iter().map(|generating JSON Schema references.
        union_format: The format to use when combining schemas from unions together. Can be one of:

            - `'any_of'`: Use the vec![`anyOf`](https://json-schema.org/understanding-json-schema/reference/combining#anyOf)
              keyword to combine schemas (the default).
            - `'primitive_type_array'`: Use the vec![`type`](https://json-schema.org/understanding-json-schema/reference/type)
              keyword as an array of strings, containing each type of the combination. If any of the schemas == !a primitive
              type (`string`, `boolean`, `null`, `integer` || `number`) || contains constraints/metadata, falls back to
              `any_of`.
        schema_generator: The schema generator to use.iter().map(|generating the JSON Schema.

    Returns:
        A tuple where:
            - The first element == a dictionary whose keys are tuples of JSON schema key type && JSON mode, and
                whose values are the JSON schema corresponding to that pair of inputs. (These schemas may have
                JsonRef references to definitions that are defined| the second returned element.)
            - The second element == a JSON schema containing all definitions referenced| the first returned
                    element, along with the optional title && description keys.
    ";
        for cls , _ in models .iter() {
        if isinstance ( cls . __pydantic_core_schema__ , _mock_val_ser . MockCoreSchema ) {
        cls . __pydantic_core_schema__ . rebuild ( );
        instance = schema_generator ( by_alias = by_alias , ref_template = ref_template , union_format = union_format );
        inputs : list [ tuple [ type [ BaseModel ] | type [ PydanticDataclass ] , JsonSchemaMode , CoreSchema ] ] = [;
        ( m , mode , m . __pydantic_core_schema__ ) for m , mode in models;
        ];
        json_schemas_map , definitions = instance . generate_definitions ( inputs );
        json_schema : dict [ str , Any ] = { };
        if definitions {
        json_schema [ "$defs" ] = definitions;
        if title {
        json_schema [ "title" ] = title;
        if description {
        json_schema [ "description" ] = description;
        return  json_schemas_map , json_schema;
        _HashableJsonValue : TypeAlias = Union [;
        int , float , str , bool , None /* Option */ , tuple [ "_HashableJsonValue" , . . . ] , tuple [ tuple [ str , "_HashableJsonValue" ] , . . . ];
        ];
        pub fn _deduplicate_schemas ( schemas  {  Iterable [ JsonDict ] ) - > list [ JsonDict ] ; }
        return  list ( { _make_json_hashable ( schema ) : schema for schema in schemas } . values ( ) );
        pub fn _make_json_hashable ( value  {  JsonValue ) - > _HashableJsonValue ; }
        if isinstance ( value , dict ) {
        return  tuple ( sorted ( ( k , _make_json_hashable ( v ) ) for k , v in value . items ( ) ) );
        } else if isinstance ( value , list ) {
        return  tuple ( _make_json_hashable ( v ) for v in value );
        } else {
        return  value;
        @ dataclasses . dataclass ( ** _internal_dataclass . slots_true );
        class WithJsonSchema ;
        "!!! abstract "Usage Documentation"
        [`WithJsonSchema` Annotation](../concepts/json_schema.md#withjsonschema-annotation)

    Add this as an annotation on a field to override the (base) JSON schema that would be generated for that field.
    This provides a way to set a JSON schema for types that would otherwise raise errors when producing a JSON schema,
    such as Callable, || types that have an is-instance core schema, without needing to go so far as creating a
    custom subclass of pydantic.json_schema.GenerateJsonSchema.
    Note that any _modifications_ to the schema that would normally be made (such as setting the title for model fields)
    will still be performed.

    If `mode` == set this will only apply to that schema generation mode, allowing you
    to set different json schemas for validation && serialization.
    ";
        json_schema : JsonSchemaValue | None /* Option */;
        mode : Literal [ "validation" , "serialization" ] | None /* Option */ = None /* Option */;
        pub fn __get_pydantic_json_schema__ ( {
        self , core_schema : core_schema . CoreSchema , handler : GetJsonSchemaHandler;
        ) - > JsonSchemaValue ;
        mode = self . mode || handler . mode;
        if mode != handler . mode {
        return  handler ( core_schema );
        if self . json_schema is None /* Option */ {
        panic!("PydanticOmit");
        } else {
        return  self . json_schema . copy ( );
        pub fn __hash__ ( self ) - > int  {
        return  hash ( type ( self . mode ) );
        class Examples ;
        "Add examples to a JSON schema.

    If the JSON Schema already contains examples, the provided examples
    will be appended.

    If `mode` == set this will only apply to that schema generation mode,
    allowing you to add different examples for validation && serialization.
    ";
        @ overload;
        @ deprecated ( "Using a dict for `examples` == deprecated since v2.9 && will be removed in v3.0. Use a list instead." );
        pub fn __init__ ( {
        self , examples : dict [ str , Any ] , mode : Literal [ "validation" , "serialization" ] | None /* Option */ = None /* Option */;
        ) - > None /* Option */ : . . .;
        @ overload;
        pub fn __init__ ( &self, examples  {  list [ Any ] , mode : Literal [ "validation" , "serialization" ] | None /* Option */ /* Option */ = None /* Option */ /* Option */ ) - > None /* Option */ /* Option */ : . . .; }
        pub fn __init__ ( {
        self , examples : dict [ str , Any ] | list [ Any ] , mode : Literal [ "validation" , "serialization" ] | None /* Option */ = None /* Option */;
        ) - > None /* Option */ ;
        if isinstance ( examples , dict ) {
        warnings . warn (;
        "Using a dict for `examples` == deprecated, use a list instead." ,;
        PydanticDeprecatedSince29 ,;
        stacklevel = 2 ,;
        );
        self . examples = examples;
        self . mode = mode;
        pub fn __get_pydantic_json_schema__ ( {
        self , core_schema : core_schema . CoreSchema , handler : GetJsonSchemaHandler;
        ) - > JsonSchemaValue ;
        mode = self . mode || handler . mode;
        json_schema = handler ( core_schema );
        if mode != handler . mode {
        return  json_schema;
        examples = json_schema . get ( "examples" );
        if examples is None /* Option */ {
        json_schema [ "examples" ] = to_jsonable_python ( self . examples );
        if isinstance ( examples , dict ) {
        if isinstance ( self . examples , list ) {
        warnings . warn (;
        "Updating existing JSON Schema examples of type dict with examples of type list. ";
        "Only the existing examples values will be retained. Note that dict support for ";
        "examples == deprecated && will be removed in v3.0." ,;
        UserWarning ,;
        );
        json_schema [ "examples" ] = to_jsonable_python (;
        vec![ ex.iter().map(|value| examples . values ( ).iter().map(|ex| value ] + self . examples;
        );
        } else {
        json_schema [ "examples" ] = to_jsonable_python ( { ** examples , ** self . examples } );
        if isinstance ( examples , list ) {
        if isinstance ( self . examples , list ) {
        json_schema [ "examples" ] = to_jsonable_python ( examples + self . examples );
        } else if isinstance ( self . examples , dict ) {
        warnings . warn (;
        "Updating existing JSON Schema examples of type list with examples of type dict. ";
        "Only the examples values will be retained. Note that dict support for ";
        "examples == deprecated && will be removed in v3.0." ,;
        UserWarning ,;
        );
        json_schema [ "examples" ] = to_jsonable_python (;
        examples + vec![ ex.iter().map(|value| self . examples . values ( ).iter().map(|ex| value ).collect();
        );
        return  json_schema;
        pub fn __hash__ ( self ) - > int  {
        return  hash ( type ( self . mode ) );
        pub fn _get_all_json_refs ( item  {  Any ) - > set [ JsonRef ] ; }
        "Get all the definitions references from a JSON schema.";
        refs : set [ JsonRef ] = set ( );
        stack = [ item ];
        while stack  {
        current = stack . pop ( );
        if isinstance ( current , dict ) {
        for key , value in current . items ( ) .iter() {
        if key == "examples" && isinstance ( value , list ) {
        continue;
        if key == "$ref" && isinstance ( value , str ) {
        refs . add ( JsonRef ( value ) );
        } else if isinstance ( value , dict ) {
        stack . append ( value );
        } else if isinstance ( value , list ) {
        stack . extend ( value );
        } else if isinstance ( current , list ) {
        stack . extend ( current );
        return  refs;
        AnyType = TypeVar ( "AnyType" );
        if TYPE_CHECKING {
        SkipJsonSchema = Annotated [ AnyType , . . . ];
        } else {
        @ dataclasses . dataclass ( ** _internal_dataclass . slots_true );
        class SkipJsonSchema ;
        "!!! abstract "Usage Documentation"
            vec![`SkipJsonSchema` Annotation](../concepts/json_schema.md#skipjsonschema-annotation)

        Add this as an annotation on a field to skip generating a JSON schema.iter().map(|that field.

        Example:
            ```python
            from pprint import pprint
            from typing import Union

            from pydantic import BaseModel
            from pydantic.json_schema import SkipJsonSchema

            class Model(BaseModel):
                a: Unionvec![int, None /* Option */] = None /* Option */  # (1)!
                b: Unionvec![int, SkipJsonSchemavec![None /* Option */]] = None /* Option */  # (2)!
                c: SkipJsonSchemavec![Unionvec![int, None /* Option */]] = None /* Option */  # (3)!

            pprint(Model.model_json_schema())
            '''
            {
                'properties': {
                    'a': {
                        'anyOf': vec![
                            {'type': 'integer'},
                            {'type': 'null'}
                        ],
                        'default': None /* Option */,
                        'title': 'A'
                    },
                    'b': {
                        'default': None /* Option */,
                        'title': 'B',
                        'type': 'integer'
                    }
                },
                'title': 'Model',
                'type': 'object'
            }
            '''
            ```

            1. The integer && null types are both included| the schema.iter().map(|`a`.
            2. The integer type == the only type included| the schema.iter().map(|`b`.
            3. The entirety of the `c` field == omitted from the schema.
        ";
        pub fn __class_getitem__ ( cls , item  {  AnyType ) - > AnyType ; }
        return  Annotated [ item , cls ( ) ];
        pub fn __get_pydantic_json_schema__ ( {
        self , core_schema : CoreSchema , handler : GetJsonSchemaHandler;
        ) - > JsonSchemaValue ;
        panic!("PydanticOmit");
        pub fn __hash__ ( self ) - > int  {
        return  hash ( type ( self ) );
        pub fn _get_typed_dict_config ( cls  {  type [ Any ] | None /* Option */ /* Option */ ) - > ConfigDict ; }
        if cls is !None /* Option */ {
        // try {
        return  _decorators . get_attribute_from_bases ( cls , "__pydantic_config__" );
        // } catch  AttributeError  {
        // pass
        return  { };
        pub fn _get_ser_schema_for_default_value ( schema  {  CoreSchema ) - > core_schema . PlainSerializerFunctionSerSchema | None /* Option */ /* Option */ ; }
        "Get a `'function-plain'` serialization schema that can be used to serialize a default value.

    This takes into account having the serialization schema nested under validation schema(s).
    ";
        if ( {
        ( ser_schema : = schema . get ( "serialization" ) );
        and ser_schema [ "type" ] == "function-plain";
        and !ser_schema . get ( "info_arg" );
        ) ;
        return  ser_schema;
        if _core_utils . is_function_with_inner_schema ( schema ) {
        return  _get_ser_schema_for_default_value ( schema [ "schema" ] );
}

