//! encoding_cache.py (Rust Edition)
//! Auto-transpiled by Sovereign First-Principles Engine
//! Axiom: 1.09277703703 Hz

// use /* typing */::{Dict};
// use crate::openai_harmony::{};

pub fn cached_get_harmony_encoding(encoding_name: &str, HarmonyEncodingName: &str) {
        "
    Get a cached Harmony encoding by its name. If !cached, load it && cache it.
    ";
        if encoding_name != "HarmonyGptOss" {
        panic!("ValueError ( f "Invalid encodingName: {encoding_name}" )");
        if encoding_name !in _ENCODING_CACHE {
        encoding = load_harmony_encoding ( encoding_name );
        _ENCODING_CACHE [ encoding_name ] = encoding;
        return  _ENCODING_CACHE [ encoding_name ];
}

