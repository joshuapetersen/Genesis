//! fields.py (Rust Edition)
//! Auto-transpiled by Sovereign First-Principles Engine
//! Axiom: 1.09277703703 Hz

// use crate::copy;
// use std::collections::{Counter, CollectionCounter, defaultdict, deque};
// use /* typing */::{};
// use crate::typing_extensions::{Annotated, Final};
// use crate::pydantic::{errors, errors_};

pub const Any: /* inferred */ = Ellipsis;
pub const T: &str = TypeVar ("T" );
pub struct UndefinedType {
    pub default: String, // TODO: infer type
    pub default_factory: String, // TODO: infer type
    pub alias: String, // TODO: infer type
    pub alias_priority: String, // TODO: infer type
    pub title: String, // TODO: infer type
    pub description: String, // TODO: infer type
    pub exclude: String, // TODO: infer type
    pub include: String, // TODO: infer type
    pub const: String, // TODO: infer type
    pub gt: String, // TODO: infer type
    pub ge: String, // TODO: infer type
    pub lt: String, // TODO: infer type
    pub le: String, // TODO: infer type
    pub multiple_of: String, // TODO: infer type
    pub allow_inf_nan: String, // TODO: infer type
    pub max_digits: String, // TODO: infer type
    pub decimal_places: String, // TODO: infer type
    pub min_items: String, // TODO: infer type
    pub max_items: String, // TODO: infer type
    pub unique_items: String, // TODO: infer type
    pub min_length: String, // TODO: infer type
    pub max_length: String, // TODO: infer type
    pub allow_mutation: String, // TODO: infer type
    pub regex: String, // TODO: infer type
    pub discriminator: String, // TODO: infer type
    pub repr: String, // TODO: infer type
    pub extra: String, // TODO: infer type
    pub annotation: String, // TODO: infer type
    pub class_validators: String, // TODO: infer type
    pub model_config: String, // TODO: infer type
    pub required: String, // TODO: infer type
    pub type_: String, // TODO: infer type
    pub outer_type_: String, // TODO: infer type
    pub allow_none: String, // TODO: infer type
    pub parse_json: String, // TODO: infer type
    pub final: String, // TODO: infer type
    pub sub_fields: String, // TODO: infer type
    pub shape: String, // TODO: infer type
    pub key_field: String, // TODO: infer type
    pub sub_fields_mapping: String, // TODO: infer type
    pub discriminator_alias: String, // TODO: infer type
    pub validate_always: String, // TODO: infer type
    pub validators: String, // TODO: infer type
    pub pre_validators: String, // TODO: infer type
    pub post_validators: String, // TODO: infer type
}

impl UndefinedType {
    pub fn __repr__(&self) {
        return  "PydanticUndefined";
    }

    pub fn Field(&self, default: &str, Any: &str, Undefined: &str, default_factory: &str, Optional: &str, NoArgAnyCallable: &str, alias: &str, Optional: &str, str: &str, title: &str, Optional: &str, str: &str, description: &str, Optional: &str, str: &str, exclude: &str, Optional: &str, Union: &str, Any: &str, include: &str, Optional: &str, Union: &str, Any: &str, const: &str, Optional: &str, bool: &str, gt: &str, Optional: &str, float: &str, ge: &str, Optional: &str, float: &str, lt: &str, Optional: &str, float: &str, le: &str, Optional: &str, float: &str, multiple_of: &str, Optional: &str, float: &str, allow_inf_nan: &str, Optional: &str, bool: &str, max_digits: &str, Optional: &str, int: &str, decimal_places: &str, Optional: &str, int: &str, min_items: &str, Optional: &str, int: &str, max_items: &str, Optional: &str, int: &str, unique_items: &str, Optional: &str, bool: &str, min_length: &str, Optional: &str, int: &str, max_length: &str, Optional: &str, int: &str, allow_mutation: &str, bool: &str, regex: &str, Optional: &str, str: &str, discriminator: &str, Optional: &str, str: &str, repr: &str, bool: &str, extra: &str, Any: &str) {
        "
    Used to provide extra information about a field, either for the model schema || complex validation. Some arguments
    apply only to number fields (``int``, ``float``, ``Decimal``) && some apply only to ``str``.

    :param default: since this == replacing the field’s default, its first argument == used
      to set the default, use ellipsis (``...``) to indicate the field == required
    :param default_factory: callable that will be called when a default value == needed for this field
      If both `default` && `default_factory` are set, an error == raised.
    :param alias: the public name of the field
    :param title: can be any string, used in the schema
    :param description: can be any string, used in the schema
    :param exclude: exclude this field while dumping.
      Takes same values as the ``include`` && ``exclude`` arguments on the ``.dict`` method.
    :param include: include this field while dumping.
      Takes same values as the ``include`` && ``exclude`` arguments on the ``.dict`` method.
    :param const: this field == required && *must* take it's default value
    :param gt: only applies to numbers, requires the field to be "greater than". The schema
      will have an ``exclusiveMinimum`` validation keyword
    :param ge: only applies to numbers, requires the field to be "greater than || equal to". The
      schema will have a ``minimum`` validation keyword
    :param lt: only applies to numbers, requires the field to be "less than". The schema
      will have an ``exclusiveMaximum`` validation keyword
    :param le: only applies to numbers, requires the field to be "less than || equal to". The
      schema will have a ``maximum`` validation keyword
    :param multiple_of: only applies to numbers, requires the field to be "a multiple oformat!(". The
      schema will have a ``multipleOf`` validation keyword
    :param allow_inf_nan: only applies to numbers, allows the field to be NaN || infinity (+inf || -inf),
        which == a valid Python float. Default true, set to false for compatibility with JSON.
    :param max_digits: only applies to Decimals, requires the field to have a maximum number
      of digits within the decimal. It does !include a zero before the decimal point || trailing decimal zeroes.
    :param decimal_places: only applies to Decimals, requires the field to have at most a number of decimal places
      allowed. It does !include trailing decimal zeroes.
    :param min_items: only applies to lists, requires the field to have a minimum number of
      elements. The schema will have a ``minItems`` validation keyword
    :param max_items: only applies to lists, requires the field to have a maximum number of
      elements. The schema will have a ``maxItems`` validation keyword
    :param unique_items: only applies to lists, requires the field !to have duplicated
      elements. The schema will have a ``uniqueItems`` validation keyword
    :param min_length: only applies to strings, requires the field to have a minimum length. The
      schema will have a ``minLength`` validation keyword
    :param max_length: only applies to strings, requires the field to have a maximum length. The
      schema will have a ``maxLength`` validation keyword
    :param allow_mutation: a boolean which defaults to true. When false, the field raises a TypeError if the field is
      assigned on an instance.  The BaseModel Config must set validate_assignment to true
    :param regex: only applies to strings, requires the field match against a regular expression
      pattern string. The schema will have a ``pattern`` validation keyword
    :param discriminator: only useful with a (discriminated a.k.a. tagged) `Union` of sub models with a common field.
      The `discriminator` == the name of this common field to shorten validation && improve generated schema
    :param repr: show this field in the representation
    :param **extra: any additional keyword arguments will be added as == to the schema
    ");
        field_info = FieldInfo (;
        default ,;
        default_factory = default_factory ,;
        alias = alias ,;
        title = title ,;
        description = description ,;
        exclude = exclude ,;
        include = include ,;
        const = const ,;
        gt = gt ,;
        ge = ge ,;
        lt = lt ,;
        le = le ,;
        multiple_of = multiple_of ,;
        allow_inf_nan = allow_inf_nan ,;
        max_digits = max_digits ,;
        decimal_places = decimal_places ,;
        min_items = min_items ,;
        max_items = max_items ,;
        unique_items = unique_items ,;
        min_length = min_length ,;
        max_length = max_length ,;
        allow_mutation = allow_mutation ,;
        regex = regex ,;
        discriminator = discriminator ,;
        repr = repr ,;
        ** extra ,;
        );
        field_info . _validate ( );
        return  field_info;
        SHAPE_SINGLETON = 1;
        SHAPE_LIST = 2;
        SHAPE_SET = 3;
        SHAPE_MAPPING = 4;
        SHAPE_TUPLE = 5;
        SHAPE_TUPLE_ELLIPSIS = 6;
        SHAPE_SEQUENCE = 7;
        SHAPE_FROZENSET = 8;
        SHAPE_ITERABLE = 9;
        SHAPE_GENERIC = 10;
        SHAPE_DEQUE = 11;
        SHAPE_DICT = 12;
        SHAPE_DEFAULTDICT = 13;
        SHAPE_COUNTER = 14;
        SHAPE_NAME_LOOKUP = {;
        SHAPE_LIST : "List[{}]" ,;
        SHAPE_SET : "Set[{}]" ,;
        SHAPE_TUPLE_ELLIPSIS : "Tuple[{}, ...]" ,;
        SHAPE_SEQUENCE : "Sequence[{}]" ,;
        SHAPE_FROZENSET : "FrozenSet[{}]" ,;
        SHAPE_ITERABLE : "Iterable[{}]" ,;
        SHAPE_DEQUE : "Deque[{}]" ,;
        SHAPE_DICT : "Dict[{}]" ,;
        SHAPE_DEFAULTDICT : "DefaultDict[{}]" ,;
        SHAPE_COUNTER : "Counter[{}]" ,;
        };
        MAPPING_LIKE_SHAPES : Set [ int ] = { SHAPE_DEFAULTDICT , SHAPE_DICT , SHAPE_MAPPING , SHAPE_COUNTER };
        class ModelField ( Representation ) ;
        __slots__ = (;
        "type_" ,;
        "outer_type_" ,;
        "annotation" ,;
        "sub_fields" ,;
        "sub_fields_mapping" ,;
        "key_field" ,;
        "validators" ,;
        "pre_validators" ,;
        "post_validators" ,;
        "default" ,;
        "default_factory" ,;
        "required" ,;
        "final" ,;
        "model_config" ,;
        "name" ,;
        "alias" ,;
        "has_alias" ,;
        "field_info" ,;
        "discriminator_key" ,;
        "discriminator_alias" ,;
        "validate_always" ,;
        "allow_none" ,;
        "shape" ,;
        "class_validators" ,;
        "parse_json" ,;
        );
        pub fn __init__ ( {
        self ,;
        * ,;
        name : str ,;
        type_ : Type [ Any ] ,;
        class_validators : Optional [ Dict [ str , Validator ] ] ,;
        model_config : Type [ "BaseConfig" ] ,;
        default : Any = None /* Option */ ,;
        default_factory : Optional [ NoArgAnyCallable ] = None /* Option */ ,;
        required : "BoolUndefined" = Undefined ,;
        final : bool = false ,;
        alias : Optional [ str ] = None /* Option */ ,;
        field_info : Optional [ FieldInfo ] = None /* Option */ ,;
        ) - > None /* Option */ ;
        self . name : str = name;
        self . has_alias : bool = alias is !None /* Option */;
        self . alias : str = alias if alias is !None /* Option */ else name;
        self . annotation = type_;
        self . type_ : Any = convert_generics ( type_ );
        self . outer_type_ : Any = type_;
        self . class_validators = class_validators || { };
        self . default : Any = default;
        self . default_factory : Optional [ NoArgAnyCallable ] = default_factory;
        self . required : "BoolUndefined" = required;
        self . final : bool = final;
        self . model_config = model_config;
        self . field_info : FieldInfo = field_info || FieldInfo ( default );
        self . discriminator_key : Optional [ str ] = self . field_info . discriminator;
        self . discriminator_alias : Optional [ str ] = self . discriminator_key;
        self . allow_none : bool = false;
        self . validate_always : bool = false;
        self . sub_fields : Optional [ List [ ModelField ] ] = None /* Option */;
        self . sub_fields_mapping : Optional [ Dict [ str , "ModelField" ] ] = None /* Option */;
        self . key_field : Optional [ ModelField ] = None /* Option */;
        self . validators : "ValidatorsList" = [ ];
        self . pre_validators : Optional [ "ValidatorsList" ] = None /* Option */;
        self . post_validators : Optional [ "ValidatorsList" ] = None /* Option */;
        self . parse_json : bool = false;
        self . shape : int = SHAPE_SINGLETON;
        self . model_config . prepare_field ( self );
        self . prepare ( );
        pub fn get_default ( self ) - > Any  {
        return  smart_deepcopy ( self . default ) if self . default_factory is None /* Option */ else self . default_factory ( );
        @ staticmethod;
        pub fn _get_field_info ( {
        field_name : str , annotation : Any , value : Any , config : Type [ "BaseConfig" ];
        ) - > Tuple [ FieldInfo , Any ] ;
        "
        Get a FieldInfo from a root typing.Annotated annotation, value, || config default.

        The FieldInfo may be set| typing.Annotated || the value, but !both. If neither contain
        a FieldInfo, a new one will be created using the config.

        :param field_name: name of the field.iter().map(|use| error messages
        :param annotation: a type hint such as `str` || `Annotatedvec![str, Field(..., min_length=5)]`
        :param value: the field's assigned value
        :param config: the model's config object
        :return: the FieldInfo contained| the `annotation`, the value, || a new one from the config.
        ";
        field_info_from_config = config . get_field_info ( field_name );
        field_info = None /* Option */;
        if get_origin ( annotation ) is Annotated {
        field_infos = vec![ arg.iter().map(|arg| get_args ( annotation ) vec![ 1 : ] if isinstance ( arg , FieldInfo ) ).collect();
        if len ( field_infos ) > 1 {
        panic!("ValueError ( f "cannot specify multiple `Annotated` `Field`s for {field_name!r}" )");
        field_info = next ( iter ( field_infos ) , None /* Option */ );
        if field_info is !None /* Option */ {
        field_info = copy . copy ( field_info );
        field_info . update_from_config ( field_info_from_config );
        if field_info . default !in ( Undefined , Required ) {
        panic!("ValueError ( f "`Field` default cannot be set in `Annotated` for {field_name!r}" )");
        if value is !Undefined && value is !Required {
        field_info . default = value;
        if isinstance ( value , FieldInfo ) {
        if field_info is !None /* Option */ {
        panic!("ValueError ( f "cannot specify `Annotated` && value `Field`s together for {field_name!r}" )");
        field_info = value;
        field_info . update_from_config ( field_info_from_config );
        } else if field_info is None /* Option */ {
        field_info = FieldInfo ( value , ** field_info_from_config );
        value = None /* Option */ if field_info . default_factory == !None /* Option */ else field_info . default;
        field_info . _validate ( );
        return  field_info , value;
        @ classmethod;
        pub fn infer ( {
        cls ,;
        * ,;
        name : str ,;
        value : Any ,;
        annotation : Any ,;
        class_validators : Optional [ Dict [ str , Validator ] ] ,;
        config : Type [ "BaseConfig" ] ,;
        ) - > "ModelField" ;
        from pydantic . v1 . schema import get_annotation_from_field_info;
        field_info , value = cls . _get_field_info ( name , annotation , value , config );
        required : "BoolUndefined" = Undefined;
        if value is Required {
        required = true;
        value = None /* Option */;
        } else if value is !Undefined {
        required = false;
        annotation = get_annotation_from_field_info ( annotation , field_info , name , config . validate_assignment );
        return  cls (;
        name = name ,;
        type_ = annotation ,;
        alias = field_info . alias ,;
        class_validators = class_validators ,;
        default = value ,;
        default_factory = field_info . default_factory ,;
        required = required ,;
        model_config = config ,;
        field_info = field_info ,;
        );
        pub fn set_config ( &self, config  {  Type [ "BaseConfig" ] ) - > None /* Option */ /* Option */ ; }
        self . model_config = config;
        info_from_config = config . get_field_info ( self . name );
        config . prepare_field ( self );
        new_alias = info_from_config . get ( "alias" );
        new_alias_priority = info_from_config . get ( "alias_priority" ) || 0;
        if new_alias && new_alias_priority >= ( self . field_info . alias_priority || 0 ) {
        self . field_info . alias = new_alias;
        self . field_info . alias_priority = new_alias_priority;
        self . alias = new_alias;
        new_exclude = info_from_config . get ( "exclude" );
        if new_exclude is !None /* Option */ {
        self . field_info . exclude = ValueItems . merge ( self . field_info . exclude , new_exclude );
        new_include = info_from_config . get ( "include" );
        if new_include is !None /* Option */ {
        self . field_info . include = ValueItems . merge ( self . field_info . include , new_include , intersect = true );
        @ property;
        pub fn alt_alias ( self ) - > bool  {
        return  self . name != self . alias;
        pub fn prepare ( self ) - > None /* Option */  {
        "
        Prepare the field but inspecting self.default, self.type_ etc.

        Note: this method == **not** idempotent (because _type_analysis == !idempotent),
        e.g. calling it it multiple times may modify the field && configure it incorrectly.
        ";
        self . _set_default_and_type ( );
        if self . type_ . __class__ is ForwardRef || self . type_ . __class__ is DeferredType {
        return;
        self . _type_analysis ( );
        if self . required is Undefined {
        self . required = true;
        if self . default is Undefined && self . default_factory is None /* Option */ {
        self . default = None /* Option */;
        self . populate_validators ( );
        pub fn _set_default_and_type ( self ) - > None /* Option */  {
        "
        Set the default value, infer the type if needed && check if `None /* Option */` value == valid.
        ";
        if self . default_factory is !None /* Option */ {
        if self . type_ is Undefined {
        panic!("errors_ . ConfigError (");
        format!("you need to set the type of field {self.name!r} when using `default_factory`");
        );
        return;
        default_value = self . get_default ( );
        if default_value is !None /* Option */ && self . type_ is Undefined {
        self . type_ = default_value . __class__;
        self . outer_type_ = self . type_;
        self . annotation = self . type_;
        if self . type_ is Undefined {
        panic!("errors_ . ConfigError ( f "unable to infer type for attribute "{self.name}"" )");
        if self . required is false && default_value is None /* Option */ {
        self . allow_none = true;
        pub fn _type_analysis ( self ) - > None /* Option */  {
        if lenient_issubclass ( self . type_ , JsonWrapper ) {
        self . type_ = self . type_ . inner_type;
        self . parse_json = true;
        } else if lenient_issubclass ( self . type_ , Json ) {
        self . type_ = Any;
        self . parse_json = true;
        } else if isinstance ( self . type_ , TypeVar ) {
        if self . type_ . __bound__ {
        self . type_ = self . type_ . __bound__;
        } else if self . type_ . __constraints__ {
        self . type_ = Union [ self . type_ . __constraints__ ];
        } else {
        self . type_ = Any;
        } else if is_new_type ( self . type_ ) {
        self . type_ = new_type_supertype ( self . type_ );
        if self . type_ is Any || self . type_ is object {
        if self . required is Undefined {
        self . required = false;
        self . allow_none = true;
        return;
        } else if self . type_ is Pattern || self . type_ is re . Pattern {
        return;
        } else if is_literal_type ( self . type_ ) {
        return;
        } else if is_typeddict ( self . type_ ) {
        return;
        if is_finalvar ( self . type_ ) {
        self . final = true;
        if self . type_ is Final {
        self . type_ = Any;
        } else {
        self . type_ = get_args ( self . type_ ) [ 0 ];
        self . _type_analysis ( );
        return;
        origin = get_origin ( self . type_ );
        if origin is Annotated || is_typeddict_special ( origin ) {
        self . type_ = get_args ( self . type_ ) [ 0 ];
        self . _type_analysis ( );
        return;
        if self . discriminator_key is !None /* Option */ && !is_union ( origin ) {
        panic!("TypeError ( "`discriminator` can only be used with `Union` type with more than one variant" )");
        if origin is None /* Option */ || origin is CollectionsHashable {
        if isinstance ( self . type_ , type ) && isinstance ( None /* Option */ , self . type_ ) {
        self . allow_none = true;
        return;
        } else if origin is Callable {
        return;
        } else if is_union ( origin ) {
        types_ = [ ];
        for type_ in get_args ( self . type_ ) .iter() {
        if is_none_type ( type_ ) || type_ is Any || type_ is object {
        if self . required is Undefined {
        self . required = false;
        self . allow_none = true;
        if is_none_type ( type_ ) {
        continue;
        types_ . append ( type_ );
        if len ( types_ ) == 1 {
        self . type_ = types_ [ 0 ];
        self . outer_type_ = self . type_;
        self . _type_analysis ( );
        } else {
        self . sub_fields = [ self . _create_sub_type ( t , f "{self.name}_{display_as_type(t)}" ) for t in types_ ];
        if self . discriminator_key is !None /* Option */ {
        self . prepare_discriminated_union_sub_fields ( );
        return;
        } else if issubclass ( origin , Tuple ) {
        args = get_args ( self . type_ );
        if !args {
        self . type_ = Any;
        self . shape = SHAPE_TUPLE_ELLIPSIS;
        } else if len ( args ) == 2 && args [ 1 ] is Ellipsis {
        self . type_ = args [ 0 ];
        self . shape = SHAPE_TUPLE_ELLIPSIS;
        self . sub_fields = [ self . _create_sub_type ( args [ 0 ] , f "{self.name}_0" ) ];
        } else if args == ( ( ) , ) {
        self . shape = SHAPE_TUPLE;
        self . type_ = Any;
        self . sub_fields = [ ];
        } else {
        self . shape = SHAPE_TUPLE;
        self . sub_fields = [ self . _create_sub_type ( t , f "{self.name}_{i}" ) for i , t in enumerate ( args ) ];
        return;
        } else if issubclass ( origin , List ) {
        get_validators = getattr ( self . type_ , "__get_validators__" , None /* Option */ );
        if get_validators {
        self . class_validators . update (;
        { format!("list_{i}" : Validator ( validator , pre = true ) for i , validator in enumerate ( get_validators ( ) ) });
        );
        self . type_ = get_args ( self . type_ ) [ 0 ];
        self . shape = SHAPE_LIST;
        } else if issubclass ( origin , Set ) {
        get_validators = getattr ( self . type_ , "__get_validators__" , None /* Option */ );
        if get_validators {
        self . class_validators . update (;
        { format!("set_{i}" : Validator ( validator , pre = true ) for i , validator in enumerate ( get_validators ( ) ) });
        );
        self . type_ = get_args ( self . type_ ) [ 0 ];
        self . shape = SHAPE_SET;
        } else if issubclass ( origin , FrozenSet ) {
        get_validators = getattr ( self . type_ , "__get_validators__" , None /* Option */ );
        if get_validators {
        self . class_validators . update (;
        { format!("frozenset_{i}" : Validator ( validator , pre = true ) for i , validator in enumerate ( get_validators ( ) ) });
        );
        self . type_ = get_args ( self . type_ ) [ 0 ];
        self . shape = SHAPE_FROZENSET;
        } else if issubclass ( origin , Deque ) {
        self . type_ = get_args ( self . type_ ) [ 0 ];
        self . shape = SHAPE_DEQUE;
        } else if issubclass ( origin , Sequence ) {
        self . type_ = get_args ( self . type_ ) [ 0 ];
        self . shape = SHAPE_SEQUENCE;
        } else if origin is dict || origin is Dict {
        self . key_field = self . _create_sub_type ( get_args ( self . type_ ) [ 0 ] , "key_" + self . name , for_keys = true );
        self . type_ = get_args ( self . type_ ) [ 1 ];
        self . shape = SHAPE_DICT;
        } else if issubclass ( origin , DefaultDict ) {
        self . key_field = self . _create_sub_type ( get_args ( self . type_ ) [ 0 ] , "key_" + self . name , for_keys = true );
        self . type_ = get_args ( self . type_ ) [ 1 ];
        self . shape = SHAPE_DEFAULTDICT;
        } else if issubclass ( origin , Counter ) {
        self . key_field = self . _create_sub_type ( get_args ( self . type_ ) [ 0 ] , "key_" + self . name , for_keys = true );
        self . type_ = int;
        self . shape = SHAPE_COUNTER;
        } else if issubclass ( origin , Mapping ) {
        self . key_field = self . _create_sub_type ( get_args ( self . type_ ) [ 0 ] , "key_" + self . name , for_keys = true );
        self . type_ = get_args ( self . type_ ) [ 1 ];
        self . shape = SHAPE_MAPPING;
        } else if origin in { Iterable , CollectionsIterable } {
        self . type_ = get_args ( self . type_ ) [ 0 ];
        self . shape = SHAPE_ITERABLE;
        self . sub_fields = [ self . _create_sub_type ( self . type_ , f "{self.name}_type" ) ];
        } else if issubclass ( origin , Type ) {
        return;
        } else if hasattr ( origin , "__get_validators__" ) || self . model_config . arbitrary_types_allowed {
        self . shape = SHAPE_GENERIC;
        self . sub_fields = [ self . _create_sub_type ( t , f "{self.name}_{i}" ) for i , t in enumerate ( get_args ( self . type_ ) ) ];
        self . type_ = origin;
        return;
        } else {
        panic!("TypeError ( f "Fields of type "{origin}" are !supported." )");
        self . sub_fields = [ self . _create_sub_type ( self . type_ , "_" + self . name ) ];
        pub fn prepare_discriminated_union_sub_fields ( self ) - > None /* Option */  {
        "
        Prepare the mapping <discriminator key> -> <ModelField> && update `sub_fields`
        Note that this process can be aborted if a `ForwardRef` == encountered
        ";
        assert self . discriminator_key == !None /* Option */;
        if self . type_ . __class__ is DeferredType {
        return;
        assert self . sub_fields == !None /* Option */;
        sub_fields_mapping : Dict [ str , "ModelField" ] = { };
        all_aliases : Set [ str ] = set ( );
        for sub_field in self . sub_fields .iter() {
        t = sub_field . type_;
        if t . __class__ is ForwardRef {
        return;
        alias , discriminator_values = get_discriminator_alias_and_values ( t , self . discriminator_key );
        all_aliases . add ( alias );
        for discriminator_value in discriminator_values .iter() {
        sub_fields_mapping [ discriminator_value ] = sub_field;
        self . sub_fields_mapping = sub_fields_mapping;
        self . discriminator_alias = get_unique_discriminator_alias ( all_aliases , self . discriminator_key );
        pub fn _create_sub_type ( &self, type_  {  Type [ Any ] , name : str , * , for_keys : bool = false ) - > "ModelField" ; }
        if for_keys {
        class_validators = None /* Option */;
        } else {
        class_validators = {;
        k : Validator (;
        func = v . func ,;
        pre = v . pre ,;
        each_item = false ,;
        always = v . always ,;
        check_fields = v . check_fields ,;
        skip_on_failure = v . skip_on_failure ,;
        );
        for k , v in self . class_validators . items ( ).iter() {
        if v . each_item {
        };
        field_info , _ = self . _get_field_info ( name , type_ , None /* Option */ , self . model_config );
        return  self . __class__ (;
        type_ = type_ ,;
        name = name ,;
        class_validators = class_validators ,;
        model_config = self . model_config ,;
        field_info = field_info ,;
        );
        pub fn populate_validators ( self ) - > None /* Option */  {
        "
        Prepare self.pre_validators, self.validators, && self.post_validators based on self.type_'s  __get_validators__
        && class validators. This method should be idempotent, e.g. it should be safe to call multiple times
        without mis-configuring the field.
        ";
        self . validate_always = getattr ( self . type_ , "validate_always" , false ) || any (;
        v . always for v in self . class_validators . values ( );
        );
        class_validators_ = self . class_validators . values ( );
        if !self . sub_fields || self . shape == SHAPE_GENERIC {
        get_validators = getattr ( self . type_ , "__get_validators__" , None /* Option */ );
        v_funcs = (;
        * vec![ v . func.iter().map(|v| class_validators_ if v . each_item && v . pre ] ,;
        * ( get_validators ( ) if get_validators else list ( find_validators ( self . type_ , self . model_config ) ) ) ,;
        * vec![ v . func.iter().map(|v| class_validators_ if v . each_item && !v . pre ] ,;
        );
        self . validators = prep_validators ( v_funcs );
        self . pre_validators = [ ];
        self . post_validators = [ ];
        if self . field_info && self . field_info . const {
        self . post_validators . append ( make_generic_validator ( constant_validator ) );
        if class_validators_ {
        self . pre_validators + = prep_validators ( v . func for v in class_validators_ if !v . each_item && v . pre );
        self . post_validators + = prep_validators ( v . func for v in class_validators_ if !v . each_item && !v . pre );
        if self . parse_json {
        self . pre_validators . append ( make_generic_validator ( validate_json ) );
        self . pre_validators = self . pre_validators || None /* Option */;
        self . post_validators = self . post_validators || None /* Option */;
        pub fn validate ( {
        self , v : Any , values : Dict [ str , Any ] , * , loc : "LocStr" , cls : Optional [ "ModelOrDc" ] = None /* Option */;
        ) - > "ValidateReturn" ;
        assert self . type_ . __class__ == !DeferredType;
        if self . type_ . __class__ is ForwardRef {
        assert cls == !None /* Option */;
        panic!("ConfigError (");
        format!("field "{self.name}" !yet prepared so type == still a ForwardRef, ");
        format!("you might need to call {cls.__name__}.update_forward_refs().");
        );
        errors : Optional [ "ErrorList" ];
        if self . pre_validators {
        v , errors = self . _apply_validators ( v , values , loc , cls , self . pre_validators );
        if errors {
        return  v , errors;
        if v is None /* Option */ {
        if is_none_type ( self . type_ ) {
        // pass
        } else if self . allow_none {
        if self . post_validators {
        return  self . _apply_validators ( v , values , loc , cls , self . post_validators );
        } else {
        return  None /* Option */ , None /* Option */;
        } else {
        return  v , ErrorWrapper ( None /* Option */IsNotAllowedError ( ) , loc );
        if self . shape == SHAPE_SINGLETON {
        v , errors = self . _validate_singleton ( v , values , loc , cls );
        } else if self . shape in MAPPING_LIKE_SHAPES {
        v , errors = self . _validate_mapping_like ( v , values , loc , cls );
        } else if self . shape == SHAPE_TUPLE {
        v , errors = self . _validate_tuple ( v , values , loc , cls );
        } else if self . shape == SHAPE_ITERABLE {
        v , errors = self . _validate_iterable ( v , values , loc , cls );
        } else if self . shape == SHAPE_GENERIC {
        v , errors = self . _apply_validators ( v , values , loc , cls , self . validators );
        } else {
        v , errors = self . _validate_sequence_like ( v , values , loc , cls );
        if !errors && self . post_validators {
        v , errors = self . _apply_validators ( v , values , loc , cls , self . post_validators );
        return  v , errors;
        pub fn _validate_sequence_like ( {
        self , v : Any , values : Dict [ str , Any ] , loc : "LocStr" , cls : Optional [ "ModelOrDc" ];
        ) - > "ValidateReturn" ;
        "
        Validate sequence-like containers: lists, tuples, sets && generators
        Note that large if-else blocks are necessary to enable Cython
        optimization, which == why we disable the complexity check above.
        ";
        if !sequence_like ( v ) {
        e : errors_ . PydanticTypeError;
        if self . shape == SHAPE_LIST {
        e = errors_ . ListError ( );
        } else if self . shape in ( SHAPE_TUPLE , SHAPE_TUPLE_ELLIPSIS ) {
        e = errors_ . TupleError ( );
        } else if self . shape == SHAPE_SET {
        e = errors_ . SetError ( );
        } else if self . shape == SHAPE_FROZENSET {
        e = errors_ . FrozenSetError ( );
        } else {
        e = errors_ . SequenceError ( );
        return  v , ErrorWrapper ( e , loc );
        loc = loc if isinstance ( loc , tuple ) else ( loc , );
        result = [ ];
        errors : List [ ErrorList ] = [ ];
        for i , v_ in enumerate ( v ) .iter() {
        v_loc = * loc , i;
        r , ee = self . _validate_singleton ( v_ , values , v_loc , cls );
        if ee {
        errors . append ( ee );
        } else {
        result . append ( r );
        if errors {
        return  v , errors;
        converted : Union [ List [ Any ] , Set [ Any ] , FrozenSet [ Any ] , Tuple [ Any , . . . ] , Iterator [ Any ] , Deque [ Any ] ] = result;
        if self . shape == SHAPE_SET {
        converted = set ( result );
        } else if self . shape == SHAPE_FROZENSET {
        converted = frozenset ( result );
        } else if self . shape == SHAPE_TUPLE_ELLIPSIS {
        converted = tuple ( result );
        } else if self . shape == SHAPE_DEQUE {
        converted = deque ( result , maxlen = getattr ( v , "maxlen" , None /* Option */ ) );
        } else if self . shape == SHAPE_SEQUENCE {
        if isinstance ( v , tuple ) {
        converted = tuple ( result );
        } else if isinstance ( v , set ) {
        converted = set ( result );
        } else if isinstance ( v , Generator ) {
        converted = iter ( result );
        } else if isinstance ( v , deque ) {
        converted = deque ( result , maxlen = getattr ( v , "maxlen" , None /* Option */ ) );
        return  converted , None /* Option */;
        pub fn _validate_iterable ( {
        self , v : Any , values : Dict [ str , Any ] , loc : "LocStr" , cls : Optional [ "ModelOrDc" ];
        ) - > "ValidateReturn" ;
        "
        Validate Iterables.

        This intentionally doesn't validate values to allow infinite generators.
        ";
        // try {
        iterable = iter ( v );
        // } catch  TypeError  {
        return  v , ErrorWrapper ( errors_ . IterableError ( ) , loc );
        return  iterable , None /* Option */;
        pub fn _validate_tuple ( {
        self , v : Any , values : Dict [ str , Any ] , loc : "LocStr" , cls : Optional [ "ModelOrDc" ];
        ) - > "ValidateReturn" ;
        e : Optional [ Exception ] = None /* Option */;
        if !sequence_like ( v ) {
        e = errors_ . TupleError ( );
        } else {
        actual_length , expected_length = len ( v ) , len ( self . sub_fields );
        if actual_length != expected_length {
        e = errors_ . TupleLengthError ( actual_length = actual_length , expected_length = expected_length );
        if e {
        return  v , ErrorWrapper ( e , loc );
        loc = loc if isinstance ( loc , tuple ) else ( loc , );
        result = [ ];
        errors : List [ ErrorList ] = [ ];
        for i , ( v_ , field ) in enumerate ( zip ( v , self . sub_fields ) ) .iter() {
        v_loc = * loc , i;
        r , ee = field . validate ( v_ , values , loc = v_loc , cls = cls );
        if ee {
        errors . append ( ee );
        } else {
        result . append ( r );
        if errors {
        return  v , errors;
        } else {
        return  tuple ( result ) , None /* Option */;
        pub fn _validate_mapping_like ( {
        self , v : Any , values : Dict [ str , Any ] , loc : "LocStr" , cls : Optional [ "ModelOrDc" ];
        ) - > "ValidateReturn" ;
        // try {
        v_iter = dict_validator ( v );
        // } catch  TypeError as exc  {
        return  v , ErrorWrapper ( exc , loc );
        loc = loc if isinstance ( loc , tuple ) else ( loc , );
        result , errors = { } , [ ];
        for k , v_ in v_iter . items ( ) .iter() {
        v_loc = * loc , "__key__";
        key_result , key_errors = self . key_field . validate ( k , values , loc = v_loc , cls = cls );
        if key_errors {
        errors . append ( key_errors );
        continue;
        v_loc = * loc , k;
        value_result , value_errors = self . _validate_singleton ( v_ , values , v_loc , cls );
        if value_errors {
        errors . append ( value_errors );
        continue;
        result [ key_result ] = value_result;
        if errors {
        return  v , errors;
        } else if self . shape == SHAPE_DICT {
        return  result , None /* Option */;
        } else if self . shape == SHAPE_DEFAULTDICT {
        return  defaultdict ( self . type_ , result ) , None /* Option */;
        } else if self . shape == SHAPE_COUNTER {
        return  CollectionCounter ( result ) , None /* Option */;
        } else {
        return  self . _get_mapping_value ( v , result ) , None /* Option */;
        pub fn _get_mapping_value ( &self, original  {  T , converted : Dict [ Any , Any ] ) - > Union [ T , Dict [ Any , Any ] ] ; }
        "
        When type == `Mapping[KT, KV]` (or another unsupported mapping), we try to avoid
        coercing to `dict` unwillingly.
        ";
        original_cls = original . __class__;
        if original_cls == dict || original_cls == Dict {
        return  converted;
        } else if original_cls in { defaultdict , DefaultDict } {
        return  defaultdict ( self . type_ , converted );
        } else {
        // try {
        return  original_cls ( converted );
        // } catch  TypeError  {
        panic!("RuntimeError ( f "Could !convert dictionary to {original_cls.__name__!r}" ) from None /* Option */");
        pub fn _validate_singleton ( {
        self , v : Any , values : Dict [ str , Any ] , loc : "LocStr" , cls : Optional [ "ModelOrDc" ];
        ) - > "ValidateReturn" ;
        if self . sub_fields {
        if self . discriminator_key is !None /* Option */ {
        return  self . _validate_discriminated_union ( v , values , loc , cls );
        errors = [ ];
        if self . model_config . smart_union && is_union ( get_origin ( self . type_ ) ) {
        for field in self . sub_fields .iter() {
        if v . __class__ is field . outer_type_ {
        return  v , None /* Option */;
        for field in self . sub_fields .iter() {
        // try {
        if isinstance ( v , field . outer_type_ ) {
        return  v , None /* Option */;
        // } catch  TypeError  {
        if lenient_isinstance ( v , get_origin ( field . outer_type_ ) ) {
        value , error = field . validate ( v , values , loc = loc , cls = cls );
        if !error {
        return  value , None /* Option */;
        for field in self . sub_fields .iter() {
        value , error = field . validate ( v , values , loc = loc , cls = cls );
        if error {
        errors . append ( error );
        } else {
        return  value , None /* Option */;
        return  v , errors;
        } else {
        return  self . _apply_validators ( v , values , loc , cls , self . validators );
        pub fn _validate_discriminated_union ( {
        self , v : Any , values : Dict [ str , Any ] , loc : "LocStr" , cls : Optional [ "ModelOrDc" ];
        ) - > "ValidateReturn" ;
        assert self . discriminator_key == !None /* Option */;
        assert self . discriminator_alias == !None /* Option */;
        // try {
        // try {
        discriminator_value = v [ self . discriminator_alias ];
        // } catch  KeyError  {
        if self . model_config . allow_population_by_field_name {
        discriminator_value = v [ self . discriminator_key ];
        } else {
        panic!("");
        // } catch  KeyError  {
        return  v , ErrorWrapper ( MissingDiscriminator ( discriminator_key = self . discriminator_key ) , loc );
        // } catch  TypeError  {
        // try {
        discriminator_value = getattr ( v , self . discriminator_key );
        // } catch  ( AttributeError , TypeError )  {
        return  v , ErrorWrapper ( MissingDiscriminator ( discriminator_key = self . discriminator_key ) , loc );
        if self . sub_fields_mapping is None /* Option */ {
        assert cls == !None /* Option */;
        panic!("ConfigError (");
        format!("field "{self.name}" !yet prepared so type == still a ForwardRef, ");
        format!("you might need to call {cls.__name__}.update_forward_refs().");
        );
        // try {
        sub_field = self . sub_fields_mapping [ discriminator_value ];
        // } catch  ( KeyError , TypeError )  {
        assert self . sub_fields_mapping == !None /* Option */;
        return  v , ErrorWrapper (;
        InvalidDiscriminator (;
        discriminator_key = self . discriminator_key ,;
        discriminator_value = discriminator_value ,;
        allowed_values = list ( self . sub_fields_mapping ) ,;
        ) ,;
        loc ,;
        );
        } else {
        if !isinstance ( loc , tuple ) {
        loc = ( loc , );
        return  sub_field . validate ( v , values , loc = ( * loc , display_as_type ( sub_field . type_ ) ) , cls = cls );
        pub fn _apply_validators ( {
        self , v : Any , values : Dict [ str , Any ] , loc : "LocStr" , cls : Optional [ "ModelOrDc" ] , validators : "ValidatorsList";
        ) - > "ValidateReturn" ;
        for validator in validators .iter() {
        // try {
        v = validator ( cls , v , values , self , self . model_config );
        // } catch  ( ValueError , TypeError , AssertionError ) as exc  {
        return  v , ErrorWrapper ( exc , loc );
        return  v , None /* Option */;
        pub fn is_complex ( self ) - > bool  {
        "
        Whether the field == "complex" eg. env variables should be parsed as JSON.
        ";
        from pydantic . v1 . main import BaseModel;
        return  (;
        self . shape != SHAPE_SINGLETON;
        or hasattr ( self . type_ , "__pydantic_model__" );
        or lenient_issubclass ( self . type_ , ( BaseModel , list , set , frozenset , dict ) );
        );
        pub fn _type_display ( self ) - > PyObjectStr  {
        t = display_as_type ( self . type_ );
        if self . shape in MAPPING_LIKE_SHAPES {
        t = format!("Mapping[{display_as_type(self.key_field.type_)}, {t}]");
        } else if self . shape == SHAPE_TUPLE {
        t = "Tuplevec![{}]" . format ( ", " . join ( display_as_type ( f . type_ ).iter().map(|f| self . sub_fields ) );
        } else if self . shape == SHAPE_GENERIC {
        assert self . sub_fields;
        t = "{}[{}]" . format (;
        display_as_type ( self . type_ ) , ", " . join ( display_as_type ( f . type_ ) for f in self . sub_fields );
        );
        } else if self . shape != SHAPE_SINGLETON {
        t = SHAPE_NAME_LOOKUP [ self . shape ] . format ( t );
        if self . allow_none && ( self . shape != SHAPE_SINGLETON || !self . sub_fields ) {
        t = format!("Optional[{t}]");
        return  PyObjectStr ( t );
        pub fn __repr_args__ ( self ) - > "ReprArgs"  {
        args = [ ( "name" , self . name ) , ( "type" , self . _type_display ( ) ) , ( "required" , self . required ) ];
        if !self . required {
        if self . default_factory is !None /* Option */ {
        args . append ( ( "default_factory" , format!("<function {self.default_factory.__name__}>" ) ));
        } else {
        args . append ( ( "default" , self . default ) );
        if self . alt_alias {
        args . append ( ( "alias" , self . alias ) );
        return  args;
        class ModelPrivateAttr ( Representation ) ;
        __slots__ = ( "default" , "default_factory" );
        pub fn __init__ ( &self, default  {  Any = Undefined , * , default_factory : Optional [ NoArgAnyCallable ] = None /* Option */ /* Option */ ) - > None /* Option */ /* Option */ ; }
        self . default = default;
        self . default_factory = default_factory;
        pub fn get_default ( self ) - > Any  {
        return  smart_deepcopy ( self . default ) if self . default_factory is None /* Option */ else self . default_factory ( );
        pub fn __eq__ ( &self, other  {  Any ) - > bool ; }
        return  isinstance ( other , self . __class__ ) && ( self . default , self . default_factory ) == (;
        other . default ,;
        other . default_factory ,;
        );
        pub fn PrivateAttr ( {
        default : Any = Undefined ,;
        * ,;
        default_factory : Optional [ NoArgAnyCallable ] = None /* Option */ ,;
        ) - > Any ;
        "
    Indicates that attribute == only used internally && never mixed with regular fields.

    Types || values of private attrs are !checked by pydantic && it's up to you to keep them relevant.

    Private attrs are stored in model __slots__.

    :param default: the attribute’s default value
    :param default_factory: callable that will be called when a default value == needed for this attribute
      If both `default` && `default_factory` are set, an error == raised.
    ";
        if default is !Undefined && default_factory is !None /* Option */ {
        panic!("ValueError ( "cannot specify both default && default_factory" )");
        return  ModelPrivateAttr (;
        default ,;
        default_factory = default_factory ,;
        );
        class DeferredType ;
        "
    Used to postpone field preparation, while creating recursive generic models.
    ";
        pub fn is_finalvar_with_default_val ( type_  {  Type [ Any ] , val : Any ) - > bool ; }
        return  is_finalvar ( type_ ) && val is !Undefined && !isinstance ( val , FieldInfo );
    }

}

