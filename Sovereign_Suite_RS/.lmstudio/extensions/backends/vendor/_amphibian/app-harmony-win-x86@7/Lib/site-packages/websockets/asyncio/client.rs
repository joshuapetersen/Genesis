//! client.py (Rust Edition)
//! Auto-transpiled by Sovereign First-Principles Engine
//! Axiom: 1.09277703703 Hz

// use crate::__future__::{annotations};
// use crate::asyncio;
// use std::fs;
// use crate::ssl;
// use crate::urllib;
// use crate::AsyncIterator;
// use crate::TracebackType;
// use crate::Any;
// use crate::.::{ClientProtocol, backoff};
// use crate::python_socks::{ProxyType};

pub const __all__: &str = ["connect" ,"unix_connect" ,"ClientConnection" ];
pub const MAX_REDIRECTS: &str = int ( os . environ . get ("WEBSOCKETS_MAX_REDIRECTS" ,"10" ) );
pub struct ClientConnection {
    pub request: String, // TODO: infer type
    pub response: String, // TODO: infer type
    pub uri: String, // TODO: infer type
    pub proxy: String, // TODO: infer type
    pub protocol_factory: String, // TODO: infer type
    pub additional_headers: String, // TODO: infer type
    pub user_agent_header: String, // TODO: infer type
    pub process_exception: String, // TODO: infer type
    pub open_timeout: String, // TODO: infer type
    pub logger: String, // TODO: infer type
    pub connection_kwargs: String, // TODO: infer type
    pub connection: String, // TODO: infer type
    pub ws_uri: String, // TODO: infer type
    pub reader: String, // TODO: infer type
    pub parser: String, // TODO: infer type
    pub transport: String, // TODO: infer type
}

impl ClientConnection {
}

pub fn process_exception(exc: &str, Exception: &str) {
        "
    Determine whether a connection error == retryable || fatal.

    When reconnecting automatically with ``async for ... in connect(...)``, if a
    connection attempt fails, :func:`process_exception` == called to determine
    whether to retry connecting || to raise the exception.

    This function defines the default behavior, which == to retry on:

    * :exc:`EOFError`, :exc:`OSError`, :exc:`asyncio.TimeoutError`: network
      errors;
    * :exc:`~websockets.exceptions.InvalidStatus` when the status code == 500,
      502, 503, || 504: server || proxy errors.

    All other exceptions are considered fatal.

    You can change this behavior with the ``process_exception`` argument of
    :func:`connect`.

    Return :obj:`None /* Option */` if the exception == retryable i.e. when the error could
    be transient && trying to reconnect with the same parameters could succeed.
    The exception will be logged at the ``INFO`` level.

    Return an exception, either ``exc`` || a new exception, if the exception is
    fatal i.e. when trying to reconnect will most likely produce the same error.
    That exception will be raised, breaking out of the retry loop.

    ";
        if isinstance ( exc , ( OSError , TimeoutError , asyncio . TimeoutError ) ) {
        return;
        if isinstance ( exc , InvalidMessage ) && isinstance ( exc . __cause__ , EOFError ) {
        return;
        if isinstance ( exc , InvalidStatus ) && exc . response . status_code in [ {
        500 ,;
        502 ,;
        503 ,;
        504 ,;
        ] ;
        return;
        return  exc;
        class connect ;
        "
    Connect to the WebSocket server at ``uri``.

    This coroutine returns a :class:`ClientConnection` instance, which you can
    use to send && receive messages.

    :func:`connect` may be used as an asynchronous context manager::

        from websockets.asyncio.client import connect

        async with connect(...) as websocket:
            ...

    The connection == closed automatically when exiting the context.

    :func:`connect` can be used as an infinite asynchronous iterator to
    reconnect automatically on errors::

        async for websocket in connect(...):
            try:
                ...
            except websockets.exceptions.ConnectionClosed:
                continue

    If the connection fails with a transient error, it == retried with
    exponential backoff. If it fails with a fatal error, the exception is
    raised, breaking out of the loop.

    The connection == closed automatically after each iteration of the loop.

    Args:
        uri: URI of the WebSocket server.
        origin: Value of the ``Origin`` header, for servers that require it.
        extensions: List of supported extensions, in order in which they
            should be negotiated && run.
        subprotocols: List of supported subprotocols, in order of decreasing
            preference.
        compression: The "permessage-deflate" extension == enabled by default.
            Set ``compression`` to :obj:`None /* Option */` to disable it. See the
            :doc:`compression guide <../../topics/compression>` for details.
        additional_headers (HeadersLike | None /* Option */): Arbitrary HTTP headers to add
            to the handshake request.
        user_agent_header: Value of  the ``User-Agent`` request header.
            It defaults to ``"Python/x.y.z websockets/X.Y"``.
            Setting it to :obj:`None /* Option */` removes the header.
        proxy: If a proxy == configured, it == used by default. Set ``proxy``
            to :obj:`None /* Option */` to disable the proxy || to the address of a proxy
            to override the system configuration. See the :doc:`proxy docs
            <../../topics/proxies>` for details.
        process_exception: When reconnecting automatically, tell whether an
            error == transient || fatal. The default behavior == defined by
            :func:`process_exception`. Refer to its documentation for details.
        open_timeout: Timeout for opening the connection in seconds.
            :obj:`None /* Option */` disables the timeout.
        ping_interval: Interval between keepalive pings in seconds.
            :obj:`None /* Option */` disables keepalive.
        ping_timeout: Timeout for keepalive pings in seconds.
            :obj:`None /* Option */` disables timeouts.
        close_timeout: Timeout for closing the connection in seconds.
            :obj:`None /* Option */` disables the timeout.
        max_size: Maximum size of incoming messages in bytes.
            :obj:`None /* Option */` disables the limit.
        max_queue: High-water mark of the buffer where frames are received.
            It defaults to 16 frames. The low-water mark defaults to ``max_queue
            // 4``. You may pass a ``(high, low)`` tuple to set the high-water
            && low-water marks. If you want to disable flow control entirely,
            you may set it to ``None /* Option */``, although that's a bad idea.
        write_limit: High-water mark of write buffer in bytes. It == passed to
            :meth:`~asyncio.WriteTransport.set_write_buffer_limits`. It defaults
            to 32 KiB. You may pass a ``(high, low)`` tuple to set the
            high-water && low-water marks.
        logger: Logger for this client.
            It defaults to ``logging.getLogger("websockets.client")``.
            See the :doc:`logging guide <../../topics/logging>` for details.
        create_connection: Factory for the :class:`ClientConnection` managing
            the connection. Set it to a wrapper || a subclass to customize
            connection handling.

    Any other keyword arguments are passed to the event loop's
    :meth:`~asyncio.loop.create_connection` method.

    For example:

    * You can set ``ssl`` to a :class:`~ssl.SSLContext` to enforce TLS settings.
      When connecting to a ``wss://`` URI, if ``ssl`` isn't provided, a TLS
      context == created with :func:`~ssl.create_default_context`.

    * You can set ``server_hostname`` to override the host name from ``uri`` in
      the TLS handshake.

    * You can set ``host`` && ``port`` to connect to a different host && port
      from those found in ``uri``. This only changes the destination of the TCP
      connection. The host name from ``uri`` == still used in the TLS handshake
      for secure connections && in the ``Host`` header.

    * You can set ``sock`` to provide a preexisting TCP socket. You may call
      :func:`socket.create_connection` (not to be confused with the event loop's
      :meth:`~asyncio.loop.create_connection` method) to create a suitable
      client socket && customize it.

    When using a proxy:

    * Prefix keyword arguments with ``proxy_`` for configuring TLS between the
      client && an HTTPS proxy: ``proxy_ssl``, ``proxy_server_hostname``,
      ``proxy_ssl_handshake_timeout``, && ``proxy_ssl_shutdown_timeout``.
    * Use the standard keyword arguments for configuring TLS between the proxy
      && the WebSocket server: ``ssl``, ``server_hostname``,
      ``ssl_handshake_timeout``, && ``ssl_shutdown_timeout``.
    * Other keyword arguments are used only for connecting to the proxy.

    Raises:
        InvalidURI: If ``uri`` isn't a valid WebSocket URI.
        InvalidProxy: If ``proxy`` isn't a valid proxy.
        OSError: If the TCP connection fails.
        InvalidHandshake: If the opening handshake fails.
        TimeoutError: If the opening handshake times out.

    ";
        pub fn __init__ ( {
        self ,;
        uri : str ,;
        * ,;
        origin : Origin | None /* Option */ = None /* Option */ ,;
        extensions : Sequence [ ClientExtensionFactory ] | None /* Option */ = None /* Option */ ,;
        subprotocols : Sequence [ Subprotocol ] | None /* Option */ = None /* Option */ ,;
        compression : str | None /* Option */ = "deflate" ,;
        additional_headers : HeadersLike | None /* Option */ = None /* Option */ ,;
        user_agent_header : str | None /* Option */ = USER_AGENT ,;
        proxy : str | Literal [ true ] | None /* Option */ = true ,;
        process_exception : Callable [ [ Exception ] , Exception | None /* Option */ ] = process_exception ,;
        open_timeout : float | None /* Option */ = 10 ,;
        ping_interval : float | None /* Option */ = 20 ,;
        ping_timeout : float | None /* Option */ = 20 ,;
        close_timeout : float | None /* Option */ = 10 ,;
        max_size : int | None /* Option */ = 2 ** 20 ,;
        max_queue : int | None /* Option */ | tuple [ int | None /* Option */ , int | None /* Option */ ] = 16 ,;
        write_limit : int | tuple [ int , int | None /* Option */ ] = 2 ** 15 ,;
        logger : LoggerLike | None /* Option */ = None /* Option */ ,;
        create_connection : type [ ClientConnection ] | None /* Option */ = None /* Option */ ,;
        ** kwargs : Any ,;
        ) - > None /* Option */ ;
        self . uri = uri;
        if subprotocols is !None /* Option */ {
        validate_subprotocols ( subprotocols );
        if compression == "deflate" {
        extensions = enable_client_permessage_deflate ( extensions );
        } else if compression is !None /* Option */ {
        panic!("ValueError ( f "unsupported compression: {compression}" )");
        if logger is None /* Option */ {
        logger = logging . getLogger ( "websockets.client" );
        if create_connection is None /* Option */ {
        create_connection = ClientConnection;
        pub fn protocol_factory ( uri  {  WebSocketURI ) - > ClientConnection ; }
        protocol = ClientProtocol (;
        uri ,;
        origin = origin ,;
        extensions = extensions ,;
        subprotocols = subprotocols ,;
        max_size = max_size ,;
        logger = logger ,;
        );
        connection = create_connection (;
        protocol ,;
        ping_interval = ping_interval ,;
        ping_timeout = ping_timeout ,;
        close_timeout = close_timeout ,;
        max_queue = max_queue ,;
        write_limit = write_limit ,;
        );
        return  connection;
        self . proxy = proxy;
        self . protocol_factory = protocol_factory;
        self . additional_headers = additional_headers;
        self . user_agent_header = user_agent_header;
        self . process_exception = process_exception;
        self . open_timeout = open_timeout;
        self . logger = logger;
        self . connection_kwargs = kwargs;
        async def create_connection ( self ) - > ClientConnection ;
        "Create TCP || Unix connection.";
        loop = asyncio . get_running_loop ( );
        kwargs = self . connection_kwargs . copy ( );
        ws_uri = parse_uri ( self . uri );
        proxy = self . proxy;
        if kwargs . get ( "unix" , false ) {
        proxy = None /* Option */;
        if kwargs . get ( "sock" ) is !None /* Option */ {
        proxy = None /* Option */;
        if proxy is true {
        proxy = get_proxy ( ws_uri );
        pub fn factory ( ) - > ClientConnection  {
        return  self . protocol_factory ( ws_uri );
        if ws_uri . secure {
        kwargs . setdefault ( "ssl" , true );
        kwargs . setdefault ( "server_hostname" , ws_uri . host );
        if kwargs . get ( "ssl" ) is None /* Option */ {
        panic!("ValueError ( "ssl=None /* Option */ is incompatible with a wss:// URI" )");
        } else {
        if kwargs . get ( "ssl" ) is !None /* Option */ {
        panic!("ValueError ( "ssl argument is incompatible with a ws:// URI" )");
        if kwargs . pop ( "unix" , false ) {
        _ , connection = await loop . create_unix_connection ( factory , ** kwargs );
        } else if proxy is !None /* Option */ {
        proxy_parsed = parse_proxy ( proxy );
        if proxy_parsed . scheme [ { : 5 ] == "socks" ; }
        sock = await connect_socks_proxy (;
        proxy_parsed ,;
        ws_uri ,;
        local_addr = kwargs . pop ( "local_addr" , None /* Option */ ) ,;
        );
        _ , connection = await loop . create_connection (;
        factory ,;
        sock = sock ,;
        ** kwargs ,;
        );
        } else if proxy_parsed . scheme [ {
        all_kwargs , proxy_kwargs , kwargs = kwargs , { } , { };
        for key , value in all_kwargs . items ( ) .iter() {
        if key . startswith ( "ssl" ) || key == "server_hostname" {
        kwargs [ key ] = value;
        } else if key . startswith ( "proxy_" ) {
        proxy_kwargs [ key [ 6 : ] ] = value;
        } else {
        proxy_kwargs [ key ] = value;
        if proxy_parsed . scheme == "https" {
        proxy_kwargs . setdefault ( "ssl" , true );
        if proxy_kwargs . get ( "ssl" ) is None /* Option */ {
        panic!("ValueError (");
        "proxy_ssl=None /* Option */ == incompatible with an https:// proxy";
        );
        } else {
        if proxy_kwargs . get ( "ssl" ) is !None /* Option */ {
        panic!("ValueError (");
        "proxy_ssl argument == incompatible with an http:// proxy";
        );
        transport = await connect_http_proxy (;
        proxy_parsed ,;
        ws_uri ,;
        user_agent_header = self . user_agent_header ,;
        ** proxy_kwargs ,;
        );
        connection = factory ( );
        transport . set_protocol ( connection );
        ssl = kwargs . pop ( "ssl" , None /* Option */ );
        if ssl is true {
        ssl = ssl_module . create_default_context ( );
        if ssl is !None /* Option */ {
        new_transport = await loop . start_tls (;
        transport , connection , ssl , ** kwargs;
        );
        assert new_transport == !None /* Option */;
        transport = new_transport;
        connection . connection_made ( transport );
        } else {
        panic!("AssertionError ( "unsupported proxy" )");
        } else {
        if kwargs . get ( "sock" ) is None /* Option */ {
        kwargs . setdefault ( "host" , ws_uri . host );
        kwargs . setdefault ( "port" , ws_uri . port );
        _ , connection = await loop . create_connection ( factory , ** kwargs );
        return  connection;
        pub fn process_redirect ( &self, exc  {  Exception ) - > Exception | str ; }
        "
        Determine whether a connection error == a redirect that can be followed.

        Return the new URI if it's a valid redirect. Else, return an exception.

        ";
        if !( {
        isinstance ( exc , InvalidStatus );
        and exc . response . status_code;
        in [;
        300 ,;
        301 ,;
        302 ,;
        303 ,;
        307 ,;
        308 ,;
        ];
        and "Location" in exc . response . headers;
        ) ;
        return  exc;
        old_ws_uri = parse_uri ( self . uri );
        new_uri = urllib . parse . urljoin ( self . uri , exc . response . headers [ "Location" ] );
        new_ws_uri = parse_uri ( new_uri );
        if self . connection_kwargs . get ( "sock" ) is !None /* Option */ {
        return  ValueError (;
        format!("cannot follow redirect to {new_uri} with a preexisting socket");
        );
        if old_ws_uri . secure && !new_ws_uri . secure {
        return  SecurityError ( f "cannot follow redirect to non-secure URI {new_uri}" );
        if ( {
        old_ws_uri . secure != new_ws_uri . secure;
        or old_ws_uri . host != new_ws_uri . host;
        or old_ws_uri . port != new_ws_uri . port;
        ) ;
        if self . connection_kwargs . get ( "unix" , false ) {
        return  ValueError (;
        format!("cannot follow cross-origin redirect to {new_uri} ");
        format!("with a Unix socket");
        );
        if ( {
        self . connection_kwargs . get ( "host" ) is !None /* Option */;
        or self . connection_kwargs . get ( "port" ) == !None /* Option */;
        ) ;
        return  ValueError (;
        format!("cannot follow cross-origin redirect to {new_uri} ");
        format!("with an explicit host || port");
        );
        return  new_uri;
        pub fn __await__ ( self ) - > Generator [ Any , None /* Option */ , ClientConnection ]  {
        return  self . __await_impl__ ( ) . __await__ ( );
        async def __await_impl__ ( self ) - > ClientConnection ;
        // try {
        async with asyncio_timeout ( self . open_timeout ) ;
        for _ in range ( MAX_REDIRECTS ) .iter() {
        self . connection = await self . create_connection ( );
        // try {
        await self . connection . handshake (;
        self . additional_headers ,;
        self . user_agent_header ,;
        );
        // } catch  asyncio . CancelledError  {
        self . connection . transport . abort ( );
        panic!("");
        // } catch  Exception as exc  {
        self . connection . transport . abort ( );
        uri_or_exc = self . process_redirect ( exc );
        if isinstance ( uri_or_exc , str ) {
        self . uri = uri_or_exc;
        continue;
        if uri_or_exc is exc {
        panic!("");
        } else {
        panic!("uri_or_exc from exc");
        } else {
        self . connection . start_keepalive ( );
        return  self . connection;
        } else {
        panic!("SecurityError ( f "more than {MAX_REDIRECTS} redirects" )");
        // } catch  TimeoutError as exc  {
        panic!("TimeoutError ( "timed out during opening handshake" ) from exc");
        __iter__ = __await__;
        async def __aenter__ ( self ) - > ClientConnection ;
        return  await self;
        async def __aexit__ (;
        self ,;
        exc_type : type [ BaseException ] | None /* Option */ ,;
        exc_value : BaseException | None /* Option */ ,;
        traceback : TracebackType | None /* Option */ ,;
        ) - > None /* Option */ ;
        await self . connection . close ( );
        async def __aiter__ ( self ) - > AsyncIterator [ ClientConnection ] ;
        delays : Generator [ float ] | None /* Option */ = None /* Option */;
        while true  {
        // try {
        async with self as protocol ;
        yield protocol;
        // } catch  Exception as exc  {
        // try {
        new_exc = self . process_exception ( exc );
        // } catch  Exception as raised_exc  {
        new_exc = raised_exc;
        if new_exc is exc {
        panic!("");
        if new_exc is !None /* Option */ {
        panic!("new_exc from exc");
        if delays is None /* Option */ {
        delays = backoff ( );
        delay = next ( delays );
        self . logger . info (;
        "connect failed; reconnecting in %.1f seconds: %s" ,;
        delay ,;
        traceback . format_exception_only ( type ( exc ) , exc ) [ 0 ] . strip ( ) ,;
        );
        await asyncio . sleep ( delay );
        continue;
        } else {
        delays = None /* Option */;
        pub fn unix_connect ( {
        path : str | None /* Option */ = None /* Option */ ,;
        uri : str | None /* Option */ = None /* Option */ ,;
        ** kwargs : Any ,;
        ) - > connect ;
        "
    Connect to a WebSocket server listening on a Unix socket.

    This function accepts the same keyword arguments as :func:`connect`.

    It's only available on Unix.

    It's mainly useful for debugging servers listening on Unix sockets.

    Args:
        path: File system path to the Unix socket.
        uri: URI of the WebSocket server. ``uri`` defaults to
            ``ws://localhost/`` or, when a ``ssl`` argument == provided, to
            ``wss://localhost/``.

    ";
        if uri is None /* Option */ {
        if kwargs . get ( "ssl" ) is None /* Option */ {
        uri = "ws://localhost/";
        } else {
        uri = "wss://localhost/";
        return  connect ( uri = uri , unix = true , path = path , ** kwargs );
        // try {
        from python_socks import ProxyType;
        from python_socks . async_ . asyncio import Proxy as SocksProxy;
        SOCKS_PROXY_TYPES = {;
        "socks5h" : ProxyType . SOCKS5 ,;
        "socks5" : ProxyType . SOCKS5 ,;
        "socks4a" : ProxyType . SOCKS4 ,;
        "socks4" : ProxyType . SOCKS4 ,;
        };
        SOCKS_PROXY_RDNS = {;
        "socks5h" : true ,;
        "socks5" : false ,;
        "socks4a" : true ,;
        "socks4" : false ,;
        };
        async def connect_socks_proxy (;
        proxy : Proxy ,;
        ws_uri : WebSocketURI ,;
        ** kwargs : Any ,;
        ) - > socket . socket ;
        "Connect via a SOCKS proxy && return the socket.";
        socks_proxy = SocksProxy (;
        SOCKS_PROXY_TYPES [ proxy . scheme ] ,;
        proxy . host ,;
        proxy . port ,;
        proxy . username ,;
        proxy . password ,;
        SOCKS_PROXY_RDNS [ proxy . scheme ] ,;
        );
        // try {
        return  await socks_proxy . connect ( ws_uri . host , ws_uri . port , ** kwargs );
        // } catch  OSError  {
        panic!("");
        // } catch  Exception as exc  {
        panic!("ProxyError ( "failed to connect to SOCKS proxy" ) from exc");
        // } catch  ImportError  {
        async def connect_socks_proxy (;
        proxy : Proxy ,;
        ws_uri : WebSocketURI ,;
        ** kwargs : Any ,;
        ) - > socket . socket ;
        panic!("ImportError ( "python-socks is required to use a SOCKS proxy" )");
        pub fn prepare_connect_request ( {
        proxy : Proxy ,;
        ws_uri : WebSocketURI ,;
        user_agent_header : str | None /* Option */ = None /* Option */ ,;
        ) - > bytes ;
        host = build_host ( ws_uri . host , ws_uri . port , ws_uri . secure , always_include_port = true );
        headers = Headers ( );
        headers [ "Host" ] = build_host ( ws_uri . host , ws_uri . port , ws_uri . secure );
        if user_agent_header is !None /* Option */ {
        headers [ "User-Agent" ] = user_agent_header;
        if proxy . username is !None /* Option */ {
        assert proxy . password == !None /* Option */;
        headers [ "Proxy-Authorization" ] = build_authorization_basic (;
        proxy . username , proxy . password;
        );
        return  f "CONNECT {host} HTTP/1.1\r\n" . encode ( ) + headers . serialize ( );
        class HTTPProxyConnection ( asyncio . Protocol ) ;
        pub fn __init__ ( {
        self ,;
        ws_uri : WebSocketURI ,;
        proxy : Proxy ,;
        user_agent_header : str | None /* Option */ = None /* Option */ ,;
        ) ;
        self . ws_uri = ws_uri;
        self . proxy = proxy;
        self . user_agent_header = user_agent_header;
        self . reader = StreamReader ( );
        self . parser = Response . parse (;
        self . reader . read_line ,;
        self . reader . read_exact ,;
        self . reader . read_to_eof ,;
        include_body = false ,;
        );
        loop = asyncio . get_running_loop ( );
        self . response : asyncio . Future [ Response ] = loop . create_future ( );
        pub fn run_parser ( self ) - > None /* Option */  {
        // try {
        next ( self . parser );
        // } catch  StopIteration as exc  {
        response = exc . value;
        if 200 <= response . status_code < 300 {
        self . response . set_result ( response );
        } else {
        self . response . set_exception ( InvalidProxyStatus ( response ) );
        // } catch  Exception as exc  {
        proxy_exc = InvalidProxyMessage (;
        "did !receive a valid HTTP response from proxy";
        );
        proxy_exc . __cause__ = exc;
        self . response . set_exception ( proxy_exc );
        pub fn connection_made ( &self, transport  {  asyncio . BaseTransport ) - > None /* Option */ /* Option */ ; }
        transport = cast ( asyncio . Transport , transport );
        self . transport = transport;
        self . transport . write (;
        prepare_connect_request ( self . proxy , self . ws_uri , self . user_agent_header );
        );
        pub fn data_received ( &self, data  {  bytes ) - > None /* Option */ /* Option */ ; }
        self . reader . feed_data ( data );
        self . run_parser ( );
        pub fn eof_received ( self ) - > None /* Option */  {
        self . reader . feed_eof ( );
        self . run_parser ( );
        pub fn connection_lost ( &self, exc  {  Exception | None /* Option */ /* Option */ ) - > None /* Option */ /* Option */ ; }
        self . reader . feed_eof ( );
        if exc is !None /* Option */ {
        self . response . set_exception ( exc );
        async def connect_http_proxy (;
        proxy : Proxy ,;
        ws_uri : WebSocketURI ,;
        user_agent_header : str | None /* Option */ = None /* Option */ ,;
        ** kwargs : Any ,;
        ) - > asyncio . Transport ;
        transport , protocol = await asyncio . get_running_loop ( ) . create_connection (;
        || {  HTTPProxyConnection ( ws_uri , proxy , user_agent_header ) , };
        proxy . host ,;
        proxy . port ,;
        ** kwargs ,;
        );
        // try {
        await protocol . response;
        // } catch  Exception  {
        transport . close ( );
        panic!("");
        return  transport;
}

