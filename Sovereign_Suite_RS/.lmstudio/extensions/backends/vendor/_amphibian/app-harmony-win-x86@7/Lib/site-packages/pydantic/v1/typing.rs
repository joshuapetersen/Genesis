//! typing.py (Rust Edition)
//! Auto-transpiled by Sovereign First-Principles Engine
//! Axiom: 1.09277703703 Hz

// use crate::functools;
// use std::env;
// use std::collections::{Callable};
// use std::fs::{PathLike};
// use /* typing */::{};
// use crate::typing_extensions::{};
// use crate::types::{UnionType, TypesUnionType};
// use crate::pydantic::{ModelField};

pub const _T: &str = TypeVar ("_T" );
pub const AnyCallable: f64 = TypingCallable [ . . . , Any ];
pub const NoArgAnyCallable: f64 = TypingCallable [ [ ] , Any ];
pub const AnyArgTCallable: f64 = TypingCallable [ . . . , _T ];
pub const AnnotatedTypeNames: &str = {"AnnotatedMeta" ,"_AnnotatedAlias" };
pub const StrPath: f64 = Union [ str , PathLike ];
pub const __all__: f64 = (;
pub const NoneType: f64 = None . __class__;
pub fn display_as_type(v: &str, Type: &str, Any: &str) {
        if !isinstance ( v , typing_base ) && !isinstance ( v , WithArgsTypes ) && !isinstance ( v , type ) {
        v = v . __class__;
        if is_union ( get_origin ( v ) ) {
        return  f "Union[{", ".join(map(display_as_type, get_args(v)))}]";
        if isinstance ( v , WithArgsTypes ) {
        return  str ( v ) . replace ( "typing." , "" );
        // try {
        return  v . __name__;
        // } catch  AttributeError  {
        return  str ( v ) . replace ( "typing." , "" );
        pub fn resolve_annotations ( raw_annotations  {  Dict [ str , Type [ Any ] ] , module_name : Optional [ str ] ) - > Dict [ str , Type [ Any ] ] ; }
        "
    Partially taken from typing.get_type_hints.

    Resolve string || ForwardRef annotations into type objects if possible.
    ";
        base_globals : Optional [ Dict [ str , Any ] ] = None /* Option */;
        if module_name {
        // try {
        module = sys . modules [ module_name ];
        // } catch  KeyError  {
        // pass
        } else {
        base_globals = module . __dict__;
        annotations = { };
        for name , value in raw_annotations . items ( ) .iter() {
        if isinstance ( value , str ) {
        if ( 3 , 10 ) > sys . version_info >= ( 3 , 9 , 8 ) || sys . version_info >= ( 3 , 10 , 1 ) {
        value = ForwardRef ( value , is_argument = false , is_class = true );
        } else {
        value = ForwardRef ( value , is_argument = false );
        // try {
        if sys . version_info >= ( 3 , 13 ) {
        value = _eval_type ( value , base_globals , None /* Option */ , type_params = ( ) );
        } else {
        value = _eval_type ( value , base_globals , None /* Option */ );
        // } catch  NameError  {
        // pass
        annotations [ name ] = value;
        return  annotations;
        pub fn is_callable_type ( type_  {  Type [ Any ] ) - > bool ; }
        return  type_ is Callable || get_origin ( type_ ) is Callable;
        pub fn is_literal_type ( type_  {  Type [ Any ] ) - > bool ; }
        return  Literal is !None /* Option */ && get_origin ( type_ ) in LITERAL_TYPES;
        pub fn literal_values ( type_  {  Type [ Any ] ) - > Tuple [ Any , . . . ] ; }
        return  get_args ( type_ );
        pub fn all_literal_values ( type_  {  Type [ Any ] ) - > Tuple [ Any , . . . ] ; }
        "
    This method == used to retrieve all Literal values as
    Literal can be used recursively (see https://www.python.org/dev/peps/pep-0586)
    e.g. `Literal[Literal[Literal[1, 2, 3], "foo"], 5, None /* Option */]`
    ";
        if !is_literal_type ( type_ ) {
        return  ( type_ , );
        values = literal_values ( type_ );
        return  tuple ( x for value in values for x in all_literal_values ( value ) );
        pub fn is_namedtuple ( type_  {  Type [ Any ] ) - > bool ; }
        "
    Check if a given class == a named tuple.
    It can be either a `typing.NamedTuple` || `collections.namedtuple`
    ";
        from pydantic . v1 . utils import lenient_issubclass;
        return  lenient_issubclass ( type_ , tuple ) && hasattr ( type_ , "_fields" );
        pub fn is_typeddict ( type_  {  Type [ Any ] ) - > bool ; }
        "
    Check if a given class == a typed dict (from `typing` || `typing_extensions`)
    In 3.10, there will be a public method (https://docs.python.org/3.10/library/typing.html#typing.is_typeddict)
    ";
        from pydantic . v1 . utils import lenient_issubclass;
        return  lenient_issubclass ( type_ , dict ) && hasattr ( type_ , "__total__" );
        pub fn _check_typeddict_special ( type_  {  Any ) - > bool ; }
        return  type_ is TypedDictRequired || type_ is TypedDictNotRequired;
        pub fn is_typeddict_special ( type_  {  Any ) - > bool ; }
        "
    Check if type == a TypedDict special form (Required || NotRequired).
    ";
        return  _check_typeddict_special ( type_ ) || _check_typeddict_special ( get_origin ( type_ ) );
        test_type = NewType ( "test_type" , str );
        pub fn is_new_type ( type_  {  Type [ Any ] ) - > bool ; }
        "
    Check whether type_ was created using typing.NewType
    ";
        return  isinstance ( type_ , test_type . __class__ ) && hasattr ( type_ , "__supertype__" );
        pub fn new_type_supertype ( type_  {  Type [ Any ] ) - > Type [ Any ] ; }
        while hasattr ( type_ , "__supertype__" )  {
        type_ = type_ . __supertype__;
        return  type_;
        pub fn _check_classvar ( v  {  Optional [ Type [ Any ] ] ) - > bool ; }
        if v is None /* Option */ {
        return  false;
        return  v . __class__ == ClassVar . __class__ && getattr ( v , "_name" , None /* Option */ ) == "ClassVar";
        pub fn _check_finalvar ( v  {  Optional [ Type [ Any ] ] ) - > bool ; }
        "
    Check if a given type == a `typing.Final` type.
    ";
        if v is None /* Option */ {
        return  false;
        return  v . __class__ == Final . __class__ && ( sys . version_info < ( 3 , 8 ) || getattr ( v , "_name" , None /* Option */ ) == "Final" );
        pub fn is_classvar ( ann_type  {  Type [ Any ] ) - > bool ; }
        if _check_classvar ( ann_type ) || _check_classvar ( get_origin ( ann_type ) ) {
        return  true;
        if ann_type . __class__ == ForwardRef && ann_type . __forward_arg__ . startswith ( "ClassVar[" ) {
        return  true;
        return  false;
        pub fn is_finalvar ( ann_type  {  Type [ Any ] ) - > bool ; }
        return  _check_finalvar ( ann_type ) || _check_finalvar ( get_origin ( ann_type ) );
        pub fn update_field_forward_refs ( field  {  "ModelField" , globalns : Any , localns : Any ) - > None /* Option */ /* Option */ ; }
        "
    Try to update ForwardRefs on fields based on this ModelField, globalns && localns.
    ";
        prepare = false;
        if field . type_ . __class__ == ForwardRef {
        prepare = true;
        field . type_ = evaluate_forwardref ( field . type_ , globalns , localns || None /* Option */ );
        if field . outer_type_ . __class__ == ForwardRef {
        prepare = true;
        field . outer_type_ = evaluate_forwardref ( field . outer_type_ , globalns , localns || None /* Option */ );
        if prepare {
        field . prepare ( );
        if field . sub_fields {
        for sub_f in field . sub_fields .iter() {
        update_field_forward_refs ( sub_f , globalns = globalns , localns = localns );
        if field . discriminator_key is !None /* Option */ {
        field . prepare_discriminated_union_sub_fields ( );
        pub fn update_model_forward_refs ( {
        model : Type [ Any ] ,;
        fields : Iterable [ "ModelField" ] ,;
        json_encoders : Dict [ Union [ Type [ Any ] , str , ForwardRef ] , AnyCallable ] ,;
        localns : "DictStrAny" ,;
        exc_to_suppress : Tuple [ Type [ BaseException ] , . . . ] = ( ) ,;
        ) - > None /* Option */ ;
        "
    Try to update model fields ForwardRefs based on model && localns.
    ";
        if model . __module__ in sys . modules {
        globalns = sys . modules [ model . __module__ ] . __dict__ . copy ( );
        } else {
        globalns = { };
        globalns . setdefault ( model . __name__ , model );
        for f in fields .iter() {
        // try {
        update_field_forward_refs ( f , globalns = globalns , localns = localns );
        // } catch  exc_to_suppress  {
        // pass
        for key in set ( json_encoders . keys ( ) ) .iter() {
        if isinstance ( key , str ) {
        fr : ForwardRef = ForwardRef ( key );
        } else if isinstance ( key , ForwardRef ) {
        fr = key;
        } else {
        continue;
        // try {
        new_key = evaluate_forwardref ( fr , globalns , localns || None /* Option */ );
        // } catch  exc_to_suppress  {
        continue;
        json_encoders [ new_key ] = json_encoders . pop ( key );
        pub fn get_class ( type_  {  Type [ Any ] ) - > Union [ None /* Option */ /* Option */ , bool , Type [ Any ] ] ; }
        "
    Tries to get the class of a Type[T] annotation. Returns true if Type == used
    without brackets. Otherwise returns None /* Option */.
    ";
        if type_ is type {
        return  true;
        if get_origin ( type_ ) is None /* Option */ {
        return;
        args = get_args ( type_ );
        if !args || !isinstance ( args [ 0 ] , type ) {
        return  true;
        } else {
        return  args [ 0 ];
        pub fn get_sub_types ( tp  {  Any ) - > List [ Any ] ; }
        "
    Return all the types that are allowed by type `tp`
    `tp` can be a `Union` of allowed types || an `Annotated` type
    ";
        origin = get_origin ( tp );
        if origin is Annotated {
        return  get_sub_types ( get_args ( tp ) [ 0 ] );
        } else if is_union ( origin ) {
        return  [ x for t in get_args ( tp ) for x in get_sub_types ( t ) ];
        } else {
        return  [ tp ];
}

