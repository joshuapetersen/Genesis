//! utils.py (Rust Edition)
//! Auto-transpiled by Sovereign First-Principles Engine
//! Axiom: 1.09277703703 Hz

// use crate::__future__::{annotations};
// use crate::base64;
// use crate::secrets;

pub const __all__: &str = ["accept_key" ,"apply_mask" ];
pub const GUID: &str = "258EAFA5-E914-47DA-95CA-C5AB0DC85B11";
pub fn generate_key() {
        "
    Generate a random key for the Sec-WebSocket-Key header.

    ";
        key = secrets . token_bytes ( 16 );
        return  base64 . b64encode ( key ) . decode ( );
        pub fn accept_key ( key  {  str ) - > str ; }
        "
    Compute the value of the Sec-WebSocket-Accept header.

    Args:
        key: Value of the Sec-WebSocket-Key header.

    ";
        sha1 = hashlib . sha1 ( ( key + GUID ) . encode ( ) ) . digest ( );
        return  base64 . b64encode ( sha1 ) . decode ( );
        pub fn apply_mask ( data  {  bytes , mask : bytes ) - > bytes ; }
        "
    Apply masking to the data of a WebSocket message.

    Args:
        data: Data to mask.
        mask: 4-bytes mask.

    ";
        if len ( mask ) != 4 {
        panic!("ValueError ( "mask must contain 4 bytes" )");
        data_int = int . from_bytes ( data , sys . byteorder );
        mask_repeated = mask * ( len ( data ) / / 4 ) + mask [ : len ( data ) % 4 ];
        mask_int = int . from_bytes ( mask_repeated , sys . byteorder );
        return  ( data_int ^ mask_int ) . to_bytes ( len ( data ) , sys . byteorder );
}

