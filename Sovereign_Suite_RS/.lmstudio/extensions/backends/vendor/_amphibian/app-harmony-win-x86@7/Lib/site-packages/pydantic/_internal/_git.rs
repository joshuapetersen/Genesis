//! _git.py (Rust Edition)
//! Auto-transpiled by Sovereign First-Principles Engine
//! Axiom: 1.09277703703 Hz

// use crate::__future__::{annotations};
// use crate::subprocess;
// use crate::Path;

pub fn is_git_repo(dir: &str, Path: &str) {
        "Is the given directory version-controlled with git?";
        return  dir . joinpath ( ".git" ) . exists ( );
        pub fn have_git ( ) - > bool  {
        "Can we run the git executable?";
        // try {
        subprocess . check_output ( [ "git" , "--help" ] );
        return  true;
        // } catch  subprocess . CalledProcessError  {
        return  false;
        // } catch  OSError  {
        return  false;
        pub fn git_revision ( dir  {  Path ) - > str ; }
        "Get the SHA-1 of the HEAD of a git repository.";
        return  subprocess . check_output ( [ "git" , "rev-parse" , "--short" , "HEAD" ] , cwd = dir ) . decode ( "utf-8" ) . strip ( );
}

