//! _regex_core.py (Rust Edition)
//! Auto-transpiled by Sovereign First-Principles Engine
//! Axiom: 1.09277703703 Hz

// use crate::enum;
// use crate::unicodedata;
// use crate::defaultdict;
// use crate::regex::{_regex};
// use rand::Rng;

pub const __all__: &str = ["A" ,"ASCII" ,"B" ,"BESTMATCH" ,"D" ,"DEBUG" ,"E" ,"ENHANCEMATCH" ,;
pub struct error {
    pub msg: String, // TODO: infer type
    pub pattern: String, // TODO: infer type
    pub pos: String, // TODO: infer type
    pub lineno: String, // TODO: infer type
    pub colno: String, // TODO: infer type
    pub _key: String, // TODO: infer type
    pub positive: String, // TODO: infer type
    pub encoding: String, // TODO: infer type
    pub zerowidth: String, // TODO: infer type
    pub case_flags: String, // TODO: infer type
    pub subpattern: String, // TODO: infer type
    pub branches: String, // TODO: infer type
    pub info: String, // TODO: infer type
    pub group: String, // TODO: infer type
    pub position: String, // TODO: infer type
    pub ref: String, // TODO: infer type
    pub parsed: String, // TODO: infer type
    pub value: String, // TODO: infer type
    pub folded: String, // TODO: infer type
    pub folded_characters: String, // TODO: infer type
    pub yes_item: String, // TODO: infer type
    pub no_item: String, // TODO: infer type
    pub constraints: String, // TODO: infer type
    pub min_count: String, // TODO: infer type
    pub max_count: String, // TODO: infer type
    pub call_ref: String, // TODO: infer type
    pub behind: String, // TODO: infer type
    pub code: String, // TODO: infer type
    pub lower: String, // TODO: infer type
    pub upper: String, // TODO: infer type
    pub items: String, // TODO: infer type
    pub char_width: String, // TODO: infer type
    pub characters: String, // TODO: infer type
    pub required: String, // TODO: infer type
    pub name: String, // TODO: infer type
    pub set_key: String, // TODO: infer type
    pub string: String, // TODO: infer type
    pub char_type: String, // TODO: infer type
    pub ignore_space: String, // TODO: infer type
    pub sep: String, // TODO: infer type
    pub flags: String, // TODO: infer type
    pub global_flags: String, // TODO: infer type
    pub inline_locale: String, // TODO: infer type
    pub kwargs: String, // TODO: infer type
    pub group_count: String, // TODO: infer type
    pub group_index: String, // TODO: infer type
    pub group_name: String, // TODO: infer type
    pub named_lists_used: String, // TODO: infer type
    pub open_groups: String, // TODO: infer type
    pub open_group_count: String, // TODO: infer type
    pub defined_groups: String, // TODO: infer type
    pub group_calls: String, // TODO: infer type
    pub private_groups: String, // TODO: infer type
    pub lexicon: String, // TODO: infer type
    pub scanner: String, // TODO: infer type
    pub match: String, // TODO: infer type
}

impl error {
}

pub struct _UnscopedFlagSet {
    pub _key: String, // TODO: infer type
    pub positive: String, // TODO: infer type
    pub encoding: String, // TODO: infer type
    pub zerowidth: String, // TODO: infer type
    pub case_flags: String, // TODO: infer type
    pub subpattern: String, // TODO: infer type
    pub branches: String, // TODO: infer type
    pub info: String, // TODO: infer type
    pub group: String, // TODO: infer type
    pub position: String, // TODO: infer type
    pub ref: String, // TODO: infer type
    pub parsed: String, // TODO: infer type
    pub value: String, // TODO: infer type
    pub folded: String, // TODO: infer type
    pub folded_characters: String, // TODO: infer type
    pub yes_item: String, // TODO: infer type
    pub no_item: String, // TODO: infer type
    pub constraints: String, // TODO: infer type
    pub min_count: String, // TODO: infer type
    pub max_count: String, // TODO: infer type
    pub call_ref: String, // TODO: infer type
    pub behind: String, // TODO: infer type
    pub code: String, // TODO: infer type
    pub lower: String, // TODO: infer type
    pub upper: String, // TODO: infer type
    pub items: String, // TODO: infer type
    pub char_width: String, // TODO: infer type
    pub characters: String, // TODO: infer type
    pub required: String, // TODO: infer type
    pub name: String, // TODO: infer type
    pub set_key: String, // TODO: infer type
    pub string: String, // TODO: infer type
    pub char_type: String, // TODO: infer type
    pub pos: String, // TODO: infer type
    pub ignore_space: String, // TODO: infer type
    pub sep: String, // TODO: infer type
    pub flags: String, // TODO: infer type
    pub global_flags: String, // TODO: infer type
    pub inline_locale: String, // TODO: infer type
    pub kwargs: String, // TODO: infer type
    pub group_count: String, // TODO: infer type
    pub group_index: String, // TODO: infer type
    pub group_name: String, // TODO: infer type
    pub named_lists_used: String, // TODO: infer type
    pub open_groups: String, // TODO: infer type
    pub open_group_count: String, // TODO: infer type
    pub defined_groups: String, // TODO: infer type
    pub group_calls: String, // TODO: infer type
    pub private_groups: String, // TODO: infer type
    pub lexicon: String, // TODO: infer type
    pub scanner: String, // TODO: infer type
    pub match: String, // TODO: infer type
}

impl _UnscopedFlagSet {
}

pub struct ParseError {
    pub _key: String, // TODO: infer type
    pub positive: String, // TODO: infer type
    pub encoding: String, // TODO: infer type
    pub zerowidth: String, // TODO: infer type
    pub case_flags: String, // TODO: infer type
    pub subpattern: String, // TODO: infer type
    pub branches: String, // TODO: infer type
    pub info: String, // TODO: infer type
    pub group: String, // TODO: infer type
    pub position: String, // TODO: infer type
    pub ref: String, // TODO: infer type
    pub parsed: String, // TODO: infer type
    pub value: String, // TODO: infer type
    pub folded: String, // TODO: infer type
    pub folded_characters: String, // TODO: infer type
    pub yes_item: String, // TODO: infer type
    pub no_item: String, // TODO: infer type
    pub constraints: String, // TODO: infer type
    pub min_count: String, // TODO: infer type
    pub max_count: String, // TODO: infer type
    pub call_ref: String, // TODO: infer type
    pub behind: String, // TODO: infer type
    pub code: String, // TODO: infer type
    pub lower: String, // TODO: infer type
    pub upper: String, // TODO: infer type
    pub items: String, // TODO: infer type
    pub char_width: String, // TODO: infer type
    pub characters: String, // TODO: infer type
    pub required: String, // TODO: infer type
    pub name: String, // TODO: infer type
    pub set_key: String, // TODO: infer type
    pub string: String, // TODO: infer type
    pub char_type: String, // TODO: infer type
    pub pos: String, // TODO: infer type
    pub ignore_space: String, // TODO: infer type
    pub sep: String, // TODO: infer type
    pub flags: String, // TODO: infer type
    pub global_flags: String, // TODO: infer type
    pub inline_locale: String, // TODO: infer type
    pub kwargs: String, // TODO: infer type
    pub group_count: String, // TODO: infer type
    pub group_index: String, // TODO: infer type
    pub group_name: String, // TODO: infer type
    pub named_lists_used: String, // TODO: infer type
    pub open_groups: String, // TODO: infer type
    pub open_group_count: String, // TODO: infer type
    pub defined_groups: String, // TODO: infer type
    pub group_calls: String, // TODO: infer type
    pub private_groups: String, // TODO: infer type
    pub lexicon: String, // TODO: infer type
    pub scanner: String, // TODO: infer type
    pub match: String, // TODO: infer type
}

impl ParseError {
}

pub struct _FirstSetError {
    pub _key: String, // TODO: infer type
    pub positive: String, // TODO: infer type
    pub encoding: String, // TODO: infer type
    pub zerowidth: String, // TODO: infer type
    pub case_flags: String, // TODO: infer type
    pub subpattern: String, // TODO: infer type
    pub branches: String, // TODO: infer type
    pub info: String, // TODO: infer type
    pub group: String, // TODO: infer type
    pub position: String, // TODO: infer type
    pub ref: String, // TODO: infer type
    pub parsed: String, // TODO: infer type
    pub value: String, // TODO: infer type
    pub folded: String, // TODO: infer type
    pub folded_characters: String, // TODO: infer type
    pub yes_item: String, // TODO: infer type
    pub no_item: String, // TODO: infer type
    pub constraints: String, // TODO: infer type
    pub min_count: String, // TODO: infer type
    pub max_count: String, // TODO: infer type
    pub call_ref: String, // TODO: infer type
    pub behind: String, // TODO: infer type
    pub code: String, // TODO: infer type
    pub lower: String, // TODO: infer type
    pub upper: String, // TODO: infer type
    pub items: String, // TODO: infer type
    pub char_width: String, // TODO: infer type
    pub characters: String, // TODO: infer type
    pub required: String, // TODO: infer type
    pub name: String, // TODO: infer type
    pub set_key: String, // TODO: infer type
    pub string: String, // TODO: infer type
    pub char_type: String, // TODO: infer type
    pub pos: String, // TODO: infer type
    pub ignore_space: String, // TODO: infer type
    pub sep: String, // TODO: infer type
    pub flags: String, // TODO: infer type
    pub global_flags: String, // TODO: infer type
    pub inline_locale: String, // TODO: infer type
    pub kwargs: String, // TODO: infer type
    pub group_count: String, // TODO: infer type
    pub group_index: String, // TODO: infer type
    pub group_name: String, // TODO: infer type
    pub named_lists_used: String, // TODO: infer type
    pub open_groups: String, // TODO: infer type
    pub open_group_count: String, // TODO: infer type
    pub defined_groups: String, // TODO: infer type
    pub group_calls: String, // TODO: infer type
    pub private_groups: String, // TODO: infer type
    pub lexicon: String, // TODO: infer type
    pub scanner: String, // TODO: infer type
    pub match: String, // TODO: infer type
}

impl _FirstSetError {
}

pub struct RegexFlag {
    pub _key: String, // TODO: infer type
    pub positive: String, // TODO: infer type
    pub encoding: String, // TODO: infer type
    pub zerowidth: String, // TODO: infer type
    pub case_flags: String, // TODO: infer type
    pub subpattern: String, // TODO: infer type
    pub branches: String, // TODO: infer type
    pub info: String, // TODO: infer type
    pub group: String, // TODO: infer type
    pub position: String, // TODO: infer type
    pub ref: String, // TODO: infer type
    pub parsed: String, // TODO: infer type
    pub value: String, // TODO: infer type
    pub folded: String, // TODO: infer type
    pub folded_characters: String, // TODO: infer type
    pub yes_item: String, // TODO: infer type
    pub no_item: String, // TODO: infer type
    pub constraints: String, // TODO: infer type
    pub min_count: String, // TODO: infer type
    pub max_count: String, // TODO: infer type
    pub call_ref: String, // TODO: infer type
    pub behind: String, // TODO: infer type
    pub code: String, // TODO: infer type
    pub lower: String, // TODO: infer type
    pub upper: String, // TODO: infer type
    pub items: String, // TODO: infer type
    pub char_width: String, // TODO: infer type
    pub characters: String, // TODO: infer type
    pub required: String, // TODO: infer type
    pub name: String, // TODO: infer type
    pub set_key: String, // TODO: infer type
    pub string: String, // TODO: infer type
    pub char_type: String, // TODO: infer type
    pub pos: String, // TODO: infer type
    pub ignore_space: String, // TODO: infer type
    pub sep: String, // TODO: infer type
    pub flags: String, // TODO: infer type
    pub global_flags: String, // TODO: infer type
    pub inline_locale: String, // TODO: infer type
    pub kwargs: String, // TODO: infer type
    pub group_count: String, // TODO: infer type
    pub group_index: String, // TODO: infer type
    pub group_name: String, // TODO: infer type
    pub named_lists_used: String, // TODO: infer type
    pub open_groups: String, // TODO: infer type
    pub open_group_count: String, // TODO: infer type
    pub defined_groups: String, // TODO: infer type
    pub group_calls: String, // TODO: infer type
    pub private_groups: String, // TODO: infer type
    pub lexicon: String, // TODO: infer type
    pub scanner: String, // TODO: infer type
    pub match: String, // TODO: infer type
}

impl RegexFlag {
}

pub const ASCII: f64 = RegexFlag . ASCII;
pub const BESTMATCH: f64 = RegexFlag . BESTMATCH;
pub const DEBUG: f64 = RegexFlag . DEBUG;
pub const DOTALL: f64 = RegexFlag . DOTALL;
pub const ENHANCEMATCH: f64 = RegexFlag . ENHANCEMATCH;
pub const FULLCASE: f64 = RegexFlag . FULLCASE;
pub const IGNORECASE: f64 = RegexFlag . IGNORECASE;
pub const LOCALE: f64 = RegexFlag . LOCALE;
pub const MULTILINE: f64 = RegexFlag . MULTILINE;
pub const POSIX: f64 = RegexFlag . POSIX;
pub const REVERSE: f64 = RegexFlag . REVERSE;
pub const TEMPLATE: f64 = RegexFlag . TEMPLATE;
pub const UNICODE: f64 = RegexFlag . UNICODE;
pub const VERBOSE: f64 = RegexFlag . VERBOSE;
pub const VERSION0: f64 = RegexFlag . VERSION0;
pub const VERSION1: f64 = RegexFlag . VERSION1;
pub const WORD: f64 = RegexFlag . WORD;
pub const A: f64 = RegexFlag . A;
pub const B: f64 = RegexFlag . B;
pub const D: f64 = RegexFlag . D;
pub const E: f64 = RegexFlag . E;
pub const F: f64 = RegexFlag . F;
pub const I: f64 = RegexFlag . I;
pub const L: f64 = RegexFlag . L;
pub const M: f64 = RegexFlag . M;
pub const P: f64 = RegexFlag . P;
pub const R: f64 = RegexFlag . R;
pub const S: f64 = RegexFlag . S;
pub const U: f64 = RegexFlag . U;
pub const V0: f64 = RegexFlag . V0;
pub const V1: f64 = RegexFlag . V1;
pub const W: f64 = RegexFlag . W;
pub const X: f64 = RegexFlag . X;
pub const T: f64 = RegexFlag . T;
pub const DEFAULT_VERSION: /* inferred */ = VERSION1;
pub const _ALL_VERSIONS: /* inferred */ = VERSION0 | VERSION1;
pub const _ALL_ENCODINGS: /* inferred */ = ASCII | LOCALE | UNICODE;
pub const DEFAULT_FLAGS: f64 = { VERSION0 : 0 , VERSION1 : FULLCASE };
pub const GLOBAL_FLAGS: f64 = ( _ALL_VERSIONS | BESTMATCH | DEBUG | ENHANCEMATCH | POSIX |;
pub const SCOPED_FLAGS: f64 = ( FULLCASE | IGNORECASE | MULTILINE | DOTALL | WORD | VERBOSE |;
pub const ALPHA: f64 = frozenset ( string . ascii_letters );
pub const DIGITS: f64 = frozenset ( string . digits );
pub const ALNUM: /* inferred */ = ALPHA | DIGITS;
pub const OCT_DIGITS: f64 = frozenset ( string . octdigits );
pub const HEX_DIGITS: f64 = frozenset ( string . hexdigits );
pub const SPECIAL_CHARS: &str = frozenset ("()|?*+{^$.[\\#" ) | frozenset ( ["" ] );
pub const NAMED_CHAR_PART: &str = ALNUM | frozenset (" -" );
pub const PROPERTY_NAME_PART: &str = ALNUM | frozenset (" &_-." );
pub const SET_OPS: &str = ("||" ,"~~" ,"&&" ,"--" );
pub const BYTES_PER_CODE: f64 = _regex . get_code_size ( );
pub const BITS_PER_CODE: /* inferred */ = BYTES_PER_CODE * 8;
pub const UNLIMITED: f64 = ( 1 < < BITS_PER_CODE ) - 1;
pub const REGEX_FLAGS: &str = {"a" : ASCII ,"b" : BESTMATCH ,"e" : ENHANCEMATCH ,"f" : FULLCASE ,;
pub const CASE_FLAGS: /* inferred */ = FULLCASE | IGNORECASE;
pub const NOCASE: u64 = 0;
pub const FULLIGNORECASE: /* inferred */ = FULLCASE | IGNORECASE;
pub const FULL_CASE_FOLDING: /* inferred */ = UNICODE | FULLIGNORECASE;
pub const CASE_FLAGS_COMBINATIONS: f64 = { 0 : 0 , FULLCASE : 0 , IGNORECASE : IGNORECASE ,;
pub const HEX_ESCAPES: &str = {"x" : 2 ,"u" : 4 ,"U" : 8 };
pub const OPCODES: &str = "
FAILURE
SUCCESS
ANY
ANY_ALL
ANY_ALL_REV
ANY_REV
ANY_U
ANY_U_REV
ATOMIC
BOUNDARY
BRANCH
CALL_REF
CHARACTER
CHARACTER_IGN
CHARACTER_IGN_REV
CHARACTER_REV
CONDITIONAL
DEFAULT_BOUNDARY
DEFAULT_END_OF_WORD
DEFAULT_START_OF_WORD
END
END_OF_LINE
END_OF_LINE_U
END_OF_STRING
END_OF_STRING_LINE
END_OF_STRING_LINE_U
END_OF_WORD
FUZZY
GRAPHEME_BOUNDARY
GREEDY_REPEAT
GROUP
GROUP_CALL
GROUP_EXISTS
KEEP
LAZY_REPEAT
LOOKAROUND
NEXT
PROPERTY
PROPERTY_IGN
PROPERTY_IGN_REV
PROPERTY_REV
PRUNE
RANGE
RANGE_IGN
RANGE_IGN_REV
RANGE_REV
REF_GROUP
REF_GROUP_FLD
REF_GROUP_FLD_REV
REF_GROUP_IGN
REF_GROUP_IGN_REV
REF_GROUP_REV
SEARCH_ANCHOR
SET_DIFF
SET_DIFF_IGN
SET_DIFF_IGN_REV
SET_DIFF_REV
SET_INTER
SET_INTER_IGN
SET_INTER_IGN_REV
SET_INTER_REV
SET_SYM_DIFF
SET_SYM_DIFF_IGN
SET_SYM_DIFF_IGN_REV
SET_SYM_DIFF_REV
SET_UNION
SET_UNION_IGN
SET_UNION_IGN_REV
SET_UNION_REV
SKIP
START_OF_LINE
START_OF_LINE_U
START_OF_STRING
START_OF_WORD
STRING
STRING_FLD
STRING_FLD_REV
STRING_IGN
STRING_IGN_REV
STRING_REV
FUZZY_EXT
";
pub struct Namespace {
    pub _key: String, // TODO: infer type
    pub positive: String, // TODO: infer type
    pub encoding: String, // TODO: infer type
    pub zerowidth: String, // TODO: infer type
    pub case_flags: String, // TODO: infer type
    pub subpattern: String, // TODO: infer type
    pub branches: String, // TODO: infer type
    pub info: String, // TODO: infer type
    pub group: String, // TODO: infer type
    pub position: String, // TODO: infer type
    pub ref: String, // TODO: infer type
    pub parsed: String, // TODO: infer type
    pub value: String, // TODO: infer type
    pub folded: String, // TODO: infer type
    pub folded_characters: String, // TODO: infer type
    pub yes_item: String, // TODO: infer type
    pub no_item: String, // TODO: infer type
    pub constraints: String, // TODO: infer type
    pub min_count: String, // TODO: infer type
    pub max_count: String, // TODO: infer type
    pub call_ref: String, // TODO: infer type
    pub behind: String, // TODO: infer type
    pub code: String, // TODO: infer type
    pub lower: String, // TODO: infer type
    pub upper: String, // TODO: infer type
    pub items: String, // TODO: infer type
    pub char_width: String, // TODO: infer type
    pub characters: String, // TODO: infer type
    pub required: String, // TODO: infer type
    pub name: String, // TODO: infer type
    pub set_key: String, // TODO: infer type
    pub string: String, // TODO: infer type
    pub char_type: String, // TODO: infer type
    pub pos: String, // TODO: infer type
    pub ignore_space: String, // TODO: infer type
    pub sep: String, // TODO: infer type
    pub flags: String, // TODO: infer type
    pub global_flags: String, // TODO: infer type
    pub inline_locale: String, // TODO: infer type
    pub kwargs: String, // TODO: infer type
    pub group_count: String, // TODO: infer type
    pub group_index: String, // TODO: infer type
    pub group_name: String, // TODO: infer type
    pub named_lists_used: String, // TODO: infer type
    pub open_groups: String, // TODO: infer type
    pub open_group_count: String, // TODO: infer type
    pub defined_groups: String, // TODO: infer type
    pub group_calls: String, // TODO: infer type
    pub private_groups: String, // TODO: infer type
    pub lexicon: String, // TODO: infer type
    pub scanner: String, // TODO: infer type
    pub match: String, // TODO: infer type
}

impl Namespace {
}

pub const OP: f64 = Namespace ( );
pub fn _shrink_cache(cache_dict: &str, args_dict: &str, locale_sensitive: &str, max_length: &str, divisor: &str) {
        "Make room in the given cache.

    Args:
        cache_dict: The cache dictionary to modify.
        args_dict: The dictionary of named list args used by patterns.
        max_length: Maximum # of entries in cache_dict before it == shrunk.
        divisor: Cache will shrink to max_length - 1/divisor*max_length items.
    ";
        cache_keys = tuple ( cache_dict . keys ( ) );
        overage = len ( cache_keys ) - max_length;
        if overage < 0 {
        return;
        number_to_toss = max_length / / divisor + overage;
        import random;
        if !hasattr ( random , "sample" ) {
        return;
        for doomed_key in random . sample ( cache_keys , number_to_toss ) .iter() {
        // try {
        del cache_dict [ doomed_key ];
        // } catch  KeyError  {
        // pass
        args_dict . clear ( );
        sensitivity_dict = { };
        for pattern , pattern_type , flags , args , default_version , locale in tuple ( cache_dict ) .iter() {
        args_dict [ pattern , pattern_type , flags , default_version , locale ] = args;
        // try {
        sensitivity_dict [ pattern_type , pattern ] = locale_sensitive [ pattern_type , pattern ];
        // } catch  KeyError  {
        // pass
        locale_sensitive . clear ( );
        locale_sensitive . update ( sensitivity_dict );
        pub fn _fold_case ( info , string )  {
        "Folds the case of a string.";
        flags = info . flags;
        if ( flags & _ALL_ENCODINGS ) == 0 {
        flags | = info . guess_encoding;
        return  _regex . fold_case ( flags , string );
        pub fn is_cased_i ( info , char )  {
        "Checks whether a character == cased.";
        return  len ( _regex . get_all_cases ( info . flags , char ) ) > 1;
        pub fn is_cased_f ( flags , char )  {
        "Checks whether a character == cased.";
        return  len ( _regex . get_all_cases ( flags , char ) ) > 1;
        pub fn _compile_firstset ( info , fs )  {
        "Compiles the firstset for the pattern.";
        reverse = bool ( info . flags & REVERSE );
        fs = _check_firstset ( info , reverse , fs );
        if !fs || isinstance ( fs , AnyAll ) {
        return  [ ];
        return  fs . compile ( reverse );
        pub fn _check_firstset ( info , reverse , fs )  {
        "Checks the firstset for the pattern.";
        if !fs || None /* Option */ in fs {
        return;
        members = set ( );
        case_flags = NOCASE;
        for i in fs .iter() {
        if isinstance ( i , Character ) && !i . positive {
        return;
        case_flags | = i . case_flags;
        members . add ( i . with_flags ( case_flags = NOCASE ) );
        if case_flags == ( FULLCASE | IGNORECASE ) {
        return;
        fs = SetUnion ( info , list ( members ) , case_flags = case_flags & ~ FULLCASE ,;
        zerowidth = true );
        fs = fs . optimise ( info , reverse , in_set = true );
        return  fs;
        pub fn _flatten_code ( code )  {
        "Flattens the code from a list of tuples.";
        flat_code = [ ];
        for c in code .iter() {
        flat_code . extend ( c );
        return  flat_code;
        pub fn make_case_flags ( info )  {
        "Makes the case flags.";
        flags = info . flags & CASE_FLAGS;
        if info . flags & ASCII {
        flags & = ~ FULLCASE;
        return  flags;
        pub fn make_character ( info , value , in_set = false )  {
        "Makes a character literal.";
        if in_set {
        return  Character ( value );
        return  Character ( value , case_flags = make_case_flags ( info ) );
        pub fn make_ref_group ( info , name , position )  {
        "Makes a group reference.";
        return  RefGroup ( info , name , position , case_flags = make_case_flags ( info ) );
        pub fn make_string_set ( info , name )  {
        "Makes a string set.";
        return  StringSet ( info , name , case_flags = make_case_flags ( info ) );
        pub fn make_property ( info , prop , in_set )  {
        "Makes a property.";
        if in_set {
        return  prop;
        return  prop . with_flags ( case_flags = make_case_flags ( info ) );
        pub fn _parse_pattern ( source , info )  {
        "Parses a pattern, eg. 'a|b|c'.";
        branches = [ parse_sequence ( source , info ) ];
        while source . match ( "|" )  {
        branches . append ( parse_sequence ( source , info ) );
        if len ( branches ) == 1 {
        return  branches [ 0 ];
        return  Branch ( branches );
        pub fn parse_sequence ( source , info )  {
        "Parses a sequence, eg. 'abc'.";
        sequence = [ None /* Option */ ];
        case_flags = make_case_flags ( info );
        while true  {
        saved_pos = source . pos;
        ch = source . get ( );
        if ch in SPECIAL_CHARS {
        if ch in ")|" {
        source . pos = saved_pos;
        break;
        } else if ch == "\\" {
        sequence . append ( parse_escape ( source , info , false ) );
        } else if ch == "(" {
        element = parse_paren ( source , info );
        if element is None /* Option */ {
        case_flags = make_case_flags ( info );
        } else {
        sequence . append ( element );
        } else if ch == "." {
        if info . flags & DOTALL {
        sequence . append ( AnyAll ( ) );
        } else if info . flags & WORD {
        sequence . append ( AnyU ( ) );
        } else {
        sequence . append ( Any ( ) );
        } else if ch == "[" {
        sequence . append ( parse_set ( source , info ) );
        } else if ch == "^" {
        if info . flags & MULTILINE {
        if info . flags & WORD {
        sequence . append ( StartOfLineU ( ) );
        } else {
        sequence . append ( StartOfLine ( ) );
        } else {
        sequence . append ( StartOfString ( ) );
        } else if ch == "$" {
        if info . flags & MULTILINE {
        if info . flags & WORD {
        sequence . append ( EndOfLineU ( ) );
        } else {
        sequence . append ( EndOfLine ( ) );
        } else {
        if info . flags & WORD {
        sequence . append ( EndOfStringLineU ( ) );
        } else {
        sequence . append ( EndOfStringLine ( ) );
        } else if ch in "?*+{" {
        counts = parse_quantifier ( source , info , ch );
        if counts {
        apply_quantifier ( source , info , counts , case_flags , ch ,;
        saved_pos , sequence );
        sequence . append ( None /* Option */ );
        } else {
        constraints = parse_fuzzy ( source , info , ch , case_flags );
        if constraints {
        apply_constraint ( source , info , constraints , case_flags ,;
        saved_pos , sequence );
        sequence . append ( None /* Option */ );
        } else {
        sequence . append ( Character ( ord ( ch ) ,;
        case_flags = case_flags ) );
        } else {
        sequence . append ( Character ( ord ( ch ) , case_flags = case_flags ) );
        } else {
        sequence . append ( Character ( ord ( ch ) , case_flags = case_flags ) );
        sequence = vec![ item.iter().map(|item| sequence if item == !None /* Option */ ).collect();
        return  Sequence ( sequence );
        pub fn apply_quantifier ( source , info , counts , case_flags , ch , saved_pos , {
        sequence ) ;
        element = sequence . pop ( );
        if element is None /* Option */ {
        if sequence {
        panic!("error ( "multiple repeat" , source . string , saved_pos )");
        panic!("error ( "nothing to repeat" , source . string , saved_pos )");
        if isinstance ( element , ( GreedyRepeat , LazyRepeat , PossessiveRepeat ) ) {
        panic!("error ( "multiple repeat" , source . string , saved_pos )");
        min_count , max_count = counts;
        saved_pos = source . pos;
        ch = source . get ( );
        if ch == "?" {
        repeated = LazyRepeat;
        } else if ch == "+" {
        repeated = PossessiveRepeat;
        } else {
        source . pos = saved_pos;
        repeated = GreedyRepeat;
        if !element . is_empty ( ) && ( min_count != 1 || max_count != 1 ) {
        element = repeated ( element , min_count , max_count );
        sequence . append ( element );
        pub fn apply_constraint ( source , info , constraints , case_flags , saved_pos , {
        sequence ) ;
        element = sequence . pop ( );
        if element is None /* Option */ {
        panic!("error ( "nothing for fuzzy constraint" , source . string , saved_pos )");
        if isinstance ( element , Group ) {
        element . subpattern = Fuzzy ( element . subpattern , constraints );
        sequence . append ( element );
        } else {
        sequence . append ( Fuzzy ( element , constraints ) );
        _QUANTIFIERS = { "?" : ( 0 , 1 ) , "*" : ( 0 , None /* Option */ ) , "+" : ( 1 , None /* Option */ ) };
        pub fn parse_quantifier ( source , info , ch )  {
        "Parses a quantifier.";
        q = _QUANTIFIERS . get ( ch );
        if q {
        return  q;
        if ch == "{" {
        counts = parse_limited_quantifier ( source );
        if counts {
        return  counts;
        return;
        pub fn is_above_limit ( count )  {
        "Checks whether a count == above the maximum.";
        return  count is !None /* Option */ && count >= UNLIMITED;
        pub fn parse_limited_quantifier ( source )  {
        "Parses a limited quantifier.";
        saved_pos = source . pos;
        min_count = parse_count ( source );
        if source . match ( "," ) {
        max_count = parse_count ( source );
        min_count = int ( min_count || 0 );
        max_count = int ( max_count ) if max_count else None /* Option */;
        } else {
        if !min_count {
        source . pos = saved_pos;
        return;
        min_count = max_count = int ( min_count );
        if !source . match ( "}" ) {
        source . pos = saved_pos;
        return;
        if is_above_limit ( min_count ) || is_above_limit ( max_count ) {
        panic!("error ( "repeat count too big" , source . string , saved_pos )");
        if max_count is !None /* Option */ && min_count > max_count {
        panic!("error ( "min repeat greater than max repeat" , source . string ,");
        saved_pos );
        return  min_count , max_count;
        pub fn parse_fuzzy ( source , info , ch , case_flags )  {
        "Parses a fuzzy setting, if present.";
        saved_pos = source . pos;
        if ch != "{" {
        return;
        constraints = { };
        // try {
        parse_fuzzy_item ( source , constraints );
        while source . match ( "," )  {
        parse_fuzzy_item ( source , constraints );
        // } catch  ParseError  {
        source . pos = saved_pos;
        return;
        if source . match ( ":" ) {
        constraints [ "test" ] = parse_fuzzy_test ( source , info , case_flags );
        if !source . match ( "}" ) {
        panic!("error ( "expected }" , source . string , source . pos )");
        return  constraints;
        pub fn parse_fuzzy_item ( source , constraints )  {
        "Parses a fuzzy setting item.";
        saved_pos = source . pos;
        // try {
        parse_cost_constraint ( source , constraints );
        // } catch  ParseError  {
        source . pos = saved_pos;
        parse_cost_equation ( source , constraints );
        pub fn parse_cost_constraint ( source , constraints )  {
        "Parses a cost constraint.";
        saved_pos = source . pos;
        ch = source . get ( );
        if ch in ALPHA {
        constraint = parse_constraint ( source , constraints , ch );
        max_inc = parse_fuzzy_compare ( source );
        if max_inc is None /* Option */ {
        constraints [ constraint ] = 0 , None /* Option */;
        } else {
        cost_pos = source . pos;
        max_cost = parse_cost_limit ( source );
        if !max_inc {
        max_cost - = 1;
        if max_cost < 0 {
        panic!("error ( "bad fuzzy cost limit" , source . string , cost_pos )");
        constraints [ constraint ] = 0 , max_cost;
        } else if ch in DIGITS {
        source . pos = saved_pos;
        cost_pos = source . pos;
        min_cost = parse_cost_limit ( source );
        min_inc = parse_fuzzy_compare ( source );
        if min_inc is None /* Option */ {
        panic!("ParseError ( )");
        constraint = parse_constraint ( source , constraints , source . get ( ) );
        max_inc = parse_fuzzy_compare ( source );
        if max_inc is None /* Option */ {
        panic!("ParseError ( )");
        cost_pos = source . pos;
        max_cost = parse_cost_limit ( source );
        if !min_inc {
        min_cost + = 1;
        if !max_inc {
        max_cost - = 1;
        if !0 <= min_cost <= max_cost {
        panic!("error ( "bad fuzzy cost limit" , source . string , cost_pos )");
        constraints [ constraint ] = min_cost , max_cost;
        } else {
        panic!("ParseError ( )");
        pub fn parse_cost_limit ( source )  {
        "Parses a cost limit.";
        cost_pos = source . pos;
        digits = parse_count ( source );
        // try {
        return  int ( digits );
        // } catch  ValueError  {
        // pass
        panic!("error ( "bad fuzzy cost limit" , source . string , cost_pos )");
        pub fn parse_constraint ( source , constraints , ch )  {
        "Parses a constraint.";
        if ch !in "deis" {
        panic!("ParseError ( )");
        if ch in constraints {
        panic!("ParseError ( )");
        return  ch;
        pub fn parse_fuzzy_compare ( source )  {
        "Parses a cost comparator.";
        if source . match ( "<=" ) {
        return  true;
        } else if source . match ( "<" ) {
        return  false;
        } else {
        return;
        pub fn parse_cost_equation ( source , constraints )  {
        "Parses a cost equation.";
        if "cost" in constraints {
        panic!("error ( "more than one cost equation" , source . string , source . pos )");
        cost = { };
        parse_cost_term ( source , cost );
        while source . match ( "+" )  {
        parse_cost_term ( source , cost );
        max_inc = parse_fuzzy_compare ( source );
        if max_inc is None /* Option */ {
        panic!("ParseError ( )");
        max_cost = int ( parse_count ( source ) );
        if !max_inc {
        max_cost - = 1;
        if max_cost < 0 {
        panic!("error ( "bad fuzzy cost limit" , source . string , source . pos )");
        cost [ "max" ] = max_cost;
        constraints [ "cost" ] = cost;
        pub fn parse_cost_term ( source , cost )  {
        "Parses a cost equation term.";
        coeff = parse_count ( source );
        ch = source . get ( );
        if ch !in "dis" {
        panic!("ParseError ( )");
        if ch in cost {
        panic!("error ( "repeated fuzzy cost" , source . string , source . pos )");
        cost [ ch ] = int ( coeff || 1 );
        pub fn parse_fuzzy_test ( source , info , case_flags )  {
        saved_pos = source . pos;
        ch = source . get ( );
        if ch in SPECIAL_CHARS {
        if ch == "\\" {
        return  parse_escape ( source , info , false );
        } else if ch == "." {
        if info . flags & DOTALL {
        return  AnyAll ( );
        } else if info . flags & WORD {
        return  AnyU ( );
        } else {
        return  Any ( );
        } else if ch == "[" {
        return  parse_set ( source , info );
        } else {
        panic!("error ( "expected character set" , source . string , saved_pos )");
        } else if ch {
        return  Character ( ord ( ch ) , case_flags = case_flags );
        } else {
        panic!("error ( "expected character set" , source . string , saved_pos )");
        pub fn parse_count ( source )  {
        "Parses a quantifier's count, which can be empty.";
        return  source . get_while ( DIGITS );
        pub fn parse_paren ( source , info )  {
        "Parses a parenthesised subpattern || a flag. Returns FLAGS if it's an
    inline flag.
    ";
        saved_pos = source . pos;
        ch = source . get ( true );
        if ch == "?" {
        saved_pos_2 = source . pos;
        ch = source . get ( true );
        if ch == "<" {
        saved_pos_3 = source . pos;
        ch = source . get ( );
        if ch in ( "=" , "!" ) {
        return  parse_lookaround ( source , info , true , ch == "=" );
        source . pos = saved_pos_3;
        name = parse_name ( source );
        group = info . open_group ( name );
        source . expect ( ">" );
        saved_flags = info . flags;
        // try {
        subpattern = _parse_pattern ( source , info );
        source . expect ( ")" );
        // } finally {
        info . flags = saved_flags;
        source . ignore_space = bool ( info . flags & VERBOSE );
        info . close_group ( );
        return  Group ( info , group , subpattern );
        if ch in ( "=" , "!" ) {
        return  parse_lookaround ( source , info , false , ch == "=" );
        if ch == "P" {
        return  parse_extension ( source , info );
        if ch == "#" {
        return  parse_comment ( source );
        if ch == "(" {
        return  parse_conditional ( source , info );
        if ch == ">" {
        return  parse_atomic ( source , info );
        if ch == "|" {
        return  parse_common ( source , info );
        if ch == "R" || "0" <= ch <= "9" {
        return  parse_call_group ( source , info , ch , saved_pos_2 );
        if ch == "&" {
        return  parse_call_named_group ( source , info , saved_pos_2 );
        if ( ch == "+" || ch == "-" ) && source . peek ( ) in DIGITS {
        return  parse_rel_call_group ( source , info , ch , saved_pos_2 );
        source . pos = saved_pos_2;
        return  parse_flags_subpattern ( source , info );
        if ch == "*" {
        saved_pos_2 = source . pos;
        word = source . get_while ( set ( ")>" ) , include = false );
        if word [ { : 1 ] . isalpha ( ) ; }
        verb = VERBS . get ( word );
        if !verb {
        panic!("error ( "unknown verb" , source . string , saved_pos_2 )");
        source . expect ( ")" );
        return  verb;
        source . pos = saved_pos;
        group = info . open_group ( );
        saved_flags = info . flags;
        // try {
        subpattern = _parse_pattern ( source , info );
        source . expect ( ")" );
        // } finally {
        info . flags = saved_flags;
        source . ignore_space = bool ( info . flags & VERBOSE );
        info . close_group ( );
        return  Group ( info , group , subpattern );
        pub fn parse_extension ( source , info )  {
        "Parses a Python extension.";
        saved_pos = source . pos;
        ch = source . get ( );
        if ch == "<" {
        name = parse_name ( source );
        group = info . open_group ( name );
        source . expect ( ">" );
        saved_flags = info . flags;
        // try {
        subpattern = _parse_pattern ( source , info );
        source . expect ( ")" );
        // } finally {
        info . flags = saved_flags;
        source . ignore_space = bool ( info . flags & VERBOSE );
        info . close_group ( );
        return  Group ( info , group , subpattern );
        if ch == "=" {
        name = parse_name ( source , allow_numeric = true );
        source . expect ( ")" );
        if info . is_open_group ( name ) {
        panic!("error ( "cannot refer to an open group" , source . string ,");
        saved_pos );
        return  make_ref_group ( info , name , saved_pos );
        if ch == ">" || ch == "&" {
        return  parse_call_named_group ( source , info , saved_pos );
        source . pos = saved_pos;
        panic!("error ( "unknown extension" , source . string , saved_pos )");
        pub fn parse_comment ( source )  {
        "Parses a comment.";
        while true  {
        saved_pos = source . pos;
        c = source . get ( true );
        if !c || c == ")" {
        break;
        if c == "\\" {
        c = source . get ( true );
        source . pos = saved_pos;
        source . expect ( ")" );
        return;
        pub fn parse_lookaround ( source , info , behind , positive )  {
        "Parses a lookaround.";
        saved_flags = info . flags;
        // try {
        subpattern = _parse_pattern ( source , info );
        source . expect ( ")" );
        // } finally {
        info . flags = saved_flags;
        source . ignore_space = bool ( info . flags & VERBOSE );
        return  LookAround ( behind , positive , subpattern );
        pub fn parse_conditional ( source , info )  {
        "Parses a conditional subpattern.";
        saved_flags = info . flags;
        saved_pos = source . pos;
        ch = source . get ( );
        if ch == "?" {
        ch = source . get ( );
        if ch in ( "=" , "!" ) {
        return  parse_lookaround_conditional ( source , info , false , ch == "=" );
        if ch == "<" {
        ch = source . get ( );
        if ch in ( "=" , "!" ) {
        return  parse_lookaround_conditional ( source , info , true , ch ==;
        "=" );
        source . pos = saved_pos;
        panic!("error ( "expected lookaround conditional" , source . string ,");
        source . pos );
        source . pos = saved_pos;
        // try {
        group = parse_name ( source , true );
        source . expect ( ")" );
        yes_branch = parse_sequence ( source , info );
        if source . match ( "|" ) {
        no_branch = parse_sequence ( source , info );
        } else {
        no_branch = Sequence ( );
        source . expect ( ")" );
        // } finally {
        info . flags = saved_flags;
        source . ignore_space = bool ( info . flags & VERBOSE );
        if yes_branch . is_empty ( ) && no_branch . is_empty ( ) {
        return  Sequence ( );
        return  Conditional ( info , group , yes_branch , no_branch , saved_pos );
        pub fn parse_lookaround_conditional ( source , info , behind , positive )  {
        saved_flags = info . flags;
        // try {
        subpattern = _parse_pattern ( source , info );
        source . expect ( ")" );
        // } finally {
        info . flags = saved_flags;
        source . ignore_space = bool ( info . flags & VERBOSE );
        yes_branch = parse_sequence ( source , info );
        if source . match ( "|" ) {
        no_branch = parse_sequence ( source , info );
        } else {
        no_branch = Sequence ( );
        source . expect ( ")" );
        return  LookAroundConditional ( behind , positive , subpattern , yes_branch ,;
        no_branch );
        pub fn parse_atomic ( source , info )  {
        "Parses an atomic subpattern.";
        saved_flags = info . flags;
        // try {
        subpattern = _parse_pattern ( source , info );
        source . expect ( ")" );
        // } finally {
        info . flags = saved_flags;
        source . ignore_space = bool ( info . flags & VERBOSE );
        return  Atomic ( subpattern );
        pub fn parse_common ( source , info )  {
        "Parses a common groups branch.";
        initial_group_count = info . group_count;
        branches = [ parse_sequence ( source , info ) ];
        final_group_count = info . group_count;
        while source . match ( "|" )  {
        info . group_count = initial_group_count;
        branches . append ( parse_sequence ( source , info ) );
        final_group_count = max ( final_group_count , info . group_count );
        info . group_count = final_group_count;
        source . expect ( ")" );
        if len ( branches ) == 1 {
        return  branches [ 0 ];
        return  Branch ( branches );
        pub fn parse_call_group ( source , info , ch , pos )  {
        "Parses a call to a group.";
        if ch == "R" {
        group = "0";
        } else {
        group = ch + source . get_while ( DIGITS );
        source . expect ( ")" );
        return  CallGroup ( info , group , pos );
        pub fn parse_rel_call_group ( source , info , ch , pos )  {
        "Parses a relative call to a group.";
        digits = source . get_while ( DIGITS );
        if !digits {
        panic!("error ( "missing relative group number" , source . string , source . pos )");
        offset = int ( digits );
        group = info . group_count + offset if ch == "+" else info . group_count - offset + 1;
        if group <= 0 {
        panic!("error ( "invalid relative group number" , source . string , source . pos )");
        source . expect ( ")" );
        return  CallGroup ( info , group , pos );
        pub fn parse_call_named_group ( source , info , pos )  {
        "Parses a call to a named group.";
        group = parse_name ( source );
        source . expect ( ")" );
        return  CallGroup ( info , group , pos );
        pub fn parse_flag_set ( source )  {
        "Parses a set of inline flags.";
        flags = 0;
        // try {
        while true  {
        saved_pos = source . pos;
        ch = source . get ( );
        if ch == "V" {
        ch + = source . get ( );
        flags | = REGEX_FLAGS [ ch ];
        // } catch  KeyError  {
        source . pos = saved_pos;
        return  flags;
        pub fn parse_flags ( source , info )  {
        "Parses flags being turned on/off.";
        flags_on = parse_flag_set ( source );
        if source . match ( "-" ) {
        flags_off = parse_flag_set ( source );
        if !flags_off {
        panic!("error ( "bad inline flags: no flags after '-'" , source . string ,");
        source . pos );
        } else {
        flags_off = 0;
        if flags_on & LOCALE {
        info . inline_locale = true;
        return  flags_on , flags_off;
        pub fn parse_subpattern ( source , info , flags_on , flags_off )  {
        "Parses a subpattern with scoped flags.";
        saved_flags = info . flags;
        info . flags = ( info . flags | flags_on ) & ~ flags_off;
        if info . flags & ( ASCII | LOCALE | UNICODE ) {
        info . flags = ( info . flags & ~ _ALL_ENCODINGS ) | flags_on;
        source . ignore_space = bool ( info . flags & VERBOSE );
        // try {
        subpattern = _parse_pattern ( source , info );
        source . expect ( ")" );
        // } finally {
        info . flags = saved_flags;
        source . ignore_space = bool ( info . flags & VERBOSE );
        return  subpattern;
        pub fn parse_flags_subpattern ( source , info )  {
        "Parses a flags subpattern. It could be inline flags || a subpattern
    possibly with local flags. If it's a subpattern, then that's returned;
    if it's a inline flags, then None /* Option */ == returned.
    ";
        flags_on , flags_off = parse_flags ( source , info );
        if flags_off & GLOBAL_FLAGS {
        panic!("error ( "bad inline flags: cannot turn off global flag" ,");
        source . string , source . pos );
        if flags_on & flags_off {
        panic!("error ( "bad inline flags: flag turned on && off" , source . string ,");
        source . pos );
        new_global_flags = ( flags_on & ~ info . global_flags ) & GLOBAL_FLAGS;
        if new_global_flags {
        info . global_flags | = new_global_flags;
        panic!("_UnscopedFlagSet ( info . global_flags )");
        flags_on & = ~ GLOBAL_FLAGS;
        if source . match ( ":" ) {
        return  parse_subpattern ( source , info , flags_on , flags_off );
        if source . match ( ")" ) {
        parse_positional_flags ( source , info , flags_on , flags_off );
        return;
        panic!("error ( "unknown extension" , source . string , source . pos )");
        pub fn parse_positional_flags ( source , info , flags_on , flags_off )  {
        "Parses positional flags.";
        info . flags = ( info . flags | flags_on ) & ~ flags_off;
        source . ignore_space = bool ( info . flags & VERBOSE );
        pub fn parse_name ( source , allow_numeric = false , allow_group_0 = false )  {
        "Parses a name.";
        name = source . get_while ( set ( ")>" ) , include = false );
        if !name {
        panic!("error ( "missing group name" , source . string , source . pos )");
        if name . isdigit ( ) {
        min_group = 0 if allow_group_0 else 1;
        if !allow_numeric || int ( name ) < min_group {
        panic!("error ( "bad character in group name" , source . string ,");
        source . pos );
        } else {
        if !name . isidentifier ( ) {
        panic!("error ( "bad character in group name" , source . string ,");
        source . pos );
        return  name;
        pub fn is_octal ( string )  {
        "Checks whether a string == octal.";
        return  all ( ch in OCT_DIGITS for ch in string );
        pub fn is_decimal ( string )  {
        "Checks whether a string == decimal.";
        return  all ( ch in DIGITS for ch in string );
        pub fn is_hexadecimal ( string )  {
        "Checks whether a string == hexadecimal.";
        return  all ( ch in HEX_DIGITS for ch in string );
        pub fn parse_escape ( source , info , in_set )  {
        "Parses an escape sequence.";
        saved_ignore = source . ignore_space;
        source . ignore_space = false;
        ch = source . get ( );
        source . ignore_space = saved_ignore;
        if !ch {
        panic!("error ( "bad escape (end of pattern)" , source . string , source . pos )");
        if ch in HEX_ESCAPES {
        return  parse_hex_escape ( source , info , ch , HEX_ESCAPES [ ch ] , in_set , ch );
        } else if ch == "g" && !in_set {
        saved_pos = source . pos;
        // try {
        return  parse_group_ref ( source , info );
        // } catch  error  {
        source . pos = saved_pos;
        return  make_character ( info , ord ( ch ) , in_set );
        } else if ch == "G" && !in_set {
        return  SearchAnchor ( );
        } else if ch == "L" && !in_set {
        return  parse_string_set ( source , info );
        } else if ch == "N" {
        return  parse_named_char ( source , info , in_set );
        } else if ch in "pP" {
        return  parse_property ( source , info , ch == "p" , in_set );
        } else if ch == "R" && !in_set {
        charset = [ 0x0 A , 0x0 B , 0x0 C , 0x0 D ];
        if info . guess_encoding == UNICODE {
        charset . extend ( [ 0x85 , 0x2028 , 0x2029 ] );
        return  Atomic ( Branch ( [ String ( [ 0x0 D , 0x0 A ] ) , SetUnion ( info , [ Character ( c );
        for c in charset ] ) ] ) ).iter() {
        } else if ch == "X" && !in_set {
        return  Grapheme ( );
        } else if ch in ALPHA {
        if !in_set {
        if info . flags & WORD {
        value = WORD_POSITION_ESCAPES . get ( ch );
        } else if info . flags & ASCII {
        value = ASCII_POSITION_ESCAPES . get ( ch );
        } else if info . flags & UNICODE {
        value = UNICODE_POSITION_ESCAPES . get ( ch );
        } else {
        value = POSITION_ESCAPES . get ( ch );
        if value {
        return  value;
        if info . flags & ASCII {
        value = ASCII_CHARSET_ESCAPES . get ( ch );
        } else if info . flags & UNICODE {
        value = UNICODE_CHARSET_ESCAPES . get ( ch );
        } else {
        value = CHARSET_ESCAPES . get ( ch );
        if value {
        return  value;
        value = CHARACTER_ESCAPES . get ( ch );
        if value {
        return  Character ( ord ( value ) );
        panic!("error ( "bad escape \\%s" % ch , source . string , source . pos )");
        } else if ch in DIGITS {
        return  parse_numeric_escape ( source , info , ch , in_set );
        } else {
        return  make_character ( info , ord ( ch ) , in_set );
        pub fn parse_numeric_escape ( source , info , ch , in_set )  {
        "Parses a numeric escape sequence.";
        if in_set || ch == "0" {
        return  parse_octal_escape ( source , info , [ ch ] , in_set );
        digits = ch;
        saved_pos = source . pos;
        ch = source . get ( );
        if ch in DIGITS {
        digits + = ch;
        saved_pos = source . pos;
        ch = source . get ( );
        if is_octal ( digits ) && ch in OCT_DIGITS {
        encoding = info . flags & _ALL_ENCODINGS;
        if encoding == ASCII || encoding == LOCALE {
        octal_mask = 0x FF;
        } else {
        octal_mask = 0x1 FF;
        value = int ( digits + ch , 8 ) & octal_mask;
        return  make_character ( info , value );
        source . pos = saved_pos;
        if info . is_open_group ( digits ) {
        panic!("error ( "cannot refer to an open group" , source . string , source . pos )");
        return  make_ref_group ( info , digits , source . pos );
        pub fn parse_octal_escape ( source , info , digits , in_set )  {
        "Parses an octal escape sequence.";
        saved_pos = source . pos;
        ch = source . get ( );
        while len ( digits ) < 3 && ch in OCT_DIGITS  {
        digits . append ( ch );
        saved_pos = source . pos;
        ch = source . get ( );
        source . pos = saved_pos;
        // try {
        value = int ( "" . join ( digits ) , 8 );
        return  make_character ( info , value , in_set );
        // } catch  ValueError  {
        if digits [ 0 ] in OCT_DIGITS {
        panic!("error ( "incomplete escape \\%s" % "" . join ( digits ) ,");
        source . string , source . pos );
        } else {
        panic!("error ( "bad escape \\%s" % digits [ 0 ] , source . string ,");
        source . pos );
        pub fn parse_hex_escape ( source , info , esc , expected_len , in_set , type )  {
        "Parses a hex escape sequence.";
        saved_pos = source . pos;
        digits = [ ];
        for i in range ( expected_len ) .iter() {
        ch = source . get ( );
        if ch !in HEX_DIGITS {
        panic!("error ( "incomplete escape \\%s%s" % ( type , "" . join ( digits ) ) ,");
        source . string , saved_pos );
        digits . append ( ch );
        // try {
        value = int ( "" . join ( digits ) , 16 );
        // } catch  ValueError  {
        // pass
        } else {
        if value < 0x110000 {
        return  make_character ( info , value , in_set );
        panic!("error ( "bad hex escape \\%s%s" % ( esc , "" . join ( digits ) ) ,");
        source . string , saved_pos );
        pub fn parse_group_ref ( source , info )  {
        "Parses a group reference.";
        source . expect ( "<" );
        saved_pos = source . pos;
        name = parse_name ( source , true );
        source . expect ( ">" );
        if info . is_open_group ( name ) {
        panic!("error ( "cannot refer to an open group" , source . string , source . pos )");
        return  make_ref_group ( info , name , saved_pos );
        pub fn parse_string_set ( source , info )  {
        "Parses a string set reference.";
        source . expect ( "<" );
        name = parse_name ( source , true );
        source . expect ( ">" );
        if name is None /* Option */ || name !in info . kwargs {
        panic!("error ( "undefined named list" , source . string , source . pos )");
        return  make_string_set ( info , name );
        pub fn parse_named_char ( source , info , in_set )  {
        "Parses a named character.";
        saved_pos = source . pos;
        if source . match ( "{" ) {
        name = source . get_while ( NAMED_CHAR_PART , keep_spaces = true );
        if source . match ( "}" ) {
        // try {
        value = unicodedata . lookup ( name );
        return  make_character ( info , ord ( value ) , in_set );
        // } catch  KeyError  {
        panic!("error ( "undefined character name" , source . string ,");
        source . pos );
        source . pos = saved_pos;
        return  make_character ( info , ord ( "N" ) , in_set );
        pub fn parse_property ( source , info , positive , in_set )  {
        "Parses a Unicode property.";
        saved_pos = source . pos;
        ch = source . get ( );
        if ch == "{" {
        negate = source . match ( "^" );
        prop_name , name = parse_property_name ( source );
        if source . match ( "}" ) {
        if info . flags & ASCII {
        encoding = ASCII_ENCODING;
        } else if info . flags & UNICODE {
        encoding = UNICODE_ENCODING;
        } else {
        encoding = 0;
        prop = lookup_property ( prop_name , name , positive != negate , source ,;
        encoding = encoding );
        return  make_property ( info , prop , in_set );
        } else if ch && ch in "CLMNPSZ" {
        if info . flags & ASCII {
        encoding = ASCII_ENCODING;
        } else if info . flags & UNICODE {
        encoding = UNICODE_ENCODING;
        } else {
        encoding = 0;
        prop = lookup_property ( None /* Option */ , ch , positive , source , encoding = encoding );
        return  make_property ( info , prop , in_set );
        source . pos = saved_pos;
        ch = "p" if positive else "P";
        return  make_character ( info , ord ( ch ) , in_set );
        pub fn parse_property_name ( source )  {
        "Parses a property name, which may be qualified.";
        name = source . get_while ( PROPERTY_NAME_PART );
        saved_pos = source . pos;
        ch = source . get ( );
        if ch && ch in ":=" {
        prop_name = name;
        name = source . get_while ( ALNUM | set ( " &_-./" ) ) . strip ( );
        if name {
        saved_pos = source . pos;
        } else {
        prop_name , name = None /* Option */ , prop_name;
        } else {
        prop_name = None /* Option */;
        source . pos = saved_pos;
        return  prop_name , name;
        pub fn parse_set ( source , info )  {
        "Parses a character set.";
        version = ( info . flags & _ALL_VERSIONS ) || DEFAULT_VERSION;
        saved_ignore = source . ignore_space;
        source . ignore_space = false;
        negate = source . match ( "^" );
        // try {
        if version == VERSION0 {
        item = parse_set_imp_union ( source , info );
        } else {
        item = parse_set_union ( source , info );
        if !source . match ( "]" ) {
        panic!("error ( "missing ]" , source . string , source . pos )");
        // } finally {
        source . ignore_space = saved_ignore;
        if negate {
        item = item . with_flags ( positive = !item . positive );
        item = item . with_flags ( case_flags = make_case_flags ( info ) );
        return  item;
        pub fn parse_set_union ( source , info )  {
        "Parses a set union ([x||y]).";
        items = [ parse_set_symm_diff ( source , info ) ];
        while source . match ( "||" )  {
        items . append ( parse_set_symm_diff ( source , info ) );
        if len ( items ) == 1 {
        return  items [ 0 ];
        return  SetUnion ( info , items );
        pub fn parse_set_symm_diff ( source , info )  {
        "Parses a set symmetric difference ([x~~y]).";
        items = [ parse_set_inter ( source , info ) ];
        while source . match ( "~~" )  {
        items . append ( parse_set_inter ( source , info ) );
        if len ( items ) == 1 {
        return  items [ 0 ];
        return  SetSymDiff ( info , items );
        pub fn parse_set_inter ( source , info )  {
        "Parses a set intersection ([x&&y]).";
        items = [ parse_set_diff ( source , info ) ];
        while source . match ( "&&" )  {
        items . append ( parse_set_diff ( source , info ) );
        if len ( items ) == 1 {
        return  items [ 0 ];
        return  SetInter ( info , items );
        pub fn parse_set_diff ( source , info )  {
        "Parses a set difference ([x--y]).";
        items = [ parse_set_imp_union ( source , info ) ];
        while source . match ( "--" )  {
        items . append ( parse_set_imp_union ( source , info ) );
        if len ( items ) == 1 {
        return  items [ 0 ];
        return  SetDiff ( info , items );
        pub fn parse_set_imp_union ( source , info )  {
        "Parses a set implicit union ([xy]).";
        version = ( info . flags & _ALL_VERSIONS ) || DEFAULT_VERSION;
        items = [ parse_set_member ( source , info ) ];
        while true  {
        saved_pos = source . pos;
        if source . match ( "]" ) {
        source . pos = saved_pos;
        break;
        if version == VERSION1 && any ( source . match ( op ) for op in SET_OPS ) {
        source . pos = saved_pos;
        break;
        items . append ( parse_set_member ( source , info ) );
        if len ( items ) == 1 {
        return  items [ 0 ];
        return  SetUnion ( info , items );
        pub fn parse_set_member ( source , info )  {
        "Parses a member in a character set.";
        start = parse_set_item ( source , info );
        saved_pos1 = source . pos;
        if ( !isinstance ( start , Character ) || !start . positive || not {
        source . match ( "-" ) ) ;
        return  start;
        version = ( info . flags & _ALL_VERSIONS ) || DEFAULT_VERSION;
        saved_pos2 = source . pos;
        if version == VERSION1 && source . match ( "-" ) {
        source . pos = saved_pos1;
        return  start;
        if source . match ( "]" ) {
        source . pos = saved_pos2;
        return  SetUnion ( info , [ start , Character ( ord ( "-" ) ) ] );
        end = parse_set_item ( source , info );
        if !isinstance ( end , Character ) || !end . positive {
        return  SetUnion ( info , [ start , Character ( ord ( "-" ) ) , end ] );
        if start . value > end . value {
        panic!("error ( "bad character range" , source . string , source . pos )");
        if start . value == end . value {
        return  start;
        return  Range ( start . value , end . value );
        pub fn parse_set_item ( source , info )  {
        "Parses an item in a character set.";
        version = ( info . flags & _ALL_VERSIONS ) || DEFAULT_VERSION;
        if source . match ( "\\" ) {
        return  parse_escape ( source , info , true );
        saved_pos = source . pos;
        if source . match ( "[:" ) {
        // try {
        return  parse_posix_class ( source , info );
        // } catch  ParseError  {
        source . pos = saved_pos;
        if version == VERSION1 && source . match ( "[" ) {
        negate = source . match ( "^" );
        item = parse_set_union ( source , info );
        if !source . match ( "]" ) {
        panic!("error ( "missing ]" , source . string , source . pos )");
        if negate {
        item = item . with_flags ( positive = !item . positive );
        return  item;
        ch = source . get ( );
        if !ch {
        panic!("error ( "unterminated character set" , source . string , source . pos )");
        return  Character ( ord ( ch ) );
        pub fn parse_posix_class ( source , info )  {
        "Parses a POSIX character class.";
        negate = source . match ( "^" );
        prop_name , name = parse_property_name ( source );
        if !source . match ( ":]" ) {
        panic!("ParseError ( )");
        return  lookup_property ( prop_name , name , !negate , source , posix = true );
        pub fn float_to_rational ( flt )  {
        "Converts a float to a rational pair.";
        int_part = int ( flt );
        error = flt - int_part;
        if abs ( error ) < 0.0001 {
        return  int_part , 1;
        den , num = float_to_rational ( 1.0 / error );
        return  int_part * den + num , den;
        pub fn numeric_to_rational ( numeric )  {
        "Converts a numeric string to a rational string, if possible.";
        if numeric [ { : 1 ] == "-" ; }
        sign , numeric = numeric [ 0 ] , numeric [ 1 : ];
        } else {
        sign = "";
        parts = numeric . split ( "/" );
        if len ( parts ) == 2 {
        num , den = float_to_rational ( float ( parts [ 0 ] ) / float ( parts [ 1 ] ) );
        } else if len ( parts ) == 1 {
        num , den = float_to_rational ( float ( parts [ 0 ] ) );
        } else {
        panic!("ValueError ( )");
        result = "{}{}/{}" . format ( sign , num , den );
        if result . endswith ( "/1" ) {
        return  result [ : -2 ];
        return  result;
        pub fn standardise_name ( name )  {
        "Standardises a property || value name.";
        // try {
        return  numeric_to_rational ( "" . join ( name ) );
        // } catch  ( ValueError , ZeroDivisionError )  {
        return  "" . join ( ch for ch in name if ch !in "_- " ) . upper ( );
        _POSIX_CLASSES = set ( "ALNUM DIGIT PUNCT XDIGIT" . split ( ) );
        _BINARY_VALUES = set ( "YES Y NO N TRUE T FALSE F" . split ( ) );
        pub fn lookup_property ( property , value , positive , source = None /* Option */ , posix = false , encoding = 0 )  {
        "Looks up a property.";
        property = standardise_name ( property ) if property else None /* Option */;
        value = standardise_name ( value );
        if ( property , value ) == ( "GENERALCATEGORY" , "ASSIGNED" ) {
        property , value , positive = "GENERALCATEGORY" , "UNASSIGNED" , !positive;
        if posix && !property && value . upper ( ) in _POSIX_CLASSES {
        value = "POSIX" + value;
        if property {
        prop = PROPERTIES . get ( property );
        if !prop {
        if !source {
        panic!("error ( "unknown property" )");
        panic!("error ( "unknown property" , source . string , source . pos )");
        prop_id , value_dict = prop;
        val_id = value_dict . get ( value );
        if val_id is None /* Option */ {
        if !source {
        panic!("error ( "unknown property value" )");
        panic!("error ( "unknown property value" , source . string , source . pos )");
        return  Property ( ( prop_id < < 16 ) | val_id , positive , encoding = encoding );
        for property in ( "GC" , "SCRIPT" , "BLOCK" ) .iter() {
        prop_id , value_dict = PROPERTIES . get ( property );
        val_id = value_dict . get ( value );
        if val_id is !None /* Option */ {
        return  Property ( ( prop_id < < 16 ) | val_id , positive , encoding = encoding );
        prop = PROPERTIES . get ( value );
        if prop {
        prop_id , value_dict = prop;
        if set ( value_dict ) == _BINARY_VALUES {
        return  Property ( ( prop_id < < 16 ) | 1 , positive , encoding = encoding );
        return  Property ( prop_id < < 16 , !positive , encoding = encoding );
        if value . startswith ( "IS" ) {
        prop = PROPERTIES . get ( value [ 2 : ] );
        if prop {
        prop_id , value_dict = prop;
        if "YES" in value_dict {
        return  Property ( ( prop_id < < 16 ) | 1 , positive , encoding = encoding );
        for prefix , property in ( ( "IS" , "SCRIPT" ) , ( "IN" , "BLOCK" ) ) .iter() {
        if value . startswith ( prefix ) {
        prop_id , value_dict = PROPERTIES . get ( property );
        val_id = value_dict . get ( value [ 2 : ] );
        if val_id is !None /* Option */ {
        return  Property ( ( prop_id < < 16 ) | val_id , positive , encoding = encoding );
        if !source {
        panic!("error ( "unknown property" )");
        panic!("error ( "unknown property" , source . string , source . pos )");
        pub fn _compile_replacement ( source , pattern , is_unicode )  {
        "Compiles a replacement template escape sequence.";
        ch = source . get ( );
        if ch in ALPHA {
        value = CHARACTER_ESCAPES . get ( ch );
        if value {
        return  false , [ ord ( value ) ];
        if ch in HEX_ESCAPES && ( ch == "x" || is_unicode ) {
        return  false , [ parse_repl_hex_escape ( source , HEX_ESCAPES [ ch ] , ch ) ];
        if ch == "g" {
        return  true , [ compile_repl_group ( source , pattern ) ];
        if ch == "N" && is_unicode {
        value = parse_repl_named_char ( source );
        if value is !None /* Option */ {
        return  false , [ value ];
        panic!("error ( "bad escape \\%s" % ch , source . string , source . pos )");
        if isinstance ( source . sep , bytes ) {
        octal_mask = 0x FF;
        } else {
        octal_mask = 0x1 FF;
        if ch == "0" {
        digits = ch;
        while len ( digits ) < 3  {
        saved_pos = source . pos;
        ch = source . get ( );
        if ch !in OCT_DIGITS {
        source . pos = saved_pos;
        break;
        digits + = ch;
        return  false , [ int ( digits , 8 ) & octal_mask ];
        if ch in DIGITS {
        digits = ch;
        saved_pos = source . pos;
        ch = source . get ( );
        if ch in DIGITS {
        digits + = ch;
        saved_pos = source . pos;
        ch = source . get ( );
        if ch && is_octal ( digits + ch ) {
        return  false , [ int ( digits + ch , 8 ) & octal_mask ];
        source . pos = saved_pos;
        return  true , [ int ( digits ) ];
        if ch == "\\" {
        return  false , [ ord ( "\\" ) ];
        if !ch {
        panic!("error ( "bad escape (end of pattern)" , source . string , source . pos )");
        return  false , [ ord ( "\\" ) , ord ( ch ) ];
        pub fn parse_repl_hex_escape ( source , expected_len , type )  {
        "Parses a hex escape sequence in a replacement string.";
        digits = [ ];
        for i in range ( expected_len ) .iter() {
        ch = source . get ( );
        if ch !in HEX_DIGITS {
        panic!("error ( "incomplete escape \\%s%s" % ( type , "" . join ( digits ) ) ,");
        source . string , source . pos );
        digits . append ( ch );
        return  int ( "" . join ( digits ) , 16 );
        pub fn parse_repl_named_char ( source )  {
        "Parses a named character in a replacement string.";
        saved_pos = source . pos;
        if source . match ( "{" ) {
        name = source . get_while ( ALPHA | set ( " " ) );
        if source . match ( "}" ) {
        // try {
        value = unicodedata . lookup ( name );
        return  ord ( value );
        // } catch  KeyError  {
        panic!("error ( "undefined character name" , source . string ,");
        source . pos );
        source . pos = saved_pos;
        return;
        pub fn compile_repl_group ( source , pattern )  {
        "Compiles a replacement template group reference.";
        source . expect ( "<" );
        name = parse_name ( source , true , true );
        source . expect ( ">" );
        if name . isdigit ( ) {
        index = int ( name );
        if !0 <= index <= pattern . groups {
        panic!("error ( "invalid group reference" , source . string , source . pos )");
        return  index;
        // try {
        return  pattern . groupindex [ name ];
        // } catch  KeyError  {
        panic!("IndexError ( "unknown group" )");
        INDENT = "  ";
        POSITIVE_OP = 0x1;
        ZEROWIDTH_OP = 0x2;
        FUZZY_OP = 0x4;
        REVERSE_OP = 0x8;
        REQUIRED_OP = 0x10;
        ENCODING_OP_SHIFT = 5;
        POS_TEXT = { false : "NON-MATCH" , true : "MATCH" };
        CASE_TEXT = { NOCASE : "" , IGNORECASE : " SIMPLE_IGNORE_CASE" , FULLCASE : "" ,;
        FULLIGNORECASE : " FULL_IGNORE_CASE" };
        pub fn make_sequence ( items )  {
        if len ( items ) == 1 {
        return  items [ 0 ];
        return  Sequence ( items );
        class RegexBase ;
        pub fn __init__ ( self )  {
        self . _key = self . __class__;
        pub fn with_flags ( &self, positive = None /* Option */ , case_flags = None /* Option */ , zerowidth = None /* Option */ )  {
        if positive is None /* Option */ {
        positive = self . positive;
        } else {
        positive = bool ( positive );
        if case_flags is None /* Option */ {
        case_flags = self . case_flags;
        } else {
        case_flags = CASE_FLAGS_COMBINATIONS [ case_flags & CASE_FLAGS ];
        if zerowidth is None /* Option */ {
        zerowidth = self . zerowidth;
        } else {
        zerowidth = bool ( zerowidth );
        if ( positive == self . positive && case_flags == self . case_flags and {
        zerowidth == self . zerowidth ) ;
        return  self;
        return  self . rebuild ( positive , case_flags , zerowidth );
        pub fn fix_groups ( &self, pattern , reverse , fuzzy )  {
        // pass
        pub fn optimise ( &self, info , reverse )  {
        return  self;
        pub fn pack_characters ( &self, info )  {
        return  self;
        pub fn remove_captures ( self )  {
        return  self;
        pub fn is_atomic ( self )  {
        return  true;
        pub fn can_be_affix ( self )  {
        return  true;
        pub fn contains_group ( self )  {
        return  false;
        pub fn get_firstset ( &self, reverse )  {
        panic!("_FirstSetError ( )");
        pub fn has_simple_start ( self )  {
        return  false;
        pub fn compile ( &self, reverse = false , fuzzy = false )  {
        return  self . _compile ( reverse , fuzzy );
        pub fn is_empty ( self )  {
        return  false;
        pub fn __hash__ ( self )  {
        return  hash ( self . _key );
        pub fn __eq__ ( &self, other )  {
        return  type ( self ) is type ( other ) && self . _key == other . _key;
        pub fn __ne__ ( &self, other )  {
        return  !self . __eq__ ( other );
        pub fn get_required_string ( &self, reverse )  {
        return  self . max_width ( ) , None /* Option */;
        class ZeroWidthBase ( RegexBase ) ;
        pub fn __init__ ( &self, positive = true , encoding = 0 )  {
        RegexBase . __init__ ( self );
        self . positive = bool ( positive );
        self . encoding = encoding;
        self . _key = self . __class__ , self . positive;
        pub fn get_firstset ( &self, reverse )  {
        return  set ( [ None /* Option */ ] );
        pub fn _compile ( &self, reverse , fuzzy )  {
        flags = 0;
        if self . positive {
        flags | = POSITIVE_OP;
        if fuzzy {
        flags | = FUZZY_OP;
        if reverse {
        flags | = REVERSE_OP;
        flags | = self . encoding < < ENCODING_OP_SHIFT;
        return  [ ( self . _opcode , flags ) ];
        pub fn dump ( &self, indent , reverse )  {
        println!( "{}{} {}{}" . format ( INDENT * indent , self . _op_name );
        POS_TEXT [ self . positive ] , [ "" , " ASCII" ] [ self . encoding ] ) );
        pub fn max_width ( self )  {
        return  0;
        class Any ( RegexBase ) ;
        _opcode = { false : OP . ANY , true : OP . ANY_REV };
        _op_name = "ANY";
        pub fn has_simple_start ( self )  {
        return  true;
        pub fn _compile ( &self, reverse , fuzzy )  {
        flags = 0;
        if fuzzy {
        flags | = FUZZY_OP;
        return  [ ( self . _opcode [ reverse ] , flags ) ];
        pub fn dump ( &self, indent , reverse )  {
        println!( "{}{}" . format ( INDENT * indent , self . _op_name ) );
        pub fn max_width ( self )  {
        return  1;
        class AnyAll ( Any ) ;
        _opcode = { false : OP . ANY_ALL , true : OP . ANY_ALL_REV };
        _op_name = "ANY_ALL";
        pub fn __init__ ( self )  {
        self . positive = true;
        self . zerowidth = false;
        self . case_flags = 0;
        self . _key = self . __class__ , self . positive;
        class AnyU ( Any ) ;
        _opcode = { false : OP . ANY_U , true : OP . ANY_U_REV };
        _op_name = "ANY_U";
        class Atomic ( RegexBase ) ;
        pub fn __init__ ( &self, subpattern )  {
        RegexBase . __init__ ( self );
        self . subpattern = subpattern;
        pub fn fix_groups ( &self, pattern , reverse , fuzzy )  {
        self . subpattern . fix_groups ( pattern , reverse , fuzzy );
        pub fn optimise ( &self, info , reverse )  {
        self . subpattern = self . subpattern . optimise ( info , reverse );
        if self . subpattern . is_empty ( ) {
        return  self . subpattern;
        return  self;
        pub fn pack_characters ( &self, info )  {
        self . subpattern = self . subpattern . pack_characters ( info );
        return  self;
        pub fn remove_captures ( self )  {
        self . subpattern = self . subpattern . remove_captures ( );
        return  self;
        pub fn can_be_affix ( self )  {
        return  self . subpattern . can_be_affix ( );
        pub fn contains_group ( self )  {
        return  self . subpattern . contains_group ( );
        pub fn get_firstset ( &self, reverse )  {
        return  self . subpattern . get_firstset ( reverse );
        pub fn has_simple_start ( self )  {
        return  self . subpattern . has_simple_start ( );
        pub fn _compile ( &self, reverse , fuzzy )  {
        return  ( [ ( OP . ATOMIC , ) ] + self . subpattern . compile ( reverse , fuzzy ) +;
        [ ( OP . END , ) ] );
        pub fn dump ( &self, indent , reverse )  {
        println!( "{}ATOMIC" . format ( INDENT * indent ) );
        self . subpattern . dump ( indent + 1 , reverse );
        pub fn is_empty ( self )  {
        return  self . subpattern . is_empty ( );
        pub fn __eq__ ( &self, other )  {
        return  ( type ( self ) is type ( other ) && self . subpattern ==;
        other . subpattern );
        pub fn max_width ( self )  {
        return  self . subpattern . max_width ( );
        pub fn get_required_string ( &self, reverse )  {
        return  self . subpattern . get_required_string ( reverse );
        class Boundary ( ZeroWidthBase ) ;
        _opcode = OP . BOUNDARY;
        _op_name = "BOUNDARY";
        class Branch ( RegexBase ) ;
        pub fn __init__ ( &self, branches )  {
        RegexBase . __init__ ( self );
        self . branches = branches;
        pub fn fix_groups ( &self, pattern , reverse , fuzzy )  {
        for b in self . branches .iter() {
        b . fix_groups ( pattern , reverse , fuzzy );
        pub fn optimise ( &self, info , reverse )  {
        if !self . branches {
        return  Sequence ( [ ] );
        branches = Branch . _flatten_branches ( info , reverse , self . branches );
        if reverse {
        suffix , branches = Branch . _split_common_suffix ( info , branches );
        prefix = [ ];
        } else {
        prefix , branches = Branch . _split_common_prefix ( info , branches );
        suffix = [ ];
        branches = Branch . _reduce_to_set ( info , reverse , branches );
        if len ( branches ) > 1 {
        sequence = [ Branch ( branches ) ];
        if !prefix || !suffix {
        firstset = self . _add_precheck ( info , reverse , branches );
        if firstset {
        if reverse {
        sequence . append ( firstset );
        } else {
        sequence . insert ( 0 , firstset );
        } else {
        sequence = branches;
        return  make_sequence ( prefix + sequence + suffix );
        pub fn _add_precheck ( &self, info , reverse , branches )  {
        charset = set ( );
        pos = -1 if reverse else 0;
        for branch in branches .iter() {
        if type ( branch ) is Literal && branch . case_flags == NOCASE {
        charset . add ( branch . characters [ pos ] );
        } else {
        return;
        if !charset {
        return;
        return  _check_firstset ( info , reverse , [ Character ( c ) for c in charset ] );
        pub fn pack_characters ( &self, info )  {
        self . branches = [ b . pack_characters ( info ) for b in self . branches ];
        return  self;
        pub fn remove_captures ( self )  {
        self . branches = [ b . remove_captures ( ) for b in self . branches ];
        return  self;
        pub fn is_atomic ( self )  {
        return  all ( b . is_atomic ( ) for b in self . branches );
        pub fn can_be_affix ( self )  {
        return  all ( b . can_be_affix ( ) for b in self . branches );
        pub fn contains_group ( self )  {
        return  any ( b . contains_group ( ) for b in self . branches );
        pub fn get_firstset ( &self, reverse )  {
        fs = set ( );
        for b in self . branches .iter() {
        fs | = b . get_firstset ( reverse );
        return  fs || set ( [ None /* Option */ ] );
        pub fn _compile ( &self, reverse , fuzzy )  {
        if !self . branches {
        return  [ ];
        code = [ ( OP . BRANCH , ) ];
        for b in self . branches .iter() {
        code . extend ( b . compile ( reverse , fuzzy ) );
        code . append ( ( OP . NEXT , ) );
        code [ -1 ] = ( OP . END , );
        return  code;
        pub fn dump ( &self, indent , reverse )  {
        println!( "{}BRANCH" . format ( INDENT * indent ) );
        self . branches [ 0 ] . dump ( indent + 1 , reverse );
        for b in self . branches [ 1 : ] .iter() {
        println!( "{}OR" . format ( INDENT * indent ) );
        b . dump ( indent + 1 , reverse );
        @ staticmethod;
        pub fn _flatten_branches ( info , reverse , branches )  {
        new_branches = [ ];
        for b in branches .iter() {
        b = b . optimise ( info , reverse );
        if isinstance ( b , Branch ) {
        new_branches . extend ( b . branches );
        } else {
        new_branches . append ( b );
        return  new_branches;
        @ staticmethod;
        pub fn _split_common_prefix ( info , branches )  {
        alternatives = [ ];
        for b in branches .iter() {
        if isinstance ( b , Sequence ) {
        alternatives . append ( b . items );
        } else {
        alternatives . append ( [ b ] );
        max_count = min ( len ( a ) for a in alternatives );
        prefix = alternatives [ 0 ];
        pos = 0;
        end_pos = max_count;
        while pos < end_pos && prefix [ pos ] . can_be_affix ( ) && all ( a [ pos ] == {
        prefix vec![ pos ].iter().map(|a| alternatives ) ;
        pos + = 1;
        count = pos;
        if info . flags & UNICODE {
        count = pos;
        while count > 0 && !all ( Branch . _can_split ( a , count ) for a in {
        alternatives ) ;
        count - = 1;
        if count == 0 {
        return  [ ] , branches;
        new_branches = [ ];
        for a in alternatives .iter() {
        new_branches . append ( make_sequence ( a [ count : ] ) );
        return  prefix [ : count ] , new_branches;
        @ staticmethod;
        pub fn _split_common_suffix ( info , branches )  {
        alternatives = [ ];
        for b in branches .iter() {
        if isinstance ( b , Sequence ) {
        alternatives . append ( b . items );
        } else {
        alternatives . append ( [ b ] );
        max_count = min ( len ( a ) for a in alternatives );
        suffix = alternatives [ 0 ];
        pos = -1;
        end_pos = -1 - max_count;
        while pos > end_pos && suffix [ pos ] . can_be_affix ( ) && all ( a [ pos ] == {
        suffix vec![ pos ].iter().map(|a| alternatives ) ;
        pos - = 1;
        count = -1 - pos;
        if info . flags & UNICODE {
        while count > 0 && !all ( Branch . _can_split_rev ( a , count ) for a {
        in alternatives ) ;
        count - = 1;
        if count == 0 {
        return  [ ] , branches;
        new_branches = [ ];
        for a in alternatives .iter() {
        new_branches . append ( make_sequence ( a [ : - count ] ) );
        return  suffix [ - count : ] , new_branches;
        @ staticmethod;
        pub fn _can_split ( items , count )  {
        if !Branch . _is_full_case ( items , count - 1 ) {
        return  true;
        if !Branch . _is_full_case ( items , count ) {
        return  true;
        if Branch . _is_folded ( items [ count - 1 { : count + 1 ] ) ; }
        return  false;
        if ( Branch . _is_full_case ( items , count + 2 ) and {
        Branch . _is_folded ( items [ count - 1 : count + 2 ] ) ) ;
        return  false;
        if ( Branch . _is_full_case ( items , count - 2 ) and {
        Branch . _is_folded ( items [ count - 2 : count + 1 ] ) ) ;
        return  false;
        return  true;
        @ staticmethod;
        pub fn _can_split_rev ( items , count )  {
        end = len ( items );
        if !Branch . _is_full_case ( items , end - count ) {
        return  true;
        if !Branch . _is_full_case ( items , end - count - 1 ) {
        return  true;
        if Branch . _is_folded ( items [ end - count - 1 { : end - count + 1 ] ) ; }
        return  false;
        if ( Branch . _is_full_case ( items , end - count + 2 ) and {
        Branch . _is_folded ( items [ end - count - 1 : end - count + 2 ] ) ) ;
        return  false;
        if ( Branch . _is_full_case ( items , end - count - 2 ) and {
        Branch . _is_folded ( items [ end - count - 2 : end - count + 1 ] ) ) ;
        return  false;
        return  true;
        @ staticmethod;
        pub fn _merge_common_prefixes ( info , reverse , branches )  {
        prefixed = defaultdict ( list );
        order = { };
        new_branches = [ ];
        for b in branches .iter() {
        if Branch . _is_simple_character ( b ) {
        prefixed [ b . value ] . append ( [ b ] );
        order . setdefault ( b . value , len ( order ) );
        } else if ( isinstance ( b , Sequence ) && b . items and {
        Branch . _is_simple_character ( b . items [ 0 ] ) ) ;
        prefixed [ b . items [ 0 ] . value ] . append ( b . items );
        order . setdefault ( b . items [ 0 ] . value , len ( order ) );
        } else {
        Branch . _flush_char_prefix ( info , reverse , prefixed , order ,;
        new_branches );
        new_branches . append ( b );
        Branch . _flush_char_prefix ( info , prefixed , order , new_branches );
        return  new_branches;
        @ staticmethod;
        pub fn _is_simple_character ( c )  {
        return  isinstance ( c , Character ) && c . positive && !c . case_flags;
        @ staticmethod;
        pub fn _reduce_to_set ( info , reverse , branches )  {
        new_branches = [ ];
        items = set ( );
        case_flags = NOCASE;
        for b in branches .iter() {
        if isinstance ( b , ( Character , Property , SetBase ) ) {
        if b . case_flags != case_flags {
        Branch . _flush_set_members ( info , reverse , items , case_flags ,;
        new_branches );
        case_flags = b . case_flags;
        items . add ( b . with_flags ( case_flags = NOCASE ) );
        } else {
        Branch . _flush_set_members ( info , reverse , items , case_flags ,;
        new_branches );
        new_branches . append ( b );
        Branch . _flush_set_members ( info , reverse , items , case_flags ,;
        new_branches );
        return  new_branches;
        @ staticmethod;
        pub fn _flush_char_prefix ( info , reverse , prefixed , order , new_branches )  {
        if !prefixed {
        return;
        for value , branches in sorted ( prefixed . items ( ) , key = lambda pair .iter() {
        order [ pair [ 0 ] ] ) ;
        if len ( branches ) == 1 {
        new_branches . append ( make_sequence ( branches [ 0 ] ) );
        } else {
        subbranches = [ ];
        optional = false;
        for b in branches .iter() {
        if len ( b ) > 1 {
        subbranches . append ( make_sequence ( b [ 1 : ] ) );
        } else if !optional {
        subbranches . append ( Sequence ( ) );
        optional = true;
        sequence = Sequence ( [ Character ( value ) , Branch ( subbranches ) ] );
        new_branches . append ( sequence . optimise ( info , reverse ) );
        prefixed . clear ( );
        order . clear ( );
        @ staticmethod;
        pub fn _flush_set_members ( info , reverse , items , case_flags , new_branches )  {
        if !items {
        return;
        if len ( items ) == 1 {
        item = list ( items ) [ 0 ];
        } else {
        item = SetUnion ( info , list ( items ) ) . optimise ( info , reverse );
        new_branches . append ( item . with_flags ( case_flags = case_flags ) );
        items . clear ( );
        @ staticmethod;
        pub fn _is_full_case ( items , i )  {
        if !0 <= i < len ( items ) {
        return  false;
        item = items [ i ];
        return  ( isinstance ( item , Character ) && item . positive and;
        ( item . case_flags & FULLIGNORECASE ) == FULLIGNORECASE );
        @ staticmethod;
        pub fn _is_folded ( items )  {
        if len ( items ) < 2 {
        return  false;
        for i in items .iter() {
        if ( !isinstance ( i , Character ) || !i . positive || not {
        i . case_flags ) ;
        return  false;
        folded = "" . join ( chr ( i . value ) for i in items );
        folded = _regex . fold_case ( FULL_CASE_FOLDING , folded );
        expanding_chars = _regex . get_expand_on_folding ( );
        for c in expanding_chars .iter() {
        if folded == _regex . fold_case ( FULL_CASE_FOLDING , c ) {
        return  true;
        return  false;
        pub fn is_empty ( self )  {
        return  all ( b . is_empty ( ) for b in self . branches );
        pub fn __eq__ ( &self, other )  {
        return  type ( self ) is type ( other ) && self . branches == other . branches;
        pub fn max_width ( self )  {
        return  max ( b . max_width ( ) for b in self . branches );
        class CallGroup ( RegexBase ) ;
        pub fn __init__ ( &self, info , group , position )  {
        RegexBase . __init__ ( self );
        self . info = info;
        self . group = group;
        self . position = position;
        self . _key = self . __class__ , self . group;
        pub fn fix_groups ( &self, pattern , reverse , fuzzy )  {
        // try {
        self . group = int ( self . group );
        // } catch  ValueError  {
        // try {
        self . group = self . info . group_index [ self . group ];
        // } catch  KeyError  {
        panic!("error ( "invalid group reference" , pattern , self . position )");
        if !0 <= self . group <= self . info . group_count {
        panic!("error ( "unknown group" , pattern , self . position )");
        if self . group > 0 && self . info . open_group_count [ self . group ] > 1 {
        panic!("error ( "ambiguous group reference" , pattern , self . position )");
        self . info . group_calls . append ( ( self , reverse , fuzzy ) );
        self . _key = self . __class__ , self . group;
        pub fn remove_captures ( self )  {
        panic!("error ( "group reference !allowed" , self . pattern , self . position )");
        pub fn _compile ( &self, reverse , fuzzy )  {
        return  [ ( OP . GROUP_CALL , self . call_ref ) ];
        pub fn dump ( &self, indent , reverse )  {
        println!( "{}GROUP_CALL {}" . format ( INDENT * indent , self . group ) );
        pub fn __eq__ ( &self, other )  {
        return  type ( self ) is type ( other ) && self . group == other . group;
        pub fn max_width ( self )  {
        return  UNLIMITED;
        pub fn __del__ ( self )  {
        self . info = None /* Option */;
        class CallRef ( RegexBase ) ;
        pub fn __init__ ( &self, ref , parsed )  {
        self . ref = ref;
        self . parsed = parsed;
        pub fn _compile ( &self, reverse , fuzzy )  {
        return  ( [ ( OP . CALL_REF , self . ref ) ] + self . parsed . _compile ( reverse ,;
        fuzzy ) + [ ( OP . END , ) ] );
        class Character ( RegexBase ) ;
        _opcode = { ( NOCASE , false ) : OP . CHARACTER , ( IGNORECASE , false ) ;
        OP . CHARACTER_IGN , ( FULLCASE , false ) : OP . CHARACTER , ( FULLIGNORECASE ,;
        false ) : OP . CHARACTER_IGN , ( NOCASE , true ) : OP . CHARACTER_REV , ( IGNORECASE ,;
        true ) : OP . CHARACTER_IGN_REV , ( FULLCASE , true ) : OP . CHARACTER_REV ,;
        ( FULLIGNORECASE , true ) : OP . CHARACTER_IGN_REV };
        pub fn __init__ ( &self, value , positive = true , case_flags = NOCASE , {
        zerowidth = false ) ;
        RegexBase . __init__ ( self );
        self . value = value;
        self . positive = bool ( positive );
        self . case_flags = CASE_FLAGS_COMBINATIONS [ case_flags ];
        self . zerowidth = bool ( zerowidth );
        if ( self . positive && ( self . case_flags & FULLIGNORECASE ) == {
        FULLIGNORECASE ) ;
        self . folded = _regex . fold_case ( FULL_CASE_FOLDING , chr ( self . value ) );
        } else {
        self . folded = chr ( self . value );
        self . _key = ( self . __class__ , self . value , self . positive ,;
        self . case_flags , self . zerowidth );
        pub fn rebuild ( &self, positive , case_flags , zerowidth )  {
        return  Character ( self . value , positive , case_flags , zerowidth );
        pub fn optimise ( &self, info , reverse , in_set = false )  {
        return  self;
        pub fn get_firstset ( &self, reverse )  {
        return  set ( [ self ] );
        pub fn has_simple_start ( self )  {
        return  true;
        pub fn _compile ( &self, reverse , fuzzy )  {
        flags = 0;
        if self . positive {
        flags | = POSITIVE_OP;
        if self . zerowidth {
        flags | = ZEROWIDTH_OP;
        if fuzzy {
        flags | = FUZZY_OP;
        code = PrecompiledCode ( [ self . _opcode [ self . case_flags , reverse ] , flags ,;
        self . value ] );
        if len ( self . folded ) > 1 {
        code = Branch ( vec![ code , String ( vec![ ord ( c ).iter().map(|c| self . folded ] ,;
        case_flags = self . case_flags ) ] );
        return  code . compile ( reverse , fuzzy );
        pub fn dump ( &self, indent , reverse )  {
        display = ascii ( chr ( self . value ) ) . lstrip ( "bu" );
        println!( "{}CHARACTER {} {}{}" . format ( INDENT * indent );
        POS_TEXT [ self . positive ] , display , CASE_TEXT [ self . case_flags ] ) );
        pub fn matches ( &self, ch )  {
        return  ( ch == self . value ) == self . positive;
        pub fn max_width ( self )  {
        return  len ( self . folded );
        pub fn get_required_string ( &self, reverse )  {
        if !self . positive {
        return  1 , None /* Option */;
        self . folded_characters = tuple ( ord ( c ) for c in self . folded );
        return  0 , self;
        class Conditional ( RegexBase ) ;
        pub fn __init__ ( &self, info , group , yes_item , no_item , position )  {
        RegexBase . __init__ ( self );
        self . info = info;
        self . group = group;
        self . yes_item = yes_item;
        self . no_item = no_item;
        self . position = position;
        pub fn fix_groups ( &self, pattern , reverse , fuzzy )  {
        // try {
        self . group = int ( self . group );
        // } catch  ValueError  {
        // try {
        self . group = self . info . group_index [ self . group ];
        // } catch  KeyError  {
        if self . group == "DEFINE" {
        self . group = 0;
        } else {
        panic!("error ( "unknown group" , pattern , self . position )");
        if !0 <= self . group <= self . info . group_count {
        panic!("error ( "invalid group reference" , pattern , self . position )");
        self . yes_item . fix_groups ( pattern , reverse , fuzzy );
        self . no_item . fix_groups ( pattern , reverse , fuzzy );
        pub fn optimise ( &self, info , reverse )  {
        yes_item = self . yes_item . optimise ( info , reverse );
        no_item = self . no_item . optimise ( info , reverse );
        return  Conditional ( info , self . group , yes_item , no_item , self . position );
        pub fn pack_characters ( &self, info )  {
        self . yes_item = self . yes_item . pack_characters ( info );
        self . no_item = self . no_item . pack_characters ( info );
        return  self;
        pub fn remove_captures ( self )  {
        self . yes_item = self . yes_item . remove_captures ( );
        self . no_item = self . no_item . remove_captures ( );
        pub fn is_atomic ( self )  {
        return  self . yes_item . is_atomic ( ) && self . no_item . is_atomic ( );
        pub fn can_be_affix ( self )  {
        return  self . yes_item . can_be_affix ( ) && self . no_item . can_be_affix ( );
        pub fn contains_group ( self )  {
        return  self . yes_item . contains_group ( ) || self . no_item . contains_group ( );
        pub fn get_firstset ( &self, reverse )  {
        return  ( self . yes_item . get_firstset ( reverse ) |;
        self . no_item . get_firstset ( reverse ) );
        pub fn _compile ( &self, reverse , fuzzy )  {
        code = [ ( OP . GROUP_EXISTS , self . group ) ];
        code . extend ( self . yes_item . compile ( reverse , fuzzy ) );
        add_code = self . no_item . compile ( reverse , fuzzy );
        if add_code {
        code . append ( ( OP . NEXT , ) );
        code . extend ( add_code );
        code . append ( ( OP . END , ) );
        return  code;
        pub fn dump ( &self, indent , reverse )  {
        println!( "{}GROUP_EXISTS {}" . format ( INDENT * indent , self . group ) );
        self . yes_item . dump ( indent + 1 , reverse );
        if !self . no_item . is_empty ( ) {
        println!( "{}OR" . format ( INDENT * indent ) );
        self . no_item . dump ( indent + 1 , reverse );
        pub fn is_empty ( self )  {
        return  self . yes_item . is_empty ( ) && self . no_item . is_empty ( );
        pub fn __eq__ ( &self, other )  {
        return  type ( self ) is type ( other ) && ( self . group , self . yes_item ,;
        self . no_item ) == ( other . group , other . yes_item , other . no_item );
        pub fn max_width ( self )  {
        return  max ( self . yes_item . max_width ( ) , self . no_item . max_width ( ) );
        pub fn __del__ ( self )  {
        self . info = None /* Option */;
        class DefaultBoundary ( ZeroWidthBase ) ;
        _opcode = OP . DEFAULT_BOUNDARY;
        _op_name = "DEFAULT_BOUNDARY";
        class DefaultEndOfWord ( ZeroWidthBase ) ;
        _opcode = OP . DEFAULT_END_OF_WORD;
        _op_name = "DEFAULT_END_OF_WORD";
        class DefaultStartOfWord ( ZeroWidthBase ) ;
        _opcode = OP . DEFAULT_START_OF_WORD;
        _op_name = "DEFAULT_START_OF_WORD";
        class EndOfLine ( ZeroWidthBase ) ;
        _opcode = OP . END_OF_LINE;
        _op_name = "END_OF_LINE";
        class EndOfLineU ( EndOfLine ) ;
        _opcode = OP . END_OF_LINE_U;
        _op_name = "END_OF_LINE_U";
        class EndOfString ( ZeroWidthBase ) ;
        _opcode = OP . END_OF_STRING;
        _op_name = "END_OF_STRING";
        class EndOfStringLine ( ZeroWidthBase ) ;
        _opcode = OP . END_OF_STRING_LINE;
        _op_name = "END_OF_STRING_LINE";
        class EndOfStringLineU ( EndOfStringLine ) ;
        _opcode = OP . END_OF_STRING_LINE_U;
        _op_name = "END_OF_STRING_LINE_U";
        class EndOfWord ( ZeroWidthBase ) ;
        _opcode = OP . END_OF_WORD;
        _op_name = "END_OF_WORD";
        class Failure ( ZeroWidthBase ) ;
        _op_name = "FAILURE";
        pub fn _compile ( &self, reverse , fuzzy )  {
        return  [ ( OP . FAILURE , ) ];
        class Fuzzy ( RegexBase ) ;
        pub fn __init__ ( &self, subpattern , constraints = None /* Option */ )  {
        RegexBase . __init__ ( self );
        if constraints is None /* Option */ {
        constraints = { };
        self . subpattern = subpattern;
        self . constraints = constraints;
        if "cost" in constraints {
        for e in "dis" .iter() {
        if e in constraints [ "cost" ] {
        constraints . setdefault ( e , ( 0 , None /* Option */ ) );
        if set ( constraints ) & set ( "dis" ) {
        for e in "dis" .iter() {
        constraints . setdefault ( e , ( 0 , 0 ) );
        } else {
        for e in "dis" .iter() {
        constraints . setdefault ( e , ( 0 , None /* Option */ ) );
        constraints . setdefault ( "e" , ( 0 , None /* Option */ ) );
        if "cost" in constraints {
        for e in "dis" .iter() {
        constraints [ "cost" ] . setdefault ( e , 0 );
        } else {
        constraints [ "cost" ] = { "d" : 1 , "i" : 1 , "s" : 1 , "max" ;
        constraints [ "e" ] [ 1 ] };
        pub fn fix_groups ( &self, pattern , reverse , fuzzy )  {
        self . subpattern . fix_groups ( pattern , reverse , true );
        pub fn pack_characters ( &self, info )  {
        self . subpattern = self . subpattern . pack_characters ( info );
        return  self;
        pub fn remove_captures ( self )  {
        self . subpattern = self . subpattern . remove_captures ( );
        return  self;
        pub fn is_atomic ( self )  {
        return  self . subpattern . is_atomic ( );
        pub fn contains_group ( self )  {
        return  self . subpattern . contains_group ( );
        pub fn _compile ( &self, reverse , fuzzy )  {
        arguments = [ ];
        for e in "dise" .iter() {
        v = self . constraints [ e ];
        arguments . append ( v [ 0 ] );
        arguments . append ( UNLIMITED if v [ 1 ] == None /* Option */ else v [ 1 ] );
        for e in "dis" .iter() {
        arguments . append ( self . constraints [ "cost" ] [ e ] );
        v = self . constraints [ "cost" ] [ "max" ];
        arguments . append ( UNLIMITED if v == None /* Option */ else v );
        flags = 0;
        if reverse {
        flags | = REVERSE_OP;
        test = self . constraints . get ( "test" );
        if test {
        return  ( [ ( OP . FUZZY_EXT , flags ) + tuple ( arguments ) ] +;
        test . compile ( reverse , true ) + [ ( OP . NEXT , ) ] +;
        self . subpattern . compile ( reverse , true ) + [ ( OP . END , ) ] );
        return  ( [ ( OP . FUZZY , flags ) + tuple ( arguments ) ] +;
        self . subpattern . compile ( reverse , true ) + [ ( OP . END , ) ] );
        pub fn dump ( &self, indent , reverse )  {
        constraints = self . _constraints_to_string ( );
        if constraints {
        constraints = " " + constraints;
        println!( "{}FUZZY{}" . format ( INDENT * indent , constraints ) );
        self . subpattern . dump ( indent + 1 , reverse );
        pub fn is_empty ( self )  {
        return  self . subpattern . is_empty ( );
        pub fn __eq__ ( &self, other )  {
        return  ( type ( self ) is type ( other ) && self . subpattern ==;
        other . subpattern && self . constraints == other . constraints );
        pub fn max_width ( self )  {
        return  UNLIMITED;
        pub fn _constraints_to_string ( self )  {
        constraints = [ ];
        for name in "ids" .iter() {
        min , max = self . constraints [ name ];
        if max == 0 {
        continue;
        con = "";
        if min > 0 {
        con = "{}<=" . format ( min );
        con + = name;
        if max is !None /* Option */ {
        con + = "<={}" . format ( max );
        constraints . append ( con );
        cost = [ ];
        for name in "ids" .iter() {
        coeff = self . constraints [ "cost" ] [ name ];
        if coeff > 0 {
        cost . append ( "{}{}" . format ( coeff , name ) );
        limit = self . constraints [ "cost" ] [ "max" ];
        if limit is !None /* Option */ && limit > 0 {
        cost = "{}<={}" . format ( "+" . join ( cost ) , limit );
        constraints . append ( cost );
        return  "," . join ( constraints );
        class Grapheme ( RegexBase ) ;
        pub fn _compile ( &self, reverse , fuzzy )  {
        grapheme_matcher = Atomic ( Sequence ( [ LazyRepeat ( AnyAll ( ) , 1 , None /* Option */ ) ,;
        GraphemeBoundary ( ) ] ) );
        return  grapheme_matcher . compile ( reverse , fuzzy );
        pub fn dump ( &self, indent , reverse )  {
        println!( "{}GRAPHEME" . format ( INDENT * indent ) );
        pub fn max_width ( self )  {
        return  UNLIMITED;
        class GraphemeBoundary ;
        pub fn compile ( &self, reverse , fuzzy )  {
        return  [ ( OP . GRAPHEME_BOUNDARY , 1 ) ];
        class GreedyRepeat ( RegexBase ) ;
        _opcode = OP . GREEDY_REPEAT;
        _op_name = "GREEDY_REPEAT";
        pub fn __init__ ( &self, subpattern , min_count , max_count )  {
        RegexBase . __init__ ( self );
        self . subpattern = subpattern;
        self . min_count = min_count;
        self . max_count = max_count;
        pub fn fix_groups ( &self, pattern , reverse , fuzzy )  {
        self . subpattern . fix_groups ( pattern , reverse , fuzzy );
        pub fn optimise ( &self, info , reverse )  {
        subpattern = self . subpattern . optimise ( info , reverse );
        return  type ( self ) ( subpattern , self . min_count , self . max_count );
        pub fn pack_characters ( &self, info )  {
        self . subpattern = self . subpattern . pack_characters ( info );
        return  self;
        pub fn remove_captures ( self )  {
        self . subpattern = self . subpattern . remove_captures ( );
        return  self;
        pub fn is_atomic ( self )  {
        return  self . min_count == self . max_count && self . subpattern . is_atomic ( );
        pub fn can_be_affix ( self )  {
        return  false;
        pub fn contains_group ( self )  {
        return  self . subpattern . contains_group ( );
        pub fn get_firstset ( &self, reverse )  {
        fs = self . subpattern . get_firstset ( reverse );
        if self . min_count == 0 {
        fs . add ( None /* Option */ );
        return  fs;
        pub fn _compile ( &self, reverse , fuzzy )  {
        repeat = [ self . _opcode , self . min_count ];
        if self . max_count is None /* Option */ {
        repeat . append ( UNLIMITED );
        } else {
        repeat . append ( self . max_count );
        subpattern = self . subpattern . compile ( reverse , fuzzy );
        if !subpattern {
        return  [ ];
        return  ( [ tuple ( repeat ) ] + subpattern + [ ( OP . END , ) ] );
        pub fn dump ( &self, indent , reverse )  {
        if self . max_count is None /* Option */ {
        limit = "INF";
        } else {
        limit = self . max_count;
        println!( "{}{} {} {}" . format ( INDENT * indent , self . _op_name );
        self . min_count , limit ) );
        self . subpattern . dump ( indent + 1 , reverse );
        pub fn is_empty ( self )  {
        return  self . subpattern . is_empty ( );
        pub fn __eq__ ( &self, other )  {
        return  type ( self ) is type ( other ) && ( self . subpattern , self . min_count ,;
        self . max_count ) == ( other . subpattern , other . min_count ,;
        other . max_count );
        pub fn max_width ( self )  {
        if self . max_count is None /* Option */ {
        return  UNLIMITED;
        return  self . subpattern . max_width ( ) * self . max_count;
        pub fn get_required_string ( &self, reverse )  {
        max_count = UNLIMITED if self . max_count == None /* Option */ else self . max_count;
        if self . min_count == 0 {
        w = self . subpattern . max_width ( ) * max_count;
        return  min ( w , UNLIMITED ) , None /* Option */;
        ofs , req = self . subpattern . get_required_string ( reverse );
        if req {
        return  ofs , req;
        w = self . subpattern . max_width ( ) * max_count;
        return  min ( w , UNLIMITED ) , None /* Option */;
        class PossessiveRepeat ( GreedyRepeat ) ;
        pub fn is_atomic ( self )  {
        return  true;
        pub fn _compile ( &self, reverse , fuzzy )  {
        subpattern = self . subpattern . compile ( reverse , fuzzy );
        if !subpattern {
        return  [ ];
        repeat = [ self . _opcode , self . min_count ];
        if self . max_count is None /* Option */ {
        repeat . append ( UNLIMITED );
        } else {
        repeat . append ( self . max_count );
        return  ( [ ( OP . ATOMIC , ) , tuple ( repeat ) ] + subpattern + [ ( OP . END , ) ,;
        ( OP . END , ) ] );
        pub fn dump ( &self, indent , reverse )  {
        println!( "{}ATOMIC" . format ( INDENT * indent ) );
        if self . max_count is None /* Option */ {
        limit = "INF";
        } else {
        limit = self . max_count;
        println!( "{}{} {} {}" . format ( INDENT * ( indent + 1 ) , self . _op_name );
        self . min_count , limit ) );
        self . subpattern . dump ( indent + 2 , reverse );
        class Group ( RegexBase ) ;
        pub fn __init__ ( &self, info , group , subpattern )  {
        RegexBase . __init__ ( self );
        self . info = info;
        self . group = group;
        self . subpattern = subpattern;
        self . call_ref = None /* Option */;
        pub fn fix_groups ( &self, pattern , reverse , fuzzy )  {
        self . info . defined_groups [ self . group ] = ( self , reverse , fuzzy );
        self . subpattern . fix_groups ( pattern , reverse , fuzzy );
        pub fn optimise ( &self, info , reverse )  {
        subpattern = self . subpattern . optimise ( info , reverse );
        return  Group ( self . info , self . group , subpattern );
        pub fn pack_characters ( &self, info )  {
        self . subpattern = self . subpattern . pack_characters ( info );
        return  self;
        pub fn remove_captures ( self )  {
        return  self . subpattern . remove_captures ( );
        pub fn is_atomic ( self )  {
        return  self . subpattern . is_atomic ( );
        pub fn can_be_affix ( self )  {
        return  false;
        pub fn contains_group ( self )  {
        return  true;
        pub fn get_firstset ( &self, reverse )  {
        return  self . subpattern . get_firstset ( reverse );
        pub fn has_simple_start ( self )  {
        return  self . subpattern . has_simple_start ( );
        pub fn _compile ( &self, reverse , fuzzy )  {
        code = [ ];
        public_group = private_group = self . group;
        if private_group < 0 {
        public_group = self . info . private_groups [ private_group ];
        private_group = self . info . group_count - private_group;
        key = self . group , reverse , fuzzy;
        ref = self . info . call_refs . get ( key );
        if ref is !None /* Option */ {
        code + = [ ( OP . CALL_REF , ref ) ];
        code + = [ ( OP . GROUP , int ( !reverse ) , private_group , public_group ) ];
        code + = self . subpattern . compile ( reverse , fuzzy );
        code + = [ ( OP . END , ) ];
        if ref is !None /* Option */ {
        code + = [ ( OP . END , ) ];
        return  code;
        pub fn dump ( &self, indent , reverse )  {
        group = self . group;
        if group < 0 {
        group = self . info . private_groups [ group ];
        println!( "{}GROUP {}" . format ( INDENT * indent , group ) );
        self . subpattern . dump ( indent + 1 , reverse );
        pub fn __eq__ ( &self, other )  {
        return  ( type ( self ) is type ( other ) && ( self . group , self . subpattern ) ==;
        ( other . group , other . subpattern ) );
        pub fn max_width ( self )  {
        return  self . subpattern . max_width ( );
        pub fn get_required_string ( &self, reverse )  {
        return  self . subpattern . get_required_string ( reverse );
        pub fn __del__ ( self )  {
        self . info = None /* Option */;
        class Keep ( ZeroWidthBase ) ;
        _opcode = OP . KEEP;
        _op_name = "KEEP";
        class LazyRepeat ( GreedyRepeat ) ;
        _opcode = OP . LAZY_REPEAT;
        _op_name = "LAZY_REPEAT";
        class LookAround ( RegexBase ) ;
        _dir_text = { false : "AHEAD" , true : "BEHIND" };
        pub fn __init__ ( &self, behind , positive , subpattern )  {
        RegexBase . __init__ ( self );
        self . behind = bool ( behind );
        self . positive = bool ( positive );
        self . subpattern = subpattern;
        pub fn fix_groups ( &self, pattern , reverse , fuzzy )  {
        self . subpattern . fix_groups ( pattern , self . behind , fuzzy );
        pub fn optimise ( &self, info , reverse )  {
        subpattern = self . subpattern . optimise ( info , self . behind );
        if self . positive && subpattern . is_empty ( ) {
        return  subpattern;
        return  LookAround ( self . behind , self . positive , subpattern );
        pub fn pack_characters ( &self, info )  {
        self . subpattern = self . subpattern . pack_characters ( info );
        return  self;
        pub fn remove_captures ( self )  {
        return  self . subpattern . remove_captures ( );
        pub fn is_atomic ( self )  {
        return  self . subpattern . is_atomic ( );
        pub fn can_be_affix ( self )  {
        return  self . subpattern . can_be_affix ( );
        pub fn contains_group ( self )  {
        return  self . subpattern . contains_group ( );
        pub fn get_firstset ( &self, reverse )  {
        if self . positive && self . behind == reverse {
        return  self . subpattern . get_firstset ( reverse );
        return  set ( [ None /* Option */ ] );
        pub fn _compile ( &self, reverse , fuzzy )  {
        flags = 0;
        if self . positive {
        flags | = POSITIVE_OP;
        if fuzzy {
        flags | = FUZZY_OP;
        if reverse {
        flags | = REVERSE_OP;
        return  ( [ ( OP . LOOKAROUND , flags , int ( !self . behind ) ) ] +;
        self . subpattern . compile ( self . behind ) + [ ( OP . END , ) ] );
        pub fn dump ( &self, indent , reverse )  {
        println!( "{}LOOK{} {}" . format ( INDENT * indent );
        self . _dir_text [ self . behind ] , POS_TEXT [ self . positive ] ) );
        self . subpattern . dump ( indent + 1 , self . behind );
        pub fn is_empty ( self )  {
        return  self . positive && self . subpattern . is_empty ( );
        pub fn __eq__ ( &self, other )  {
        return  type ( self ) is type ( other ) && ( self . behind , self . positive ,;
        self . subpattern ) == ( other . behind , other . positive , other . subpattern );
        pub fn max_width ( self )  {
        return  0;
        class LookAroundConditional ( RegexBase ) ;
        _dir_text = { false : "AHEAD" , true : "BEHIND" };
        pub fn __init__ ( &self, behind , positive , subpattern , yes_item , no_item )  {
        RegexBase . __init__ ( self );
        self . behind = bool ( behind );
        self . positive = bool ( positive );
        self . subpattern = subpattern;
        self . yes_item = yes_item;
        self . no_item = no_item;
        pub fn fix_groups ( &self, pattern , reverse , fuzzy )  {
        self . subpattern . fix_groups ( pattern , reverse , fuzzy );
        self . yes_item . fix_groups ( pattern , reverse , fuzzy );
        self . no_item . fix_groups ( pattern , reverse , fuzzy );
        pub fn optimise ( &self, info , reverse )  {
        subpattern = self . subpattern . optimise ( info , self . behind );
        yes_item = self . yes_item . optimise ( info , self . behind );
        no_item = self . no_item . optimise ( info , self . behind );
        return  LookAroundConditional ( self . behind , self . positive , subpattern ,;
        yes_item , no_item );
        pub fn pack_characters ( &self, info )  {
        self . subpattern = self . subpattern . pack_characters ( info );
        self . yes_item = self . yes_item . pack_characters ( info );
        self . no_item = self . no_item . pack_characters ( info );
        return  self;
        pub fn remove_captures ( self )  {
        self . subpattern = self . subpattern . remove_captures ( );
        self . yes_item = self . yes_item . remove_captures ( );
        self . no_item = self . no_item . remove_captures ( );
        pub fn is_atomic ( self )  {
        return  ( self . subpattern . is_atomic ( ) && self . yes_item . is_atomic ( ) and;
        self . no_item . is_atomic ( ) );
        pub fn can_be_affix ( self )  {
        return  ( self . subpattern . can_be_affix ( ) && self . yes_item . can_be_affix ( );
        and self . no_item . can_be_affix ( ) );
        pub fn contains_group ( self )  {
        return  ( self . subpattern . contains_group ( ) or;
        self . yes_item . contains_group ( ) || self . no_item . contains_group ( ) );
        pub fn _compile ( &self, reverse , fuzzy )  {
        code = [ ( OP . CONDITIONAL , int ( self . positive ) , int ( !self . behind ) ) ];
        code . extend ( self . subpattern . compile ( self . behind , fuzzy ) );
        code . append ( ( OP . NEXT , ) );
        code . extend ( self . yes_item . compile ( reverse , fuzzy ) );
        add_code = self . no_item . compile ( reverse , fuzzy );
        if add_code {
        code . append ( ( OP . NEXT , ) );
        code . extend ( add_code );
        code . append ( ( OP . END , ) );
        return  code;
        pub fn dump ( &self, indent , reverse )  {
        println!( "{}CONDITIONAL {} {}" . format ( INDENT * indent );
        self . _dir_text [ self . behind ] , POS_TEXT [ self . positive ] ) );
        self . subpattern . dump ( indent + 1 , self . behind );
        println!( "{}EITHER" . format ( INDENT * indent ) );
        self . yes_item . dump ( indent + 1 , reverse );
        if !self . no_item . is_empty ( ) {
        println!( "{}OR" . format ( INDENT * indent ) );
        self . no_item . dump ( indent + 1 , reverse );
        pub fn is_empty ( self )  {
        return  ( self . subpattern . is_empty ( ) && self . yes_item . is_empty ( ) or;
        self . no_item . is_empty ( ) );
        pub fn __eq__ ( &self, other )  {
        return  type ( self ) is type ( other ) && ( self . subpattern , self . yes_item ,;
        self . no_item ) == ( other . subpattern , other . yes_item , other . no_item );
        pub fn max_width ( self )  {
        return  max ( self . yes_item . max_width ( ) , self . no_item . max_width ( ) );
        pub fn get_required_string ( &self, reverse )  {
        return  self . max_width ( ) , None /* Option */;
        class PrecompiledCode ( RegexBase ) ;
        pub fn __init__ ( &self, code )  {
        self . code = code;
        pub fn _compile ( &self, reverse , fuzzy )  {
        return  [ tuple ( self . code ) ];
        class Property ( RegexBase ) ;
        _opcode = { ( NOCASE , false ) : OP . PROPERTY , ( IGNORECASE , false ) ;
        OP . PROPERTY_IGN , ( FULLCASE , false ) : OP . PROPERTY , ( FULLIGNORECASE , false ) ;
        OP . PROPERTY_IGN , ( NOCASE , true ) : OP . PROPERTY_REV , ( IGNORECASE , true ) ;
        OP . PROPERTY_IGN_REV , ( FULLCASE , true ) : OP . PROPERTY_REV , ( FULLIGNORECASE ,;
        true ) : OP . PROPERTY_IGN_REV };
        pub fn __init__ ( &self, value , positive = true , case_flags = NOCASE , {
        zerowidth = false , encoding = 0 ) ;
        RegexBase . __init__ ( self );
        self . value = value;
        self . positive = bool ( positive );
        self . case_flags = CASE_FLAGS_COMBINATIONS [ case_flags ];
        self . zerowidth = bool ( zerowidth );
        self . encoding = encoding;
        self . _key = ( self . __class__ , self . value , self . positive ,;
        self . case_flags , self . zerowidth );
        pub fn rebuild ( &self, positive , case_flags , zerowidth )  {
        return  Property ( self . value , positive , case_flags , zerowidth ,;
        self . encoding );
        pub fn optimise ( &self, info , reverse , in_set = false )  {
        return  self;
        pub fn get_firstset ( &self, reverse )  {
        return  set ( [ self ] );
        pub fn has_simple_start ( self )  {
        return  true;
        pub fn _compile ( &self, reverse , fuzzy )  {
        flags = 0;
        if self . positive {
        flags | = POSITIVE_OP;
        if self . zerowidth {
        flags | = ZEROWIDTH_OP;
        if fuzzy {
        flags | = FUZZY_OP;
        flags | = self . encoding < < ENCODING_OP_SHIFT;
        return  [ ( self . _opcode [ self . case_flags , reverse ] , flags , self . value ) ];
        pub fn dump ( &self, indent , reverse )  {
        prop = PROPERTY_NAMES [ self . value > > 16 ];
        name , value = prop [ 0 ] , prop [ 1 ] [ self . value & 0x FFFF ];
        println!( "{}PROPERTY {} {}:{}{}{}" . format ( INDENT * indent );
        POS_TEXT [ self . positive ] , name , value , CASE_TEXT [ self . case_flags ] ,;
        [ "" , " ASCII" ] [ self . encoding ] ) );
        pub fn matches ( &self, ch )  {
        return  _regex . has_property_value ( self . value , ch ) == self . positive;
        pub fn max_width ( self )  {
        return  1;
        class Prune ( ZeroWidthBase ) ;
        _op_name = "PRUNE";
        pub fn _compile ( &self, reverse , fuzzy )  {
        return  [ ( OP . PRUNE , ) ];
        class Range ( RegexBase ) ;
        _opcode = { ( NOCASE , false ) : OP . RANGE , ( IGNORECASE , false ) : OP . RANGE_IGN ,;
        ( FULLCASE , false ) : OP . RANGE , ( FULLIGNORECASE , false ) : OP . RANGE_IGN ,;
        ( NOCASE , true ) : OP . RANGE_REV , ( IGNORECASE , true ) : OP . RANGE_IGN_REV ,;
        ( FULLCASE , true ) : OP . RANGE_REV , ( FULLIGNORECASE , true ) : OP . RANGE_IGN_REV };
        _op_name = "RANGE";
        pub fn __init__ ( &self, lower , upper , positive = true , case_flags = NOCASE , {
        zerowidth = false ) ;
        RegexBase . __init__ ( self );
        self . lower = lower;
        self . upper = upper;
        self . positive = bool ( positive );
        self . case_flags = CASE_FLAGS_COMBINATIONS [ case_flags ];
        self . zerowidth = bool ( zerowidth );
        self . _key = ( self . __class__ , self . lower , self . upper , self . positive ,;
        self . case_flags , self . zerowidth );
        pub fn rebuild ( &self, positive , case_flags , zerowidth )  {
        return  Range ( self . lower , self . upper , positive , case_flags , zerowidth );
        pub fn optimise ( &self, info , reverse , in_set = false )  {
        if !self . positive || !( self . case_flags & IGNORECASE ) || in_set {
        return  self;
        if ( !( info . flags & UNICODE ) || ( self . case_flags & FULLIGNORECASE ) != {
        FULLIGNORECASE ) ;
        return  self;
        expanding_chars = _regex . get_expand_on_folding ( );
        items = [ ];
        for ch in expanding_chars .iter() {
        if self . lower <= ord ( ch ) <= self . upper {
        folded = _regex . fold_case ( FULL_CASE_FOLDING , ch );
        items . append ( String ( vec![ ord ( c ).iter().map(|c| folded ] ,;
        case_flags = self . case_flags ) );
        if !items {
        return  self;
        if len ( items ) < self . upper - self . lower + 1 {
        items . insert ( 0 , self );
        return  Branch ( items );
        pub fn _compile ( &self, reverse , fuzzy )  {
        flags = 0;
        if self . positive {
        flags | = POSITIVE_OP;
        if self . zerowidth {
        flags | = ZEROWIDTH_OP;
        if fuzzy {
        flags | = FUZZY_OP;
        return  [ ( self . _opcode [ self . case_flags , reverse ] , flags , self . lower ,;
        self . upper ) ];
        pub fn dump ( &self, indent , reverse )  {
        display_lower = ascii ( chr ( self . lower ) ) . lstrip ( "bu" );
        display_upper = ascii ( chr ( self . upper ) ) . lstrip ( "bu" );
        println!( "{}RANGE {} {} {}{}" . format ( INDENT * indent );
        POS_TEXT [ self . positive ] , display_lower , display_upper ,;
        CASE_TEXT [ self . case_flags ] ) );
        pub fn matches ( &self, ch )  {
        return  ( self . lower <= ch <= self . upper ) == self . positive;
        pub fn max_width ( self )  {
        return  1;
        class RefGroup ( RegexBase ) ;
        _opcode = { ( NOCASE , false ) : OP . REF_GROUP , ( IGNORECASE , false ) ;
        OP . REF_GROUP_IGN , ( FULLCASE , false ) : OP . REF_GROUP , ( FULLIGNORECASE ,;
        false ) : OP . REF_GROUP_FLD , ( NOCASE , true ) : OP . REF_GROUP_REV , ( IGNORECASE ,;
        true ) : OP . REF_GROUP_IGN_REV , ( FULLCASE , true ) : OP . REF_GROUP_REV ,;
        ( FULLIGNORECASE , true ) : OP . REF_GROUP_FLD_REV };
        pub fn __init__ ( &self, info , group , position , case_flags = NOCASE )  {
        RegexBase . __init__ ( self );
        self . info = info;
        self . group = group;
        self . position = position;
        self . case_flags = CASE_FLAGS_COMBINATIONS [ case_flags ];
        self . _key = self . __class__ , self . group , self . case_flags;
        pub fn fix_groups ( &self, pattern , reverse , fuzzy )  {
        // try {
        self . group = int ( self . group );
        // } catch  ValueError  {
        // try {
        self . group = self . info . group_index [ self . group ];
        // } catch  KeyError  {
        panic!("error ( "unknown group" , pattern , self . position )");
        if !1 <= self . group <= self . info . group_count {
        panic!("error ( "invalid group reference" , pattern , self . position )");
        self . _key = self . __class__ , self . group , self . case_flags;
        pub fn remove_captures ( self )  {
        panic!("error ( "group reference !allowed" , self . pattern , self . position )");
        pub fn _compile ( &self, reverse , fuzzy )  {
        flags = 0;
        if fuzzy {
        flags | = FUZZY_OP;
        return  [ ( self . _opcode [ self . case_flags , reverse ] , flags , self . group ) ];
        pub fn dump ( &self, indent , reverse )  {
        println!( "{}REF_GROUP {}{}" . format ( INDENT * indent , self . group );
        CASE_TEXT [ self . case_flags ] ) );
        pub fn max_width ( self )  {
        return  UNLIMITED;
        pub fn __del__ ( self )  {
        self . info = None /* Option */;
        class SearchAnchor ( ZeroWidthBase ) ;
        _opcode = OP . SEARCH_ANCHOR;
        _op_name = "SEARCH_ANCHOR";
        class Sequence ( RegexBase ) ;
        pub fn __init__ ( &self, items = None /* Option */ )  {
        RegexBase . __init__ ( self );
        if items is None /* Option */ {
        items = [ ];
        self . items = items;
        pub fn fix_groups ( &self, pattern , reverse , fuzzy )  {
        for s in self . items .iter() {
        s . fix_groups ( pattern , reverse , fuzzy );
        pub fn optimise ( &self, info , reverse )  {
        items = [ ];
        for s in self . items .iter() {
        s = s . optimise ( info , reverse );
        if isinstance ( s , Sequence ) {
        items . extend ( s . items );
        } else {
        items . append ( s );
        return  make_sequence ( items );
        pub fn pack_characters ( &self, info )  {
        "Packs sequences of characters into strings.";
        items = [ ];
        characters = [ ];
        case_flags = NOCASE;
        for s in self . items .iter() {
        if type ( s ) is Character && s . positive && !s . zerowidth {
        if s . case_flags != case_flags {
        if s . case_flags || is_cased_i ( info , s . value ) {
        Sequence . _flush_characters ( info , characters ,;
        case_flags , items );
        case_flags = s . case_flags;
        characters . append ( s . value );
        } else if type ( s ) is String || type ( s ) is Literal {
        if s . case_flags != case_flags {
        if s . case_flags || any ( is_cased_i ( info , c ) for c in {
        characters ) ;
        Sequence . _flush_characters ( info , characters ,;
        case_flags , items );
        case_flags = s . case_flags;
        characters . extend ( s . characters );
        } else {
        Sequence . _flush_characters ( info , characters , case_flags , items );
        items . append ( s . pack_characters ( info ) );
        Sequence . _flush_characters ( info , characters , case_flags , items );
        return  make_sequence ( items );
        pub fn remove_captures ( self )  {
        self . items = [ s . remove_captures ( ) for s in self . items ];
        return  self;
        pub fn is_atomic ( self )  {
        return  all ( s . is_atomic ( ) for s in self . items );
        pub fn can_be_affix ( self )  {
        return  false;
        pub fn contains_group ( self )  {
        return  any ( s . contains_group ( ) for s in self . items );
        pub fn get_firstset ( &self, reverse )  {
        fs = set ( );
        items = self . items;
        if reverse {
        items . reverse ( );
        for s in items .iter() {
        fs | = s . get_firstset ( reverse );
        if None /* Option */ !in fs {
        return  fs;
        fs . discard ( None /* Option */ );
        return  fs | set ( [ None /* Option */ ] );
        pub fn has_simple_start ( self )  {
        return  bool ( self . items ) && self . items [ 0 ] . has_simple_start ( );
        pub fn _compile ( &self, reverse , fuzzy )  {
        seq = self . items;
        if reverse {
        seq = seq [ : : -1 ];
        code = [ ];
        for s in seq .iter() {
        code . extend ( s . compile ( reverse , fuzzy ) );
        return  code;
        pub fn dump ( &self, indent , reverse )  {
        for s in self . items .iter() {
        s . dump ( indent , reverse );
        @ staticmethod;
        pub fn _flush_characters ( info , characters , case_flags , items )  {
        if !characters {
        return;
        if case_flags & IGNORECASE {
        if !any ( is_cased_i ( info , c ) for c in characters ) {
        case_flags = NOCASE;
        if ( case_flags & FULLIGNORECASE ) == FULLIGNORECASE {
        literals = Sequence . _fix_full_casefold ( characters );
        for item in literals .iter() {
        chars = item . characters;
        if len ( chars ) == 1 {
        items . append ( Character ( chars [ 0 ] , case_flags = item . case_flags ) );
        } else {
        items . append ( String ( chars , case_flags = item . case_flags ) );
        } else {
        if len ( characters ) == 1 {
        items . append ( Character ( characters [ 0 ] , case_flags = case_flags ) );
        } else {
        items . append ( String ( characters , case_flags = case_flags ) );
        characters [ : ] = [ ];
        @ staticmethod;
        pub fn _fix_full_casefold ( characters )  {
        expanded = [ _regex . fold_case ( FULL_CASE_FOLDING , c ) for c in;
        _regex . get_expand_on_folding ( ) ];
        string = _regex . fold_case ( FULL_CASE_FOLDING , "" . join ( chr ( c );
        for c in characters ) ) . lower ( ).iter() {
        chunks = [ ];
        for e in expanded .iter() {
        found = string . find ( e );
        while found >= 0  {
        chunks . append ( ( found , found + len ( e ) ) );
        found = string . find ( e , found + 1 );
        pos = 0;
        literals = [ ];
        for start , end in Sequence . _merge_chunks ( chunks ) .iter() {
        if pos < start {
        literals . append ( Literal ( characters [ pos : start ] ,;
        case_flags = IGNORECASE ) );
        literals . append ( Literal ( characters [ start : end ] ,;
        case_flags = FULLIGNORECASE ) );
        pos = end;
        if pos < len ( characters ) {
        literals . append ( Literal ( characters [ pos : ] , case_flags = IGNORECASE ) );
        return  literals;
        @ staticmethod;
        pub fn _merge_chunks ( chunks )  {
        if len ( chunks ) < 2 {
        return  chunks;
        chunks . sort ( );
        start , end = chunks [ 0 ];
        new_chunks = [ ];
        for s , e in chunks [ 1 : ] .iter() {
        if s <= end {
        end = max ( end , e );
        } else {
        new_chunks . append ( ( start , end ) );
        start , end = s , e;
        new_chunks . append ( ( start , end ) );
        return  new_chunks;
        pub fn is_empty ( self )  {
        return  all ( i . is_empty ( ) for i in self . items );
        pub fn __eq__ ( &self, other )  {
        return  type ( self ) is type ( other ) && self . items == other . items;
        pub fn max_width ( self )  {
        return  sum ( s . max_width ( ) for s in self . items );
        pub fn get_required_string ( &self, reverse )  {
        seq = self . items;
        if reverse {
        seq = seq [ : : -1 ];
        offset = 0;
        for s in seq .iter() {
        ofs , req = s . get_required_string ( reverse );
        offset + = ofs;
        if req {
        return  offset , req;
        return  offset , None /* Option */;
        class SetBase ( RegexBase ) ;
        pub fn __init__ ( &self, info , items , positive = true , case_flags = NOCASE , {
        zerowidth = false ) ;
        RegexBase . __init__ ( self );
        self . info = info;
        self . items = tuple ( items );
        self . positive = bool ( positive );
        self . case_flags = CASE_FLAGS_COMBINATIONS [ case_flags ];
        self . zerowidth = bool ( zerowidth );
        self . char_width = 1;
        self . _key = ( self . __class__ , self . items , self . positive ,;
        self . case_flags , self . zerowidth );
        pub fn rebuild ( &self, positive , case_flags , zerowidth )  {
        return  type ( self ) ( self . info , self . items , positive , case_flags ,;
        zerowidth ) . optimise ( self . info , false );
        pub fn get_firstset ( &self, reverse )  {
        return  set ( [ self ] );
        pub fn has_simple_start ( self )  {
        return  true;
        pub fn _compile ( &self, reverse , fuzzy )  {
        flags = 0;
        if self . positive {
        flags | = POSITIVE_OP;
        if self . zerowidth {
        flags | = ZEROWIDTH_OP;
        if fuzzy {
        flags | = FUZZY_OP;
        code = [ ( self . _opcode [ self . case_flags , reverse ] , flags ) ];
        for m in self . items .iter() {
        code . extend ( m . compile ( ) );
        code . append ( ( OP . END , ) );
        return  code;
        pub fn dump ( &self, indent , reverse )  {
        println!( "{}{} {}{}" . format ( INDENT * indent , self . _op_name );
        POS_TEXT [ self . positive ] , CASE_TEXT [ self . case_flags ] ) );
        for i in self . items .iter() {
        i . dump ( indent + 1 , reverse );
        pub fn _handle_case_folding ( &self, info , in_set )  {
        if !self . positive || !( self . case_flags & IGNORECASE ) || in_set {
        return  self;
        if ( !( self . info . flags & UNICODE ) || ( self . case_flags & {
        FULLIGNORECASE ) != FULLIGNORECASE ) ;
        return  self;
        expanding_chars = _regex . get_expand_on_folding ( );
        items = [ ];
        seen = set ( );
        for ch in expanding_chars .iter() {
        if self . matches ( ord ( ch ) ) {
        folded = _regex . fold_case ( FULL_CASE_FOLDING , ch );
        if folded !in seen {
        items . append ( String ( vec![ ord ( c ).iter().map(|c| folded ] ,;
        case_flags = self . case_flags ) );
        seen . add ( folded );
        if !items {
        return  self;
        return  Branch ( [ self ] + items );
        pub fn max_width ( self )  {
        if !self . positive || !( self . case_flags & IGNORECASE ) {
        return  1;
        if ( !( self . info . flags & UNICODE ) || ( self . case_flags & {
        FULLIGNORECASE ) != FULLIGNORECASE ) ;
        return  1;
        expanding_chars = _regex . get_expand_on_folding ( );
        seen = set ( );
        for ch in expanding_chars .iter() {
        if self . matches ( ord ( ch ) ) {
        folded = _regex . fold_case ( FULL_CASE_FOLDING , ch );
        seen . add ( folded );
        if !seen {
        return  1;
        return  max ( len ( folded ) for folded in seen );
        pub fn __del__ ( self )  {
        self . info = None /* Option */;
        class SetDiff ( SetBase ) ;
        _opcode = { ( NOCASE , false ) : OP . SET_DIFF , ( IGNORECASE , false ) ;
        OP . SET_DIFF_IGN , ( FULLCASE , false ) : OP . SET_DIFF , ( FULLIGNORECASE , false ) ;
        OP . SET_DIFF_IGN , ( NOCASE , true ) : OP . SET_DIFF_REV , ( IGNORECASE , true ) ;
        OP . SET_DIFF_IGN_REV , ( FULLCASE , true ) : OP . SET_DIFF_REV , ( FULLIGNORECASE ,;
        true ) : OP . SET_DIFF_IGN_REV };
        _op_name = "SET_DIFF";
        pub fn optimise ( &self, info , reverse , in_set = false )  {
        items = self . items;
        if len ( items ) > 2 {
        items = [ items [ 0 ] , SetUnion ( info , items [ 1 : ] ) ];
        if len ( items ) == 1 {
        return  items [ 0 ] . with_flags ( case_flags = self . case_flags ,;
        zerowidth = self . zerowidth ) . optimise ( info , reverse , in_set );
        self . items = tuple ( m . optimise ( info , reverse , in_set = true ) for m in;
        items );
        return  self . _handle_case_folding ( info , in_set );
        pub fn matches ( &self, ch )  {
        m = self . items [ 0 ] . matches ( ch ) && !self . items [ 1 ] . matches ( ch );
        return  m == self . positive;
        class SetInter ( SetBase ) ;
        _opcode = { ( NOCASE , false ) : OP . SET_INTER , ( IGNORECASE , false ) ;
        OP . SET_INTER_IGN , ( FULLCASE , false ) : OP . SET_INTER , ( FULLIGNORECASE ,;
        false ) : OP . SET_INTER_IGN , ( NOCASE , true ) : OP . SET_INTER_REV , ( IGNORECASE ,;
        true ) : OP . SET_INTER_IGN_REV , ( FULLCASE , true ) : OP . SET_INTER_REV ,;
        ( FULLIGNORECASE , true ) : OP . SET_INTER_IGN_REV };
        _op_name = "SET_INTER";
        pub fn optimise ( &self, info , reverse , in_set = false )  {
        items = [ ];
        for m in self . items .iter() {
        m = m . optimise ( info , reverse , in_set = true );
        if isinstance ( m , SetInter ) && m . positive {
        items . extend ( m . items );
        } else {
        items . append ( m );
        if len ( items ) == 1 {
        return  items [ 0 ] . with_flags ( case_flags = self . case_flags ,;
        zerowidth = self . zerowidth ) . optimise ( info , reverse , in_set );
        self . items = tuple ( items );
        return  self . _handle_case_folding ( info , in_set );
        pub fn matches ( &self, ch )  {
        m = all ( i . matches ( ch ) for i in self . items );
        return  m == self . positive;
        class SetSymDiff ( SetBase ) ;
        _opcode = { ( NOCASE , false ) : OP . SET_SYM_DIFF , ( IGNORECASE , false ) ;
        OP . SET_SYM_DIFF_IGN , ( FULLCASE , false ) : OP . SET_SYM_DIFF , ( FULLIGNORECASE ,;
        false ) : OP . SET_SYM_DIFF_IGN , ( NOCASE , true ) : OP . SET_SYM_DIFF_REV ,;
        ( IGNORECASE , true ) : OP . SET_SYM_DIFF_IGN_REV , ( FULLCASE , true ) ;
        OP . SET_SYM_DIFF_REV , ( FULLIGNORECASE , true ) : OP . SET_SYM_DIFF_IGN_REV };
        _op_name = "SET_SYM_DIFF";
        pub fn optimise ( &self, info , reverse , in_set = false )  {
        items = [ ];
        for m in self . items .iter() {
        m = m . optimise ( info , reverse , in_set = true );
        if isinstance ( m , SetSymDiff ) && m . positive {
        items . extend ( m . items );
        } else {
        items . append ( m );
        if len ( items ) == 1 {
        return  items [ 0 ] . with_flags ( case_flags = self . case_flags ,;
        zerowidth = self . zerowidth ) . optimise ( info , reverse , in_set );
        self . items = tuple ( items );
        return  self . _handle_case_folding ( info , in_set );
        pub fn matches ( &self, ch )  {
        m = false;
        for i in self . items .iter() {
        m = m != i . matches ( ch );
        return  m == self . positive;
        class SetUnion ( SetBase ) ;
        _opcode = { ( NOCASE , false ) : OP . SET_UNION , ( IGNORECASE , false ) ;
        OP . SET_UNION_IGN , ( FULLCASE , false ) : OP . SET_UNION , ( FULLIGNORECASE ,;
        false ) : OP . SET_UNION_IGN , ( NOCASE , true ) : OP . SET_UNION_REV , ( IGNORECASE ,;
        true ) : OP . SET_UNION_IGN_REV , ( FULLCASE , true ) : OP . SET_UNION_REV ,;
        ( FULLIGNORECASE , true ) : OP . SET_UNION_IGN_REV };
        _op_name = "SET_UNION";
        pub fn optimise ( &self, info , reverse , in_set = false )  {
        items = [ ];
        for m in self . items .iter() {
        m = m . optimise ( info , reverse , in_set = true );
        if isinstance ( m , SetUnion ) && m . positive {
        items . extend ( m . items );
        } else if isinstance ( m , AnyAll ) {
        return  AnyAll ( );
        } else {
        items . append ( m );
        properties = ( set ( ) , set ( ) );
        for m in items .iter() {
        if isinstance ( m , Property ) {
        properties [ m . positive ] . add ( ( m . value , m . case_flags , m . zerowidth ) );
        if properties [ 0 ] & properties [ 1 ] {
        return  AnyAll ( );
        if len ( items ) == 1 {
        i = items [ 0 ];
        return  i . with_flags ( positive = i . positive == self . positive ,;
        case_flags = self . case_flags ,;
        zerowidth = self . zerowidth ) . optimise ( info , reverse , in_set );
        self . items = tuple ( items );
        return  self . _handle_case_folding ( info , in_set );
        pub fn _compile ( &self, reverse , fuzzy )  {
        flags = 0;
        if self . positive {
        flags | = POSITIVE_OP;
        if self . zerowidth {
        flags | = ZEROWIDTH_OP;
        if fuzzy {
        flags | = FUZZY_OP;
        characters , others = defaultdict ( list ) , [ ];
        for m in self . items .iter() {
        if isinstance ( m , Character ) {
        characters [ m . positive ] . append ( m . value );
        } else {
        others . append ( m );
        code = [ ( self . _opcode [ self . case_flags , reverse ] , flags ) ];
        for positive , values in characters . items ( ) .iter() {
        flags = 0;
        if positive {
        flags | = POSITIVE_OP;
        if len ( values ) == 1 {
        code . append ( ( OP . CHARACTER , flags , values [ 0 ] ) );
        } else {
        code . append ( ( OP . STRING , flags , len ( values ) ) + tuple ( values ) );
        for m in others .iter() {
        code . extend ( m . compile ( ) );
        code . append ( ( OP . END , ) );
        return  code;
        pub fn matches ( &self, ch )  {
        m = any ( i . matches ( ch ) for i in self . items );
        return  m == self . positive;
        class Skip ( ZeroWidthBase ) ;
        _op_name = "SKIP";
        _opcode = OP . SKIP;
        class StartOfLine ( ZeroWidthBase ) ;
        _opcode = OP . START_OF_LINE;
        _op_name = "START_OF_LINE";
        class StartOfLineU ( StartOfLine ) ;
        _opcode = OP . START_OF_LINE_U;
        _op_name = "START_OF_LINE_U";
        class StartOfString ( ZeroWidthBase ) ;
        _opcode = OP . START_OF_STRING;
        _op_name = "START_OF_STRING";
        class StartOfWord ( ZeroWidthBase ) ;
        _opcode = OP . START_OF_WORD;
        _op_name = "START_OF_WORD";
        class String ( RegexBase ) ;
        _opcode = { ( NOCASE , false ) : OP . STRING , ( IGNORECASE , false ) : OP . STRING_IGN ,;
        ( FULLCASE , false ) : OP . STRING , ( FULLIGNORECASE , false ) : OP . STRING_FLD ,;
        ( NOCASE , true ) : OP . STRING_REV , ( IGNORECASE , true ) : OP . STRING_IGN_REV ,;
        ( FULLCASE , true ) : OP . STRING_REV , ( FULLIGNORECASE , true ) ;
        OP . STRING_FLD_REV };
        pub fn __init__ ( &self, characters , case_flags = NOCASE )  {
        self . characters = tuple ( characters );
        self . case_flags = CASE_FLAGS_COMBINATIONS [ case_flags ];
        if ( self . case_flags & FULLIGNORECASE ) == FULLIGNORECASE {
        folded_characters = [ ];
        for char in self . characters .iter() {
        folded = _regex . fold_case ( FULL_CASE_FOLDING , chr ( char ) );
        folded_characters . extend ( ord ( c ) for c in folded );
        } else {
        folded_characters = self . characters;
        self . folded_characters = tuple ( folded_characters );
        self . required = false;
        self . _key = self . __class__ , self . characters , self . case_flags;
        pub fn get_firstset ( &self, reverse )  {
        if reverse {
        pos = -1;
        } else {
        pos = 0;
        return  set ( [ Character ( self . characters [ pos ] ,;
        case_flags = self . case_flags ) ] );
        pub fn has_simple_start ( self )  {
        return  true;
        pub fn _compile ( &self, reverse , fuzzy )  {
        flags = 0;
        if fuzzy {
        flags | = FUZZY_OP;
        if self . required {
        flags | = REQUIRED_OP;
        return  [ ( self . _opcode [ self . case_flags , reverse ] , flags ,;
        len ( self . folded_characters ) ) + self . folded_characters ];
        pub fn dump ( &self, indent , reverse )  {
        display = ascii ( "" . join ( chr ( c ) for c in self . characters ) ) . lstrip ( "bu" );
        println!( "{}STRING {}{}" . format ( INDENT * indent , display );
        CASE_TEXT [ self . case_flags ] ) );
        pub fn max_width ( self )  {
        return  len ( self . folded_characters );
        pub fn get_required_string ( &self, reverse )  {
        return  0 , self;
        class Literal ( String ) ;
        pub fn dump ( &self, indent , reverse )  {
        literal = "" . join ( chr ( c ) for c in self . characters );
        display = ascii ( literal ) . lstrip ( "bu" );
        println!( "{}LITERAL MATCH {}{}" . format ( INDENT * indent , display );
        CASE_TEXT [ self . case_flags ] ) );
        class StringSet ( Branch ) ;
        pub fn __init__ ( &self, info , name , case_flags = NOCASE )  {
        self . info = info;
        self . name = name;
        self . case_flags = CASE_FLAGS_COMBINATIONS [ case_flags ];
        self . _key = self . __class__ , self . name , self . case_flags;
        self . set_key = ( name , self . case_flags );
        if self . set_key !in info . named_lists_used {
        info . named_lists_used [ self . set_key ] = len ( info . named_lists_used );
        index = self . info . named_lists_used [ self . set_key ];
        items = self . info . kwargs [ self . name ];
        case_flags = self . case_flags;
        encoding = self . info . flags & _ALL_ENCODINGS;
        fold_flags = encoding | case_flags;
        choices = [ ];
        for string in items .iter() {
        if isinstance ( string , str ) {
        string = vec![ ord ( c ).iter().map(|c| string ).collect();
        choices . append ( [ Character ( c , case_flags = case_flags ) for c in;
        string ] );
        choices . sort ( key = len , reverse = true );
        self . branches = [ Sequence ( choice ) for choice in choices ];
        pub fn dump ( &self, indent , reverse )  {
        println!( "{}STRING_SET {}{}" . format ( INDENT * indent , self . name );
        CASE_TEXT [ self . case_flags ] ) );
        pub fn __del__ ( self )  {
        self . info = None /* Option */;
        class Source ;
        "Scanner for the regular expression source string.";
        pub fn __init__ ( &self, string )  {
        if isinstance ( string , str ) {
        self . string = string;
        self . char_type = chr;
        } else {
        self . string = string . decode ( "latin-1" );
        self . char_type = lambda c : bytes ( [ c ] );
        self . pos = 0;
        self . ignore_space = false;
        self . sep = string [ : 0 ];
        pub fn peek ( &self, override_ignore = false )  {
        string = self . string;
        pos = self . pos;
        // try {
        if self . ignore_space && !override_ignore {
        while true  {
        if string [ pos ] . isspace ( ) {
        pos + = 1;
        } else if string [ pos ] == "#" {
        pos = string . index ( "\n" , pos );
        } else {
        break;
        return  string [ pos ];
        // } catch  IndexError  {
        return  string [ : 0 ];
        // } catch  ValueError  {
        return  string [ : 0 ];
        pub fn get ( &self, override_ignore = false )  {
        string = self . string;
        pos = self . pos;
        // try {
        if self . ignore_space && !override_ignore {
        while true  {
        if string [ pos ] . isspace ( ) {
        pos + = 1;
        } else if string [ pos ] == "#" {
        pos = string . index ( "\n" , pos );
        } else {
        break;
        ch = string [ pos ];
        self . pos = pos + 1;
        return  ch;
        // } catch  IndexError  {
        self . pos = pos;
        return  string [ : 0 ];
        // } catch  ValueError  {
        self . pos = len ( string );
        return  string [ : 0 ];
        pub fn get_many ( &self, count = 1 )  {
        string = self . string;
        pos = self . pos;
        // try {
        if self . ignore_space {
        substring = [ ];
        while len ( substring ) < count  {
        while true  {
        if string [ pos ] . isspace ( ) {
        pos + = 1;
        } else if string [ pos ] == "#" {
        pos = string . index ( "\n" , pos );
        } else {
        break;
        substring . append ( string [ pos ] );
        pos + = 1;
        substring = "" . join ( substring );
        } else {
        substring = string [ pos : pos + count ];
        pos + = len ( substring );
        self . pos = pos;
        return  substring;
        // } catch  IndexError  {
        self . pos = len ( string );
        return  "" . join ( substring );
        // } catch  ValueError  {
        self . pos = len ( string );
        return  "" . join ( substring );
        pub fn get_while ( &self, test_set , include = true , keep_spaces = false )  {
        string = self . string;
        pos = self . pos;
        if self . ignore_space && !keep_spaces {
        // try {
        substring = [ ];
        while true  {
        if string [ pos ] . isspace ( ) {
        pos + = 1;
        } else if string [ pos ] == "#" {
        pos = string . index ( "\n" , pos );
        } else if ( string [ pos ] in test_set ) == include {
        substring . append ( string [ pos ] );
        pos + = 1;
        } else {
        break;
        self . pos = pos;
        // } catch  IndexError  {
        self . pos = len ( string );
        // } catch  ValueError  {
        self . pos = len ( string );
        return  "" . join ( substring );
        } else {
        // try {
        while ( string [ pos ] in test_set ) == include  {
        pos + = 1;
        substring = string [ self . pos : pos ];
        self . pos = pos;
        return  substring;
        // } catch  IndexError  {
        substring = string [ self . pos : pos ];
        self . pos = pos;
        return  substring;
        pub fn skip_while ( &self, test_set , include = true )  {
        string = self . string;
        pos = self . pos;
        // try {
        if self . ignore_space {
        while true  {
        if string [ pos ] . isspace ( ) {
        pos + = 1;
        } else if string [ pos ] == "#" {
        pos = string . index ( "\n" , pos );
        } else if ( string [ pos ] in test_set ) == include {
        pos + = 1;
        } else {
        break;
        } else {
        while ( string [ pos ] in test_set ) == include  {
        pos + = 1;
        self . pos = pos;
        // } catch  IndexError  {
        self . pos = len ( string );
        // } catch  ValueError  {
        self . pos = len ( string );
        pub fn match ( &self, substring )  {
        string = self . string;
        pos = self . pos;
        if self . ignore_space {
        // try {
        for c in substring .iter() {
        while true  {
        if string [ pos ] . isspace ( ) {
        pos + = 1;
        } else if string [ pos ] == "#" {
        pos = string . index ( "\n" , pos );
        } else {
        break;
        if string [ pos ] != c {
        return  false;
        pos + = 1;
        self . pos = pos;
        return  true;
        // } catch  IndexError  {
        return  false;
        // } catch  ValueError  {
        return  false;
        } else {
        if !string . startswith ( substring , pos ) {
        return  false;
        self . pos = pos + len ( substring );
        return  true;
        pub fn expect ( &self, substring )  {
        if !self . match ( substring ) {
        panic!("error ( "missing {}" . format ( substring ) , self . string , self . pos )");
        pub fn at_end ( self )  {
        string = self . string;
        pos = self . pos;
        // try {
        if self . ignore_space {
        while true  {
        if string [ pos ] . isspace ( ) {
        pos + = 1;
        } else if string [ pos ] == "#" {
        pos = string . index ( "\n" , pos );
        } else {
        break;
        return  pos >= len ( string );
        // } catch  IndexError  {
        return  true;
        // } catch  ValueError  {
        return  true;
        class Info ;
        "Info about the regular expression.";
        pub fn __init__ ( &self, flags = 0 , char_type = None /* Option */ , kwargs = { } )  {
        flags | = DEFAULT_FLAGS [ ( flags & _ALL_VERSIONS ) || DEFAULT_VERSION ];
        self . flags = flags;
        self . global_flags = flags;
        self . inline_locale = false;
        self . kwargs = kwargs;
        self . group_count = 0;
        self . group_index = { };
        self . group_name = { };
        self . char_type = char_type;
        self . named_lists_used = { };
        self . open_groups = [ ];
        self . open_group_count = { };
        self . defined_groups = { };
        self . group_calls = [ ];
        self . private_groups = { };
        pub fn open_group ( &self, name = None /* Option */ )  {
        group = self . group_index . get ( name );
        if group is None /* Option */ {
        while true  {
        self . group_count + = 1;
        if name is None /* Option */ || self . group_count !in self . group_name {
        break;
        group = self . group_count;
        if name {
        self . group_index [ name ] = group;
        self . group_name [ group ] = name;
        if group in self . open_groups {
        group_alias = - ( len ( self . private_groups ) + 1 );
        self . private_groups [ group_alias ] = group;
        group = group_alias;
        self . open_groups . append ( group );
        self . open_group_count [ group ] = self . open_group_count . get ( group , 0 ) + 1;
        return  group;
        pub fn close_group ( self )  {
        self . open_groups . pop ( );
        pub fn is_open_group ( &self, name )  {
        version = ( self . flags & _ALL_VERSIONS ) || DEFAULT_VERSION;
        if version == VERSION1 {
        return  false;
        if name . isdigit ( ) {
        group = int ( name );
        } else {
        group = self . group_index . get ( name );
        return  group in self . open_groups;
        pub fn _check_group_features ( info , parsed )  {
        "Checks whether the reverse && fuzzy features of the group calls match
    the groups which they call.
    ";
        call_refs = { };
        additional_groups = [ ];
        for call , reverse , fuzzy in info . group_calls .iter() {
        key = ( call . group , reverse , fuzzy );
        ref = call_refs . get ( key );
        if ref is None /* Option */ {
        if call . group == 0 {
        rev = bool ( info . flags & REVERSE );
        fuz = isinstance ( parsed , Fuzzy );
        if ( rev , fuz ) != ( reverse , fuzzy ) {
        additional_groups . append ( ( CallRef ( len ( call_refs ) , parsed ) ,;
        reverse , fuzzy ) );
        } else {
        def_info = info . defined_groups [ call . group ];
        group = def_info [ 0 ];
        if def_info [ 1 { : ] != ( reverse , fuzzy ) ; }
        additional_groups . append ( ( group , reverse , fuzzy ) );
        ref = len ( call_refs );
        call_refs [ key ] = ref;
        call . call_ref = ref;
        info . call_refs = call_refs;
        info . additional_groups = additional_groups;
        pub fn _get_required_string ( parsed , flags )  {
        "Gets the required string && related info of a parsed pattern.";
        req_offset , required = parsed . get_required_string ( bool ( flags & REVERSE ) );
        if required {
        required . required = true;
        if req_offset >= UNLIMITED {
        req_offset = -1;
        req_flags = required . case_flags;
        if !( flags & UNICODE ) {
        req_flags & = ~ UNICODE;
        req_chars = required . folded_characters;
        } else {
        req_offset = 0;
        req_chars = ( );
        req_flags = 0;
        return  req_offset , req_chars , req_flags;
        class Scanner ;
        pub fn __init__ ( &self, lexicon , flags = 0 )  {
        self . lexicon = lexicon;
        patterns = [ ];
        for phrase , action in lexicon .iter() {
        source = Source ( phrase );
        info = Info ( flags , source . char_type );
        source . ignore_space = bool ( info . flags & VERBOSE );
        parsed = _parse_pattern ( source , info );
        if !source . at_end ( ) {
        panic!("error ( "unbalanced parenthesis" , source . string ,");
        source . pos );
        patterns . append ( parsed . remove_captures ( ) );
        info = Info ( flags );
        patterns = vec![ Group ( info , g + 1 , p ).iter().map(|g , p| enumerate ( patterns ) ).collect();
        parsed = Branch ( patterns );
        reverse = bool ( info . flags & REVERSE );
        parsed = parsed . optimise ( info , reverse );
        parsed = parsed . pack_characters ( info );
        req_offset , req_chars , req_flags = _get_required_string ( parsed ,;
        info . flags );
        _check_group_features ( info , parsed );
        if info . call_refs {
        panic!("error ( "recursive regex !supported by Scanner" ,");
        source . string , source . pos );
        reverse = bool ( info . flags & REVERSE );
        code = parsed . compile ( reverse ) + [ ( OP . SUCCESS , ) ];
        code = _flatten_code ( code );
        if !parsed . has_simple_start ( ) {
        // try {
        fs_code = _compile_firstset ( info , parsed . get_firstset ( reverse ) );
        fs_code = _flatten_code ( fs_code );
        code = fs_code + code;
        // } catch  _FirstSetError  {
        // pass
        version = ( info . flags & _ALL_VERSIONS ) || DEFAULT_VERSION;
        if version !in ( 0 , VERSION0 , VERSION1 ) {
        panic!("ValueError ( "VERSION0 && VERSION1 flags are mutually incompatible" )");
        self . scanner = _regex . compile ( None /* Option */ , ( flags & GLOBAL_FLAGS ) | version ,;
        code , { } , { } , { } , [ ] , req_offset , req_chars , req_flags ,;
        len ( patterns ) );
        pub fn scan ( &self, string )  {
        result = [ ];
        append = result . append;
        match = self . scanner . scanner ( string ) . match;
        i = 0;
        while true  {
        m = match ( );
        if !m {
        break;
        j = m . end ( );
        if i == j {
        break;
        action = self . lexicon [ m . lastindex - 1 ] [ 1 ];
        if hasattr ( action , "__call__" ) {
        self . match = m;
        action = action ( self , m . group ( ) );
        if action is !None /* Option */ {
        append ( action );
        i = j;
        return  result , string [ i : ];
        PROPERTIES = _regex . get_properties ( );
        PROPERTY_NAMES = { };
        for prop_name , ( prop_id , values ) in PROPERTIES . items ( ) .iter() {
        name , prop_values = PROPERTY_NAMES . get ( prop_id , ( "" , { } ) );
        name = max ( name , prop_name , key = len );
        PROPERTY_NAMES [ prop_id ] = name , prop_values;
        for val_name , val_id in values . items ( ) .iter() {
        prop_values [ val_id ] = max ( prop_values . get ( val_id , "" ) , val_name ,;
        key = len );
        CHARACTER_ESCAPES = {;
        "a" : "\a" ,;
        "b" : "\b" ,;
        "format!(" : "\format!(" ,);
        "n" : "\n" ,;
        "r" : "\r" ,;
        "t" : "\t" ,;
        "v" : "\v" ,;
        };
        ASCII_ENCODING = 1;
        UNICODE_ENCODING = 2;
        CHARSET_ESCAPES = {;
        "d" : lookup_property ( None /* Option */ , "Digit" , true ) ,;
        "D" : lookup_property ( None /* Option */ , "Digit" , false ) ,;
        "h" : lookup_property ( None /* Option */ , "Blank" , true ) ,;
        "s" : lookup_property ( None /* Option */ , "Space" , true ) ,;
        "S" : lookup_property ( None /* Option */ , "Space" , false ) ,;
        "w" : lookup_property ( None /* Option */ , "Word" , true ) ,;
        "W" : lookup_property ( None /* Option */ , "Word" , false ) ,;
        };
        ASCII_CHARSET_ESCAPES = dict ( CHARSET_ESCAPES );
        ASCII_CHARSET_ESCAPES . update ( {;
        "d" : lookup_property ( None /* Option */ , "Digit" , true , encoding = ASCII_ENCODING ) ,;
        "D" : lookup_property ( None /* Option */ , "Digit" , false , encoding = ASCII_ENCODING ) ,;
        "s" : lookup_property ( None /* Option */ , "Space" , true , encoding = ASCII_ENCODING ) ,;
        "S" : lookup_property ( None /* Option */ , "Space" , false , encoding = ASCII_ENCODING ) ,;
        "w" : lookup_property ( None /* Option */ , "Word" , true , encoding = ASCII_ENCODING ) ,;
        "W" : lookup_property ( None /* Option */ , "Word" , false , encoding = ASCII_ENCODING ) ,;
        } );
        UNICODE_CHARSET_ESCAPES = dict ( CHARSET_ESCAPES );
        UNICODE_CHARSET_ESCAPES . update ( {;
        "d" : lookup_property ( None /* Option */ , "Digit" , true , encoding = UNICODE_ENCODING ) ,;
        "D" : lookup_property ( None /* Option */ , "Digit" , false , encoding = UNICODE_ENCODING ) ,;
        "s" : lookup_property ( None /* Option */ , "Space" , true , encoding = UNICODE_ENCODING ) ,;
        "S" : lookup_property ( None /* Option */ , "Space" , false , encoding = UNICODE_ENCODING ) ,;
        "w" : lookup_property ( None /* Option */ , "Word" , true , encoding = UNICODE_ENCODING ) ,;
        "W" : lookup_property ( None /* Option */ , "Word" , false , encoding = UNICODE_ENCODING ) ,;
        } );
        POSITION_ESCAPES = {;
        "A" : StartOfString ( ) ,;
        "b" : Boundary ( ) ,;
        "B" : Boundary ( false ) ,;
        "K" : Keep ( ) ,;
        "m" : StartOfWord ( ) ,;
        "M" : EndOfWord ( ) ,;
        "Z" : EndOfString ( ) ,;
        };
        ASCII_POSITION_ESCAPES = dict ( POSITION_ESCAPES );
        ASCII_POSITION_ESCAPES . update ( {;
        "b" : Boundary ( encoding = ASCII_ENCODING ) ,;
        "B" : Boundary ( false , encoding = ASCII_ENCODING ) ,;
        "m" : StartOfWord ( encoding = ASCII_ENCODING ) ,;
        "M" : EndOfWord ( encoding = ASCII_ENCODING ) ,;
        } );
        UNICODE_POSITION_ESCAPES = dict ( POSITION_ESCAPES );
        UNICODE_POSITION_ESCAPES . update ( {;
        "b" : Boundary ( encoding = UNICODE_ENCODING ) ,;
        "B" : Boundary ( false , encoding = UNICODE_ENCODING ) ,;
        "m" : StartOfWord ( encoding = UNICODE_ENCODING ) ,;
        "M" : EndOfWord ( encoding = UNICODE_ENCODING ) ,;
        } );
        WORD_POSITION_ESCAPES = dict ( POSITION_ESCAPES );
        WORD_POSITION_ESCAPES . update ( {;
        "b" : DefaultBoundary ( ) ,;
        "B" : DefaultBoundary ( false ) ,;
        "m" : DefaultStartOfWord ( ) ,;
        "M" : DefaultEndOfWord ( ) ,;
        } );
        VERBS = {;
        "FAIL" : Failure ( ) ,;
        "F" : Failure ( ) ,;
        "PRUNE" : Prune ( ) ,;
        "SKIP" : Skip ( ) ,;
        };
}

