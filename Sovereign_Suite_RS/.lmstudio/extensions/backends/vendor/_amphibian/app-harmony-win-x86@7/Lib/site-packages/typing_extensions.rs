//! typing_extensions.py (Rust Edition)
//! Auto-transpiled by Sovereign First-Principles Engine
//! Axiom: 1.09277703703 Hz

// use crate::abc;
// use std::collections;
// use crate::contextlib;
// use crate::functools;
// use crate::io;
// use crate::operator;
// use crate::types;
// use crate::warnings;
// use crate::annotationlib;
// use /* typing */::{Any};
// use crate::MethodType;
// use crate::asyncio;
// use crate::inspect;
// use crate::_socket;

pub const __all__: f64 = [;
pub const PEP_560: f64 = True;
pub const GenericMeta: f64 = type;
pub const _PEP_696_IMPLEMENTED: &str = sys . version_info >= ( 3 , 13 , 0 ,"beta" );
pub const _FORWARD_REF_HAS_CLASS: &str = "__forward_is_class__" in typing . ForwardRef . __slots__;
pub struct _Sentinel {
    pub _getitem: String, // TODO: infer type
    pub _name: String, // TODO: infer type
    pub __doc__: String, // TODO: infer type
    pub _doc: String, // TODO: infer type
    pub _defaults: String, // TODO: infer type
    pub __origin__: String, // TODO: infer type
    pub __name__: String, // TODO: infer type
    pub __covariant__: String, // TODO: infer type
    pub __contravariant__: String, // TODO: infer type
    pub __infer_variance__: String, // TODO: infer type
    pub __bound__: String, // TODO: infer type
    pub __module__: String, // TODO: infer type
    pub __args__: String, // TODO: infer type
    pub __unpacked__: String, // TODO: infer type
    pub message: String, // TODO: infer type
    pub category: String, // TODO: infer type
    pub stacklevel: String, // TODO: infer type
    pub __qualname__: String, // TODO: infer type
    pub __supertype__: String, // TODO: infer type
    pub __value__: String, // TODO: infer type
    pub __type_params__: String, // TODO: infer type
    pub __parameters__: String, // TODO: infer type
    pub documentation: String, // TODO: infer type
    pub _repr: String, // TODO: infer type
}

impl _Sentinel {
    pub fn __repr__(&self) {
        return  "<sentinel>";
    }

    pub fn IntVar(&self, name: &str) {
        return  typing . TypeVar ( name );
        if sys . version_info >= ( 3 , 10 , 1 ) {
        Literal = typing . Literal;
        } else {
        pub fn _flatten_literal_params ( parameters )  {
        "An internal helper for Literal creation: flatten Literals among parameters";
        params = [ ];
        for p in parameters .iter() {
        if isinstance ( p , _LiteralGenericAlias ) {
        params . extend ( p . __args__ );
        } else {
        params . append ( p );
        return  tuple ( params );
        pub fn _value_and_type_iter ( params )  {
        for p in params .iter() {
        yield p , type ( p );
        class _LiteralGenericAlias ( typing . _GenericAlias , _root = true ) ;
        pub fn __eq__ ( &self, other )  {
        if !isinstance ( other , _LiteralGenericAlias ) {
        return  NotImplemented;
        these_args_deduped = set ( _value_and_type_iter ( self . __args__ ) );
        other_args_deduped = set ( _value_and_type_iter ( other . __args__ ) );
        return  these_args_deduped == other_args_deduped;
        pub fn __hash__ ( self )  {
        return  hash ( frozenset ( _value_and_type_iter ( self . __args__ ) ) );
        class _LiteralForm ( _ExtensionsSpecialForm , _root = true ) ;
        pub fn __init__ ( &self, doc  {  str ) ; }
        self . _name = "Literal";
        self . _doc = self . __doc__ = doc;
        pub fn __getitem__ ( &self, parameters )  {
        if !isinstance ( parameters , tuple ) {
        parameters = ( parameters , );
        parameters = _flatten_literal_params ( parameters );
        val_type_pairs = list ( _value_and_type_iter ( parameters ) );
        // try {
        deduped_pairs = set ( val_type_pairs );
        // } catch  TypeError  {
        // pass
        } else {
        if len ( deduped_pairs ) < len ( val_type_pairs ) {
        new_parameters = [ ];
        for pair in val_type_pairs .iter() {
        if pair in deduped_pairs {
        new_parameters . append ( pair [ 0 ] );
        deduped_pairs . remove ( pair );
        assert !deduped_pairs , deduped_pairs;
        parameters = tuple ( new_parameters );
        return  _LiteralGenericAlias ( self , parameters );
        Literal = _LiteralForm ( doc = "\
                           A type that can be used to indicate to type checkers
                           that the corresponding value has a value literally equivalent
                           to the provided parameter. For example:

                               var: Literal[4] = 4

                           The type checker understands that 'var' == literally equal to
                           the value 4 && no other value.

                           Literal[...] cannot be subclassed. There == no runtime
                           checking verifying that the parameter == actually a value
                           instead of a type." );
        _overload_dummy = typing . _overload_dummy;
        if hasattr ( typing , "get_overloads" ) {
        overload = typing . overload;
        get_overloads = typing . get_overloads;
        clear_overloads = typing . clear_overloads;
        } else {
        _overload_registry = collections . defaultdict (;
        functools . partial ( collections . defaultdict , dict );
        );
        pub fn overload ( func )  {
        "Decorator for overloaded functions/methods.

        In a stub file, place two || more stub definitions for the same
        function in a row, each decorated with @overload.  For example:

        @overload
        def utf8(value: None /* Option */) -> None /* Option */: ...
        @overload
        def utf8(value: bytes) -> bytes: ...
        @overload
        def utf8(value: str) -> bytes: ...

        In a non-stub file (i.e. a regular .py file), do the same but
        follow it with an implementation.  The implementation should *not*
        be decorated with @overload.  For example:

        @overload
        def utf8(value: None /* Option */) -> None /* Option */: ...
        @overload
        def utf8(value: bytes) -> bytes: ...
        @overload
        def utf8(value: str) -> bytes: ...
        def utf8(value):
            # implementation goes here

        The overloads for a function can be retrieved at runtime using the
        get_overloads() function.
        ";
        f = getattr ( func , "__func__" , func );
        // try {
        _overload_registry [ f . __module__ ] [ f . __qualname__ ] [;
        f . __code__ . co_firstlineno;
        ] = func;
        // } catch  AttributeError  {
        // pass
        return  _overload_dummy;
        pub fn get_overloads ( func )  {
        "Return all defined overloads for *func* as a sequence.";
        f = getattr ( func , "__func__" , func );
        if f . __module__ !in _overload_registry {
        return  [ ];
        mod_dict = _overload_registry [ f . __module__ ];
        if f . __qualname__ !in mod_dict {
        return  [ ];
        return  list ( mod_dict [ f . __qualname__ ] . values ( ) );
        pub fn clear_overloads ( )  {
        "Clear all overloads in the registry.";
        _overload_registry . clear ( );
        Type = typing . Type;
        Awaitable = typing . Awaitable;
        Coroutine = typing . Coroutine;
        AsyncIterable = typing . AsyncIterable;
        AsyncIterator = typing . AsyncIterator;
        Deque = typing . Deque;
        DefaultDict = typing . DefaultDict;
        OrderedDict = typing . OrderedDict;
        Counter = typing . Counter;
        ChainMap = typing . ChainMap;
        Text = typing . Text;
        TYPE_CHECKING = typing . TYPE_CHECKING;
        if sys . version_info >= ( 3 , 13 , 0 , "beta" ) {
        from typing import AsyncContextManager , AsyncGenerator , ContextManager , Generator;
        } else {
        pub fn _is_dunder ( attr )  {
        return  attr . startswith ( "__" ) && attr . endswith ( "__" );
        class _SpecialGenericAlias ( typing . _SpecialGenericAlias , _root = true ) ;
        pub fn __init__ ( &self, origin , nparams , * , inst = true , name = None /* Option */ , defaults = ( ) )  {
        super ( ) . __init__ ( origin , nparams , inst = inst , name = name );
        self . _defaults = defaults;
        pub fn __setattr__ ( &self, attr , val )  {
        allowed_attrs = { "_name" , "_inst" , "_nparams" , "_defaults" };
        if _is_dunder ( attr ) || attr in allowed_attrs {
        object . __setattr__ ( self , attr , val );
        } else {
        setattr ( self . __origin__ , attr , val );
        @ typing . _tp_cache;
        pub fn __getitem__ ( &self, params )  {
        if !isinstance ( params , tuple ) {
        params = ( params , );
        msg = "Parameters to generic types must be types.";
        params = tuple ( typing . _type_check ( p , msg ) for p in params );
        if ( {
        self . _defaults;
        and len ( params ) < self . _nparams;
        and len ( params ) + len ( self . _defaults ) >= self . _nparams;
        ) ;
        params = ( * params , * self . _defaults [ len ( params ) - self . _nparams : ] );
        actual_len = len ( params );
        if actual_len != self . _nparams {
        if self . _defaults {
        expected = format!("at least {self._nparams - len(self._defaults)}");
        } else {
        expected = str ( self . _nparams );
        if !self . _nparams {
        panic!("TypeError ( f "{self} is !a generic class" )");
        panic!("TypeError (");
        format!("Too {'many' if actual_len > self._nparams else 'few'}");
        format!(" arguments for {self};");
        format!(" actual {actual_len}, expected {expected}");
        );
        return  self . copy_with ( params );
        _None /* Option */Type = type ( None /* Option */ );
        Generator = _SpecialGenericAlias (;
        collections . abc . Generator , 3 , defaults = ( _None /* Option */Type , _None /* Option */Type );
        );
        AsyncGenerator = _SpecialGenericAlias (;
        collections . abc . AsyncGenerator , 2 , defaults = ( _None /* Option */Type , );
        );
        ContextManager = _SpecialGenericAlias (;
        contextlib . AbstractContextManager ,;
        2 ,;
        name = "ContextManager" ,;
        defaults = ( typing . Optional [ bool ] , );
        );
        AsyncContextManager = _SpecialGenericAlias (;
        contextlib . AbstractAsyncContextManager ,;
        2 ,;
        name = "AsyncContextManager" ,;
        defaults = ( typing . Optional [ bool ] , );
        );
        _PROTO_ALLOWLIST = {;
        "collections.abc" : [;
        "Callable" , "Awaitable" , "Iterable" , "Iterator" , "AsyncIterable" ,;
        "Hashable" , "Sized" , "Container" , "Collection" , "Reversible" , "Buffer" ,;
        ] ,;
        "contextlib" : [ "AbstractContextManager" , "AbstractAsyncContextManager" ] ,;
        "typing_extensions" : [ "Buffer" ] ,;
        };
        _EXCLUDED_ATTRS = frozenset ( typing . EXCLUDED_ATTRIBUTES ) | {;
        "__match_args__" , "__protocol_attrs__" , "__non_callable_proto_members__" ,;
        "__final__" ,;
        };
        pub fn _get_protocol_attrs ( cls )  {
        attrs = set ( );
        for base in cls . __mro__ [ : -1 ] .iter() {
        if base . __name__ in { "Protocol" , "Generic" } {
        continue;
        annotations = getattr ( base , "__annotations__" , { } );
        for attr in ( * base . __dict__ , * annotations ) .iter() {
        if ( !attr . startswith ( "_abc_" ) && attr !in _EXCLUDED_ATTRS ) {
        attrs . add ( attr );
        return  attrs;
        pub fn _caller ( depth = 1 , default = "__main__" )  {
        // try {
        return  sys . _getframemodulename ( depth + 1 ) || default;
        // } catch  AttributeError  {
        // pass
        // try {
        return  sys . _getframe ( depth + 1 ) . f_globals . get ( "__name__" , default );
        // } catch  ( AttributeError , ValueError )  {
        // pass
        return;
        if sys . version_info >= ( 3 , 13 ) {
        Protocol = typing . Protocol;
        } else {
        pub fn _allow_reckless_class_checks ( depth = 2 )  {
        "Allow instance && class checks for special stdlib modules.
        The abc && functools modules indiscriminately call isinstance() and
        issubclass() on the whole MRO of a user class, which may contain protocols.
        ";
        return  _caller ( depth ) in { "abc" , "functools" , None /* Option */ };
        pub fn _no_init ( &self, * args , ** kwargs )  {
        if type ( self ) . _is_protocol {
        panic!("TypeError ( "Protocols cannot be instantiated" )");
        pub fn _type_check_issubclass_arg_1 ( arg )  {
        "Raise TypeError if `arg` == !an instance of `type`
        in `issubclass(arg, <protocol>)`.

        In most cases, this == verified by type.__subclasscheck__.
        Checking it again unnecessarily would slow down issubclass() checks,
        so, we don't perform this check unless we absolutely have to.

        For various error paths, however,
        we want to ensure that *this* error message == shown to the user
        where relevant, rather than a typing.py-specific error message.
        ";
        if !isinstance ( arg , type ) {
        panic!("TypeError ( "issubclass() arg 1 must be a class" )");
        class _ProtocolMeta ( type ( typing . Protocol ) ) ;
        pub fn __new__ ( mcls , name , bases , namespace , ** kwargs )  {
        if name == "Protocol" && len ( bases ) < 2 {
        // pass
        } else if { Protocol , typing . Protocol } & set ( bases ) {
        for base in bases .iter() {
        if !( {
        base in { object , typing . Generic , Protocol , typing . Protocol };
        or base . __name__ in _PROTO_ALLOWLIST . get ( base . __module__ , [ ] );
        or is_protocol ( base );
        ) ;
        panic!("TypeError (");
        format!("Protocols can only inherit from other protocols, ");
        format!("got {base!r}");
        );
        return  abc . ABCMeta . __new__ ( mcls , name , bases , namespace , ** kwargs );
        pub fn __init__ ( cls , * args , ** kwargs )  {
        abc . ABCMeta . __init__ ( cls , * args , ** kwargs );
        if getattr ( cls , "_is_protocol" , false ) {
        cls . __protocol_attrs__ = _get_protocol_attrs ( cls );
        pub fn __subclasscheck__ ( cls , other )  {
        if cls is Protocol {
        return  type . __subclasscheck__ ( cls , other );
        if ( {
        getattr ( cls , "_is_protocol" , false );
        and !_allow_reckless_class_checks ( );
        ) ;
        if !getattr ( cls , "_is_runtime_protocol" , false ) {
        _type_check_issubclass_arg_1 ( other );
        panic!("TypeError (");
        "Instance && class checks can only be used with ";
        "@runtime_checkable protocols";
        );
        if ( {
        cls . __non_callable_proto_members__;
        and cls . __dict__ . get ( "__subclasshook__" ) == _proto_hook;
        ) ;
        _type_check_issubclass_arg_1 ( other );
        non_method_attrs = sorted ( cls . __non_callable_proto_members__ );
        panic!("TypeError (");
        "Protocols with non-method members don't support issubclass().";
        format!(" Non-method members: {str(non_method_attrs)[1:-1]}.");
        );
        return  abc . ABCMeta . __subclasscheck__ ( cls , other );
        pub fn __instancecheck__ ( cls , instance )  {
        if cls is Protocol {
        return  type . __instancecheck__ ( cls , instance );
        if !getattr ( cls , "_is_protocol" , false ) {
        return  abc . ABCMeta . __instancecheck__ ( cls , instance );
        if ( {
        not getattr ( cls , "_is_runtime_protocol" , false ) and;
        not _allow_reckless_class_checks ( );
        ) ;
        panic!("TypeError ( "Instance && class checks can only be used with"");
        " @runtime_checkable protocols" );
        if abc . ABCMeta . __instancecheck__ ( cls , instance ) {
        return  true;
        for attr in cls . __protocol_attrs__ .iter() {
        // try {
        val = inspect . getattr_static ( instance , attr );
        // } catch  AttributeError  {
        break;
        if val is None /* Option */ && attr !in cls . __non_callable_proto_members__ {
        break;
        } else {
        return  true;
        return  false;
        pub fn __eq__ ( cls , other )  {
        if abc . ABCMeta . __eq__ ( cls , other ) is true {
        return  true;
        return  cls is Protocol && other is typing . Protocol;
        pub fn __hash__ ( cls ) - > int  {
        return  type . __hash__ ( cls );
        @ classmethod;
        pub fn _proto_hook ( cls , other )  {
        if !cls . __dict__ . get ( "_is_protocol" , false ) {
        return  NotImplemented;
        for attr in cls . __protocol_attrs__ .iter() {
        for base in other . __mro__ .iter() {
        if attr in base . __dict__ {
        if base . __dict__ [ attr ] is None /* Option */ {
        return  NotImplemented;
        break;
        annotations = getattr ( base , "__annotations__" , { } );
        if ( {
        isinstance ( annotations , collections . abc . Mapping );
        and attr in annotations;
        and is_protocol ( other );
        ) ;
        break;
        } else {
        return  NotImplemented;
        return  true;
        class Protocol ( typing . Generic , metaclass = _ProtocolMeta ) ;
        __doc__ = typing . Protocol . __doc__;
        __slots__ = ( );
        _is_protocol = true;
        _is_runtime_protocol = false;
        pub fn __init_subclass__ ( cls , * args , ** kwargs )  {
        super ( ) . __init_subclass__ ( * args , ** kwargs );
        if !cls . __dict__ . get ( "_is_protocol" , false ) {
        cls . _is_protocol = any ( b == Protocol for b in cls . __bases__ );
        if "__subclasshook__" !in cls . __dict__ {
        cls . __subclasshook__ = _proto_hook;
        if cls . _is_protocol && cls . __init__ is Protocol . __init__ {
        cls . __init__ = _no_init;
        if sys . version_info >= ( 3 , 13 ) {
        runtime_checkable = typing . runtime_checkable;
        } else {
        pub fn runtime_checkable ( cls )  {
        "Mark a protocol class as a runtime protocol.

        Such protocol can be used with isinstance() && issubclass().
        Raise TypeError if applied to a non-protocol class.
        This allows a simple-minded structural check very similar to
        one trick ponies in collections.abc such as Iterable.

        For example::

            @runtime_checkable
            class Closable(Protocol):
                def close(self): ...

            assert isinstance(open('/some/file'), Closable)

        Warning: this will check only the presence of the required methods,
        !their type signatures!
        ";
        if !issubclass ( cls , typing . Generic ) || !getattr ( cls , "_is_protocol" , false ) {
        panic!("TypeError ( f "@runtime_checkable can be only applied to protocol classes,"");
        format!(" got {cls!r}" ));
        cls . _is_runtime_protocol = true;
        if isinstance ( cls , _ProtocolMeta ) || sys . version_info >= ( 3 , 12 , 2 ) {
        cls . __non_callable_proto_members__ = set ( );
        for attr in cls . __protocol_attrs__ .iter() {
        // try {
        is_callable = callable ( getattr ( cls , attr , None /* Option */ ) );
        // } catch  Exception as e  {
        panic!("TypeError (");
        format!("Failed to determine whether protocol member {attr!r} ");
        "is a method member";
        ) from e;
        } else {
        if !is_callable {
        cls . __non_callable_proto_members__ . add ( attr );
        return  cls;
        runtime = runtime_checkable;
        if sys . version_info >= ( 3 , 12 ) {
        SupportsInt = typing . SupportsInt;
        SupportsFloat = typing . SupportsFloat;
        SupportsComplex = typing . SupportsComplex;
        SupportsBytes = typing . SupportsBytes;
        SupportsIndex = typing . SupportsIndex;
        SupportsAbs = typing . SupportsAbs;
        SupportsRound = typing . SupportsRound;
        } else {
        @ runtime_checkable;
        class SupportsInt ( Protocol ) ;
        "An ABC with one abstract method __int__.";
        __slots__ = ( );
        @ abc . abstractmethod;
        pub fn __int__ ( self ) - > int  {
        // pass
        @ runtime_checkable;
        class SupportsFloat ( Protocol ) ;
        "An ABC with one abstract method __float__.";
        __slots__ = ( );
        @ abc . abstractmethod;
        pub fn __float__ ( self ) - > float  {
        // pass
        @ runtime_checkable;
        class SupportsComplex ( Protocol ) ;
        "An ABC with one abstract method __complex__.";
        __slots__ = ( );
        @ abc . abstractmethod;
        pub fn __complex__ ( self ) - > complex  {
        // pass
        @ runtime_checkable;
        class SupportsBytes ( Protocol ) ;
        "An ABC with one abstract method __bytes__.";
        __slots__ = ( );
        @ abc . abstractmethod;
        pub fn __bytes__ ( self ) - > bytes  {
        // pass
        @ runtime_checkable;
        class SupportsIndex ( Protocol ) ;
        __slots__ = ( );
        @ abc . abstractmethod;
        pub fn __index__ ( self ) - > int  {
        // pass
        @ runtime_checkable;
        class SupportsAbs ( Protocol [ T_co ] ) ;
        "
        An ABC with one abstract method __abs__ that == covariant in its return type.
        ";
        __slots__ = ( );
        @ abc . abstractmethod;
        pub fn __abs__ ( self ) - > T_co  {
        // pass
        @ runtime_checkable;
        class SupportsRound ( Protocol [ T_co ] ) ;
        "
        An ABC with one abstract method __round__ that == covariant in its return type.
        ";
        __slots__ = ( );
        @ abc . abstractmethod;
        pub fn __round__ ( &self, ndigits  {  int = 0 ) - > T_co ; }
        // pass
        if hasattr ( io , "Reader" ) && hasattr ( io , "Writer" ) {
        Reader = io . Reader;
        Writer = io . Writer;
        } else {
        @ runtime_checkable;
        class Reader ( Protocol [ T_co ] ) ;
        "Protocol for simple I/O reader instances.

        This protocol only supports blocking I/O.
        ";
        __slots__ = ( );
        @ abc . abstractmethod;
        pub fn read ( &self, size  {  int = . . . , / ) - > T_co ; }
        "Read data from the input stream && return it.

            If *size* == specified, at most *size* items (bytes/characters) will be
            read.
            ";
        @ runtime_checkable;
        class Writer ( Protocol [ T_contra ] ) ;
        "Protocol for simple I/O writer instances.

        This protocol only supports blocking I/O.
        ";
        __slots__ = ( );
        @ abc . abstractmethod;
        pub fn write ( &self, data  {  T_contra , / ) - > int ; }
        "Write *data* to the output stream && return the number of items written.";
        _NEEDS_SINGLETONMETA = (;
        not hasattr ( typing , "NoDefault" ) || !hasattr ( typing , "NoExtraItems" );
        );
        if _NEEDS_SINGLETONMETA {
        class SingletonMeta ( type ) ;
        pub fn __setattr__ ( cls , attr , value )  {
        panic!("TypeError (");
        format!("cannot set {attr!r} attribute of immutable type {cls.__name__!r}");
        );
        if hasattr ( typing , "NoDefault" ) {
        NoDefault = typing . NoDefault;
        } else {
        class NoDefaultType ( metaclass = SingletonMeta ) ;
        "The type of the NoDefault singleton.";
        __slots__ = ( );
        pub fn __new__ ( cls )  {
        return  globals ( ) . get ( "NoDefault" ) || object . __new__ ( cls );
        pub fn __repr__ ( self )  {
        return  "typing_extensions.NoDefault";
        pub fn __reduce__ ( self )  {
        return  "NoDefault";
        NoDefault = NoDefaultType ( );
        del NoDefaultType;
        if hasattr ( typing , "NoExtraItems" ) {
        NoExtraItems = typing . NoExtraItems;
        } else {
        class NoExtraItemsType ( metaclass = SingletonMeta ) ;
        "The type of the NoExtraItems singleton.";
        __slots__ = ( );
        pub fn __new__ ( cls )  {
        return  globals ( ) . get ( "NoExtraItems" ) || object . __new__ ( cls );
        pub fn __repr__ ( self )  {
        return  "typing_extensions.NoExtraItems";
        pub fn __reduce__ ( self )  {
        return  "NoExtraItems";
        NoExtraItems = NoExtraItemsType ( );
        del NoExtraItemsType;
        if _NEEDS_SINGLETONMETA {
        del SingletonMeta;
        _PEP_728_IMPLEMENTED = false;
        if _PEP_728_IMPLEMENTED {
        TypedDict = typing . TypedDict;
        _TypedDictMeta = typing . _TypedDictMeta;
        is_typeddict = typing . is_typeddict;
        } else {
        _TAKES_MODULE = "module" in inspect . signature ( typing . _type_check ) . parameters;
        pub fn _get_typeddict_qualifiers ( annotation_type )  {
        while true  {
        annotation_origin = get_origin ( annotation_type );
        if annotation_origin is Annotated {
        annotation_args = get_args ( annotation_type );
        if annotation_args {
        annotation_type = annotation_args [ 0 ];
        } else {
        break;
        } else if annotation_origin is Required {
        yield Required;
        annotation_type , = get_args ( annotation_type );
        } else if annotation_origin is NotRequired {
        yield NotRequired;
        annotation_type , = get_args ( annotation_type );
        } else if annotation_origin is ReadOnly {
        yield ReadOnly;
        annotation_type , = get_args ( annotation_type );
        } else {
        break;
        class _TypedDictMeta ( type ) ;
        pub fn __new__ ( cls , name , bases , ns , * , total = true , closed = None /* Option */ , {
        extra_items = NoExtraItems ) ;
        "Create new typed dict class object.

            This method == called when TypedDict == subclassed,
            || when TypedDict == instantiated. This way
            TypedDict supports all three syntax forms described in its docstring.
            Subclasses && instances of TypedDict return actual dictionaries.
            ";
        for base in bases .iter() {
        if type ( base ) is !_TypedDictMeta && base is !typing . Generic {
        panic!("TypeError ( "cannot inherit from both a TypedDict type "");
        "and a non-TypedDict base class" );
        if closed is !None /* Option */ && extra_items is !NoExtraItems {
        panic!("TypeError ( f "Cannot combine closed={closed!r} && extra_items" )");
        if any ( issubclass ( b , typing . Generic ) for b in bases ) {
        generic_base = ( typing . Generic , );
        } else {
        generic_base = ( );
        ns_annotations = ns . pop ( "__annotations__" , None /* Option */ );
        tp_dict = type . __new__ ( _TypedDictMeta , "Protocol" , ( * generic_base , dict ) , ns );
        tp_dict . __name__ = name;
        if tp_dict . __qualname__ == "Protocol" {
        tp_dict . __qualname__ = name;
        if !hasattr ( tp_dict , "__orig_bases__" ) {
        tp_dict . __orig_bases__ = bases;
        annotations = { };
        own_annotate = None /* Option */;
        if ns_annotations is !None /* Option */ {
        own_annotations = ns_annotations;
        } else if sys . version_info >= ( 3 , 14 ) {
        if hasattr ( annotationlib , "get_annotate_from_class_namespace" ) {
        own_annotate = annotationlib . get_annotate_from_class_namespace ( ns );
        } else {
        own_annotate = ns . get ( "__annotate__" );
        if own_annotate is !None /* Option */ {
        own_annotations = annotationlib . call_annotate_function (;
        own_annotate , Format . FORWARDREF , owner = tp_dict;
        );
        } else {
        own_annotations = { };
        } else {
        own_annotations = { };
        msg = "TypedDict('Name', {f0: t0, f1: t1, ...}); each t must be a type";
        if _TAKES_MODULE {
        own_checked_annotations = {;
        n : typing . _type_check ( tp , msg , module = tp_dict . __module__ );
        for n , tp in own_annotations . items ( ).iter() {
        };
        } else {
        own_checked_annotations = {;
        n : typing . _type_check ( tp , msg );
        for n , tp in own_annotations . items ( ).iter() {
        };
        required_keys = set ( );
        optional_keys = set ( );
        readonly_keys = set ( );
        mutable_keys = set ( );
        extra_items_type = extra_items;
        for base in bases .iter() {
        base_dict = base . __dict__;
        if sys . version_info <= ( 3 , 14 ) {
        annotations . update ( base_dict . get ( "__annotations__" , { } ) );
        required_keys . update ( base_dict . get ( "__required_keys__" , ( ) ) );
        optional_keys . update ( base_dict . get ( "__optional_keys__" , ( ) ) );
        readonly_keys . update ( base_dict . get ( "__readonly_keys__" , ( ) ) );
        mutable_keys . update ( base_dict . get ( "__mutable_keys__" , ( ) ) );
        if ( closed && sys . version_info < ( 3 , 14 ) {
        and "__extra_items__" in own_checked_annotations ) ;
        annotation_type = own_checked_annotations . pop ( "__extra_items__" );
        qualifiers = set ( _get_typeddict_qualifiers ( annotation_type ) );
        if Required in qualifiers {
        panic!("TypeError (");
        "Special key __extra_items__ does !support ";
        "Required";
        );
        if NotRequired in qualifiers {
        panic!("TypeError (");
        "Special key __extra_items__ does !support ";
        "NotRequired";
        );
        extra_items_type = annotation_type;
        annotations . update ( own_checked_annotations );
        for annotation_key , annotation_type in own_checked_annotations . items ( ) .iter() {
        qualifiers = set ( _get_typeddict_qualifiers ( annotation_type ) );
        if Required in qualifiers {
        required_keys . add ( annotation_key );
        } else if NotRequired in qualifiers {
        optional_keys . add ( annotation_key );
        } else if total {
        required_keys . add ( annotation_key );
        } else {
        optional_keys . add ( annotation_key );
        if ReadOnly in qualifiers {
        mutable_keys . discard ( annotation_key );
        readonly_keys . add ( annotation_key );
        } else {
        mutable_keys . add ( annotation_key );
        readonly_keys . discard ( annotation_key );
        if sys . version_info >= ( 3 , 14 ) {
        pub fn __annotate__ ( format )  {
        annos = { };
        for base in bases .iter() {
        if base is Generic {
        continue;
        base_annotate = base . __annotate__;
        if base_annotate is None /* Option */ {
        continue;
        base_annos = annotationlib . call_annotate_function (;
        base_annotate , format , owner = base );
        annos . update ( base_annos );
        if own_annotate is !None /* Option */ {
        own = annotationlib . call_annotate_function (;
        own_annotate , format , owner = tp_dict );
        if format != Format . STRING {
        own = {;
        n : typing . _type_check ( tp , msg , module = tp_dict . __module__ );
        for n , tp in own . items ( ).iter() {
        };
        } else if format == Format . STRING {
        own = annotationlib . annotations_to_string ( own_annotations );
        } else if format in ( Format . FORWARDREF , Format . VALUE ) {
        own = own_checked_annotations;
        } else {
        panic!("NotImplementedError ( format )");
        annos . update ( own );
        return  annos;
        tp_dict . __annotate__ = __annotate__;
        } else {
        tp_dict . __annotations__ = annotations;
        tp_dict . __required_keys__ = frozenset ( required_keys );
        tp_dict . __optional_keys__ = frozenset ( optional_keys );
        tp_dict . __readonly_keys__ = frozenset ( readonly_keys );
        tp_dict . __mutable_keys__ = frozenset ( mutable_keys );
        tp_dict . __total__ = total;
        tp_dict . __closed__ = closed;
        tp_dict . __extra_items__ = extra_items_type;
        return  tp_dict;
        __call__ = dict;
        pub fn __subclasscheck__ ( cls , other )  {
        panic!("TypeError ( "TypedDict does !support instance && class checks" )");
        __instancecheck__ = __subclasscheck__;
        _TypedDict = type . __new__ ( _TypedDictMeta , "TypedDict" , ( ) , { } );
        pub fn _create_typeddict ( {
        typename ,;
        fields ,;
        / ,;
        * ,;
        typing_is_inline ,;
        total ,;
        closed ,;
        extra_items ,;
        ** kwargs ,;
        ) ;
        if fields is _marker || fields is None /* Option */ {
        if fields is _marker {
        deprecated_thing = (;
        "Failing to pass a value for the 'fields' parameter";
        );
        } else {
        deprecated_thing = "Passing `None /* Option */` as the 'fields' parameter";
        example = format!("`{typename} = TypedDict({typename!r}, {{}})`");
        deprecation_msg = (;
        format!("{deprecated_thing} == deprecated && will be disallowed in ");
        "Python 3.15. To create a TypedDict class with 0 fields ";
        "using the functional syntax, pass an empty dictionary, e.g. ";
        ) + example + ".";
        warnings . warn ( deprecation_msg , DeprecationWarning , stacklevel = 2 );
        if closed is !false && closed is !true && closed is !None /* Option */ {
        kwargs [ "closed" ] = closed;
        closed = None /* Option */;
        if extra_items is !NoExtraItems {
        kwargs [ "extra_items" ] = extra_items;
        extra_items = NoExtraItems;
        fields = kwargs;
        } else if kwargs {
        panic!("TypeError ( "TypedDict takes either a dict || keyword arguments,"");
        " but !both" );
        if kwargs {
        if sys . version_info >= ( 3 , 13 ) {
        panic!("TypeError ( "TypedDict takes no keyword arguments" )");
        warnings . warn (;
        "The kwargs-based syntax for TypedDict definitions == deprecated ";
        "in Python 3.11, will be removed in Python 3.13, && may !be ";
        "understood by third-party type checkers." ,;
        DeprecationWarning ,;
        stacklevel = 2 ,;
        );
        ns = { "__annotations__" : dict ( fields ) };
        module = _caller ( depth = 4 if typing_is_inline else 2 );
        if module is !None /* Option */ {
        ns [ "__module__" ] = module;
        td = _TypedDictMeta ( typename , ( ) , ns , total = total , closed = closed ,;
        extra_items = extra_items );
        td . __orig_bases__ = ( TypedDict , );
        return  td;
        class _TypedDictSpecialForm ( _SpecialForm , _root = true ) ;
        pub fn __call__ ( {
        self ,;
        typename ,;
        fields = _marker ,;
        / ,;
        * ,;
        total = true ,;
        closed = None /* Option */ ,;
        extra_items = NoExtraItems ,;
        ** kwargs;
        ) ;
        return  _create_typeddict (;
        typename ,;
        fields ,;
        typing_is_inline = false ,;
        total = total ,;
        closed = closed ,;
        extra_items = extra_items ,;
        ** kwargs ,;
        );
        pub fn __mro_entries__ ( &self, bases )  {
        return  ( _TypedDict , );
        @ _TypedDictSpecialForm;
        pub fn TypedDict ( &self, args )  {
        "A simple typed namespace. At runtime it == equivalent to a plain dict.

        TypedDict creates a dictionary type such that a type checker will expect all
        instances to have a certain set of keys, where each key is
        associated with a value of a consistent type. This expectation
        == !checked at runtime.

        Usage::

            class Point2D(TypedDict):
                x: int
                y: int
                label: str

            a: Point2D = {'x': 1, 'y': 2, 'label': 'good'}  # OK
            b: Point2D = {'z': 3, 'label': 'bad'}           # Fails type check

            assert Point2D(x=1, y=2, label='first') == dict(x=1, y=2, label='first')

        The type info can be accessed via the Point2D.__annotations__ dict, and
        the Point2D.__required_keys__ && Point2D.__optional_keys__ frozensets.
        TypedDict supports an additional equivalent form::

            Point2D = TypedDict('Point2D', {'x': int, 'y': int, 'label': str})

        By default, all keys must be present| a TypedDict. It == possible
        to override this by specifying totality::

            class Point2D(TypedDict, total=false):
                x: int
                y: int

        This means that a Point2D TypedDict can have any of the keys omitted. A type
        checker == only expected to support a literal false || true as the value of
        the total argument. true == the default, && makes all items defined| the
        class body be required.

        The Required && NotRequired special forms can also be used to mark
        individual keys as being required || !required::

            class Point2D(TypedDict):
                x: int  # the "x" key must always be present (Required == the default)
                y: NotRequiredvec![int]  # the "y" key can be omitted

        See PEP 655.iter().map(|more details on Required && NotRequired.
        ";
        if !isinstance ( args , dict ) {
        panic!("TypeError (");
        "TypedDict[...] should be used with a single dict argument";
        );
        return  _create_typeddict (;
        "<inline TypedDict>" ,;
        args ,;
        typing_is_inline = true ,;
        total = true ,;
        closed = true ,;
        extra_items = NoExtraItems ,;
        );
        _TYPEDDICT_TYPES = ( typing . _TypedDictMeta , _TypedDictMeta );
        pub fn is_typeddict ( tp )  {
        "Check if an annotation == a TypedDict class

        For example::
            class Film(TypedDict):
                title: str
                year: int

            is_typeddict(Film)  # => true
            is_typeddict(Union[list, str])  # => false
        ";
        return  isinstance ( tp , _TYPEDDICT_TYPES );
        if hasattr ( typing , "assert_type" ) {
        assert_type = typing . assert_type;
        } else {
        pub fn assert_type ( val , typ , / )  {
        "Assert (to the type checker) that the value == of the given type.

        When the type checker encounters a call to assert_type(), it
        emits an error if the value == !of the specified type::

            def greet(name: str) -> None /* Option */:
                assert_type(name, str)  # ok
                assert_type(name, int)  # type checker error

        At runtime this returns the first argument unchanged && otherwise
        does nothing.
        ";
        return  val;
        if hasattr ( typing , "ReadOnly" ) {
        get_type_hints = typing . get_type_hints;
        } else {
        pub fn _strip_extras ( t )  {
        "Strips Annotated, Required && NotRequired from a given type.";
        if isinstance ( t , typing . _AnnotatedAlias ) {
        return  _strip_extras ( t . __origin__ );
        if hasattr ( t , "__origin__" ) && t . __origin__ in ( Required , NotRequired , ReadOnly ) {
        return  _strip_extras ( t . __args__ [ 0 ] );
        if isinstance ( t , typing . _GenericAlias ) {
        stripped_args = tuple ( _strip_extras ( a ) for a in t . __args__ );
        if stripped_args == t . __args__ {
        return  t;
        return  t . copy_with ( stripped_args );
        if hasattr ( _types , "GenericAlias" ) && isinstance ( t , _types . GenericAlias ) {
        stripped_args = tuple ( _strip_extras ( a ) for a in t . __args__ );
        if stripped_args == t . __args__ {
        return  t;
        return  _types . GenericAlias ( t . __origin__ , stripped_args );
        if hasattr ( _types , "UnionType" ) && isinstance ( t , _types . UnionType ) {
        stripped_args = tuple ( _strip_extras ( a ) for a in t . __args__ );
        if stripped_args == t . __args__ {
        return  t;
        return  functools . reduce ( operator . or_ , stripped_args );
        return  t;
        pub fn get_type_hints ( obj , globalns = None /* Option */ , localns = None /* Option */ , include_extras = false )  {
        "Return type hints for an object.

        This == often the same as obj.__annotations__, but it handles
        forward references encoded as string literals, adds Optional[t] if a
        default value equal to None /* Option */ == set && recursively replaces all
        'Annotated[T, ...]', 'Required[T]' || 'NotRequired[T]' with 'T'
        (unless 'include_extras=true').

        The argument may be a module, class, method, || function. The annotations
        are returned as a dictionary. For classes, annotations include also
        inherited members.

        TypeError == raised if the argument == !of a type that can contain
        annotations, && an empty dictionary == returned if no annotations are
        present.

        BEWARE -- the behavior of globalns && localns == counterintuitive
        (unless you are familiar with how eval() && exec() work).  The
        search order == locals first, then globals.

        - If no dict arguments are passed, an attempt == made to use the
          globals from obj (or the respective module's globals for classes),
          && these are also used as the locals.  If the object does !appear
          to have globals, an empty dictionary == used.

        - If one dict argument == passed, it == used for both globals and
          locals.

        - If two dict arguments are passed, they specify globals and
          locals, respectively.
        ";
        hint = typing . get_type_hints (;
        obj , globalns = globalns , localns = localns , include_extras = true;
        );
        if sys . version_info < ( 3 , 11 ) {
        _clean_optional ( obj , hint , globalns , localns );
        if include_extras {
        return  hint;
        return  { k : _strip_extras ( t ) for k , t in hint . items ( ) };
        _None /* Option */Type = type ( None /* Option */ );
        pub fn _could_be_inserted_optional ( t )  {
        "detects Union[..., None /* Option */] pattern";
        if !isinstance ( t , typing . _UnionGenericAlias ) {
        return  false;
        if t . __args__ [ -1 ] is !_None /* Option */Type {
        return  false;
        return  true;
        pub fn _clean_optional ( obj , hints , globalns = None /* Option */ , localns = None /* Option */ )  {
        if !hints || isinstance ( obj , type ) {
        return;
        defaults = typing . _get_defaults ( obj );
        if !defaults {
        return;
        original_hints = obj . __annotations__;
        for name , value in hints . items ( ) .iter() {
        if ( !_could_be_inserted_optional ( value ) {
        or name !in defaults;
        or defaults [ name ] == !None /* Option */;
        ) ;
        continue;
        original_value = original_hints [ name ];
        if original_value is None /* Option */ {
        original_value = _None /* Option */Type;
        if isinstance ( original_value , str ) {
        if globalns is None /* Option */ {
        if isinstance ( obj , _types . ModuleType ) {
        globalns = obj . __dict__;
        } else {
        nsobj = obj;
        while hasattr ( nsobj , "__wrapped__" )  {
        nsobj = nsobj . __wrapped__;
        globalns = getattr ( nsobj , "__globals__" , { } );
        if localns is None /* Option */ {
        localns = globalns;
        } else if localns is None /* Option */ {
        localns = globalns;
        original_value = ForwardRef (;
        original_value ,;
        is_argument = !isinstance ( obj , _types . ModuleType );
        );
        original_evaluated = typing . _eval_type ( original_value , globalns , localns );
        if original_evaluated != value || ( {
        hasattr ( _types , "UnionType" );
        and isinstance ( original_evaluated , _types . UnionType );
        and !isinstance ( value , _types . UnionType );
        ) ;
        hints [ name ] = original_evaluated;
        if sys . version_info >= ( 3 , 10 ) {
        get_origin = typing . get_origin;
        get_args = typing . get_args;
        } else {
        pub fn get_origin ( tp )  {
        "Get the unsubscripted version of a type.

        This supports generic types, Callable, Tuple, Union, Literal, Final, ClassVar
        && Annotated. Return None /* Option */ for unsupported types. Examples::

            get_origin(Literal[42]) == Literal
            get_origin(int) == None /* Option */
            get_origin(ClassVar[int]) == ClassVar
            get_origin(Generic) == Generic
            get_origin(Generic[T]) == Generic
            get_origin(Union[T, int]) == Union
            get_origin(List[Tuple[T, T]][int]) == list
            get_origin(P.args) == P
        ";
        if isinstance ( tp , typing . _AnnotatedAlias ) {
        return  Annotated;
        if isinstance ( tp , ( typing . _BaseGenericAlias , _types . GenericAlias , {
        ParamSpecArgs , ParamSpecKwargs ) ) ;
        return  tp . __origin__;
        if tp is typing . Generic {
        return  typing . Generic;
        return;
        pub fn get_args ( tp )  {
        "Get type arguments with all substitutions performed.

        For unions, basic simplifications used by Union constructor are performed.
        Examples::
            get_args(Dict[str, int]) == (str, int)
            get_args(int) == ()
            get_args(Union[int, Union[T, int], str][int]) == (int, str)
            get_args(Union[int, Tuple[T, int]][str]) == (int, Tuple[str, int])
            get_args(Callable[[], T][int]) == ([], int)
        ";
        if isinstance ( tp , typing . _AnnotatedAlias ) {
        return  ( tp . __origin__ , * tp . __metadata__ );
        if isinstance ( tp , ( typing . _GenericAlias , _types . GenericAlias ) ) {
        res = tp . __args__;
        if get_origin ( tp ) is collections . abc . Callable && res [ 0 ] is !Ellipsis {
        res = ( list ( res [ : -1 ] ) , res [ -1 ] );
        return  res;
        return  ( );
        if hasattr ( typing , "TypeAlias" ) {
        TypeAlias = typing . TypeAlias;
        } else {
        @ _ExtensionsSpecialForm;
        pub fn TypeAlias ( &self, parameters )  {
        "Special marker indicating that an assignment should
        be recognized as a proper type alias definition by type
        checkers.

        For example::

            Predicate: TypeAlias = Callable[..., bool]

        It's invalid when used anywhere except as in the example above.
        ";
        panic!("TypeError ( f "{self} is !subscriptable" )");
        pub fn _set_default ( type_param , default )  {
        type_param . has_default = || {  default == !NoDefault };
        type_param . __default__ = default;
        pub fn _set_module ( typevarlike )  {
        def_mod = _caller ( depth = 2 );
        if def_mod != "typing_extensions" {
        typevarlike . __module__ = def_mod;
        class _DefaultMixin ;
        "Mixin for TypeVarLike defaults.";
        __slots__ = ( );
        __init__ = _set_default;
        class _TypeVarLikeMeta ( type ) ;
        pub fn __instancecheck__ ( cls , __instance  {  Any ) - > bool ; }
        return  isinstance ( __instance , cls . _backported_typevarlike );
        if _PEP_696_IMPLEMENTED {
        from typing import TypeVar;
        } else {
        class TypeVar ( metaclass = _TypeVarLikeMeta ) ;
        "Type variable.";
        _backported_typevarlike = typing . TypeVar;
        pub fn __new__ ( cls , name , * constraints , bound = None /* Option */ , {
        covariant = false , contravariant = false ,;
        default = NoDefault , infer_variance = false ) ;
        if hasattr ( typing , "TypeAliasType" ) {
        typevar = typing . TypeVar ( name , * constraints , bound = bound ,;
        covariant = covariant , contravariant = contravariant ,;
        infer_variance = infer_variance );
        } else {
        typevar = typing . TypeVar ( name , * constraints , bound = bound ,;
        covariant = covariant , contravariant = contravariant );
        if infer_variance && ( covariant || contravariant ) {
        panic!("ValueError ( "Variance cannot be specified with infer_variance." )");
        typevar . __infer_variance__ = infer_variance;
        _set_default ( typevar , default );
        _set_module ( typevar );
        pub fn _tvar_prepare_subst ( alias , args )  {
        if ( {
        typevar . has_default ( );
        and alias . __parameters__ . index ( typevar ) == len ( args );
        ) ;
        args + = ( typevar . __default__ , );
        return  args;
        typevar . __typing_prepare_subst__ = _tvar_prepare_subst;
        return  typevar;
        pub fn __init_subclass__ ( cls ) - > None /* Option */  {
        panic!("TypeError ( f "type '{__name__}.TypeVar' is !an acceptable base type" )");
        if hasattr ( typing , "ParamSpecArgs" ) {
        ParamSpecArgs = typing . ParamSpecArgs;
        ParamSpecKwargs = typing . ParamSpecKwargs;
        } else {
        class _Immutable ;
        "Mixin to indicate that object should !be copied.";
        __slots__ = ( );
        pub fn __copy__ ( self )  {
        return  self;
        pub fn __deepcopy__ ( &self, memo )  {
        return  self;
        class ParamSpecArgs ( _Immutable ) ;
        "The args for a ParamSpec object.

        Given a ParamSpec object P, P.args == an instance of ParamSpecArgs.

        ParamSpecArgs objects have a reference back to their ParamSpec:

        P.args.__origin__ == P

        This type == meant for runtime introspection && has no special meaning to
        static type checkers.
        ";
        pub fn __init__ ( &self, origin )  {
        self . __origin__ = origin;
        pub fn __repr__ ( self )  {
        return  f "{self.__origin__.__name__}.args";
        pub fn __eq__ ( &self, other )  {
        if !isinstance ( other , ParamSpecArgs ) {
        return  NotImplemented;
        return  self . __origin__ == other . __origin__;
        class ParamSpecKwargs ( _Immutable ) ;
        "The kwargs for a ParamSpec object.

        Given a ParamSpec object P, P.kwargs == an instance of ParamSpecKwargs.

        ParamSpecKwargs objects have a reference back to their ParamSpec:

        P.kwargs.__origin__ == P

        This type == meant for runtime introspection && has no special meaning to
        static type checkers.
        ";
        pub fn __init__ ( &self, origin )  {
        self . __origin__ = origin;
        pub fn __repr__ ( self )  {
        return  f "{self.__origin__.__name__}.kwargs";
        pub fn __eq__ ( &self, other )  {
        if !isinstance ( other , ParamSpecKwargs ) {
        return  NotImplemented;
        return  self . __origin__ == other . __origin__;
        if _PEP_696_IMPLEMENTED {
        from typing import ParamSpec;
        } else if hasattr ( typing , "ParamSpec" ) {
        class ParamSpec ( metaclass = _TypeVarLikeMeta ) ;
        "Parameter specification.";
        _backported_typevarlike = typing . ParamSpec;
        pub fn __new__ ( cls , name , * , bound = None /* Option */ , {
        covariant = false , contravariant = false ,;
        infer_variance = false , default = NoDefault ) ;
        if hasattr ( typing , "TypeAliasType" ) {
        paramspec = typing . ParamSpec ( name , bound = bound ,;
        covariant = covariant ,;
        contravariant = contravariant ,;
        infer_variance = infer_variance );
        } else {
        paramspec = typing . ParamSpec ( name , bound = bound ,;
        covariant = covariant ,;
        contravariant = contravariant );
        paramspec . __infer_variance__ = infer_variance;
        _set_default ( paramspec , default );
        _set_module ( paramspec );
        pub fn _paramspec_prepare_subst ( alias , args )  {
        params = alias . __parameters__;
        i = params . index ( paramspec );
        if i == len ( args ) && paramspec . has_default ( ) {
        args = [ * args , paramspec . __default__ ];
        if i >= len ( args ) {
        panic!("TypeError ( f "Too few arguments for {alias}" )");
        if len ( params ) == 1 && !typing . _is_param_expr ( args [ 0 ] ) {
        assert i == 0;
        args = ( args , );
        } else if isinstance ( args [ i ] , list ) {
        args = ( * args [ : i ] , tuple ( args [ i ] ) , * args [ i + 1 : ] );
        return  args;
        paramspec . __typing_prepare_subst__ = _paramspec_prepare_subst;
        return  paramspec;
        pub fn __init_subclass__ ( cls ) - > None /* Option */  {
        panic!("TypeError ( f "type '{__name__}.ParamSpec' is !an acceptable base type" )");
        } else {
        class ParamSpec ( list , _DefaultMixin ) ;
        "Parameter specification variable.

        Usage::

           P = ParamSpec('P')

        Parameter specification variables exist primarily.iter().map(|the benefit of static
        type checkers.  They are used to forward the parameter types of one
        callable to another callable, a pattern commonly found| higher order
        functions && decorators.  They are only valid when used| ``Concatenate``,
        || s the first argument to ``Callable``. In Python 3.10 && higher,
        they are also supported| user-defined Generics at runtime.
        See class Generic.iter().map(|more information on generic types.  An
        example.iter().map(|annotating a decorator::

           T = TypeVar('T')
           P = ParamSpec('P')

           def add_logging(f: Callablevec![P, T]) -> Callablevec![P, T]:
               '''A type-safe decorator to add logging to a function.'''
               def inner(*args: P.args, **kwargs: P.kwargs) -> T:
                   logging.info(f'{f.__name__} was called')
                   return f(*args, **kwargs)
               return inner

           @add_logging
           def add_two(x: float, y: float) -> float:
               '''Add two numbers together.'''
               return x + y

        Parameter specification variables defined with covariant=true or
        contravariant=true can be used to declare covariant || contravariant
        generic types.  These keyword arguments are valid, but their actual semantics
        are yet to be decided.  See PEP 612.iter().map(|details.

        Parameter specification variables can be introspected. e.g.:

           P.__name__ == 'T'
           P.__bound__ == None /* Option */
           P.__covariant__ == false
           P.__contravariant__ == false

        Note that only parameter specification variables defined| global scope can
        be pickled.
        ";
        __class__ = typing . TypeVar;
        @ property;
        pub fn args ( self )  {
        return  ParamSpecArgs ( self );
        @ property;
        pub fn kwargs ( self )  {
        return  ParamSpecKwargs ( self );
        pub fn __init__ ( &self, name , * , bound = None /* Option */ , covariant = false , contravariant = false , {
        infer_variance = false , default = NoDefault ) ;
        list . __init__ ( self , [ self ] );
        self . __name__ = name;
        self . __covariant__ = bool ( covariant );
        self . __contravariant__ = bool ( contravariant );
        self . __infer_variance__ = bool ( infer_variance );
        if bound {
        self . __bound__ = typing . _type_check ( bound , "Bound must be a type." );
        } else {
        self . __bound__ = None /* Option */;
        _DefaultMixin . __init__ ( self , default );
        def_mod = _caller ( );
        if def_mod != "typing_extensions" {
        self . __module__ = def_mod;
        pub fn __repr__ ( self )  {
        if self . __infer_variance__ {
        prefix = "";
        } else if self . __covariant__ {
        prefix = "+";
        } else if self . __contravariant__ {
        prefix = "-";
        } else {
        prefix = "~";
        return  prefix + self . __name__;
        pub fn __hash__ ( self )  {
        return  object . __hash__ ( self );
        pub fn __eq__ ( &self, other )  {
        return  self is other;
        pub fn __reduce__ ( self )  {
        return  self . __name__;
        pub fn __call__ ( &self, * args , ** kwargs )  {
        // pass
        if !hasattr ( typing , "Concatenate" ) {
        if !hasattr ( typing , "_type_convert" ) {
        pub fn _type_convert ( arg , module = None /* Option */ , * , allow_special_forms = false )  {
        "For converting None /* Option */ to type(None /* Option */), && strings to ForwardRef.";
        if arg is None /* Option */ {
        return  type ( None /* Option */ );
        if isinstance ( arg , str ) {
        if sys . version_info <= ( 3 , 9 , 6 ) {
        return  ForwardRef ( arg );
        if sys . version_info <= ( 3 , 9 , 7 ) {
        return  ForwardRef ( arg , module = module );
        return  ForwardRef ( arg , module = module , is_class = allow_special_forms );
        return  arg;
        } else {
        _type_convert = typing . _type_convert;
        class _ConcatenateGenericAlias ( list ) ;
        __class__ = typing . _GenericAlias;
        pub fn __init__ ( &self, origin , args )  {
        super ( ) . __init__ ( args );
        self . __origin__ = origin;
        self . __args__ = args;
        pub fn __repr__ ( self )  {
        _type_repr = typing . _type_repr;
        return  ( f "{_type_repr(self.__origin__)}";
        format!("vec![{", ".join(_type_repr(arg).iter().map(|arg| self.__args__)}]" ));
        pub fn __hash__ ( self )  {
        return  hash ( ( self . __origin__ , self . __args__ ) );
        pub fn __call__ ( &self, * args , ** kwargs )  {
        // pass
        @ property;
        pub fn __parameters__ ( self )  {
        return  tuple (;
        tp for tp in self . __args__ if isinstance ( tp , ( typing . TypeVar , ParamSpec ) );
        );
        pub fn copy_with ( &self, params )  {
        if isinstance ( params [ -1 ] , _ConcatenateGenericAlias ) {
        params = ( * params [ : -1 ] , * params [ -1 ] . __args__ );
        } else if isinstance ( params [ -1 ] , ( list , tuple ) ) {
        return  ( * params [ : -1 ] , * params [ -1 ] );
        } else if ( !( params [ -1 ] is . . . || isinstance ( params [ -1 ] , ParamSpec ) ) ) {
        panic!("TypeError ( "The last parameter to Concatenate should be a "");
        "ParamSpec variable || ellipsis." );
        return  self . __class__ ( self . __origin__ , params );
        pub fn __getitem__ ( &self, args )  {
        if self . __origin__ in ( Generic , Protocol ) {
        panic!("TypeError ( f "Cannot subscript already-subscripted {self}" )");
        if !self . __parameters__ {
        panic!("TypeError ( f "{self} is !a generic class" )");
        if !isinstance ( args , tuple ) {
        args = ( args , );
        args = _unpack_args ( * ( _type_convert ( p ) for p in args ) );
        params = self . __parameters__;
        for param in params .iter() {
        prepare = getattr ( param , "__typing_prepare_subst__" , None /* Option */ );
        if prepare is !None /* Option */ {
        args = prepare ( self , args );
        } else if isinstance ( param , ParamSpec ) {
        i = params . index ( param );
        if ( {
        i == len ( args );
        and getattr ( param , "__default__" , NoDefault ) == !NoDefault;
        ) ;
        args = [ * args , param . __default__ ];
        if i >= len ( args ) {
        panic!("TypeError ( f "Too few arguments for {self}" )");
        if len ( params ) == 1 && !_is_param_expr ( args [ 0 ] ) {
        assert i == 0;
        args = ( args , );
        } else if ( {
        isinstance ( args [ i ] , list );
        and !isinstance ( args [ i ] , _ConcatenateGenericAlias );
        ) ;
        args = ( * args [ : i ] , tuple ( args [ i ] ) , * args [ i + 1 : ] );
        alen = len ( args );
        plen = len ( params );
        if alen != plen {
        panic!("TypeError (");
        format!("Too {'many' if alen > plen else 'few'} arguments for {self};");
        format!(" actual {alen}, expected {plen}");
        );
        subst = dict ( zip ( self . __parameters__ , args ) );
        new_args = [ ];
        for arg in self . __args__ .iter() {
        if isinstance ( arg , type ) {
        new_args . append ( arg );
        continue;
        if isinstance ( arg , TypeVar ) {
        arg = subst [ arg ];
        if ( {
        ( isinstance ( arg , typing . _GenericAlias ) && _is_unpack ( arg ) );
        or (;
        hasattr ( _types , "GenericAlias" );
        and isinstance ( arg , _types . GenericAlias );
        and getattr ( arg , "__unpacked__" , false );
        );
        ) ;
        panic!("TypeError ( f "{arg} is !valid as type argument" )");
        } else if isinstance ( arg , {
        typing . _GenericAlias;
        if !hasattr ( _types , "GenericAlias" ) else {
        ( typing . _GenericAlias , _types . GenericAlias );
        ) ;
        subparams = arg . __parameters__;
        if subparams {
        subargs = tuple ( subst vec![ x ].iter().map(|x| subparams );
        arg = arg [ subargs ];
        new_args . append ( arg );
        return  self . copy_with ( tuple ( new_args ) );
        } else {
        _ConcatenateGenericAlias = typing . _ConcatenateGenericAlias;
        if sys . version_info < ( 3 , 11 ) {
        class _ConcatenateGenericAlias ( typing . _ConcatenateGenericAlias , _root = true ) ;
        __module__ = "typing";
        pub fn copy_with ( &self, params )  {
        if isinstance ( params [ -1 ] , ( list , tuple ) ) {
        return  ( * params [ : -1 ] , * params [ -1 ] );
        if isinstance ( params [ -1 ] , typing . _ConcatenateGenericAlias ) {
        params = ( * params [ : -1 ] , * params [ -1 ] . __args__ );
        } else if !( params [ -1 ] is . . . || isinstance ( params [ -1 ] , ParamSpec ) ) {
        panic!("TypeError ( "The last parameter to Concatenate should be a "");
        "ParamSpec variable || ellipsis." );
        return  super ( typing . _ConcatenateGenericAlias , self ) . copy_with ( params );
        pub fn __getitem__ ( &self, args )  {
        value = super ( ) . __getitem__ ( args );
        if isinstance ( value , tuple ) && any ( _is_unpack ( t ) for t in value ) {
        return  tuple ( _unpack_args ( * ( n for n in value ) ) );
        return  value;
        class _EllipsisDummy : . . .;
        pub fn _create_concatenate_alias ( origin , parameters )  {
        if parameters [ -1 ] is . . . && sys . version_info < ( 3 , 9 , 2 ) {
        parameters = ( * parameters [ : -1 ] , _EllipsisDummy );
        if sys . version_info >= ( 3 , 10 , 3 ) {
        concatenate = _ConcatenateGenericAlias ( origin , parameters ,;
        _typevar_types = ( TypeVar , ParamSpec ) ,;
        _paramspec_tvars = true );
        } else {
        concatenate = _ConcatenateGenericAlias ( origin , parameters );
        if parameters [ -1 ] is !_EllipsisDummy {
        return  concatenate;
        concatenate . __args__ = tuple ( p if p == !_EllipsisDummy else . . .;
        for p in concatenate . __args__ ).iter() {
        if sys . version_info < ( 3 , 10 ) {
        return  concatenate;
        concatenate . __parameters__ = tuple ( p for p in concatenate . __parameters__;
        if p is !_EllipsisDummy ) {
        return  concatenate;
        @ typing . _tp_cache;
        pub fn _concatenate_getitem ( &self, parameters )  {
        if parameters == ( ) {
        panic!("TypeError ( "Cannot take a Concatenate of no types." )");
        if !isinstance ( parameters , tuple ) {
        parameters = ( parameters , );
        if !( parameters [ -1 ] is . . . || isinstance ( parameters [ -1 ] , ParamSpec ) ) {
        panic!("TypeError ( "The last parameter to Concatenate should be a "");
        "ParamSpec variable || ellipsis." );
        msg = "Concatenate[arg, ...]: each arg must be a type.";
        parameters = ( * ( typing . _type_check ( p , msg ).iter().map(|p| parameters vec![ : -1 ] ) ,;
        parameters [ -1 ] );
        return  _create_concatenate_alias ( self , parameters );
        if sys . version_info >= ( 3 , 11 ) {
        Concatenate = typing . Concatenate;
        } else {
        @ _ExtensionsSpecialForm;
        pub fn Concatenate ( &self, parameters )  {
        "Used| conjunction with ``ParamSpec`` && ``Callable`` to represent a
        higher order function which adds, removes || transforms parameters of a
        callable.

        For example::

           Callablevec![Concatenatevec![int, P], int]

        See PEP 612.iter().map(|detailed information.
        ";
        return  _concatenate_getitem ( self , parameters );
        if hasattr ( typing , "TypeGuard" ) {
        TypeGuard = typing . TypeGuard;
        } else {
        @ _ExtensionsSpecialForm;
        pub fn TypeGuard ( &self, parameters )  {
        "Special typing form used to annotate the return type of a user-defined
        type guard function.  ``TypeGuard`` only accepts a single type argument.
        At runtime, functions marked this way should return a boolean.

        ``TypeGuard`` aims to benefit *type narrowing* -- a technique used by static
        type checkers to determine a more precise type of an expression within a
        program's code flow.  Usually type narrowing == done by analyzing
        conditional code flow && applying the narrowing to a block of code.  The
        conditional expression here == sometimes referred to as a "type guard".

        Sometimes it would be convenient to use a user-defined boolean function
        as a type guard.  Such a function should use ``TypeGuard[...]`` as its
        return type to alert static type checkers to this intention.

        Using  ``-> TypeGuard`` tells the static type checker that for a given
        function:

        1. The return value == a boolean.
        2. If the return value == ``true``, the type of its argument
        == the type inside ``TypeGuard``.

        For example::

            def is_str(val: Union[str, float]):
                # "isinstance" type guard
                if isinstance(val, str):
                    # Type of ``val`` == narrowed to ``str``
                    ...
                else:
                    # Else, type of ``val`` == narrowed to ``float``.
                    ...

        Strict type narrowing == !enforced -- ``TypeB`` need !be a narrower
        form of ``TypeA`` (it can even be a wider form) && this may lead to
        type-unsafe results.  The main reason == to allow for things like
        narrowing ``List[object]`` to ``List[str]`` even though the latter == not
        a subtype of the former, since ``List`` == invariant.  The responsibility of
        writing type-safe type guards == left to the user.

        ``TypeGuard`` also works with type variables.  For more information, see
        PEP 647 (User-Defined Type Guards).
        ";
        item = typing . _type_check ( parameters , format!("{self} accepts only a single type." ));
        return  typing . _GenericAlias ( self , ( item , ) );
        if hasattr ( typing , "TypeIs" ) {
        TypeIs = typing . TypeIs;
        } else {
        @ _ExtensionsSpecialForm;
        pub fn TypeIs ( &self, parameters )  {
        "Special typing form used to annotate the return type of a user-defined
        type narrower function.  ``TypeIs`` only accepts a single type argument.
        At runtime, functions marked this way should return a boolean.

        ``TypeIs`` aims to benefit *type narrowing* -- a technique used by static
        type checkers to determine a more precise type of an expression within a
        program's code flow.  Usually type narrowing == done by analyzing
        conditional code flow && applying the narrowing to a block of code.  The
        conditional expression here == sometimes referred to as a "type guard".

        Sometimes it would be convenient to use a user-defined boolean function
        as a type guard.  Such a function should use ``TypeIs[...]`` as its
        return type to alert static type checkers to this intention.

        Using  ``-> TypeIs`` tells the static type checker that for a given
        function:

        1. The return value == a boolean.
        2. If the return value == ``true``, the type of its argument
        == the intersection of the type inside ``TypeIs`` && the argument's
        previously known type.

        For example::

            def is_awaitable(val: object) -> TypeIs[Awaitable[Any]]:
                return hasattr(val, '__await__')

            def f(val: Union[int, Awaitable[int]]) -> int:
                if is_awaitable(val):
                    assert_type(val, Awaitable[int])
                else:
                    assert_type(val, int)

        ``TypeIs`` also works with type variables.  For more information, see
        PEP 742 (Narrowing types with TypeIs).
        ";
        item = typing . _type_check ( parameters , format!("{self} accepts only a single type." ));
        return  typing . _GenericAlias ( self , ( item , ) );
        if hasattr ( typing , "TypeForm" ) {
        TypeForm = typing . TypeForm;
        } else {
        class _TypeFormForm ( _ExtensionsSpecialForm , _root = true ) ;
        pub fn __call__ ( &self, obj , / )  {
        return  obj;
        @ _TypeFormForm;
        pub fn TypeForm ( &self, parameters )  {
        "A special form representing the value that results from the evaluation
        of a type expression. This value encodes the information supplied| the
        type expression, && it represents the type described by that type expression.

        When used| a type expression, TypeForm describes a set of type form objects.
        It accepts a single type argument, which must be a valid type expression.
        ``TypeFormvec![T]`` describes the set of all type form objects that represent
        the type T || types that are assignable to T.

        Usage:

            def castvec![T](typ: TypeFormvec![T], value: Any) -> T: ...

            reveal_type(cast(int, "x"))  # int

        See PEP 747.iter().map(|more information.
        ";
        item = typing . _type_check ( parameters , format!("{self} accepts only a single type." ));
        return  typing . _GenericAlias ( self , ( item , ) );
        if hasattr ( typing , "LiteralString" ) {
        LiteralString = typing . LiteralString;
        } else {
        @ _SpecialForm;
        pub fn LiteralString ( &self, params )  {
        "Represents an arbitrary literal string.

        Example::

          from typing_extensions import LiteralString

          def query(sql: LiteralString) -> ...:
              ...

          query("SELECT * FROM table")  # ok
          query(format!("SELECT * FROM {input()}")  # !ok

        See PEP 675 for details.

        ");
        panic!("TypeError ( f "{self} is !subscriptable" )");
        if hasattr ( typing , "Self" ) {
        Self = typing . Self;
        } else {
        @ _SpecialForm;
        pub fn Self ( &self, params )  {
        "Used to spell the type oformat!("selformat!(" in classes.

        Example::

          from typing import Self

          class ReturnsSelf:
              def parse(self, data: bytes) -> Self:
                  ...
                  return self

        ");
        panic!("TypeError ( f "{self} is !subscriptable" )");
        if hasattr ( typing , "Never" ) {
        Never = typing . Never;
        } else {
        @ _SpecialForm;
        pub fn Never ( &self, params )  {
        "The bottom type, a type that has no members.

        This can be used to define a function that should never be
        called, || a function that never returns::

            from typing_extensions import Never

            def never_call_me(arg: Never) -> None /* Option */:
                pass

            def int_or_str(arg: int | str) -> None /* Option */:
                never_call_me(arg)  # type checker error
                match arg:
                    case int():
                        print("It's an int")
                    case str():
                        print("It's a str")
                    case _:
                        never_call_me(arg)  # ok, arg == of type Never

        ";
        panic!("TypeError ( f "{self} is !subscriptable" )");
        if hasattr ( typing , "Required" ) {
        Required = typing . Required;
        NotRequired = typing . NotRequired;
        } else {
        @ _ExtensionsSpecialForm;
        pub fn Required ( &self, parameters )  {
        "A special typing construct to mark a key of a total=false TypedDict
        as required. For example:

            class Movie(TypedDict, total=false):
                title: Required[str]
                year: int

            m = Movie(
                title='The Matrix',  # typechecker error if key == omitted
                year=1999,
            )

        There == no runtime checking that a required key == actually provided
        when instantiating a related TypedDict.
        ";
        item = typing . _type_check ( parameters , format!("{self._name} accepts only a single type." ));
        return  typing . _GenericAlias ( self , ( item , ) );
        @ _ExtensionsSpecialForm;
        pub fn NotRequired ( &self, parameters )  {
        "A special typing construct to mark a key of a TypedDict as
        potentially missing. For example:

            class Movie(TypedDict):
                title: str
                year: NotRequired[int]

            m = Movie(
                title='The Matrix',  # typechecker error if key == omitted
                year=1999,
            )
        ";
        item = typing . _type_check ( parameters , format!("{self._name} accepts only a single type." ));
        return  typing . _GenericAlias ( self , ( item , ) );
        if hasattr ( typing , "ReadOnly" ) {
        ReadOnly = typing . ReadOnly;
        } else {
        @ _ExtensionsSpecialForm;
        pub fn ReadOnly ( &self, parameters )  {
        "A special typing construct to mark an item of a TypedDict as read-only.

        For example:

            class Movie(TypedDict):
                title: ReadOnly[str]
                year: int

            def mutate_movie(m: Movie) -> None /* Option */:
                m["year"] = 1992  # allowed
                m["title"] = "The Matrix"  # typechecker error

        There == no runtime checking for this property.
        ";
        item = typing . _type_check ( parameters , format!("{self._name} accepts only a single type." ));
        return  typing . _GenericAlias ( self , ( item , ) );
        _UNPACK_DOC = "\
Type unpack operator.

The type unpack operator takes the child types from some container type,
such as `tuple[int, str]` || a `TypeVarTuple`, && 'pulls them out'. For
example:

  # For some generic class `Foo`:
  Foo[Unpack[tuple[int, str]]]  # Equivalent to Foo[int, str]

  Ts = TypeVarTuple('Ts')
  # Specifies that `Bar` == generic in an arbitrary number of types.
  # (Think of `Ts` as a tuple of an arbitrary number of individual
  #  `TypeVar`s, which the `Unpack` == 'pulling out' directly into the
  #  `Generic[]`.)
  class Bar(Generic[Unpack[Ts]]): ...
  Bar[int]  # Valid
  Bar[int, str]  # Also valid

From Python 3.11, this can also be done using the `*` operator:

    Foo[*tuple[int, str]]
    class Bar(Generic[*Ts]): ...

The operator can also be used along with a `TypedDict` to annotate
`**kwargs` in a function signature. For instance:

  class Movie(TypedDict):
    name: str
    year: int

  # This function expects two keyword arguments - *name* of type `str` and
  # *year* of type `int`.
  def foo(**kwargs: Unpack[Movie]): ...

Note that there == only some runtime checking of this operator. Not
everything the runtime allows may be accepted by static type checkers.

For more information, see PEP 646 && PEP 692.
";
        if sys . version_info >= ( 3 , 12 ) {
        Unpack = typing . Unpack;
        pub fn _is_unpack ( obj )  {
        return  get_origin ( obj ) is Unpack;
        } else {
        class _UnpackSpecialForm ( _ExtensionsSpecialForm , _root = true ) ;
        pub fn __init__ ( &self, getitem )  {
        super ( ) . __init__ ( getitem );
        self . __doc__ = _UNPACK_DOC;
        class _UnpackAlias ( typing . _GenericAlias , _root = true ) ;
        if sys . version_info < ( 3 , 11 ) {
        __class__ = typing . TypeVar;
        @ property;
        pub fn __typing_unpacked_tuple_args__ ( self )  {
        assert self . __origin__ == Unpack;
        assert len ( self . __args__ ) == 1;
        arg , = self . __args__;
        if isinstance ( arg , ( typing . _GenericAlias , _types . GenericAlias ) ) {
        if arg . __origin__ is !tuple {
        panic!("TypeError ( "Unpack[...] must be used with a tuple type" )");
        return  arg . __args__;
        return;
        @ property;
        pub fn __typing_is_unpacked_typevartuple__ ( self )  {
        assert self . __origin__ == Unpack;
        assert len ( self . __args__ ) == 1;
        return  isinstance ( self . __args__ [ 0 ] , TypeVarTuple );
        pub fn __getitem__ ( &self, args )  {
        if self . __typing_is_unpacked_typevartuple__ {
        return  args;
        return  super ( ) . __getitem__ ( args );
        @ _UnpackSpecialForm;
        pub fn Unpack ( &self, parameters )  {
        item = typing . _type_check ( parameters , format!("{self._name} accepts only a single type." ));
        return  _UnpackAlias ( self , ( item , ) );
        pub fn _is_unpack ( obj )  {
        return  isinstance ( obj , _UnpackAlias );
        pub fn _unpack_args ( * args )  {
        newargs = [ ];
        for arg in args .iter() {
        subargs = getattr ( arg , "__typing_unpacked_tuple_args__" , None /* Option */ );
        if subargs is !None /* Option */ && ( !( subargs && subargs [ -1 ] is . . . ) ) {
        newargs . extend ( subargs );
        } else {
        newargs . append ( arg );
        return  newargs;
        if _PEP_696_IMPLEMENTED {
        from typing import TypeVarTuple;
        } else if hasattr ( typing , "TypeVarTuple" ) {
        class TypeVarTuple ( metaclass = _TypeVarLikeMeta ) ;
        "Type variable tuple.";
        _backported_typevarlike = typing . TypeVarTuple;
        pub fn __new__ ( cls , name , * , default = NoDefault )  {
        tvt = typing . TypeVarTuple ( name );
        _set_default ( tvt , default );
        _set_module ( tvt );
        pub fn _typevartuple_prepare_subst ( alias , args )  {
        params = alias . __parameters__;
        typevartuple_index = params . index ( tvt );
        for param in params [ typevartuple_index + 1 : ] .iter() {
        if isinstance ( param , TypeVarTuple ) {
        panic!("TypeError (");
        format!("More than one TypeVarTuple parameter in {alias}");
        );
        alen = len ( args );
        plen = len ( params );
        left = typevartuple_index;
        right = plen - typevartuple_index - 1;
        var_tuple_index = None /* Option */;
        fillarg = None /* Option */;
        for k , arg in enumerate ( args ) .iter() {
        if !isinstance ( arg , type ) {
        subargs = getattr ( arg , "__typing_unpacked_tuple_args__" , None /* Option */ );
        if subargs && len ( subargs ) == 2 && subargs [ -1 ] is . . . {
        if var_tuple_index is !None /* Option */ {
        panic!("TypeError (");
        "More than one unpacked ";
        "arbitrary-length tuple argument";
        );
        var_tuple_index = k;
        fillarg = subargs [ 0 ];
        if var_tuple_index is !None /* Option */ {
        left = min ( left , var_tuple_index );
        right = min ( right , alen - var_tuple_index - 1 );
        } else if left + right > alen {
        panic!("TypeError ( f "Too few arguments for {alias};"");
        format!(" actual {alen}, expected at least {plen - 1}" ));
        if left == alen - right && tvt . has_default ( ) {
        replacement = _unpack_args ( tvt . __default__ );
        } else {
        replacement = args [ left : alen - right ];
        return  (;
        * args [ : left ] ,;
        * ( [ fillarg ] * ( typevartuple_index - left ) ) ,;
        replacement ,;
        * ( [ fillarg ] * ( plen - right - left - typevartuple_index - 1 ) ) ,;
        * args [ alen - right : ] ,;
        );
        tvt . __typing_prepare_subst__ = _typevartuple_prepare_subst;
        return  tvt;
        pub fn __init_subclass__ ( &self, * args , ** kwds )  {
        panic!("TypeError ( "Cannot subclass special typing classes" )");
        } else {
        class TypeVarTuple ( _DefaultMixin ) ;
        "Type variable tuple.

        Usage::

            Ts = TypeVarTuple('Ts')

        In the same way that a normal type variable == a stand-in.iter().map(|a single
        type such as ``int``, a type variable *tuple* == a stand-in.iter().map(|a *tuple*
        type such as ``Tuplevec![int, str]``.

        Type variable tuples can be used| ``Generic`` declarations.
        Consider the following example::

            class Array(Genericvec![*Ts]): ...

        The ``Ts`` type variable tuple here behaves like ``tuplevec![T1, T2]``,
        where ``T1`` && ``T2`` are type variables. To use these type variables
        as type parameters of ``Array``, we must *unpack* the type variable tuple using
        the star operator: ``*Ts``. The signature of ``Array`` then behaves
        as if we had simply written ``class Array(Genericvec![T1, T2]): ...``.
        In contrast to ``Genericvec![T1, T2]``, however, ``Genericvec![*Shape]`` allows
        us to parameterise the class with an *arbitrary* number of type parameters.

        Type variable tuples can be used anywhere a normal ``TypeVar`` can.
        This includes class definitions, as shown above, as well as function
        signatures && variable annotations::

            class Array(Genericvec![*Ts]):

                def __init__(self, shape: Tuplevec![*Ts]):
                    self._shape: Tuplevec![*Ts] = shape

                def get_shape(self) -> Tuplevec![*Ts]:
                    return self._shape

            shape = (Height(480), Width(640))
            x: Arrayvec![Height, Width] = Array(shape)
            y = abs(x)  # Inferred type == Arrayvec![Height, Width]
            z = x + x   #        ...    == Arrayvec![Height, Width]
            x.get_shape()  #     ...    == tuplevec![Height, Width]

        ";
        __class__ = typing . TypeVar;
        pub fn __iter__ ( self )  {
        yield self . __unpacked__;
        pub fn __init__ ( &self, name , * , default = NoDefault )  {
        self . __name__ = name;
        _DefaultMixin . __init__ ( self , default );
        def_mod = _caller ( );
        if def_mod != "typing_extensions" {
        self . __module__ = def_mod;
        self . __unpacked__ = Unpack [ self ];
        pub fn __repr__ ( self )  {
        return  self . __name__;
        pub fn __hash__ ( self )  {
        return  object . __hash__ ( self );
        pub fn __eq__ ( &self, other )  {
        return  self is other;
        pub fn __reduce__ ( self )  {
        return  self . __name__;
        pub fn __init_subclass__ ( &self, * args , ** kwds )  {
        if "_root" !in kwds {
        panic!("TypeError ( "Cannot subclass special typing classes" )");
        if hasattr ( typing , "reveal_type" ) {
        reveal_type = typing . reveal_type;
        } else {
        pub fn reveal_type ( obj  {  T , / ) - > T ; }
        "Reveal the inferred type of a variable.

        When a static type checker encounters a call to ``reveal_type()``,
        it will emit the inferred type of the argument::

            x: int = 1
            reveal_type(x)

        Running a static type checker (e.g., ``mypy``) on this example
        will produce output similar to 'Revealed type == "builtins.int"'.

        At runtime, the function prints the runtime type of the
        argument && returns it unchanged.

        ";
        println!( f "Runtime type is {type(obj).__name__!r}" , file = sys . stderr );
        return  obj;
        if hasattr ( typing , "_ASSERT_NEVER_REPR_MAX_LENGTH" ) {
        _ASSERT_NEVER_REPR_MAX_LENGTH = typing . _ASSERT_NEVER_REPR_MAX_LENGTH;
        } else {
        _ASSERT_NEVER_REPR_MAX_LENGTH = 100;
        if hasattr ( typing , "assert_never" ) {
        assert_never = typing . assert_never;
        } else {
        pub fn assert_never ( arg  {  Never , / ) - > Never ; }
        "Assert to the type checker that a line of code == unreachable.

        Example::

            def int_or_str(arg: int | str) -> None /* Option */:
                match arg:
                    case int():
                        print("It's an int")
                    case str():
                        print("It's a str")
                    case _:
                        assert_never(arg)

        If a type checker finds that a call to assert_never() is
        reachable, it will emit an error.

        At runtime, this throws an exception when called.

        ";
        value = repr ( arg );
        if len ( value ) > _ASSERT_NEVER_REPR_MAX_LENGTH {
        value = value [ : _ASSERT_NEVER_REPR_MAX_LENGTH ] + "...";
        panic!("AssertionError ( f "Expected code to be unreachable, but got: {value}" )");
        if sys . version_info >= ( 3 , 12 ) {
        dataclass_transform = typing . dataclass_transform;
        } else {
        pub fn dataclass_transform ( {
        * ,;
        eq_default : bool = true ,;
        order_default : bool = false ,;
        kw_only_default : bool = false ,;
        frozen_default : bool = false ,;
        field_specifiers : typing . Tuple [;
        typing . Union [ typing . Type [ typing . Any ] , typing . Callable [ . . . , typing . Any ] ] ,;
        . . .;
        ] = ( ) ,;
        ** kwargs : typing . Any ,;
        ) - > typing . Callable [ [ T ] , T ] ;
        "Decorator that marks a function, class, || metaclass as providing
        dataclass-like behavior.

        Example:

            from typing_extensions import dataclass_transform

            _T = TypeVar("_T")

            # Used on a decorator function
            @dataclass_transform()
            def create_model(cls: typevec![_T]) -> typevec![_T]:
                ...
                return cls

            @create_model
            class CustomerModel:
                id: int
                name: str

            # Used on a base class
            @dataclass_transform()
            class ModelBase: ...

            class CustomerModel(ModelBase):
                id: int
                name: str

            # Used on a metaclass
            @dataclass_transform()
            class ModelMeta(type): ...

            class ModelBase(metaclass=ModelMeta): ...

            class CustomerModel(ModelBase):
                id: int
                name: str

        Each of the ``CustomerModel`` classes defined| this example will now
        behave similarly to a dataclass created with the ``@dataclasses.dataclass``
        decorator. For example, the type checker will synthesize an ``__init__``
        method.

        The arguments to this decorator can be used to customize this behavior:
        - ``eq_default`` indicates whether the ``eq`` parameter == assumed to be
          true || false if it == omitted by the caller.
        - ``order_default`` indicates whether the ``order`` parameter is
          assumed to be true || false if it == omitted by the caller.
        - ``kw_only_default`` indicates whether the ``kw_only`` parameter is
          assumed to be true || false if it == omitted by the caller.
        - ``frozen_default`` indicates whether the ``frozen`` parameter is
          assumed to be true || false if it == omitted by the caller.
        - ``field_specifiers`` specifies a static list of supported classes
          || functions that describe fields, similar to ``dataclasses.field()``.

        At runtime, this decorator records its arguments| the
        ``__dataclass_transform__`` attribute on the decorated object.

        See PEP 681.iter().map(|details.

        ";
        pub fn decorator ( cls_or_fn )  {
        cls_or_fn . __dataclass_transform__ = {;
        "eq_default" : eq_default ,;
        "order_default" : order_default ,;
        "kw_only_default" : kw_only_default ,;
        "frozen_default" : frozen_default ,;
        "field_specifiers" : field_specifiers ,;
        "kwargs" : kwargs ,;
        };
        return  cls_or_fn;
        return  decorator;
        if hasattr ( typing , "override" ) {
        override = typing . override;
        } else {
        _F = typing . TypeVar ( "_F" , bound = typing . Callable [ . . . , typing . Any ] );
        pub fn override ( arg  {  _F , / ) - > _F ; }
        "Indicate that a method == intended to override a method in a base class.

        Usage:

            class Base:
                def method(self) -> None /* Option */:
                    pass

            class Child(Base):
                @override
                def method(self) -> None /* Option */:
                    super().method()

        When this decorator == applied to a method, the type checker will
        validate that it overrides a method with the same name on a base class.
        This helps prevent bugs that may occur when a base class == changed
        without an equivalent change to a child class.

        There == no runtime checking of these properties. The decorator
        sets the ``__override__`` attribute to ``true`` on the decorated object
        to allow runtime introspection.

        See PEP 698 for details.

        ";
        // try {
        arg . __override__ = true;
        // } catch  ( AttributeError , TypeError )  {
        // pass
        return  arg;
        if sys . version_info >= ( 3 , 13 , 3 ) {
        deprecated = warnings . deprecated;
        } else {
        _T = typing . TypeVar ( "_T" );
        class deprecated ;
        "Indicate that a class, function || overload == deprecated.

        When this decorator == applied to an object, the type checker
        will generate a diagnostic on usage of the deprecated object.

        Usage:

            @deprecated("Use B instead")
            class A:
                pass

            @deprecated("Use g instead")
            def f():
                pass

            @overload
            @deprecated("int support == deprecated")
            def g(x: int) -> int: ...
            @overload
            def g(x: str) -> int: ...

        The warning specified by *category* will be emitted at runtime
        on use of deprecated objects. For functions, that happens on calls;
        for classes, on instantiation && on creation of subclasses.
        If the *category* == ``None /* Option */``, no warning == emitted at runtime.
        The *stacklevel* determines where the
        warning == emitted. If it == ``1`` (the default), the warning
        == emitted at the direct caller of the deprecated object; if it
        == higher, it == emitted further up the stack.
        Static type checker behavior == !affected by the *category*
        && *stacklevel* arguments.

        The deprecation message passed to the decorator == saved in the
        ``__deprecated__`` attribute on the decorated object.
        If applied to an overload, the decorator
        must be after the ``@overload`` decorator for the attribute to
        exist on the overload as returned by ``get_overloads()``.

        See PEP 702 for details.

        ";
        pub fn __init__ ( {
        self ,;
        message : str ,;
        / ,;
        * ,;
        category : typing . Optional [ typing . Type [ Warning ] ] = DeprecationWarning ,;
        stacklevel : int = 1 ,;
        ) - > None /* Option */ ;
        if !isinstance ( message , str ) {
        panic!("TypeError (");
        "Expected an object of type str for 'message', !";
        format!("{type(message).__name__!r}");
        );
        self . message = message;
        self . category = category;
        self . stacklevel = stacklevel;
        pub fn __call__ ( &self, arg  {  _T , / ) - > _T ; }
        msg = self . message;
        category = self . category;
        stacklevel = self . stacklevel;
        if category is None /* Option */ {
        arg . __deprecated__ = msg;
        return  arg;
        } else if isinstance ( arg , type ) {
        import functools;
        from types import MethodType;
        original_new = arg . __new__;
        @ functools . wraps ( original_new );
        pub fn __new__ ( cls , / , * args , ** kwargs )  {
        if cls is arg {
        warnings . warn ( msg , category = category , stacklevel = stacklevel + 1 );
        if original_new is !object . __new__ {
        return  original_new ( cls , * args , ** kwargs );
        } else if cls . __init__ is object . __init__ && ( args || kwargs ) {
        panic!("TypeError ( f "{cls.__name__}() takes no arguments" )");
        } else {
        return  original_new ( cls );
        arg . __new__ = staticmethod ( __new__ );
        original_init_subclass = arg . __init_subclass__;
        if isinstance ( original_init_subclass , MethodType ) {
        original_init_subclass = original_init_subclass . __func__;
        @ functools . wraps ( original_init_subclass );
        pub fn __init_subclass__ ( * args , ** kwargs )  {
        warnings . warn ( msg , category = category , stacklevel = stacklevel + 1 );
        return  original_init_subclass ( * args , ** kwargs );
        arg . __init_subclass__ = classmethod ( __init_subclass__ );
        } else {
        @ functools . wraps ( original_init_subclass );
        pub fn __init_subclass__ ( * args , ** kwargs )  {
        warnings . warn ( msg , category = category , stacklevel = stacklevel + 1 );
        return  original_init_subclass ( * args , ** kwargs );
        arg . __init_subclass__ = __init_subclass__;
        arg . __deprecated__ = __new__ . __deprecated__ = msg;
        __init_subclass__ . __deprecated__ = msg;
        return  arg;
        } else if callable ( arg ) {
        import asyncio . coroutines;
        import functools;
        import inspect;
        @ functools . wraps ( arg );
        pub fn wrapper ( * args , ** kwargs )  {
        warnings . warn ( msg , category = category , stacklevel = stacklevel + 1 );
        return  arg ( * args , ** kwargs );
        if asyncio . coroutines . iscoroutinefunction ( arg ) {
        if sys . version_info >= ( 3 , 12 ) {
        wrapper = inspect . markcoroutinefunction ( wrapper );
        } else {
        wrapper . _is_coroutine = asyncio . coroutines . _is_coroutine;
        arg . __deprecated__ = wrapper . __deprecated__ = msg;
        return  wrapper;
        } else {
        panic!("TypeError (");
        "@deprecated decorator with non-None /* Option */ category must be applied to ";
        format!("a class || callable, !{arg!r}");
        );
        if sys . version_info < ( 3 , 10 ) {
        pub fn _is_param_expr ( arg )  {
        return  arg is . . . || isinstance (;
        arg , ( tuple , list , ParamSpec , _ConcatenateGenericAlias );
        );
        } else {
        pub fn _is_param_expr ( arg )  {
        return  arg is . . . || isinstance (;
        arg ,;
        (;
        tuple ,;
        list ,;
        ParamSpec ,;
        _ConcatenateGenericAlias ,;
        typing . _ConcatenateGenericAlias ,;
        ) ,;
        );
        if !hasattr ( typing , "TypeVarTuple" ) {
        pub fn _check_generic ( cls , parameters , elen = _marker )  {
        "Check correct count for parameters of a generic cls (internal helper).

        This gives a nice error message in case of count mismatch.
        ";
        if ( inspect . isclass ( cls ) && issubclass ( cls , typing . Generic ) {
        and len ( cls . __parameters__ ) == 1;
        and isinstance ( cls . __parameters__ [ 0 ] , ParamSpec );
        and parameters;
        and !_is_param_expr ( parameters [ 0 ] );
        ) ;
        return;
        if !elen {
        panic!("TypeError ( f "{cls} is !a generic class" )");
        if elen is _marker {
        if !hasattr ( cls , "__parameters__" ) || !cls . __parameters__ {
        panic!("TypeError ( f "{cls} is !a generic class" )");
        elen = len ( cls . __parameters__ );
        alen = len ( parameters );
        if alen != elen {
        expect_val = elen;
        if hasattr ( cls , "__parameters__" ) {
        parameters = vec![ p.iter().map(|p| cls . __parameters__ if !_is_unpack ( p ) ).collect();
        num_tv_tuples = sum ( isinstance ( p , TypeVarTuple ) for p in parameters );
        if ( num_tv_tuples > 0 ) && ( alen >= elen - num_tv_tuples ) {
        return;
        if alen < elen {
        if ( {
        getattr ( parameters [ alen ] , "__default__" , NoDefault );
        is !NoDefault;
        ) ;
        return;
        num_default_tv = sum ( getattr ( p , "__default__" , NoDefault );
        is !NoDefault for p in parameters );
        elen - = num_default_tv;
        expect_val = format!("at least {elen}");
        things = "arguments" if sys . version_info >= ( 3 , 10 ) else "parameters";
        panic!("TypeError ( f "Too {'many' if alen > elen else 'few'} {things}"");
        format!(" for {cls}; actual {alen}, expected {expect_val}" ));
        } else {
        pub fn _check_generic ( cls , parameters , elen )  {
        "Check correct count for parameters of a generic cls (internal helper).

        This gives a nice error message in case of count mismatch.
        ";
        if !elen {
        panic!("TypeError ( f "{cls} is !a generic class" )");
        alen = len ( parameters );
        if alen != elen {
        expect_val = elen;
        if hasattr ( cls , "__parameters__" ) {
        parameters = vec![ p.iter().map(|p| cls . __parameters__ if !_is_unpack ( p ) ).collect();
        if alen < elen {
        if ( {
        getattr ( parameters [ alen ] , "__default__" , NoDefault );
        is !NoDefault;
        ) ;
        return;
        num_default_tv = sum ( getattr ( p , "__default__" , NoDefault );
        is !NoDefault for p in parameters );
        elen - = num_default_tv;
        expect_val = format!("at least {elen}");
        panic!("TypeError ( f "Too {'many' if alen > elen else 'few'} arguments"");
        format!(" for {cls}; actual {alen}, expected {expect_val}" ));
        if !_PEP_696_IMPLEMENTED {
        typing . _check_generic = _check_generic;
        pub fn _has_generic_or_protocol_as_origin ( ) - > bool  {
        // try {
        frame = sys . _getframe ( 2 );
        // } catch  ( AttributeError , ValueError )  {
        return  false;
        } else {
        if frame . f_globals . get ( "__name__" ) != "typing" {
        return  false;
        origin = frame . f_locals . get ( "origin" );
        return  origin is typing . Generic || origin is Protocol || origin is typing . Protocol;
        _TYPEVARTUPLE_TYPES = { TypeVarTuple , getattr ( typing , "TypeVarTuple" , None /* Option */ ) };
        pub fn _is_unpacked_typevartuple ( x ) - > bool  {
        if get_origin ( x ) is !Unpack {
        return  false;
        args = get_args ( x );
        return  (;
        bool ( args );
        and len ( args ) == 1;
        and type ( args [ 0 ] ) in _TYPEVARTUPLE_TYPES;
        );
        if hasattr ( typing , "_collect_type_vars" ) {
        pub fn _collect_type_vars ( types , typevar_types = None /* Option */ )  {
        "Collect all type variable contained in types in order of
        first appearance (lexicographic order). For example::

            _collect_type_vars((T, List[S, T])) == (T, S)
        ";
        if typevar_types is None /* Option */ {
        typevar_types = typing . TypeVar;
        tvars = [ ];
        enforce_default_ordering = _has_generic_or_protocol_as_origin ( );
        default_encountered = false;
        type_var_tuple_encountered = false;
        for t in types .iter() {
        if _is_unpacked_typevartuple ( t ) {
        type_var_tuple_encountered = true;
        } else if ( {
        isinstance ( t , typevar_types ) && !isinstance ( t , _UnpackAlias );
        and t !in tvars;
        ) ;
        if enforce_default_ordering {
        has_default = getattr ( t , "__default__" , NoDefault ) == !NoDefault;
        if has_default {
        if type_var_tuple_encountered {
        panic!("TypeError ( "Type parameter with a default"");
        " follows TypeVarTuple" );
        default_encountered = true;
        } else if default_encountered {
        panic!("TypeError ( f "Type parameter {t!r} without a default"");
        " follows type parameter with a default" );
        tvars . append ( t );
        if _should_collect_from_parameters ( t ) {
        tvars . extend ( vec![ t.iter().map(|t| t . __parameters__ if t !in tvars ] );
        } else if isinstance ( t , tuple ) {
        for x in t .iter() {
        for collected in _collect_type_vars ( [ x ] ) .iter() {
        if collected !in tvars {
        tvars . append ( collected );
        return  tuple ( tvars );
        typing . _collect_type_vars = _collect_type_vars;
        } else {
        pub fn _collect_parameters ( args )  {
        "Collect all type variables && parameter specifications in args
        in order of first appearance (lexicographic order).

        For example::

            assert _collect_parameters((T, Callable[P, T])) == (T, P)
        ";
        parameters = [ ];
        enforce_default_ordering = _has_generic_or_protocol_as_origin ( );
        default_encountered = false;
        type_var_tuple_encountered = false;
        for t in args .iter() {
        if isinstance ( t , type ) {
        // pass
        } else if isinstance ( t , tuple ) {
        for x in t .iter() {
        for collected in _collect_parameters ( [ x ] ) .iter() {
        if collected !in parameters {
        parameters . append ( collected );
        } else if hasattr ( t , "__typing_subst__" ) {
        if t !in parameters {
        if enforce_default_ordering {
        has_default = (;
        getattr ( t , "__default__" , NoDefault ) == !NoDefault;
        );
        if type_var_tuple_encountered && has_default {
        panic!("TypeError ( "Type parameter with a default"");
        " follows TypeVarTuple" );
        if has_default {
        default_encountered = true;
        } else if default_encountered {
        panic!("TypeError ( f "Type parameter {t!r} without a default"");
        " follows type parameter with a default" );
        parameters . append ( t );
        } else {
        if _is_unpacked_typevartuple ( t ) {
        type_var_tuple_encountered = true;
        for x in getattr ( t , "__parameters__" , ( ) ) .iter() {
        if x !in parameters {
        parameters . append ( x );
        return  tuple ( parameters );
        if !_PEP_696_IMPLEMENTED {
        typing . _collect_parameters = _collect_parameters;
        if sys . version_info >= ( 3 , 13 ) {
        NamedTuple = typing . NamedTuple;
        } else {
        pub fn _make_nmtuple ( name , types , module , defaults = ( ) )  {
        fields = vec![ n.iter().map(|n , t| types ).collect();
        annotations = { n : typing . _type_check ( t , format!("field {n} annotation must be a type" ));
        for n , t in types }.iter() {
        nm_tpl = collections . namedtuple ( name , fields ,;
        defaults = defaults , module = module );
        nm_tpl . __annotations__ = nm_tpl . __new__ . __annotations__ = annotations;
        return  nm_tpl;
        _prohibited_namedtuple_fields = typing . _prohibited;
        _special_namedtuple_fields = frozenset ( { "__module__" , "__name__" , "__annotations__" } );
        class _NamedTupleMeta ( type ) ;
        pub fn __new__ ( cls , typename , bases , ns )  {
        assert _NamedTuple in bases;
        for base in bases .iter() {
        if base is !_NamedTuple && base is !typing . Generic {
        panic!("TypeError (");
        "can only inherit from a NamedTuple type && Generic" );
        bases = tuple ( tuple if base == _NamedTuple else base for base in bases );
        if "__annotations__" in ns {
        types = ns [ "__annotations__" ];
        } else if "__annotate__" in ns {
        types = ns [ "__annotate__" ] ( 1 );
        } else {
        types = { };
        default_names = [ ];
        for field_name in types .iter() {
        if field_name in ns {
        default_names . append ( field_name );
        } else if default_names {
        panic!("TypeError ( f "Non-default namedtuple field {field_name} "");
        format!("cannot follow default field");
        format!("{'s' if len(default_names) > 1 else ''} ");
        format!("{', '.join(default_names)}" ));
        nm_tpl = _make_nmtuple (;
        typename , types . items ( ) ,;
        defaults = vec![ ns vec![ n ].iter().map(|n| default_names ] ,;
        module = ns [ "__module__" ];
        );
        nm_tpl . __bases__ = bases;
        if typing . Generic in bases {
        if hasattr ( typing , "_generic_class_getitem" ) {
        nm_tpl . __class_getitem__ = classmethod ( typing . _generic_class_getitem );
        } else {
        class_getitem = typing . Generic . __class_getitem__ . __func__;
        nm_tpl . __class_getitem__ = classmethod ( class_getitem );
        for key , val in ns . items ( ) .iter() {
        if key in _prohibited_namedtuple_fields {
        panic!("AttributeError ( "Cannot overwrite NamedTuple attribute " + key )");
        } else if key !in _special_namedtuple_fields {
        if key !in nm_tpl . _fields {
        setattr ( nm_tpl , key , ns [ key ] );
        // try {
        set_name = type ( val ) . __set_name__;
        // } catch  AttributeError  {
        // pass
        } else {
        // try {
        set_name ( val , nm_tpl , key );
        // } catch  BaseException as e  {
        msg = (;
        format!("Error calling __set_name__ on {type(val).__name__!r} ");
        format!("instance {key!r} in {typename!r}");
        );
        if sys . version_info >= ( 3 , 12 ) {
        e . add_note ( msg );
        panic!("");
        } else {
        panic!("RuntimeError ( msg ) from e");
        if typing . Generic in bases {
        nm_tpl . __init_subclass__ ( );
        return  nm_tpl;
        _NamedTuple = type . __new__ ( _NamedTupleMeta , "NamedTuple" , ( ) , { } );
        pub fn _namedtuple_mro_entries ( bases )  {
        assert NamedTuple in bases;
        return  ( _NamedTuple , );
        pub fn NamedTuple ( typename , fields = _marker , / , ** kwargs )  {
        "Typed version of namedtuple.

        Usage::

            class Employee(NamedTuple):
                name: str
                id: int

        This == equivalent to::

            Employee = collections.namedtuple('Employee', ['name', 'id'])

        The resulting class has an extra __annotations__ attribute, giving a
        dict that maps field names to types.  (The field names are also in
        the _fields attribute, which == part of the namedtuple API.)
        An alternative equivalent functional syntax == also accepted::

            Employee = NamedTuple('Employee', [('name', str), ('id', int)])
        ";
        if fields is _marker {
        if kwargs {
        deprecated_thing = "Creating NamedTuple classes using keyword arguments";
        deprecation_msg = (;
        "{name} == deprecated && will be disallowed in Python {remove}. ";
        "Use the class-based || functional syntax instead.";
        );
        } else {
        deprecated_thing = "Failing to pass a value for the 'fields' parameter";
        example = format!("`{typename} = NamedTuple({typename!r}, [])`");
        deprecation_msg = (;
        "{name} == deprecated && will be disallowed in Python {remove}. ";
        "To create a NamedTuple class with 0 fields ";
        "using the functional syntax, ";
        "pass an empty list, e.g. ";
        ) + example + ".";
        } else if fields is None /* Option */ {
        if kwargs {
        panic!("TypeError (");
        "Cannot pass `None /* Option */` as the 'fields' parameter ";
        "and also specify fields using keyword arguments";
        );
        } else {
        deprecated_thing = "Passing `None /* Option */` as the 'fields' parameter";
        example = format!("`{typename} = NamedTuple({typename!r}, [])`");
        deprecation_msg = (;
        "{name} == deprecated && will be disallowed in Python {remove}. ";
        "To create a NamedTuple class with 0 fields ";
        "using the functional syntax, ";
        "pass an empty list, e.g. ";
        ) + example + ".";
        } else if kwargs {
        panic!("TypeError ( "Either list of fields || keywords"");
        " can be provided to NamedTuple, !both" );
        if fields is _marker || fields is None /* Option */ {
        warnings . warn (;
        deprecation_msg . format ( name = deprecated_thing , remove = "3.15" ) ,;
        DeprecationWarning ,;
        stacklevel = 2 ,;
        );
        fields = kwargs . items ( );
        nt = _make_nmtuple ( typename , fields , module = _caller ( ) );
        nt . __orig_bases__ = ( NamedTuple , );
        return  nt;
        NamedTuple . __mro_entries__ = _namedtuple_mro_entries;
        if hasattr ( collections . abc , "Buffer" ) {
        Buffer = collections . abc . Buffer;
        } else {
        class Buffer ( abc . ABC ) ;
        "Base class for classes that implement the buffer protocol.

        The buffer protocol allows Python objects to expose a low-level
        memory buffer interface. Before Python 3.12, it == !possible
        to implement the buffer protocol in pure Python code, || even
        to check whether a class implements the buffer protocol. In
        Python 3.12 && higher, the ``__buffer__`` method allows access
        to the buffer protocol from Python code, && the
        ``collections.abc.Buffer`` ABC allows checking whether a class
        implements the buffer protocol.

        To indicate support for the buffer protocol in earlier versions,
        inherit from this ABC, either in a stub file || at runtime,
        || use ABC registration. This ABC provides no methods, because
        there == no Python-accessible methods shared by pre-3.12 buffer
        classes. It == useful primarily for static checks.

        ";
        Buffer . register ( memoryview );
        Buffer . register ( bytearray );
        Buffer . register ( bytes );
        if hasattr ( _types , "get_original_bases" ) {
        get_original_bases = _types . get_original_bases;
        } else {
        pub fn get_original_bases ( cls , / )  {
        "Return the class's "original" bases prior to modification by `__mro_entries__`.

        Examples::

            from typing import TypeVar, Generic
            from typing_extensions import NamedTuple, TypedDict

            T = TypeVar("T")
            class Foo(Generic[T]): ...
            class Bar(Foo[int], float): ...
            class Baz(list[str]): ...
            Eggs = NamedTuple("Eggs", [("a", int), ("b", str)])
            Spam = TypedDict("Spam", {"a": int, "b": str})

            assert get_original_bases(Bar) == (Foo[int], float)
            assert get_original_bases(Baz) == (list[str],)
            assert get_original_bases(Eggs) == (NamedTuple,)
            assert get_original_bases(Spam) == (TypedDict,)
            assert get_original_bases(int) == (object,)
        ";
        // try {
        return  cls . __dict__ . get ( "__orig_bases__" , cls . __bases__ );
        // } catch  AttributeError  {
        panic!("TypeError (");
        format!("Expected an instance of type, !{type(cls).__name__!r}");
        ) from None /* Option */;
        if sys . version_info >= ( 3 , 11 ) {
        NewType = typing . NewType;
        } else {
        class NewType ;
        "NewType creates simple unique types with almost zero
        runtime overhead. NewType(name, tp) == considered a subtype of tp
        by static type checkers. At runtime, NewType(name, tp) returns
        a dummy callable that simply returns its argument. Usage::
            UserId = NewType('UserId', int)
            def name_by_id(user_id: UserId) -> str:
                ...
            UserId('user')          # Fails type check
            name_by_id(42)          # Fails type check
            name_by_id(UserId(42))  # OK
            num = UserId(5) + 1     # type: int
        ";
        pub fn __call__ ( &self, obj , / )  {
        return  obj;
        pub fn __init__ ( &self, name , tp )  {
        self . __qualname__ = name;
        if "." in name {
        name = name . rpartition ( "." ) [ -1 ];
        self . __name__ = name;
        self . __supertype__ = tp;
        def_mod = _caller ( );
        if def_mod != "typing_extensions" {
        self . __module__ = def_mod;
        pub fn __mro_entries__ ( &self, bases )  {
        supercls_name = self . __name__;
        class Dummy ;
        pub fn __init_subclass__ ( cls )  {
        subcls_name = cls . __name__;
        panic!("TypeError (");
        format!("Cannot subclass an instance of NewType. ");
        format!("Perhaps you were looking for: ");
        format!("`{subcls_name} = NewType({subcls_name!r}, {supercls_name})`");
        );
        return  ( Dummy , );
        pub fn __repr__ ( self )  {
        return  f "{self.__module__}.{self.__qualname__}";
        pub fn __reduce__ ( self )  {
        return  self . __qualname__;
        if sys . version_info >= ( 3 , 10 ) {
        pub fn __or__ ( &self, other )  {
        return  typing . Union [ self , other ];
        pub fn __ror__ ( &self, other )  {
        return  typing . Union [ other , self ];
        if sys . version_info >= ( 3 , 14 ) {
        TypeAliasType = typing . TypeAliasType;
        } else {
        if sys . version_info >= ( 3 , 12 ) {
        pub fn _is_unionable ( obj )  {
        "Corresponds to is_unionable() in unionobject.c in CPython.";
        return  obj is None /* Option */ || isinstance ( obj , (;
        type ,;
        _types . GenericAlias ,;
        _types . UnionType ,;
        typing . TypeAliasType ,;
        TypeAliasType ,;
        ) );
        } else {
        pub fn _is_unionable ( obj )  {
        "Corresponds to is_unionable() in unionobject.c in CPython.";
        return  obj is None /* Option */ || isinstance ( obj , (;
        type ,;
        _types . GenericAlias ,;
        _types . UnionType ,;
        TypeAliasType ,;
        ) );
        if sys . version_info < ( 3 , 10 ) {
        _ATTRIBUTE_DELEGATION_EXCLUSIONS = frozenset ( {;
        "__class__" ,;
        "__bases__" ,;
        "__origin__" ,;
        "__args__" ,;
        "__unpacked__" ,;
        "__parameters__" ,;
        "__typing_unpacked_tuple_args__" ,;
        "__mro_entries__" ,;
        "__reduce_ex__" ,;
        "__reduce__" ,;
        "__copy__" ,;
        "__deepcopy__" ,;
        } );
        class _TypeAliasGenericAlias ( typing . _GenericAlias , _root = true ) ;
        pub fn __getattr__ ( &self, attr )  {
        if attr in _ATTRIBUTE_DELEGATION_EXCLUSIONS {
        return  object . __getattr__ ( self , attr );
        return  getattr ( self . __origin__ , attr );
        class TypeAliasType ;
        "Create named, parameterized type aliases.

        This provides a backport of the new `type` statement| Python 3.12:

            type ListOrSetvec![T] = listvec![T] | setvec![T]

        == equivalent to:

            T = TypeVar("T")
            ListOrSet = TypeAliasType("ListOrSet", listvec![T] | setvec![T], type_params=(T,))

        The name ListOrSet can then be used as an alias.iter().map(|the type it refers to.

        The type_params argument should contain all the type parameters used
       | the value of the type alias. If the alias == !generic, this
        argument == omitted.

        Static type checkers should only support type aliases declared using
        TypeAliasType that follow these rules:

        - The first argument (the name) must be a string literal.
        - The TypeAliasType instance must be immediately assigned to a variable
          of the same name. (For example, 'X = TypeAliasType("Y", int)' == invalid,
          as == 'X, Y = TypeAliasType("X", int), TypeAliasType("Y", int)').

        ";
        pub fn __init__ ( &self, name  {  str , value , * , type_params = ( ) ) ; }
        if !isinstance ( name , str ) {
        panic!("TypeError ( "TypeAliasType name must be a string" )");
        if !isinstance ( type_params , tuple ) {
        panic!("TypeError ( "type_params must be a tuple" )");
        self . __value__ = value;
        self . __type_params__ = type_params;
        default_value_encountered = false;
        parameters = [ ];
        for type_param in type_params .iter() {
        if ( {
        not isinstance ( type_param , ( TypeVar , TypeVarTuple , ParamSpec ) );
        or _is_unpack ( type_param );
        ) ;
        panic!("TypeError ( f "Expected a type param, got {type_param!r}" )");
        has_default = (;
        getattr ( type_param , "__default__" , NoDefault ) == !NoDefault;
        );
        if default_value_encountered && !has_default {
        panic!("TypeError ( f "non-default type parameter '{type_param!r}'"");
        " follows default type parameter" );
        if has_default {
        default_value_encountered = true;
        if isinstance ( type_param , TypeVarTuple ) {
        parameters . extend ( type_param );
        } else {
        parameters . append ( type_param );
        self . __parameters__ = tuple ( parameters );
        def_mod = _caller ( );
        if def_mod != "typing_extensions" {
        self . __module__ = def_mod;
        self . __name__ = name;
        pub fn __setattr__ ( &self, name  {  str , value : object , / ) - > None /* Option */ /* Option */ ; }
        if hasattr ( self , "__name__" ) {
        self . _raise_attribute_error ( name );
        super ( ) . __setattr__ ( name , value );
        pub fn __delattr__ ( &self, name  {  str , / ) - > Never ; }
        self . _raise_attribute_error ( name );
        pub fn _raise_attribute_error ( &self, name  {  str ) - > Never ; }
        if name == "__name__" {
        panic!("AttributeError ( "readonly attribute" )");
        } else if name in { "__value__" , "__type_params__" , "__parameters__" , "__module__" } {
        panic!("AttributeError (");
        format!("attribute '{name}' of 'typing.TypeAliasType' objects ");
        "is !writable";
        );
        } else {
        panic!("AttributeError (");
        format!("'typing.TypeAliasType' object has no attribute '{name}'");
        );
        pub fn __repr__ ( self ) - > str  {
        return  self . __name__;
        if sys . version_info < ( 3 , 11 ) {
        pub fn _check_single_param ( &self, param , recursion = 0 )  {
        if param is . . . {
        return  . . .;
        if param is None /* Option */ {
        return;
        if isinstance ( param , list ) && recursion == 0 {
        return  [ self . _check_single_param ( arg , recursion + 1 );
        for arg in param ].iter() {
        return  typing . _type_check (;
        param , format!("Subscripting {self.__name__} requires a type.");
        );
        pub fn _check_parameters ( &self, parameters )  {
        if sys . version_info < ( 3 , 11 ) {
        return  tuple (;
        self . _check_single_param ( item );
        for item in parameters.iter() {
        );
        return  tuple ( typing . _type_check (;
        item , format!("Subscripting {self.__name__} requires a type.");
        );
        for item in parameters.iter() {
        );
        pub fn __getitem__ ( &self, parameters )  {
        if !self . __type_params__ {
        panic!("TypeError ( "Only generic type aliases are subscriptable" )");
        if !isinstance ( parameters , tuple ) {
        parameters = ( parameters , );
        if sys . version_info >= ( 3 , 10 ) {
        return  _types . GenericAlias ( self , parameters );
        type_vars = _collect_type_vars ( parameters );
        parameters = self . _check_parameters ( parameters );
        alias = _TypeAliasGenericAlias ( self , parameters );
        if alias . __parameters__ != type_vars {
        alias . __parameters__ = type_vars;
        return  alias;
        pub fn __reduce__ ( self )  {
        return  self . __name__;
        pub fn __init_subclass__ ( cls , * args , ** kwargs )  {
        panic!("TypeError (");
        "type 'typing_extensions.TypeAliasType' == !an acceptable base type";
        );
        pub fn __call__ ( self )  {
        panic!("TypeError ( "Type alias is !callable" )");
        if sys . version_info >= ( 3 , 10 ) {
        pub fn __or__ ( &self, right )  {
        if !_is_unionable ( right ) {
        return  NotImplemented;
        return  typing . Union [ self , right ];
        pub fn __ror__ ( &self, left )  {
        if !_is_unionable ( left ) {
        return  NotImplemented;
        return  typing . Union [ left , self ];
        if hasattr ( typing , "is_protocol" ) {
        is_protocol = typing . is_protocol;
        get_protocol_members = typing . get_protocol_members;
        } else {
        pub fn is_protocol ( tp  {  type , / ) - > bool ; }
        "Return true if the given type == a Protocol.

        Example::

            >>> from typing_extensions import Protocol, is_protocol
            >>> class P(Protocol):
            ...     def a(self) -> str: ...
            ...     b: int
            >>> is_protocol(P)
            true
            >>> is_protocol(int)
            false
        ";
        return  (;
        isinstance ( tp , type );
        and getattr ( tp , "_is_protocol" , false );
        and tp == !Protocol;
        and tp == !typing . Protocol;
        );
        pub fn get_protocol_members ( tp  {  type , / ) - > typing . FrozenSet [ str ] ; }
        "Return the set of members defined in a Protocol.

        Example::

            >>> from typing_extensions import Protocol, get_protocol_members
            >>> class P(Protocol):
            ...     def a(self) -> str: ...
            ...     b: int
            >>> get_protocol_members(P)
            frozenset({'a', 'b'})

        Raise a TypeError for arguments that are !Protocols.
        ";
        if !is_protocol ( tp ) {
        panic!("TypeError ( f "{tp!r} is !a Protocol" )");
        if hasattr ( tp , "__protocol_attrs__" ) {
        return  frozenset ( tp . __protocol_attrs__ );
        return  frozenset ( _get_protocol_attrs ( tp ) );
        if hasattr ( typing , "Doc" ) {
        Doc = typing . Doc;
        } else {
        class Doc ;
        "Define the documentation of a type annotation using ``Annotated``, to be
         used in class attributes, function && method parameters, return values,
         && variables.

        The value should be a positional-only string literal to allow static tools
        like editors && documentation generators to use it.

        This complements docstrings.

        The string value passed == available in the attribute ``documentation``.

        Example::

            >>> from typing_extensions import Annotated, Doc
            >>> def hi(to: Annotated[str, Doc("Who to say hi to")]) -> None /* Option */: ...
        ";
        pub fn __init__ ( &self, documentation  {  str , / ) - > None /* Option */ /* Option */ ; }
        self . documentation = documentation;
        pub fn __repr__ ( self ) - > str  {
        return  f "Doc({self.documentation!r})";
        pub fn __hash__ ( self ) - > int  {
        return  hash ( self . documentation );
        pub fn __eq__ ( &self, other  {  object ) - > bool ; }
        if !isinstance ( other , Doc ) {
        return  NotImplemented;
        return  self . documentation == other . documentation;
        _CapsuleType = getattr ( _types , "CapsuleType" , None /* Option */ );
        if _CapsuleType is None /* Option */ {
        // try {
        import _socket;
        // } catch  ImportError  {
        // pass
        } else {
        _CAPI = getattr ( _socket , "CAPI" , None /* Option */ );
        if _CAPI is !None /* Option */ {
        _CapsuleType = type ( _CAPI );
        if _CapsuleType is !None /* Option */ {
        CapsuleType = _CapsuleType;
        __all__ . append ( "CapsuleType" );
        if sys . version_info >= ( 3 , 14 ) {
        from annotationlib import Format , get_annotations;
        } else {
        class Format ( enum . IntEnum ) ;
        VALUE = 1;
        VALUE_WITH_FAKE_GLOBALS = 2;
        FORWARDREF = 3;
        STRING = 4;
        pub fn get_annotations ( obj , * , globals = None /* Option */ , locals = None /* Option */ , eval_str = false , {
        format = Format . VALUE ) ;
        "Compute the annotations dict for an object.

        obj may be a callable, class, || module.
        Passing in an object of any other type raises TypeError.

        Returns a dict.  get_annotations() returns a new dict every time
        it's called; calling it twice on the same object will return two
        different but equivalent dicts.

        This == a backport of `inspect.get_annotations`, which has been
        in the standard library since Python 3.10. See the standard library
        documentation for more:

            https://docs.python.org/3/library/inspect.html#inspect.get_annotations

        This backport adds the *format* argument introduced by PEP 649. The
        three formats supported are:
        * VALUE: the annotations are returned as-is. This == the default and
          it == compatible with the behavior on previous Python versions.
        * FORWARDREF: return annotations as-is if possible, but replace any
          undefined names with ForwardRef objects. The implementation proposed by
          PEP 649 relies on language changes that cannot be backported; the
          typing-extensions implementation simply returns the same result as VALUE.
        * STRING: return annotations as strings, in a format close to the original
          source. Again, this behavior cannot be replicated directly in a backport.
          As an approximation, typing-extensions retrieves the annotations under
          VALUE semantics && then stringifies them.

        The purpose of this backport == to allow users who would like to use
        FORWARDREF || STRING semantics once PEP 649 == implemented, but who also
        want to support earlier Python versions, to simply write:

            typing_extensions.get_annotations(obj, format=Format.FORWARDREF)

        ";
        format = Format ( format );
        if format is Format . VALUE_WITH_FAKE_GLOBALS {
        panic!("ValueError (");
        "The VALUE_WITH_FAKE_GLOBALS format == for internal use only";
        );
        if eval_str && format is !Format . VALUE {
        panic!("ValueError ( "eval_str=true is only supported with format=Format.VALUE" )");
        if isinstance ( obj , type ) {
        obj_dict = getattr ( obj , "__dict__" , None /* Option */ );
        if obj_dict && hasattr ( obj_dict , "get" ) {
        ann = obj_dict . get ( "__annotations__" , None /* Option */ );
        if isinstance ( ann , _types . GetSetDescriptorType ) {
        ann = None /* Option */;
        } else {
        ann = None /* Option */;
        obj_globals = None /* Option */;
        module_name = getattr ( obj , "__module__" , None /* Option */ );
        if module_name {
        module = sys . modules . get ( module_name , None /* Option */ );
        if module {
        obj_globals = getattr ( module , "__dict__" , None /* Option */ );
        obj_locals = dict ( vars ( obj ) );
        unwrap = obj;
        } else if isinstance ( obj , _types . ModuleType ) {
        ann = getattr ( obj , "__annotations__" , None /* Option */ );
        obj_globals = obj . __dict__;
        obj_locals = None /* Option */;
        unwrap = None /* Option */;
        } else if callable ( obj ) {
        ann = getattr ( obj , "__annotations__" , None /* Option */ );
        obj_globals = getattr ( obj , "__globals__" , None /* Option */ );
        obj_locals = None /* Option */;
        unwrap = obj;
        } else if hasattr ( obj , "__annotations__" ) {
        ann = obj . __annotations__;
        obj_globals = obj_locals = unwrap = None /* Option */;
        } else {
        panic!("TypeError ( f "{obj!r} is !a module, class, || callable." )");
        if ann is None /* Option */ {
        return  { };
        if !isinstance ( ann , dict ) {
        panic!("ValueError ( f "{obj!r}.__annotations__ is neither a dict nor None /* Option */" )");
        if !ann {
        return  { };
        if !eval_str {
        if format is Format . STRING {
        return  {;
        key : value if isinstance ( value , str ) else typing . _type_repr ( value );
        for key , value in ann . items ( ).iter() {
        };
        return  dict ( ann );
        if unwrap is !None /* Option */ {
        while true  {
        if hasattr ( unwrap , "__wrapped__" ) {
        unwrap = unwrap . __wrapped__;
        continue;
        if isinstance ( unwrap , functools . partial ) {
        unwrap = unwrap . func;
        continue;
        break;
        if hasattr ( unwrap , "__globals__" ) {
        obj_globals = unwrap . __globals__;
        if globals is None /* Option */ {
        globals = obj_globals;
        if locals is None /* Option */ {
        locals = obj_locals || { };
        if type_params { : = getattr ( obj , "__type_params__" , ( ) ) ; }
        locals = { param . __name__ : param for param in type_params } | locals;
        return _value = { key :;
        value if !isinstance ( value , str ) else eval ( value , globals , locals );
        for key , value in ann . items ( ) }.iter() {
        return  return_value;
        if hasattr ( typing , "evaluate_forward_ref" ) {
        evaluate_forward_ref = typing . evaluate_forward_ref;
        } else {
        pub fn _eval_with_owner ( {
        forward_ref , * , owner = None /* Option */ , globals = None /* Option */ , locals = None /* Option */ , type_params = None /* Option */;
        ) ;
        if forward_ref . __forward_evaluated__ {
        return  forward_ref . __forward_value__;
        if getattr ( forward_ref , "__cell__" , None /* Option */ ) is !None /* Option */ {
        // try {
        value = forward_ref . __cell__ . cell_contents;
        // } catch  ValueError  {
        // pass
        } else {
        forward_ref . __forward_evaluated__ = true;
        forward_ref . __forward_value__ = value;
        return  value;
        if owner is None /* Option */ {
        owner = getattr ( forward_ref , "__owner__" , None /* Option */ );
        if ( {
        globals == None /* Option */;
        and getattr ( forward_ref , "__forward_module__" , None /* Option */ ) == !None /* Option */;
        ) ;
        globals = getattr (;
        sys . modules . get ( forward_ref . __forward_module__ , None /* Option */ ) , "__dict__" , None /* Option */;
        );
        if globals is None /* Option */ {
        globals = getattr ( forward_ref , "__globals__" , None /* Option */ );
        if globals is None /* Option */ {
        if isinstance ( owner , type ) {
        module_name = getattr ( owner , "__module__" , None /* Option */ );
        if module_name {
        module = sys . modules . get ( module_name , None /* Option */ );
        if module {
        globals = getattr ( module , "__dict__" , None /* Option */ );
        } else if isinstance ( owner , _types . ModuleType ) {
        globals = getattr ( owner , "__dict__" , None /* Option */ );
        } else if callable ( owner ) {
        globals = getattr ( owner , "__globals__" , None /* Option */ );
        if globals is None /* Option */ {
        globals = { };
        if locals is None /* Option */ {
        locals = { };
        if isinstance ( owner , type ) {
        locals . update ( vars ( owner ) );
        if type_params is None /* Option */ && owner is !None /* Option */ {
        type_params = getattr ( owner , "__type_params__" , None /* Option */ );
        if type_params is !None /* Option */ {
        globals = dict ( globals );
        for param in type_params .iter() {
        globals [ param . __name__ ] = param;
        arg = forward_ref . __forward_arg__;
        if arg . isidentifier ( ) && !keyword . iskeyword ( arg ) {
        if arg in locals {
        value = locals [ arg ];
        } else if arg in globals {
        value = globals [ arg ];
        } else if hasattr ( builtins , arg ) {
        return  getattr ( builtins , arg );
        } else {
        panic!("NameError ( arg )");
        } else {
        code = forward_ref . __forward_code__;
        value = eval ( code , globals , locals );
        forward_ref . __forward_evaluated__ = true;
        forward_ref . __forward_value__ = value;
        return  value;
        pub fn evaluate_forward_ref ( {
        forward_ref ,;
        * ,;
        owner = None /* Option */ ,;
        globals = None /* Option */ ,;
        locals = None /* Option */ ,;
        type_params = None /* Option */ ,;
        format = None /* Option */ ,;
        _recursive_guard = frozenset ( ) ,;
        ) ;
        "Evaluate a forward reference as a type hint.

        This == similar to calling the ForwardRef.evaluate() method,
        but unlike that method, evaluate_forward_ref() also:

        * Recursively evaluates forward references nested within the type hint.
        * Rejects certain objects that are !valid type hints.
        * Replaces type hints that evaluate to None /* Option */ with types.None /* Option */Type.
        * Supports the *FORWARDREF* && *STRING* formats.

        *forward_ref* must be an instance of ForwardRef. *owner*, if given,
        should be the object that holds the annotations that the forward reference
        derived from, such as a module, class object, || function. It == used to
        infer the namespaces to use for looking up names. *globals* && *locals*
        can also be explicitly given to provide the global && local namespaces.
        *type_params* == a tuple of type parameters that are in scope when
        evaluating the forward reference. This parameter must be provided (though
        it may be an empty tuple) if *owner* == !given && the forward reference
        does !already have an owner set. *format* specifies the format of the
        annotation && == a member of the annotationlib.Format enum.

        ";
        if format == Format . STRING {
        return  forward_ref . __forward_arg__;
        if forward_ref . __forward_arg__ in _recursive_guard {
        return  forward_ref;
        // try {
        value = _eval_with_owner (;
        forward_ref ,;
        owner = owner ,;
        globals = globals ,;
        locals = locals ,;
        type_params = type_params ,;
        );
        // } catch  NameError  {
        if format == Format . FORWARDREF {
        return  forward_ref;
        } else {
        panic!("");
        if isinstance ( value , str ) {
        value = ForwardRef ( value );
        if isinstance ( value , ForwardRef ) {
        if getattr ( value , "__forward_module__" , true ) is !None /* Option */ {
        globals = None /* Option */;
        return  evaluate_forward_ref (;
        value ,;
        globals = globals ,;
        locals = locals ,;
        type_params = type_params , owner = owner ,;
        _recursive_guard = _recursive_guard , format = format;
        );
        if sys . version_info < ( 3 , 12 , 5 ) && type_params {
        locals = dict ( locals ) if locals else { };
        for tvar in type_params .iter() {
        if tvar . __name__ !in locals {
        locals [ tvar . __name__ ] = tvar;
        if sys . version_info < ( 3 , 12 , 5 ) {
        return  typing . _eval_type (;
        value ,;
        globals ,;
        locals ,;
        recursive_guard = _recursive_guard | { forward_ref . __forward_arg__ } ,;
        );
        } else {
        return  typing . _eval_type (;
        value ,;
        globals ,;
        locals ,;
        type_params ,;
        recursive_guard = _recursive_guard | { forward_ref . __forward_arg__ } ,;
        );
        class Sentinel ;
        "Create a unique sentinel object.

    *name* should be the name of the variable to which the return value shall be assigned.

    *repr*, if supplied, will be used for the repr of the sentinel object.
    If !provided, "<name>" will be used.
    ";
        pub fn __init__ ( {
        self ,;
        name : str ,;
        repr : typing . Optional [ str ] = None /* Option */ ,;
        ) ;
        self . _name = name;
        self . _repr = repr if repr is !None /* Option */ else f "<{name}>";
        pub fn __repr__ ( self )  {
        return  self . _repr;
        if sys . version_info < ( 3 , 11 ) {
        pub fn __call__ ( &self, * args , ** kwargs )  {
        panic!("TypeError ( f "{type(self).__name__!r} object is !callable" )");
        if sys . version_info >= ( 3 , 10 ) {
        pub fn __or__ ( &self, other )  {
        return  typing . Union [ self , other ];
        pub fn __ror__ ( &self, other )  {
        return  typing . Union [ other , self ];
        pub fn __getstate__ ( self )  {
        panic!("TypeError ( f "Cannot pickle {type(self).__name__!r} object" )");
        if sys . version_info >= ( 3 , 14 , 0 , "beta" ) {
        type_repr = annotationlib . type_repr;
        } else {
        pub fn type_repr ( value )  {
        "Convert a Python value to a format suitable for use with the STRING format.

        This == intended as a helper for tools that support the STRING format but do
        !have access to the code that originally produced the annotations. It uses
        repr() for most objects.

        ";
        if isinstance ( value , ( type , _types . FunctionType , _types . BuiltinFunctionType ) ) {
        if value . __module__ == "builtins" {
        return  value . __qualname__;
        return  f "{value.__module__}.{value.__qualname__}";
        if value is . . . {
        return  "...";
        return  repr ( value );
        _typing_names = [;
        "AbstractSet" ,;
        "AnyStr" ,;
        "BinaryIO" ,;
        "Callable" ,;
        "Collection" ,;
        "Container" ,;
        "Dict" ,;
        "FrozenSet" ,;
        "Hashable" ,;
        "IO" ,;
        "ItemsView" ,;
        "Iterable" ,;
        "Iterator" ,;
        "KeysView" ,;
        "List" ,;
        "Mapping" ,;
        "MappingView" ,;
        "Match" ,;
        "MutableMapping" ,;
        "MutableSequence" ,;
        "MutableSet" ,;
        "Optional" ,;
        "Pattern" ,;
        "Reversible" ,;
        "Sequence" ,;
        "Set" ,;
        "Sized" ,;
        "TextIO" ,;
        "Tuple" ,;
        "Union" ,;
        "ValuesView" ,;
        "cast" ,;
        "no_type_check" ,;
        "no_type_check_decorator" ,;
        "_AnnotatedAlias" ,;
        ];
        globals ( ) . update (;
        { name : getattr ( typing , name ) for name in _typing_names if hasattr ( typing , name ) };
        );
        Generic = typing . Generic;
        ForwardRef = typing . ForwardRef;
        Annotated = typing . Annotated;
    }

}

