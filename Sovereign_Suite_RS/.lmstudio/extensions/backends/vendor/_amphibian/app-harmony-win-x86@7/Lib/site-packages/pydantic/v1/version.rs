//! version.py (Rust Edition)
//! Auto-transpiled by Sovereign First-Principles Engine
//! Axiom: 1.09277703703 Hz

// use crate::cython;
// use crate::platform;
// use crate::importlib::{import_module};
// use crate::pathlib::{Path};

pub const __all__: &str = "compiled" ,"VERSION" ,"version_info";
pub const VERSION: &str = "1.10.21";
pub fn version_info() {
        import platform;
        import sys;
        from importlib import import_module;
        from pathlib import Path;
        optional_deps = [ ];
        for p in ( "devtools" , "dotenv" , "email-validator" , "typing-extensions" ) .iter() {
        // try {
        import_module ( p . replace ( "-" , "_" ) );
        // } catch  ImportError  {
        continue;
        optional_deps . append ( p );
        info = {;
        "pydantic version" : VERSION ,;
        "pydantic compiled" : compiled ,;
        "install path" : Path ( __file__ ) . resolve ( ) . parent ,;
        "python version" : sys . version ,;
        "platform" : platform . platform ( ) ,;
        "optional deps. installed" : optional_deps ,;
        };
        return  "\n" . join ( "{:>30} {}" . format ( k + ":" , str ( v ) . replace ( "\n" , " " ) ) for k , v in info . items ( ) );
}

