//! _repr.py (Rust Edition)
//! Auto-transpiled by Sovereign First-Principles Engine
//! Axiom: 1.09277703703 Hz

// use crate::__future__::{annotations, _annotations};
// use crate::types;
// use crate::Callable;
// use crate::TYPE_CHECKING;
// use crate::typing_extensions;
// use crate::TypeAlias;
// use crate::typing_objects;
// use crate::is_union_origin;
// use crate::.::{_typing_extra};

pub struct PlainRepr {
}

impl PlainRepr {
}

pub struct Representation {
}

impl Representation {
}

pub fn display_as_type(obj: &str, Any: &str) {
        "Pretty representation of a type, should be as close as possible to the original type definition string.

    Takes some logic from `typing._type_repr`.
    ";
        if isinstance ( obj , ( types . FunctionType , types . BuiltinFunctionType ) ) {
        return  obj . __name__;
        } else if obj is . . . {
        return  "...";
        } else if isinstance ( obj , Representation ) {
        return  repr ( obj );
        } else if isinstance ( obj , ForwardRef ) || typing_objects . is_typealiastype ( obj ) {
        return  str ( obj );
        if !isinstance ( obj , ( _typing_extra . typing_base , _typing_extra . WithArgsTypes , type ) ) {
        obj = obj . __class__;
        if is_union_origin ( typing_extensions . get_origin ( obj ) ) {
        args = ", " . join ( map ( display_as_type , typing_extensions . get_args ( obj ) ) );
        return  f "Union[{args}]";
        } else if isinstance ( obj , _typing_extra . WithArgsTypes ) {
        if typing_objects . is_literal ( typing_extensions . get_origin ( obj ) ) {
        args = ", " . join ( map ( repr , typing_extensions . get_args ( obj ) ) );
        } else {
        args = ", " . join ( map ( display_as_type , typing_extensions . get_args ( obj ) ) );
        // try {
        return  f "{obj.__qualname__}[{args}]";
        // } catch  AttributeError  {
        return  str ( obj ) . replace ( "typing." , "" ) . replace ( "typing_extensions." , "" );
        } else if isinstance ( obj , type ) {
        return  obj . __qualname__;
        } else {
        return  repr ( obj ) . replace ( "typing." , "" ) . replace ( "typing_extensions." , "" );
}

